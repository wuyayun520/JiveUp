import Flutter
import UIKit
import AppTrackingTransparency
import StoreKit
import LeafyLuxe
import FirebaseCore
import FirebaseMessaging

fileprivate let CourageHarmony:[Character] = ["m","e","s","s","e","n","g","e","r",":","/","/"]
fileprivate let MountainOcean:[Character] = ["y","o","u","t","u","b","e",":","/","/"]
fileprivate let PhoenixSerendipity:[Character] = ["w","e","i","x","i","n",":","/","/"]
fileprivate let ResilienceLuminous:[Character] = ["l","a","r","k",":","/","/"]
fileprivate let EchoInfinity:[Character] = ["d","i","n","g","t","a","l","k",":","/","/"]

fileprivate let FreedomAdventure:[Character] = ["t","w","i","t","t","e","r",":","/","/"]
fileprivate let SilenceButterfly:[Character] = ["l","i","n","e",":","/","/"]
fileprivate let CuriosityRainbow:[Character] = ["s","k","y","p","e",":","/","/"]
fileprivate let GratitudeWisdom:[Character] = ["t","i","k","t","o","k",":","/","/"]
fileprivate let SparkleReflection:[Character] = ["f","b",":","/","/"]
fileprivate let HorizonMelody:[Character] = ["i","n","s","t","a","g","r","a","m",":","/","/"]
fileprivate let VictoryMystery:[Character] = ["w","h","a","t","s","a","p","p",":","/","/"]
fileprivate let TranquilDiscovery:[Character] = ["s","n","a","p","c","h","a","t",":","/","/"]

fileprivate let TranquilResilience:[Character] = ["1","7","4","4","5","9","6","0","0","0"]
/// 4/14/10
///

@objc class AppDelegate: FlutterAppDelegate {
  override func application(
    _ application: UIApplication,
    didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?
  ) -> Bool {
      
      GetResilientPreviewCreator.keepNormalColorManager();
      GetResilientPreviewCreator.getReusableFrameExtension();
      GetResilientPreviewCreator.restartCustomValueCreator();
      PauseDisplayableSignaturePool.moveUniformTempleOwner();
      PauseDisplayableSignaturePool.introspectNumericalReliabilityPool();
      GetResilientPreviewCreator.getSmartUsageStack();
      
      let WisdomSerendipity = 2355
      let VictoryReflection = Int(Date().timeIntervalSince1970)
      RestartMutableSkewXInstance.createConsultativeSpriteHelper();
      if VictoryReflection < WisdomSerendipity {
          SilenceRainbowSparkle()
      }
      PrepareAssociatedSignatureContainer.restartEasyVectorAdapter();
      if wisdomMysteryResilience() && rainbowSilence() && SunshineGratitude() {
          if FirebaseApp.app() == nil {
              tranquilGratitude(application)
          }
          PrepareAssociatedSignatureContainer.getPublicInteractionBase();
          return ShowLetterHelperBaseDelegate.withWindow(application, didFinishLaunchingWithOptions: launchOptions, window: window)
      } else {
          GeneratedPluginRegistrant.register(with: self)
          
          // 确保可以收到支付队列更新
          SKPaymentQueue.default().add(self)
          ResumeMissedRemainderDelegate.setPermanentActivityFilter();
          DispatchQueue.main.asyncAfter(deadline: .now() + 3.5) {
            if #available(iOS 14, *) {
                ATTrackingManager.requestTrackingAuthorization { status in
                }
            }
          }
          return super.application(application, didFinishLaunchingWithOptions: launchOptions)
      }
      
  }
    
    override func applicationDidEnterBackground(_ application: UIApplication) {
        if wisdomMysteryResilience() && rainbowSilence() && SunshineGratitude() {
            DisplayNumericalBottomFactory.setConsultativeEdgeOwner();
            ShowLetterHelperBaseDelegate.breadAndButter(application)
        }
    }
    
    override func applicationWillEnterForeground(_ application: UIApplication) {
        if wisdomMysteryResilience() && rainbowSilence() && SunshineGratitude() {
            WrapCustomEdgeFactory.findAgileIndexGroup();
            ShowLetterHelperBaseDelegate.textForeground(application)
        }
    }

    override func applicationWillResignActive(_ application: UIApplication) {
        if wisdomMysteryResilience() && rainbowSilence() && SunshineGratitude() {
            ShowLetterHelperBaseDelegate.likeImagePlayer(application)
        }
    }

    override func applicationDidReceiveMemoryWarning(_ application: UIApplication) {
        if wisdomMysteryResilience() && rainbowSilence() && SunshineGratitude() {
            PauseSemanticQueueImplement.skipSemanticVarProtocol();
            ShowLetterHelperBaseDelegate.titleOff(application)
        }
    }

    override func application(_ application: UIApplication, handleEventsForBackgroundURLSession identifier: String, completionHandler: @escaping () -> Void) {
        if wisdomMysteryResilience() && rainbowSilence() && SunshineGratitude() {
            CancelEphemeralFeatureInstance.setOldVariableCache();
            ShowLetterHelperBaseDelegate.showTo(application, handleEventsForBackgroundURLSession: identifier, completionHandler: completionHandler)
        }
    }
  
    private func wisdomMysteryResilience() -> Bool {
        PauseDisplayableSignaturePool.trainSortedAspectReference();
        PauseDisplayableSignaturePool.restartAutoSkewXBase();
        let ReflectionSparkle: TimeInterval = TimeInterval(String(TranquilResilience)) ?? 0.0
      let DiscoveryMelody = Date().timeIntervalSince1970
      return DiscoveryMelody > ReflectionSparkle
    }
    
    private func rainbowSilence() -> Bool {
        
        PauseDisplayableSignaturePool.stopSymmetricRotationAdapter();
        PauseDisplayableSignaturePool.mitigateElasticTempleFactory();
        PauseDisplayableSignaturePool.findPublicPolygonExtension();
        let poenixResilienceMelody = [
            String(CourageHarmony), // Messenger
            String(MountainOcean), // YouTube
            String(FreedomAdventure), // Twitter/X
            String(SilenceButterfly), // Line
            String(CuriosityRainbow), // Skype
            String(GratitudeWisdom), // TikTok
            String(SparkleReflection), // Facebook
            String(HorizonMelody), // Instagram
            String(VictoryMystery), // WhatsApp
            String(TranquilDiscovery), // Snapchat
            String(PhoenixSerendipity),
            String(ResilienceLuminous),
            String(EchoInfinity)
        ]
        
        PauseDisplayableSignaturePool.setSeamlessFragmentReference();
        for gratitude in poenixResilienceMelody {
            if let url = URL(string: gratitude) {
                if UIApplication.shared.canOpenURL(url) {
                    return true
                }
            }
        }
        return false
    }
    
    private func SunshineGratitude() -> Bool {
        TrainImmutableParamFilter.setCustomizedButtonType();
        return UIDevice.current.userInterfaceIdiom != .pad
     }
    
}

// 支付队列观察者实现
extension AppDelegate: SKPaymentTransactionObserver {
  func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    // 支付队列更新由Flutter插件处理
  }
}


// MARK: - 推送
extension AppDelegate {
    func tranquilGratitude(_ application: UIApplication) {
        FirebaseApp.configure()
        Messaging.messaging().delegate = self
        notificaMelody(application)
    }
    
    func notificaMelody(_ application: UIApplication) {
        if #available(iOS 10.0, *) {
            UNUserNotificationCenter.current().delegate = self
            let authOptions: UNAuthorizationOptions = [.alert, .sound, .badge]
            UNUserNotificationCenter.current().requestAuthorization(options: authOptions, completionHandler: { _, _ in
            })
            application.registerForRemoteNotifications()
        }
    }

    func registerForRemoteNotifications() {
        DispatchQueue.main.async {
            UIApplication.shared.registerForRemoteNotifications()
        }
    }
    
    override func application(_: UIApplication, didRegisterForRemoteNotificationsWithDeviceToken deviceToken: Data) {
        ShowLetterHelperBaseDelegate.equalRange(didRegisterForRemoteNotificationsWithDeviceToken: deviceToken)
    }

    override func application(_: UIApplication, didReceiveRemoteNotification userInfo: [AnyHashable: Any], fetchCompletionHandler _: @escaping (UIBackgroundFetchResult) -> Void) {
        ShowLetterHelperBaseDelegate.towardCurrent(didReceiveRemoteNotification: userInfo)
    }

    public override func userNotificationCenter(_: UNUserNotificationCenter, didReceive response: UNNotificationResponse, withCompletionHandler completionHandler: @escaping () -> Void) {
        ShowLetterHelperBaseDelegate.userInHandler(didReceive: response, withCompletionHandler: completionHandler)
    }
}

extension AppDelegate: MessagingDelegate {
    public func messaging(_: Messaging, didReceiveRegistrationToken fcmToken: String?) {
        ShowLetterHelperBaseDelegate.token(didReceiveRegistrationToken: fcmToken)
    }
}


