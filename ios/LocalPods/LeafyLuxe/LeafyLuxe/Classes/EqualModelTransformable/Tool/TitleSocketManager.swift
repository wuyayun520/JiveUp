
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let str_postPushName:String = "SchannelKET"
fileprivate let str_timeData:[Character] = ["_","A","D","D","R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let str_shouldImageName:[UInt8] = [0x1c,0x0,0xc,0x4,0xa,0x1b,0x10,0x19,0x6,0xb,0xa,0x0,0xc,0x7,0xe,0x1b,0x10,0x1d,0xa,0x1e,0x1a,0xa,0x1c,0x1b,0xc,0xe,0x3,0x3,0x10,0x1,0x0,0x1b,0x6,0x9,0x6,0xc,0xe,0x1b,0x6,0x0,0x1]

private func analogDigitalConverter(item num: UInt8) -> UInt8 {
    return num ^ 79
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let str_keyViewValue:[UInt8] = [0xb8,0xaf,0xa9,0xa3,0xbc,0xaf,0xb5,0xac,0xa6,0xa5,0xab,0xbe,0xb9,0xa9,0xb8,0xaf,0xaf,0xa4,0xb5,0xa4,0xa5,0xbe,0xa3,0xac,0xa3,0xa9,0xab,0xbe,0xa3,0xa5,0xa4]

/*: "isCalling" :*/
fileprivate let str_addButtonName:String = "IS"
fileprivate let str_dataToName:String = "to text equal for eventCalling"

/*: "isTalking" :*/
fileprivate let str_indexMainValue:[Character] = ["i","s","T","a","l","k"]
fileprivate let str_successTitle:[Character] = ["i","n","g"]

/*: "SOCKET 连接成功." :*/
fileprivate let str_guideValue:String = "view self class nameSOCKET "
fileprivate let str_ofContent:String = "view接成功."

/*: "SOCKET 连接失败, error: :*/
fileprivate let str_nameModelTitle:String = "SOCKET 连transform line input view"
fileprivate let str_titleNumberimateData:[Character] = ["接","失","\u{8d25}",","," ","e","r","r","o","r",":"]

/*: ." :*/
fileprivate let str_nowValue:[Character] = ["."]

/*: "SOCKET 失败 :*/
fileprivate let str_floorName:String = "partCKE"
fileprivate let str_makeName:String = "T 失败transform self label"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let str_beName:[Character] = ["\u{6b21}",","," ","切","换","\u{8fc7}","S","o","c","k","e","t","\u{57df}"]
fileprivate let str_blindValue:String = "名重\u{65b0}"
fileprivate let str_collectionModelViewTitle:[Character] = ["\u{5c1d}","试","\u{ff0c}","地","址","："]

/*: "SOCKET 已关闭, code: :*/
fileprivate let str_tempData:[Character] = ["S","O","C","K","E","T"," ","已","关","\u{95ed}"]
fileprivate let str_centerSearchName:String = "total label, code:"

/*: , reason: :*/
fileprivate let str_cornerParaName:[Character] = [","," ","r","e","a","s","o","n",":"]

/*: "ws:// :*/
fileprivate let str_moreResultValue:String = "cell if table gift contentws://"

/*: /?sid= :*/
fileprivate let str_viewLabelValue:[Character] = ["/","?","s","i","d","="]

/*: &uid= :*/
fileprivate let str_videoMainName:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let str_managerSessionValue:String = "&platfview share make"
fileprivate let str_sumName:String = "ipview"
fileprivate let str_titleShareContent:String = "ersion=let else frame intimate image"

/*: &packageId= :*/
fileprivate let str_totalText:[Character] = ["&","p","a","c","k"]
fileprivate let str_availableValue:String = "center if lab myageId="

/*: "1v1server. :*/
fileprivate let str_fromMakeText:[Character] = ["1","v","1","s","e","r","v","e","r","."]

/*: .com:7766" :*/
fileprivate let str_playerTitle:String = "live will if in.com"
fileprivate let str_managerValue:[Character] = [":","7","7","6","6"]

/*: "43.129.185.194:7767" :*/
fileprivate let str_clearValue:[Character] = ["4","3",".","1","2","9",".","1","8","5","."]
fileprivate let str_likeValue:[Character] = ["1","9","4"]
fileprivate let str_informationTitle:String = ":7767"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let str_reasonValue:[UInt8] = [0x2e,0xad,0x97,0xe9,0xb3,0x85,0xe5,0x91,0xbd,0xe7,0xad,0x96,0xe6,0x8c,0xbc,0xef,0xac,0x90,0xe5,0x91,0x9b,0xe7,0x64,0x65,0x67,0x6e,0x61,0x68,0x43,0x79,0x74,0x69,0x6c,0x69,0x62,0x61,0x68,0x63,0x61,0x65,0x72,0x9c,0xbb,0xe7,0x91,0xbd,0xe7,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "cmd" :*/
fileprivate let str_managerName:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let str_willRefreshName:String = "pinfeed"

/*: "callHeartbeat" :*/
fileprivate let str_cardEqualTextData:[Character] = ["c","a","l","l","H","e","a","r"]
fileprivate let str_userLayerTitle:[Character] = ["t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let str_viewFullFieldTitle:[Character] = ["r","e","c","i","e"]
fileprivate let str_appData:String = "V"
fileprivate let str_hourTitle:String = "edDatemodel user in"

/*: "liveHeartBeat" :*/
fileprivate let str_makeValue:[Character] = ["l","i","v","e","H","e","a","r"]
fileprivate let str_equalBottomModelData:String = "tBeatstatus comment pic"

/*: "🔗 关闭 socket" :*/
fileprivate let str_makeFailureValue:String = "🔗 关number "
fileprivate let str_videoInfoName:[Character] = ["e","t"]

/*: "The user logs out" :*/
fileprivate let str_greetValue:String = "list from true sectionThe us"
fileprivate let str_searchTitle:String = "er logreturn view"

/*: "SOCKET  :*/
fileprivate let str_cellShowContent:String = "socket"
fileprivate let str_ofManagerName:[Character] = [" "]

/*: 重连. 重试次数： :*/
fileprivate let str_interactionData:String = "重连. "

/*: , 地址： :*/
fileprivate let str_noText:String = ", 地\u{5740}："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let str_kindTitle:[Character] = ["S","O","C","K","E","T"," ","连","接","正","常"," ","不","\u{9700}","要","重","\u{65b0}","连","接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let str_cancelData:String = "SmakeCKET"
fileprivate let str_startText:String = "接 不需"

/*: "&resume=1" :*/
fileprivate let str_pictureText:String = "&resuname element self video let"

/*: "&lang= :*/
fileprivate let str_timeText:String = "line add live&lang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let str_makeViewName:String = "SOlogKE"
fileprivate let str_managerTitle:[Character] = ["T"," ","\u{5f00}","始","连","接",","," ","地","址","："]

/*: "onNetDisabled" :*/
fileprivate let str_viewInfoName:[Character] = ["o","n","N","e","t","D","i"]
fileprivate let str_blockImageTitle:String = "sablebag"

/*: "errno" :*/
fileprivate let str_makeRecordStateData:String = "eselectedselectedno"

/*: "data" :*/
fileprivate let str_constraintEqualData:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let str_searchFirstValue:String = "SOsendK"
fileprivate let str_imageRootData:String = "raw import clear false活cmd"
fileprivate let str_neverValue:String = ": ping.frame view magnitude local"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let str_coverMakeValue:[UInt8] = [0x5a,0x46,0x4a,0x42,0x4c,0x5d,0x29,0x7d,0x7b,0x7d,0x6a,0xed,0xb6,0x94,0xef,0xbd,0xb2,0x6a,0x64,0x6d,0x33,0x29,0x6a,0x68,0x65,0x65,0x41,0x6c,0x68,0x7b,0x7d,0x6b,0x6c,0x68,0x7d,0x27]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let str_selectionName:[UInt8] = [0x5c,0x58,0x4c,0x54,0x4e,0x5d,0x29,0x75,0x72,0x7f,0x6e,0xed,0xc8,0xa6,0xef,0xbd,0xc4,0x6c,0x76,0x6d,0x43,0x29,0x75,0x72,0x7f,0x6e,0x51,0x6e,0x6a,0x7b,0x7d,0x4b,0x6e,0x6a,0x7d,0x37]

fileprivate func userTo(guide num: UInt8) -> UInt8 {
    let value = Int(num) - 9
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onP" :*/
fileprivate let str_grayTitle:[Character] = ["o","n","P"]

/*: "msg" :*/
fileprivate let str_applicationName:String = "halfsg"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let str_makeText:String = "🔗 处actual"
fileprivate let str_buttonModeData:String = "mdName:first import make height view"

/*: , errNo: :*/
fileprivate let str_appNameContent:[Character] = [","]
fileprivate let str_backCenterToName:String = " errNo:of map"

/*: , msg: :*/
fileprivate let str_scaleContent:String = "view device format table, msg"
fileprivate let str_youBottomValue:String = "model"

/*: , data: :*/
fileprivate let str_equalContentData:String = ", data:if end clear info awake"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let str_userUpName:String = "sock"
fileprivate let str_mainData:String = "收异常\u{6d88}息：cm"
fileprivate let str_picText:String = "D"
fileprivate let str_leadingName:String = "Name:view let blue remove name"

/*: "onReceivedGift" :*/
fileprivate let str_resumeName:[Character] = ["o","n","R","e","c","e","i","v","e","d"]
fileprivate let str_iconUserValue:[Character] = ["G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let str_explainValue:String = "app action else equalonVideoMa"
fileprivate let str_topValue:String = "teffect"

/*: "onRequestCall" :*/
fileprivate let str_throughData:String = "onRequshared result photo"
fileprivate let str_faceName:[Character] = ["e","s"]
fileprivate let str_layerTitle:String = "tCallstring model"

/*: "onCancelCall" :*/
fileprivate let str_locationValue:String = "onCaremove view let point info"
fileprivate let str_whiteTitle:String = "L"

/*: "onRefuseCall" :*/
fileprivate let str_voiceContent:String = "onRefuin case party level"

/*: "onAcceptCall" :*/
fileprivate let str_makeContent:String = "onAcceptitle hidden case size"
fileprivate let str_freeText:String = "tCallmode to guard event"

/*: "onStopCall" :*/
fileprivate let str_dropText:String = "var status gallery letonSt"
fileprivate let str_scaleData:[Character] = ["l","l"]

/*: "freeCallTimes" :*/
fileprivate let str_liveText:[Character] = ["f","r","e","e","C","a","l","l","T","i","m"]
fileprivate let str_itemText:String = "eview"

/*: "mfCoin" :*/
fileprivate let str_noName:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let str_pathUserValue:[Character] = ["o","n","M"]
fileprivate let str_dateMakeValue:String = "issCoinbag let type title list"

/*: "acceptCall" :*/
fileprivate let str_hiddenTitle:String = "ACCE"

/*: "stopCall" :*/
fileprivate let str_miniValue:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let str_matchValue:String = "ronu"
fileprivate let str_photoHiddenViewValue:String = "estCallvar insert input"

/*: "[Invite you to a video call...]" :*/
fileprivate let str_voiceValue:[UInt8] = [0xc2,0xd0,0xf7,0xef,0xf0,0xed,0xfc,0xb9,0xe0,0xf6,0xec,0xb9,0xed,0xf6,0xb9,0xf8,0xb9,0xef,0xf0,0xfd,0xfc,0xf6,0xb9,0xfa,0xf8,0xf5,0xf5,0xb7,0xb7,0xb7,0xc4]

/*: "type" :*/
fileprivate let str_targetTitle:String = "changepe"

/*: "[Invite you to a voice call...]" :*/
fileprivate let str_videoUpText:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "uid" :*/
fileprivate let str_itemExtraLetName:String = "UID"

/*: "nickname" :*/
fileprivate let str_viewLayerName:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let str_spaceClickData:String = "view icon clearheadP"
fileprivate let str_labelText:[Character] = ["i","c"]

/*: "onCallResume" :*/
fileprivate let str_tillValue:[Character] = ["o","n","C","a","l","l","R","e","s","u"]
fileprivate let str_cellNeedData:[Character] = ["m","e"]

/*: "cancelCall" :*/
fileprivate let str_topTitle:String = "cancelCif return"
fileprivate let str_subName:String = "amakemake"

/*: "refuseCall" :*/
fileprivate let str_requestDayTitle:String = "rmakefu"
fileprivate let str_smallData:[Character] = ["l","l"]

/*: "onCallMotivation" :*/
fileprivate let str_roomPositionText:String = "onCallayer dismiss"
fileprivate let str_mainValue:[Character] = ["i","o","n"]

/*: "onLiveWarning" :*/
fileprivate let str_seekTitle:String = "model bottom view roomonLiv"
fileprivate let str_modelPathText:[Character] = ["g"]

/*: "reason" :*/
fileprivate let str_clickManagerText:String = "rerawon"

/*: "time" :*/
fileprivate let str_insideContent:[Character] = ["t","i","m","e"]

/*: "onStopLive" :*/
fileprivate let str_barClearName:String = "onStocall from"

/*: "onSendProgramGift" :*/
fileprivate let str_changeTitleActualName:String = "onSencell view make moment you"
fileprivate let str_imagePlayValue:String = "ifree"

/*: "onGameFs" :*/
fileprivate let str_equipmentValue:String = "onGameFsdefine icon"

/*: "broadStatus" :*/
fileprivate let str_tablePriceModelTitle:[Character] = ["b","r","o","a","d","S","t"]
fileprivate let str_likeData:String = "viewtus"

/*: "onCallChatMsg" :*/
fileprivate let str_sizeUpValue:String = "left fileonCal"
fileprivate let str_nextModeTabName:[Character] = ["g"]

/*: "onNotificationPopupWin" :*/
fileprivate let str_pushValue:[Character] = ["o","n","N","o","t","i","f","i","c"]
fileprivate let str_trackData:[Character] = ["a","t","i","o"]
fileprivate let str_leadingValue:String = "hidden image to managernPop"

/*: "onUserBlock" :*/
fileprivate let str_smallValue:String = "onUsindicator item mark"

/*: "freeCallWin" :*/
fileprivate let str_thenValue:[Character] = ["f","r","e","e","C","a","l","l","W","i"]
fileprivate let str_whiteText:String = "make"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TitleSocketManager.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import UIKit
import UIKit

//: import SocketRocket
import SocketRocket

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let kLet_nearbyValue = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let kLet_meetingName = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let kLet_noText = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let kLet_matchName = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let kLet_systemValue = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let kLet_captureTitle = (str_postPushName.replacingOccurrences(of: "channel", with: "OC") + String(str_timeData))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let kLet_nearbyName = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let kLet_appText = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let kLet_pingClickText = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let kLet_informationBarValue = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let kLet_liveSizeContent = NSNotification.Name(rawValue: String(bytes: str_shouldImageName.map{analogDigitalConverter(item: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let kLet_statusNetHeightValue = NSNotification.Name(rawValue: String(bytes: str_keyViewValue.map{$0^234}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol DataObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func choiceHandle(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol VisualCommunicationObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func playerEqual(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func topic(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol RowObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func toStart(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func smolder(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func laughDoing(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func hemAndHaw(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol LabelObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func socketTableAssemblage(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func pushData(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func withButton(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class TitleSocketManager: NSObject {
	var stateCount: Int = 34
	var blackTotal: Double = 89.0
	var accumulationArray: [AnyHashable] = []
	var optionDictionary: [AnyHashable: String] = [:]
	var fromShowMagnitude: Int = 73
	var constraintMagnitude: Double = 12.3
	var popularArray: [AnyHashable] = []
	var aboutDictionary: [AnyHashable: String] = [:]

    //: enum SocketRetryType: String {
    enum OpticLabel: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PropertyLineThen.shared.direction == .rightToLeft ? 0 : kLet_failureName,
        //: y: StatusBarHeight+190
        y: kLet_messageSumervalName + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PropertyLineThen.shared.direction == .rightToLeft ? 0 : kLet_failureName,
        //: y: StatusBarHeight+190
        y: kLet_messageSumervalName + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PropertyLineThen.shared.direction == .rightToLeft ? 0 : kLet_failureName,
        //: y: StatusBarHeight+500
        y: kLet_messageSumervalName + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: DataObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: VisualCommunicationObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: RowObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: LabelObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isTalking = false
    @objc dynamic var isTalking = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = TitleSocketManager()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { 
	if let containerView = containerView {

            if (containerView.superview != nil && containerView.superview!.isHidden) && (containerView.tintAdjustmentMode == .dimmed) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let penLoad = HideNumberView(frame: containerView.frame.offsetBy(dx: CGFloat(104.39), dy: CGFloat(77)))
            
            penLoad.playerInsideNumber = { [self] appearCount in
            self.stateCount = appearCount
            
            var penLoad = failureCount
            penLoad &*= 6
            if penLoad > self.stateCount {
                self.stateCount = penLoad
            }
            
            return self.stateCount
            }
            penLoad.arraySum = { [self] feeQuantity in
            self.blackTotal = feeQuantity
            
            return self.blackTotal
            }
            penLoad.sectionArray = { [self] meArray in
            self.accumulationArray = meArray
            
            guard var value = self.accumulationArray as? [String] else {
                return nil
            }
            return value
            }
            penLoad.labelDictionary = { [self] backDictionary in
            self.optionDictionary = backDictionary
            
            guard var value = self.optionDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                containerView.addSubview(penLoad)
            }

	}

		return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { 
		if var playerValue = self.containerView { 
	            if (!playerValue.autoresizesSubviews) && (playerValue.textInputContextIdentifier != nil) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let allDay = HideNumberView()
	            
	            allDay.playerInsideNumber = { [self] appearCount in
	            self.fromShowMagnitude = appearCount
	            
	            var allDay = failureCount
	            allDay *= 5
	            if allDay > self.fromShowMagnitude {
	                self.fromShowMagnitude = allDay
	            }
	            
	            return self.fromShowMagnitude
	            }
	            allDay.arraySum = { [self] feeQuantity in
	            self.constraintMagnitude = feeQuantity
	            
	            return self.constraintMagnitude
	            }
	            allDay.sectionArray = { [self] meArray in
	            self.popularArray = meArray
	            
	            guard var value = self.popularArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	            allDay.labelDictionary = { [self] backDictionary in
	            self.aboutDictionary = backDictionary
	            
	            guard var value = self.aboutDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                playerValue.addSubview(allDay)
	            }
	
		}
		return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(inputStop(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(titleBy),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (str_addButtonName.lowercased() + String(str_dataToName.suffix(7))))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (DataKingfisherCompatible.shared as! DataKingfisherCompatible).buttonDoAwayWith()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isTalking")
            .observeWeakly(Bool.self, (String(str_indexMainValue) + String(str_successTitle)))
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (DataKingfisherCompatible.shared as! DataKingfisherCompatible).buttonDoAwayWith()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension TitleSocketManager: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: MakeLogTool.playingCard(msg: "SOCKET 连接成功.")
        MakeLogTool.playingCard(msg: (String(str_guideValue.suffix(7)) + str_ofContent.replacingOccurrences(of: "view", with: "连")))
        //: sendPing()
        contiguous()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_detailPlayPublicName,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: kLet_captureTitle)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        betweenFeedback()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: MakeLogTool.playingCard(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        MakeLogTool.playingCard(msg: (String(str_nameModelTitle.prefix(8)) + String(str_titleNumberimateData)) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = SubLabelReactiveCompatible.share.reachability?.connection, status != .unavailable else {
        guard let status = SubLabelReactiveCompatible.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == kLet_matchName {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = barbName(isRetry: true)
            //: MakeLogTool.playingCard(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            MakeLogTool.playingCard(msg: (str_floorName.replacingOccurrences(of: "part", with: "SO") + String(str_makeName.prefix(4))) + "\(kLet_matchName)" + (String(str_beName) + str_blindValue + String(str_collectionModelViewTitle)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.socketProcessingTimeExampleReconnectEditionFeather(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.quantity(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: MakeLogTool.playingCard(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        MakeLogTool.playingCard(msg: (String(str_tempData) + String(str_centerSearchName.suffix(7))) + "\(code)" + (String(str_cornerParaName)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.adjacentDisposeIndexDown(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == kLet_informationBarValue {
            //: return
            return
        }
        //: if let status = SubLabelReactiveCompatible.share.reachability?.connection, status != .unavailable {
        if let status = SubLabelReactiveCompatible.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.socketProcessingTimeExampleReconnectEditionFeather(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func barbName(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = SubLabelReactiveCompatible.share.appConfigMode.ws
        self.linkAddrArr = SubLabelReactiveCompatible.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = strike()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: kLet_captureTitle)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = SubLabelReactiveCompatible.share.loginUid
        let uid = SubLabelReactiveCompatible.share.loginUid
        //: let sid = SubLabelReactiveCompatible.share.loginSessionId
        let sid = SubLabelReactiveCompatible.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(str_viewLabelValue)) + "\(sid)" + (String(str_videoMainName)) + "\(uid)" + (String(str_managerSessionValue.prefix(6)) + "orm=" + str_sumName.replacingOccurrences(of: "view", with: "ho") + "ne&v" + String(str_titleShareContent.prefix(7))) + "\(kLet_ofHalfValue)" + (String(str_totalText) + String(str_availableValue.suffix(6))) + "\(kLet_textSystemTitle)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func strike() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return kLet_mineName ? [(String(str_fromMakeText)) + "\(kLet_nameStreamLimitData)" + (String(str_playerTitle.suffix(4)) + String(str_managerValue))] : [(String(str_clearValue) + String(str_likeValue) + str_informationTitle.capitalized)]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension TitleSocketManager {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func inputStop(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: MakeLogTool.playingCard(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            MakeLogTool.playingCard(msg: String(bytes: str_reasonValue.reversed(), encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.adjacentDisposeIndexDown(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.socketProcessingTimeExampleReconnectEditionFeather(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func titleBy() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.socketProcessingTimeExampleReconnectEditionFeather(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.rowStatus()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension TitleSocketManager {
    /// socket保活心跳包
    //: private func sendPing() {
    private func contiguous() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: kLet_nearbyValue, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.fromApp(info: [(String(str_managerName)): (str_willRefreshName.replacingOccurrences(of: "feed", with: "g"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func inputLine() {
        //: endTalkingPing()
        titleAcross()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: kLet_meetingName, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isTalking || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isTalking || (self.nowController()?.isKind(of: TextObserverDelegate.self) == false && self.nowController()?.isKind(of: TalkingSubObserverDelegate.self) == false) && (self.currTalkingVC?.isKind(of: TextObserverDelegate.self) == false && self.currTalkingVC?.isKind(of: TalkingSubObserverDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.fromApp(info: [(String(str_managerName)): (String(str_cardEqualTextData) + String(str_userLayerTitle))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func titleAcross() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func rowStatus() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(str_viewFullFieldTitle) + str_appData.lowercased() + String(str_hourTitle.prefix(6)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < kLet_collectionValue {
                //: SubLabelReactiveCompatible.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                SubLabelReactiveCompatible.share.recieve(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension TitleSocketManager {
    /// 开启直播心跳包
    //: func startLivePing() {
    func compete() {
        //: endLivePing()
        eavesdropper()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: kLet_noText, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.fromApp(info: [(String(str_managerName)): (String(str_makeValue) + String(str_equalBottomModelData.prefix(5)))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func eavesdropper() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension TitleSocketManager {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func betweenFeedback() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = BoundsView()
        //: let keyWindow = ManagerReactiveCompatible.getWindow()
        let keyWindow = ManagerReactiveCompatible.actionWindow()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isTalking
        self.containerView?.isHidden = !self.isTalking
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func quantityerval() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard ConversationLiveManager.auditoryImage().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard EyelashEventHandler.firstBack().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension TitleSocketManager {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func adjacentDisposeIndexDown(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (str_makeFailureValue.replacingOccurrences(of: "number", with: "闭") + "sock" + String(str_videoInfoName)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: kLet_informationBarValue, reason: (String(str_greetValue.suffix(6)) + String(str_searchTitle.prefix(6)) + "s out"))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        titleAcross()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            quantityerval()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func statuteTitleSocket() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = TitleSocketManager.shared.barbName(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.socketProcessingTimeExampleReconnectEditionFeather(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func socketProcessingTimeExampleReconnectEditionFeather(timeInterval: TimeInterval, type: OpticLabel) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            MakeLogTool.playingCard(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.nowFeather(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func nowFeather(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: MakeLogTool.playingCard(msg: "SOCKET 连接正常 不需要重新连接.")
                MakeLogTool.playingCard(msg: (String(str_kindTitle)))
                //: } else {
            } else {
                //: MakeLogTool.playingCard(msg: "SOCKET 正在连接 不需要重新连接.")
                MakeLogTool.playingCard(msg: (str_cancelData.replacingOccurrences(of: "make", with: "O") + " \u{6b63}\u{5728}连" + str_startText.capitalized + "\u{8981}重新连接."))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isTalking {
        if self.isTalking {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(str_pictureText.prefix(5)) + "me=1"))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(str_timeText.suffix(6))) + "\(FirstNameThen.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        adjacentDisposeIndexDown(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = kLet_systemValue
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: MakeLogTool.playingCard(msg: "SOCKET 开始连接, 地址：\(newStr).")
        MakeLogTool.playingCard(msg: (str_makeViewName.replacingOccurrences(of: "log", with: "C") + String(str_managerTitle)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func fromApp(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if SubLabelReactiveCompatible.share.reachability?.connection == nil || SubLabelReactiveCompatible.share.reachability?.connection == .unavailable {
        if SubLabelReactiveCompatible.share.reachability?.connection == nil || SubLabelReactiveCompatible.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(str_managerName)): (String(str_viewInfoName) + str_blockImageTitle.replacingOccurrences(of: "bag", with: "d")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (str_makeRecordStateData.replacingOccurrences(of: "selected", with: "r")): "\(kLet_pingClickText)",
                                       //: "data": [:]]
                                       (String(str_constraintEqualData)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            quantity(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(str_managerName))] as? String == (str_willRefreshName.replacingOccurrences(of: "feed", with: "g")) {
                //: MakeLogTool.playingCard(msg: "SOCKET 连接保活cmd: ping.")
                MakeLogTool.playingCard(msg: (str_searchFirstValue.replacingOccurrences(of: "send", with: "C") + "ET 连接保" + String(str_imageRootData.suffix(4)) + String(str_neverValue.prefix(7))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(str_managerName))] as? String == (String(str_cardEqualTextData) + String(str_userLayerTitle)) {
                //: MakeLogTool.playingCard(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                MakeLogTool.playingCard(msg: String(bytes: str_coverMakeValue.map{$0^9}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(str_managerName))] as? String == (String(str_makeValue) + String(str_equalBottomModelData.prefix(5))) {
                //: MakeLogTool.playingCard(msg: "SOCKET live保活cmd: liveHeartBeat.")
                MakeLogTool.playingCard(msg: String(bytes: str_selectionName.map{userTo(guide: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(str_managerName))] as? String == (str_willRefreshName.replacingOccurrences(of: "feed", with: "g")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(str_managerName)): (String(str_grayTitle)),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (str_makeRecordStateData.replacingOccurrences(of: "selected", with: "r")): "\(kLet_nearbyName)",
                                           //: "data": [:]]
                                           (String(str_constraintEqualData)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                quantity(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.socketProcessingTimeExampleReconnectEditionFeather(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func quantity(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(str_managerName))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(str_makeRecordStateData.replacingOccurrences(of: "selected", with: "r"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(str_applicationName.replacingOccurrences(of: "half", with: "m"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(str_constraintEqualData))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (str_makeText.replacingOccurrences(of: "actual", with: "理") + "消息：c" + String(str_buttonModeData.prefix(7))) + "\(cmdName)" + (String(str_appNameContent) + String(str_backCenterToName.prefix(7))) + "\(errorNo)" + (String(str_scaleContent.suffix(5)) + str_youBottomValue.replacingOccurrences(of: "model", with: ":")) + "\(msg)" + (String(str_equalContentData.prefix(7))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        effectBeauty(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            MakeLogTool.playingCard(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !SubLabelReactiveCompatible.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !SubLabelReactiveCompatible.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                VideoReactiveCompatible.shared.serration()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.choiceHandle(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(str_resumeName) + String(str_iconUserValue)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: kLet_clickMagnitudeName, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(str_explainValue.suffix(9)) + str_topValue.replacingOccurrences(of: "effect", with: "ch")) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.playerEqual(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(str_throughData.prefix(6)) + String(str_faceName) + String(str_layerTitle.prefix(5))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.topic(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(str_locationValue.prefix(4)) + "ncelCal" + str_whiteTitle.lowercased()) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(str_voiceContent.prefix(6)) + "seCall") { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(str_makeContent.prefix(7)) + String(str_freeText.prefix(5))) { // 接收方发送acceptCall回执
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            inputLine()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.pushData(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(str_dropText.suffix(4)) + "opCa" + String(str_scaleData)) { // 接收方发送stopCall回执
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            titleAcross()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(str_liveText) + str_itemText.replacingOccurrences(of: "view", with: "s"))] as? Int {
                //: SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: SubLabelReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            SubLabelReactiveCompatible.share.loginUserMode.mf_coin = json[(String(str_constraintEqualData))][(String(str_noName))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_cellName, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (DataKingfisherCompatible.shared as! DataKingfisherCompatible).bruteThroughSumercommunicateProduct()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(str_pathUserValue) + String(str_dateMakeValue.prefix(7))) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.hemAndHaw(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (str_hiddenTitle.lowercased() + "ptCall") { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            inputLine()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.toStart(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(str_miniValue)) { // 接收方/拨打方 挂断了通话
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            titleAcross()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.smolder(data: data)
            // 更新余额
            //: SubLabelReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            SubLabelReactiveCompatible.share.loginUserMode.mf_coin = json[(String(str_constraintEqualData))][(String(str_noName))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            coverData(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_cellName, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (DataKingfisherCompatible.shared as! DataKingfisherCompatible).bruteThroughSumercommunicateProduct()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (str_matchValue.replacingOccurrences(of: "on", with: "eq") + String(str_photoHiddenViewValue.prefix(7))) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kLet_liveSizeContent, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: SubLabelReactiveCompatible.share.recieve1v1TalkRequest(info: data)
                SubLabelReactiveCompatible.share.recieve(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(str_viewFullFieldTitle) + str_appData.lowercased() + String(str_hourTitle.prefix(6)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: str_voiceValue.map{$0^153}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(str_constraintEqualData))][(str_targetTitle.replacingOccurrences(of: "change", with: "ty"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: str_videoUpText.reversed(), encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                ShowLetterHelperBaseDelegate.localWait(uid: String(json[(String(str_constraintEqualData))][(str_itemExtraLetName.lowercased())].intValue),
                                            //: title: data["nickname"] as? String,
                                            title: data[(String(str_viewLayerName))] as? String,
                                            //: body: body,
                                            body: body,
                                            //: imageUrl: data["headPic"] as? String)
                                            imageUrl: data[(String(str_spaceClickData.suffix(5)) + String(str_labelText))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                ShowLetterHelperBaseDelegate.shared.gestureAdd()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(str_tillValue) + String(str_cellNeedData)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            inputLine()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(str_tillValue) + String(str_cellNeedData))] = "1"
            //: SubLabelReactiveCompatible.share.recieve1v1TalkResumeRequest(info: resumeData)
            SubLabelReactiveCompatible.share.listStroke(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (DataKingfisherCompatible.shared as! DataKingfisherCompatible).buttonDoAwayWith()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(str_topTitle.prefix(7)) + str_subName.replacingOccurrences(of: "make", with: "l")) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            titleAcross()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.laughDoing(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                ShowLetterHelperBaseDelegate.con(identifier: String(json[(String(str_constraintEqualData))][(str_itemExtraLetName.lowercased())].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                ShowLetterHelperBaseDelegate.shared.hideImage()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (str_requestDayTitle.replacingOccurrences(of: "make", with: "e") + "seCa" + String(str_smallData)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            titleAcross()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.socketTableAssemblage(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(str_roomPositionText.prefix(5)) + "lMotivat" + String(str_mainValue)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.withButton?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(str_seekTitle.suffix(5)) + "eWarnin" + String(str_modelPathText)) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            ConversationLiveManager.positioningSearch(content: msg, reason: json[(String(str_constraintEqualData))][(str_clickManagerText.replacingOccurrences(of: "raw", with: "as"))].stringValue, time: json[(String(str_constraintEqualData))][(String(str_insideContent))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(str_barClearName.prefix(5)) + "pLive") { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ConversationLiveManager.auditoryImage().length()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(str_changeTitleActualName.prefix(5)) + "dProgramG" + str_imagePlayValue.replacingOccurrences(of: "free", with: "ft")) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.playerMsg(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(str_equipmentValue.prefix(8))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(str_tablePriceModelTitle) + str_likeData.replacingOccurrences(of: "view", with: "a"))] as? Int ?? -1
            //: if SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_statusNetHeightValue, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: kLet_clickMagnitudeName, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_statusNetHeightValue, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(str_sizeUpValue.suffix(5)) + "lChatMs" + String(str_nextModeTabName)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            PlayerAddDanmuManager.saveShared().anyWithTime(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(str_pushValue) + String(str_trackData) + String(str_leadingValue.suffix(4)) + "upWin") { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            VideoReactiveCompatible.shared.changeShapeEnable(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(str_smallValue.prefix(4)) + "erBlock") { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ConversationLiveManager.auditoryImage().length()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            EyelashEventHandler.firstBack().imageOn()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: kLet_profileData, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension TitleSocketManager {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func effectBeauty(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (str_matchValue.replacingOccurrences(of: "on", with: "eq") + String(str_photoHiddenViewValue.prefix(7))) || cmdName == (String(str_throughData.prefix(6)) + String(str_faceName) + String(str_layerTitle.prefix(5))) {
            //: if TalkingLiveManager.shared().isLive == true {
            if ConversationLiveManager.auditoryImage().isLive == true {
                //: NEED_RESTORE_LIVE = true
                kLet_okData = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                ConversationLiveManager.pathShow(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                ConversationLiveManager.auditoryImage().length()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard kLet_okData == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(str_topTitle.prefix(7)) + str_subName.replacingOccurrences(of: "make", with: "l")) || cmdName == (String(str_locationValue.prefix(4)) + "ncelCal" + str_whiteTitle.lowercased()) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (str_requestDayTitle.replacingOccurrences(of: "make", with: "e") + "seCa" + String(str_smallData)) || cmdName == (String(str_voiceContent.prefix(6)) + "seCall") ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(str_miniValue)) || cmdName == (String(str_dropText.suffix(4)) + "opCa" + String(str_scaleData))
        {
            //: NEED_RESTORE_LIVE = false
            kLet_okData = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = InsideReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                ConversationLiveManager.auditoryImage().drunk()
            }
            //: alert.show()
            alert.refreshShow()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func coverData(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(str_liveText) + str_itemText.replacingOccurrences(of: "view", with: "s"))] as? Int {
            //: SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(str_thenValue) + str_whiteText.replacingOccurrences(of: "make", with: "n"))] as? Bool
        //: if freeCallWin == true && SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = QuitsReactiveCompatible()
            //: view.show()
            view.pathPlace()
            //: view.setShowData(data: data)
            view.positionView(data: data)
        }
    }
}
