
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_scaleName:[UInt8] = [0x26,0x21,0x26,0x3b,0x67,0x2c,0x20,0x2b,0x2a,0x3d,0x75,0x66,0x6f,0x27,0x2e,0x3c,0x6f,0x21,0x20,0x3b,0x6f,0x2d,0x2a,0x2a,0x21,0x6f,0x26,0x22,0x3f,0x23,0x2a,0x22,0x2a,0x21,0x3b,0x2a,0x2b]

private func overTheQuantityerMedicine(price num: UInt8) -> UInt8 {
    return num ^ 79
}

/*: "F5F5F5" :*/
fileprivate let str_levelViewName:String = "FhiddenFhiddenF"
fileprivate let str_timeValue:String = "tool"

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let str_makeName:String = "list make kit ofTalki"
fileprivate let str_liveNameData:String = "count true color actiontText"
fileprivate let str_cancelName:String = "scale self unknown available mainellID"

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_playerEqualRenderName:[Character] = ["T","a","l","k","i","n","g","C","h"]
fileprivate let str_theoryValue:[Character] = ["a","t","I","m","a","g","e","M","s","g"]
fileprivate let str_imageNameValue:String = "CellIDcell let make normal"

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let str_touchName:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","V","i","d","e","o"]
fileprivate let str_appText:String = "the empty textMs"
fileprivate let str_jumpData:String = "reply makegCellID"

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let str_whenName:String = "Talkititle voice user"
fileprivate let str_sexValue:String = "birth return let imagengChat"
fileprivate let str_frontTitle:String = "sgCellIDcell var for self background"

/*: "ConfinementViewCell" :*/
fileprivate let str_indexPageLetText:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","A","s","k","F","o","r","G","i","f","t","M","s"]
fileprivate let str_recommendToName:String = "to false view makegCell"

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_viewStatusValue:String = "message superTalki"
fileprivate let str_actualTitle:[Character] = ["n","g","C","h","a","t","T","i","p","s","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let str_timeData:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","M","a","t"]
fileprivate let str_directionValue:String = "mode room m varchMsgC"

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let str_inputBarData:String = "Talkireturn data nor size greet"
fileprivate let str_itemViewData:String = "AudioMssection to path fail with"
fileprivate let str_renderValue:[Character] = ["g","C","e","l","l","I","D"]

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let str_colorShareTitle:[UInt8] = [0x0,0x35,0x38,0x3f,0x3d,0x3a,0x33,0x17,0x3c,0x35,0x20,0x7,0x2d,0x27,0x20,0x31,0x39,0x1a,0x3b,0x20,0x3d,0x32,0x1e,0x21,0x39,0x24,0x19,0x27,0x33,0x17,0x31,0x38,0x38,0x1d,0x10]

private func addName(custom num: UInt8) -> UInt8 {
    return num ^ 84
}

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_messageListData:String = "text viewTSyste"
fileprivate let str_playerName:String = "if let camera hidden celleCell"

/*: "TalkingChatMapMsgCellID" :*/
fileprivate let str_itemCellData:[Character] = ["T","a","l","k","i","n","g","C"]
fileprivate let str_transformData:String = "itemt"
fileprivate let str_maleText:String = "MapMseffect frame activity"

/*: "#128CFF" :*/
fileprivate let str_makeTitle:String = "#128Clet share return"
fileprivate let str_viewPathTitle:String = "withwith"

/*: "Feedback" :*/
fileprivate let str_intimateData:String = "Feedbackwhite view"

/*: "#8370FF" :*/
fileprivate let str_executeData:String = "of layer view than view#8370F"
fileprivate let str_clickText:String = "running"

/*: "Quick Greeting" :*/
fileprivate let str_areaItemText:[Character] = ["Q","u","i","c","k"," ","G","r"]
fileprivate let str_totalData:[Character] = ["e","e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let str_answerName:String = "main game dataBeauty"
fileprivate let str_resultContent:String = "TOS"

/*: "url" :*/
fileprivate let str_addStreamName:String = "URL"

/*: "unlockGift" :*/
fileprivate let str_elementText:[Character] = ["u","n","l","o","c","k"]
fileprivate let str_inputDataText:[Character] = ["G","i","f","t"]

/*: "%d" :*/
fileprivate let str_pushValue:[Character] = ["%","d"]

/*: "isBurn" :*/
fileprivate let str_removeLastName:[UInt8] = [0x56,0x60,0x2f,0x62,0x5f,0x5b]

fileprivate func imageSelected(make num: UInt8) -> UInt8 {
    let value = Int(num) - 237
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let str_toCenterValue:String = "model typegiftId"

/*: "rYMsgType" :*/
fileprivate let str_randomManagerData:String = "content request if regular indexrYMsgTy"
fileprivate let str_layerTitle:[Character] = ["p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let str_greetName:[Character] = ["G","J",":"]
fileprivate let str_oldData:String = "mode firstCall"

/*: "changeDirection" :*/
fileprivate let str_byViewContent:[Character] = ["c","h","a","n","g","e","D","i","r","e","c"]
fileprivate let str_lastText:String = "tsharen"

/*: "extra" :*/
fileprivate let str_countManagerText:String = "EXTRA"

/*: "msgInfo" :*/
fileprivate let str_domainData:[Character] = ["m","s","g","I","n","f","o"]

/*: "toUid" :*/
fileprivate let str_backName:String = "toUidfor var manager"

/*: "wantGift" :*/
fileprivate let str_hiddenData:String = "wantGifinteraction any message temp"
fileprivate let str_labelValue:[Character] = ["t"]

/*: "jumpKey" :*/
fileprivate let str_factorData:[Character] = ["j","u","m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let str_makeText:String = "phone directiontruePe"

/*: "logType" :*/
fileprivate let str_hiddenText:String = "manager equallogType"

/*: "msgType" :*/
fileprivate let str_outsideValue:[UInt8] = [0x5,0x1b,0xf,0x3c,0x11,0x18,0xd]

/*: "txt" :*/
fileprivate let str_sexTitle:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let str_colorRowLetData:String = "TYPE"

/*: "QuickMsg" :*/
fileprivate let str_kitName:[Character] = ["Q","u","i","c","k","M","s","g"]

/*: "params" :*/
fileprivate let str_awakeTitle:[UInt8] = [0xa7,0x98,0xa9,0x98,0xa4,0xaa]

fileprivate func comeBack(background num: UInt8) -> UInt8 {
    let value = Int(num) + 201
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let str_modelRoomData:[UInt8] = [0x7d,0x71,0x70,0x6a,0x7b,0x70,0x6a]

/*: "id" :*/
fileprivate let str_matchData:[Character] = ["i","d"]

/*: "txtId" :*/
fileprivate let str_labelData:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let str_colorSizeContent:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let str_acrossName:String = "margin"
fileprivate let str_voiceValue:String = "xtMsgcall else in"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let str_topCameraValue:[UInt8] = [0xfa,0xc3,0xc5,0xcf,0xc9,0x8c,0xc4,0xc9,0xc0,0xc0,0xc3,0x8c,0xc8,0xc3,0xdb,0xc2,0xc0,0xc3,0xcd,0xc8,0x8c,0xca,0xcd,0xc5,0xc0,0xc9,0xc8,0x82,0x8c,0xfc,0xde,0xc3,0xcf,0xc9,0xc8,0xd9,0xde,0xc9]

private func countUser(receive num: UInt8) -> UInt8 {
    return num ^ 172
}

/*: "length" :*/
fileprivate let str_makeContent:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let str_leadingName:String = "AUDIO"

/*: "audioId" :*/
fileprivate let str_sectionName:String = "audioIdlet user task lab"

/*: "audioLength" :*/
fileprivate let str_userLayerValue:[UInt8] = [0xf1,0xe5,0xf4,0xf9,0xff,0xdc,0xf5,0xfe,0xf7,0xe4,0xf8]

/*: "audioSandbox" :*/
fileprivate let str_picValue:String = "coinudi"
fileprivate let str_bagName:String = "store"

/*: "senderId" :*/
fileprivate let str_onAddTitle:[UInt8] = [0x49,0x3b,0x44,0x3a,0x3b,0x48,0x1f,0x3a]

fileprivate func modeLocation(the num: UInt8) -> UInt8 {
    let value = Int(num) - 214
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "AudioMsg" :*/
fileprivate let str_itemBackValue:String = "extension icon dataAudioMs"
fileprivate let str_collectName:String = "object"

/*: "Can't send blank message" :*/
fileprivate let str_popName:String = "Can\'t"
fileprivate let str_sectionTitle:String = "share view bla"
fileprivate let str_scaleHomeName:String = "lab"
fileprivate let str_actualName:String = "ssagmanager"

/*: "quoteMsgId" :*/
fileprivate let str_bindName:[Character] = ["q","u","o","t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let str_uniformTitle:String = "path info shared let viewquoteMs"
fileprivate let str_labelName:[Character] = ["g","I","n","f","o"]

/*: "contentType" :*/
fileprivate let str_currentIndexName:[UInt8] = [0x3e,0x32,0x33,0x29,0x38,0x33,0x29,0x9,0x24,0x2d,0x38]

private func trueStage(detail num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "ImgMsg" :*/
fileprivate let str_giftModelContent:String = "selected var unableImgMs"
fileprivate let str_whiteValue:[Character] = ["g"]

/*: "img" :*/
fileprivate let str_timeContent:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let str_rowValue:[UInt8] = [0xdb,0xdf,0xd3,0xd5,0xd7,0xe7,0xc0,0xdb]

/*: "customSend" :*/
fileprivate let str_behindName:[UInt8] = [0x17,0x29,0x27,0x28,0x23,0x21,0x7,0x19,0x22,0x18]

fileprivate func topSelf(color num: UInt8) -> UInt8 {
    let value = Int(num) + 76
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let str_normalData:String = "PIC"

/*: "VideoMsg" :*/
fileprivate let str_secondValue:String = "session let playerVideoMsg"

/*: "video" :*/
fileprivate let str_youLabName:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let str_normalIconContent:[UInt8] = [0x4e,0x42,0x5b,0x48,0x5f,0x64,0x40,0x4a]

private func varClass(by num: UInt8) -> UInt8 {
    return num ^ 45
}

/*: "videoUrl" :*/
fileprivate let str_messageTitleText:[UInt8] = [0x6c,0x72,0x55,0x6f,0x65,0x64,0x69,0x76]

/*: "duration" :*/
fileprivate let str_incomeTitle:[UInt8] = [0xdc,0xed,0xea,0xd9,0xec,0xe1,0xe7,0xe6]

fileprivate func addReturn(field num: UInt8) -> UInt8 {
    let value = Int(num) + 136
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "len" :*/
fileprivate let str_labSkirtText:[UInt8] = [0xa2,0xab,0xa0]

/*: "map" :*/
fileprivate let str_firstListenName:String = "maview"

/*: "latlon" :*/
fileprivate let str_imageToLayerValue:[UInt8] = [0xa5,0x9a,0xad,0xa5,0xa8,0xa7]

fileprivate func buttonFrom(leading num: UInt8) -> UInt8 {
    let value = Int(num) - 57
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "messageType" :*/
fileprivate let str_beanCookieName:String = "messname"
fileprivate let str_bagTitle:String = "model app screen elseeType"

/*: "MapMsg" :*/
fileprivate let str_attentionFillData:String = "bottom pause priceMapMs"
fileprivate let str_cameraRequestName:String = "G"

/*: "Documents/User/voice/" :*/
fileprivate let str_textCellValue:[Character] = ["D","o","c","u","m","e","n","t","s"]
fileprivate let str_listenerContentTitle:String = "open number self/User"

/*: "Documents/User/record/" :*/
fileprivate let str_productName:String = "Documodel to moment data"
fileprivate let str_regularName:String = "s/Usapp at player guard"
fileprivate let str_withStatusText:[Character] = ["r","d","/"]

/*: "msgIncome" :*/
fileprivate let str_micContent:String = "data individual cell to viewmsgIn"
fileprivate let str_dirtValue:String = "cleadingme"

/*: "mfBean" :*/
fileprivate let str_giftViewData:[UInt8] = [0x6e,0x61,0x65,0x42,0x66,0x6d]

/*: "WantGift" :*/
fileprivate let str_callResultViewText:String = "color self if label labelWantGift"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let str_errorLoopValue:String = "_Prireturn text view"
fileprivate let str_normalTitle:String = "max var self guard contentChatT"
fileprivate let str_managerUserYearContent:String = "ipsTistring record channel"

/*: "msgId" :*/
fileprivate let str_conversationValue:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let str_managerTitle:String = "fragmentta"

/*: "attachSysMsg" :*/
fileprivate let str_shareTitle:[Character] = ["a","t","t","a","c","h","S","y","s","M","s","g"]

/*: "intimatePhoto" :*/
fileprivate let str_appValue:String = "indomainima"
fileprivate let str_equalUserValue:[Character] = ["t","e","P","h","o","t","o"]

/*: "&type= :*/
fileprivate let str_permissionTitle:[Character] = ["&","t","y","p","e","="]

/*: _ :*/
fileprivate let str_backValue:[Character] = ["_"]

/*: "totalIntimate" :*/
fileprivate let str_topImageData:[Character] = ["t","o","t","a","l"]
fileprivate let str_whoCenterText:String = "name"
fileprivate let str_managerData:[Character] = ["n","t","i","m","a","t","e"]

/*: "uid" :*/
fileprivate let str_equalTopVoiceTitle:[UInt8] = [0x99,0x85,0x88]

private func attentionTable(intimate num: UInt8) -> UInt8 {
    return num ^ 236
}

/*: "intimate" :*/
fileprivate let str_firstContent:[UInt8] = [0x26,0x2b,0x31,0x26,0x2a,0x1e,0x31,0x22]

fileprivate func upChecked(prompt num: UInt8) -> UInt8 {
    let value = Int(num) - 189
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let str_replyTitle:[UInt8] = [0x2e,0x6c,0x65,0x76,0x65,0x6c,0x20,0x79,0x63,0x61,0x6d,0x69,0x74,0x6e,0x69,0x20,0x66,0x6f,0x20,0x6b,0x63,0x61,0x6c,0x20,0x72,0x75,0x6f,0x79,0x20,0x6f,0x74,0x20,0x65,0x75,0x64,0x20,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x20,0x65,0x68,0x74,0x20,0x65,0x73,0x75,0x20,0x74,0x27,0x6e,0x61,0x43]

/*: "giftNum" :*/
fileprivate let str_clearName:String = "to empty playergiftNum"

/*: "pkgItemsetId" :*/
fileprivate let str_languageName:String = "pkgItop user of direction"
fileprivate let str_blockTitle:String = "to"

/*: "from" :*/
fileprivate let str_channelOriginValue:String = "froindex"

/*: "wantGiftId" :*/
fileprivate let str_behaviorTitle:String = "wyellownt"

/*: "WantGiftMsg" :*/
fileprivate let str_dataTargetTitle:String = "self up price list leftWantGift"
fileprivate let str_signData:String = "Msgup video"

/*: "gift" :*/
fileprivate let str_equalSharedTitle:String = "GIFT"

/*: "View info" :*/
fileprivate let str_priceValue:String = "information title view labelView in"
fileprivate let str_underValue:String = "FO"

/*: "Notes" :*/
fileprivate let str_pendingMainText:String = "Notesif path manager selected first"

/*: "Report" :*/
fileprivate let str_noteConfirmData:[Character] = ["R","e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let str_intimateName:[Character] = ["V","i","e","w"," ","r","e","a","d"," ","r","e","c","e","i","p","t","s"]
fileprivate let str_popText:String = "?"

/*: "He" :*/
fileprivate let str_quoteModelInValue:[Character] = ["H","e"]

/*: "She" :*/
fileprivate let str_sizeVoiceName:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let str_exText:String = "%@ iself name in view not"
fileprivate let str_modelLeadingData:String = "frame effect inside viewing to"
fileprivate let str_topTitle:String = "VERSION"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let str_timeName:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," ","a","t"," ","l","e","a","s","t"," ","5"," "]
fileprivate let str_pathEqualText:[Character] = ["s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let str_maleName:String = "Uploloop self"
fileprivate let str_managerName:String = "aipathe"
fileprivate let str_arrayTotalData:[Character] = ["d"]

/*: "播放状态: :*/
fileprivate let str_labelPlayerData:String = "request状态:"

/*: "c2c_" :*/
fileprivate let str_userReasonData:String = "c2c_make range"

/*: "msg" :*/
fileprivate let str_frameVoiceTitle:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let str_pathElementData:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let str_succeedValue:[UInt8] = [0x6c,0x6c,0x61,0x63,0x20,0x61,0x20,0x40,0x25,0x20,0x65,0x76,0x69,0x67,0x20,0x6f,0x74,0x20,0x65,0x72,0x75,0x74,0x61,0x65,0x66,0x20,0x74,0x61,0x68,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x68,0x74,0x20,0x65,0x73,0x75,0x20,0x6e,0x61,0x63,0x20,0x75,0x6f,0x59]

/*: "cellData" :*/
fileprivate let str_equalPostName:[Character] = ["c"]
fileprivate let str_fatalText:String = "return true selfellData"

/*: "System notification" :*/
fileprivate let str_logTitleValue:String = "transaction to numberSyst"
fileprivate let str_fillSourceContent:String = "player"
fileprivate let str_managerText:String = "ifiselected"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TaskChatController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import UIKit
import UIKit

//: import AVFAudio
import AVFAudio

//: import Photos
import Photos

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum PlayerLabelFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class TaskChatController: EqualVideoReactiveCompatible {
	var compartmentMagnitude: Double = -3.3
	var logLabelArray: [AnyHashable] = []
	var pathQuantity: Double = -35.3
	var timeArray: [AnyHashable] = []
	var listenValueSum: Double = -15.8
	var receiveArray: [AnyHashable] = []

    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = AppMinThen()
    private var userWrap = AppMinThen() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = PlayerLabelFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: AppMinThen = AppMinThen()) {
    init(chatID: String, isFrom: PlayerLabelFromEnum = .Normal, userWrap: AppMinThen = AppMinThen()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        TaskChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: SessionChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        SessionChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == ManagerReactiveCompatible.getXiaoMiID() {
        if targetId == ManagerReactiveCompatible.heterosexuality() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == ManagerReactiveCompatible.getCustomerServiceID() {
        } else if targetId == ManagerReactiveCompatible.facilityGetWhoremonger() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_scaleName.map{overTheQuantityerMedicine(price: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SubLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
            if SubLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = HadithShieldView(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        fiftyPercentAnimation()
        //: createUI()
        domainTitle()
        //: createUIConstraints()
        free()
        //: bindInteraction()
        fileApp()
        //: addTapGestureRecognizer()
        recordContent()
        //: func__requestChatInfo()
        tingClick()
        //: func__getFillDraftText()
        velleity()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        RangeManagerDelegate.shared.combine()
        //: func__addVoiceNotification()
        towardFrom()
        //: addReadPeceiptNotification()
        titleDismiss()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            effectData()
            //: func__TipsNotification()
            viewWith()
            //: if SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue {
                //: func__setReplyTipsView()
                chat()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        cornerUser(isAdd: true)
    
            if (notesTopView.semanticContentAttribute == .forceRightToLeft) && (notesTopView.autoresizingMask == .flexibleWidth) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let coverView = LabPushView(frame: notesTopView.bounds.offsetBy(dx: CGFloat(0), dy: CGFloat(79)))
            coverView.textOn = giftEffectView.giftAnimatView.needStopAll
            coverView.upSum = self.msgInputView.duration
            coverView.showContent = self.msgInputView.chatRoomID
            
            coverView.priceInterval = { [self] coinCount in
            self.listenValueSum = coinCount
            
            var coverView = msgInputView.inputTextView.placeholderLeftOffset
                coverView += 1
                if Int(coverView) > -99 {
                    coverView = coverView - 1
                }
            if coverView > self.listenValueSum {
                self.listenValueSum = coverView
            }
            
            self.listenValueSum *= 8
            return self.listenValueSum
            }
            coverView.streamArray = { [self] barArray in
            self.receiveArray = barArray
            
            guard var value = self.receiveArray as? [String] else {
                return nil
            }
            return value
            }
                notesTopView.addSubview(coverView)
            }

	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        cornerUser(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            chromaticColor()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    
		if var titleValue = videoCallManager.uid { 
	            if (sendGreetBtn.restorationIdentifier != nil) && (sendGreetBtn.isFirstResponder) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let atCell = LabPushView()
	            atCell.textOn = self.giftEffectView.isPlayAnimat
	            atCell.upSum = userWrap.onlineStatus
	            atCell.showContent = titleValue
	            
	            atCell.priceInterval = { [self] coinCount in
	            self.compartmentMagnitude = coinCount
	            
	            var atCell = msgInputView.inputTextView.placeholderLeftOffset
	                atCell -= 1
	                if Int(atCell) > -88 {
	                    atCell = atCell + 1
	                }
	            if atCell > self.compartmentMagnitude {
	                self.compartmentMagnitude = atCell
	            }
	            
	            self.compartmentMagnitude *= 7
	            return self.compartmentMagnitude
	            }
	            atCell.streamArray = { [self] barArray in
	            self.logLabelArray = barArray
	            
	            guard var value = self.logLabelArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	                sendGreetBtn.addSubview(atCell)
	            }
	
		}
	}

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (str_levelViewName.replacingOccurrences(of: "hidden", with: "5") + str_timeValue.replacingOccurrences(of: "tool", with: "5")))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        msgVC.tableView.register(BorderMsgCell.self, forCellReuseIdentifier: (String(str_makeName.suffix(5)) + "ngCha" + String(str_liveNameData.suffix(5)) + "MsgC" + String(str_cancelName.suffix(5))))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(TalkingCellThen.self, forCellReuseIdentifier: (String(str_playerEqualRenderName) + String(str_theoryValue) + String(str_imageNameValue.prefix(6))))
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(CostOfLivingMsgCell.self, forCellReuseIdentifier: (String(str_touchName) + String(str_appText.suffix(2)) + String(str_jumpData.suffix(7))))
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        msgVC.tableView.register(AddMsgCell.self, forCellReuseIdentifier: (String(str_whenName.prefix(5)) + String(str_sexValue.suffix(6)) + "GiftM" + String(str_frontTitle.prefix(8))))
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(ConfinementViewCell.self, forCellReuseIdentifier: (String(str_indexPageLetText) + String(str_recommendToName.suffix(5))))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(SampleSearchedMsgCell.self, forCellReuseIdentifier: (String(str_viewStatusValue.suffix(5)) + String(str_actualTitle)))
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        msgVC.tableView.register(PossibilityMsgCell.self, forCellReuseIdentifier: (String(str_timeData) + String(str_directionValue.suffix(6)) + "ellID"))
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(LengthThen.self, forCellReuseIdentifier: (String(str_inputBarData.prefix(5)) + "ngChat" + String(str_itemViewData.prefix(7)) + String(str_renderValue)))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(DismissMessageCell.self, forCellReuseIdentifier: String(bytes: str_colorShareTitle.map{addName(custom: $0)}, encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_messageListData.suffix(6)) + "mMessag" + String(str_playerName.suffix(5)) + "_ReuseId"))

        //: msgVC.tableView.register(TalkingChatMapMsgCell.self, forCellReuseIdentifier: "TalkingChatMapMsgCellID")
        msgVC.tableView.register(ShareReactiveCompatible.self, forCellReuseIdentifier: (String(str_itemCellData) + str_transformData.replacingOccurrences(of: "item", with: "ha") + String(str_maleText.prefix(5)) + "gCellID"))

        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: EqualMatchDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = EqualMatchDelegate()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: BorderPlayerThen = {
        //: var type: InputViewType = .PrivateChat
        var type: PathViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = BorderPlayerThen(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.magnitudeimateBy(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(str_makeTitle.prefix(5)) + str_viewPathTitle.replacingOccurrences(of: "with", with: "F")))!, .font: UIFont.magnitudeimateBy(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_intimateData.prefix(8))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(pushByClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: OffdDoorReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = OffdDoorReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: FullMoonTitleView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = FullMoonTitleView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_executeData.suffix(6)) + str_clickText.replacingOccurrences(of: "running", with: "F"))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(str_areaItemText) + String(str_totalData)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.dataViewPush().cgColor
        btn.layer.borderColor = UIColor.dataViewPush().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(sawdustSaloon), for: .touchUpInside)
        //: btn.isHidden = !SubLabelReactiveCompatible.share.appUserConfigMode.showDirectGreet || SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !SubLabelReactiveCompatible.share.appUserConfigMode.showDirectGreet || SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue || SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: EnabledDoorReactiveCompatible? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = EnabledDoorReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: LetterThen = {
        //: let v = TalkingSelectTranslationView()
        let v = LetterThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.matrix(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: TalkingBorderVanguardView = {
        //: let v = TalkingTranslationResultView()
        let v = TalkingBorderVanguardView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.untilUp(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_executeData.suffix(6)) + str_clickText.replacingOccurrences(of: "running", with: "F"))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(str_answerName.suffix(6)) + " Pho" + str_resultContent.lowercased()).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.dataViewPush().cgColor
        btn.layer.borderColor = UIColor.dataViewPush().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(buildWrite), for: .touchUpInside)
        //: btn.isHidden = SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue || SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: PathGamesView = {
        //: let v = TalkingLiveRoomGamesView()
        let v = PathGamesView()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: WaitressThen = {
        //: let view = TalkingPrivateBannerView.init()
        let view = WaitressThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: TempScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = TempScreenView()
        //: v.isHidden = (SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: SearchedThen = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = SearchedThen()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.springEquinox()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension TaskChatController: SessionReactiveCompatible {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func freamCapacity(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.allColor(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension TaskChatController: TMessageControllerDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func sawdustSaloon() {
        //: SessionChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        SessionChatManager.photoHandler(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.barLog(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.garrison(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func buildWrite() {
        //: SessionChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        SessionChatManager.fromBy(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = HeaderDataSource()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().overMake(urlStr: dict?[(str_addStreamName.lowercased())] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(str_elementText) + String(str_inputDataText))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = SendJsonModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: str_removeLastName.map{imageSelected(make: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(str_toCenterValue.suffix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.titleTheory(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: SelectedThen.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: SelectedThen.self), self.userWrap.uid.count > 0 {
            //: cellData.msgModel.userInfoModel = self.userWrap
            cellData.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(str_randomManagerData.suffix(7)) + String(str_layerTitle))].stringValue == (String(str_greetName) + String(str_oldData.suffix(4)) + "Custom") { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(str_byViewContent) + str_lastText.replacingOccurrences(of: "share", with: "io"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData.msgModel.user.uid = jsonDic[(str_countManagerText.lowercased())][(String(str_domainData))][(String(str_backName.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData.msgModel
                let msgModel = cellData.msgModel
                //: cellData = MagnitudeernationalismReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = MagnitudeernationalismReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData.msgModel = msgModel
                cellData.msgModel = msgModel
            }
        }
        //: if cellData.isKind(of: TempRoomCellData.self) == true || cellData.isKind(of: ShareThen.self) == true || cellData.isKind(of: MagnitudeernationalismReactiveCompatible.self) == true || cellData.isKind(of: VideoCellData.self) == true || cellData.isKind(of: AcrossCellData.self) == true {
        if cellData.isKind(of: TempRoomCellData.self) == true || cellData.isKind(of: ShareThen.self) == true || cellData.isKind(of: MagnitudeernationalismReactiveCompatible.self) == true || cellData.isKind(of: VideoCellData.self) == true || cellData.isKind(of: AcrossCellData.self) == true {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
        //: SessionChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
        SessionChatManager.thanClick(cellData: cellData, msgView: self.msgView)

        // 女性记录最新回复时间, 刷新表格
        //: SessionChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
        SessionChatManager.toPublish(cellData.msgIncome, data.timestamp, msgView)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = ear(data: data)

        //: if data.isKind(of: MagnitudeernationalismReactiveCompatible.self) {
        if data.isKind(of: MagnitudeernationalismReactiveCompatible.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = BorderMsgCell(style: .default, reuseIdentifier: (String(str_makeName.suffix(5)) + "ngCha" + String(str_liveNameData.suffix(5)) + "MsgC" + String(str_cancelName.suffix(5))))
            //: let cellData = data as! MagnitudeernationalismReactiveCompatible
            let cellData = data as! MagnitudeernationalismReactiveCompatible

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.load() {
                //: if cellData.msgModel.quoteMsgInfo.uid == SubLabelReactiveCompatible.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == SubLabelReactiveCompatible.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = SubLabelReactiveCompatible.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = SubLabelReactiveCompatible.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: ShareThen.self) {
        if data.isKind(of: ShareThen.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = TalkingCellThen(style: .default, reuseIdentifier: (String(str_playerEqualRenderName) + String(str_theoryValue) + String(str_imageNameValue.prefix(6))))
            //: let cellData = data as! ShareThen
            let cellData = data as! ShareThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: VideoCellData.self) {
        if data.isKind(of: VideoCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = CostOfLivingMsgCell(style: .default, reuseIdentifier: (String(str_touchName) + String(str_appText.suffix(2)) + String(str_jumpData.suffix(7))))
            //: let cellData = data as! VideoCellData
            let cellData = data as! VideoCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ServerCellData.self) {
        if data.isKind(of: ServerCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = AddMsgCell(style: .default, reuseIdentifier: (String(str_whenName.prefix(5)) + String(str_sexValue.suffix(6)) + "GiftM" + String(str_frontTitle.prefix(8))))
            //: let cellData = data as! ServerCellData
            let cellData = data as! ServerCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: RangeCellData.self) {
        if data.isKind(of: RangeCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = ConfinementViewCell(style: .default, reuseIdentifier: (String(str_indexPageLetText) + String(str_recommendToName.suffix(5))))
            //: let cellData = data as! RangeCellData
            let cellData = data as! RangeCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = ProfessionalTransformable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.subordinate(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(str_hiddenData.prefix(7)) + String(str_labelValue)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: TempRoomCellData.self) {
        if data.isKind(of: TempRoomCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = LengthThen(style: .default, reuseIdentifier: (String(str_inputBarData.prefix(5)) + "ngChat" + String(str_itemViewData.prefix(7)) + String(str_renderValue)))
            //: let cellData = data as! TempRoomCellData
            let cellData = data as! TempRoomCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: SelectedThen.self) {
        if data.isKind(of: SelectedThen.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = PossibilityMsgCell(style: .default, reuseIdentifier: (String(str_timeData) + String(str_directionValue.suffix(6)) + "ellID"))
            //: let cellData = data as! SelectedThen
            let cellData = data as! SelectedThen
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: NoCellData.self) {
        if data.isKind(of: NoCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = SampleSearchedMsgCell(style: .default, reuseIdentifier: (String(str_viewStatusValue.suffix(5)) + String(str_actualTitle)))
            //: let cellData = data as! NoCellData
            let cellData = data as! NoCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ManagerImageReactiveCompatible.self) {
        if data.isKind(of: ManagerImageReactiveCompatible.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = DismissMessageCell(style: .default, reuseIdentifier: String(bytes: str_colorShareTitle.map{addName(custom: $0)}, encoding: .utf8)!)
            //: let cellData = data as! ManagerImageReactiveCompatible
            let cellData = data as! ManagerImageReactiveCompatible
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: AcrossCellData.self) {
        if data.isKind(of: AcrossCellData.self) {
            //: let cell = TalkingChatMapMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMapMsgCellID")
            let cell = ShareReactiveCompatible(style: .default, reuseIdentifier: (String(str_itemCellData) + str_transformData.replacingOccurrences(of: "item", with: "ha") + String(str_maleText.prefix(5)) + "gCellID"))
            //: let cellData = data as! AcrossCellData
            let cellData = data as! AcrossCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != ManagerReactiveCompatible.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != ManagerReactiveCompatible.facilityGetWhoremonger() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: TalkingPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            TalkingPushManager.share.equalAcross(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: TalkingCellThen.self) { // 图片
            //: let imageCellData = cell.messageData as! ShareThen
            let imageCellData = cell.messageData as! ShareThen
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                //: let needAlert = imageCellData.direction == .MsgDirectionIncoming && imageCellData.msgModel.msgInfo.intimatePhoto.lockStatus == 1
                let needAlert = imageCellData.direction == .MsgDirectionIncoming && imageCellData.msgModel.msgInfo.intimatePhoto.lockStatus == 1
                //: let num = SessionChatManager.checkCanPreview(isImg: true, needAlert: needAlert)
                let num = SessionChatManager.overSendManager(isImg: true, needAlert: needAlert)
                /// 需要解锁的照片
                //: if num == 0 && needAlert {
                if num == 0 && needAlert {
                    //: return
                    return
                }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId, preNum: num)
                let vc = ExposureViewController(imageCellData: imageCellData, toUid: self.targetId, preNum: num)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, _, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton, type: 1)
                        self.foolishnessType(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_tabTitle, type: 1)
                        //: return
                        return
                    }
                    //: SubLabelReactiveCompatible.share.loginUserMode.updateResidualNum(type: 1, num: max(0, num - 1))
                    SubLabelReactiveCompatible.share.loginUserMode.variation(type: 1, num: max(0, num - 1))
//                    self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
//                    let dataDict = result as! Dictionary<String, Any>
//                    self.unlockPhotoOrVideoTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                choppingBlock()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(kLet_countContent)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = VendorPhotoModel()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = LightMeasurable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [VendorPhotoModel] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = ConversationViewDelegate(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                choppingBlock()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: CostOfLivingMsgCell.self) { // 视频
            //: let videoCellData = cell.messageData as! VideoCellData
            let videoCellData = cell.messageData as! VideoCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = HaltViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                choppingBlock()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                //: let needAlert = videoCellData.direction == .MsgDirectionIncoming && videoCellData.msgModel.msgInfo.video.lockStatus == 1
                let needAlert = videoCellData.direction == .MsgDirectionIncoming && videoCellData.msgModel.msgInfo.video.lockStatus == 1
                //: let num = SessionChatManager.checkCanPreview(isImg: false, needAlert: needAlert)
                let num = SessionChatManager.overSendManager(isImg: false, needAlert: needAlert)
                //: if num == 0 && needAlert {
                if num == 0 && needAlert {
                    //: return
                    return
                }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId, preNum: num)
                let vc = NoRecognizerDelegate(videoCellData: videoCellData, toUid: self.targetId, preNum: num)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, _, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton, type: 2)
                        self.foolishnessType(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_tabTitle, type: 2)
                        //: return
                        return
                    }
                    //: SubLabelReactiveCompatible.share.loginUserMode.updateResidualNum(type: 2, num: max(0, num - 1))
                    SubLabelReactiveCompatible.share.loginUserMode.variation(type: 2, num: max(0, num - 1))

//                    self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
//                    let dataDict = result as! Dictionary<String, Any>
//                    self.unlockPhotoOrVideoTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                choppingBlock()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: LengthThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! LengthThen
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! TempRoomCellData, cellView: audioCell)
            touchDoingSpring(cellData: audioCell.cellData as! TempRoomCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: BorderMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! BorderMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_countManagerText.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_factorData))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(str_factorData))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(str_makeText.suffix(6)) + "rsonAuth") {
                    //: if SubLabelReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if SubLabelReactiveCompatible.share.loginUserMode.isTPAuth == EqualEquatable.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if SubLabelReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if SubLabelReactiveCompatible.share.loginUserMode.isTPAuth == EqualEquatable.unknown.rawValue {
                        //: TalkingPushManager.share.func__pushUserVerifyController(toast: nil)
                        TalkingPushManager.share.titleUp(toast: nil)
                        //: } else if SubLabelReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if SubLabelReactiveCompatible.share.loginUserMode.isTPAuth == EqualEquatable.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = EqualProfessionalRecognizerDelegate()
                        //: TalkingPushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        TalkingPushManager.share.constraintVc()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: TalkingPushManager.share.func__pushUserVerifyController(toast: nil)
                        TalkingPushManager.share.titleUp(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<InsideJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    TalkingPushManager.share.giftBy(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(str_randomManagerData.suffix(7)) + String(str_layerTitle))] as? String == (String(str_greetName) + String(str_oldData.suffix(4)) + "Custom") { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(str_domainData))][(String(str_hiddenText.suffix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    actionTo()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    progressScreenView()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: SampleSearchedMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! SampleSearchedMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_countManagerText.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_factorData))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<InsideJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    TalkingPushManager.share.giftBy(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: PossibilityMsgCell.self) {
            //: TalkingPushManager.share.func__pushToUserDetailVC(uid: targetId)
            TalkingPushManager.share.equalAcross(uid: targetId)
            //: }else if cell.isKind(of: TalkingChatMapMsgCell.self) {
        } else if cell.isKind(of: ShareReactiveCompatible.self) {
            //: let mapCell = cell as! TalkingChatMapMsgCell
            let mapCell = cell as! ShareReactiveCompatible
            //: let mapCellData = mapCell.cellData as! AcrossCellData
            let mapCellData = mapCell.cellData as! AcrossCellData
            //: self.func__showLocationView(latlon: mapCellData.msgModel.msgInfo.latlon, lookSelf: (mapCellData.direction == .MsgDirectionOutgoing))
            self.isCall(latlon: mapCellData.msgModel.msgInfo.latlon, lookSelf: mapCellData.direction == .MsgDirectionOutgoing)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: BorderCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: TalkingCellThen.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! TalkingCellThen
            //: cellData = imageCell.cellData as! ShareThen
            cellData = imageCell.cellData as! ShareThen
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: CostOfLivingMsgCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! CostOfLivingMsgCell
            //: cellData = videoCell.cellData as! VideoCellData
            cellData = videoCell.cellData as! VideoCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: LengthThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! LengthThen
            //: cellData = audioCell.cellData as! TempRoomCellData
            cellData = audioCell.cellData as! TempRoomCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: BorderMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! BorderMsgCell
            //: cellData = textCell.cellData as! MagnitudeernationalismReactiveCompatible
            cellData = textCell.cellData as! MagnitudeernationalismReactiveCompatible
            //: } else if cell .isKind(of: TalkingChatMapMsgCell.self) {
        } else if cell.isKind(of: ShareReactiveCompatible.self) {
            //: let mapCell = cell as! TalkingChatMapMsgCell
            let mapCell = cell as! ShareReactiveCompatible
            //: cellData = mapCell.cellData as! AcrossCellData
            cellData = mapCell.cellData as! AcrossCellData
        }

        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: VideoCellData.self) {
                if reCellData!.isKind(of: VideoCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    recognize(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    actionText(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
        //: msgInputView.quoteModel = SessionChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = SessionChatManager.tipId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
        //: SessionChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        SessionChatManager.total1(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TUIMessageCellData) -> TUIMessageCellData {
    private func ear(data: TUIMessageCellData) -> TUIMessageCellData {
        //: let isSelf = data.msgModel.user.uid == Int(SubLabelReactiveCompatible.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(SubLabelReactiveCompatible.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if SubLabelReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if SubLabelReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = SubLabelReactiveCompatible.share.loginUserMode.headPic!
                data.msgModel.user.headPic = SubLabelReactiveCompatible.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = SubLabelReactiveCompatible.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = SubLabelReactiveCompatible.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension TaskChatController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func deedWithinResponse() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!: (String(str_sexTitle))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(str_colorRowLetData.lowercased()): (String(str_kitName))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_domainData)): msgInfo, String(bytes: str_awakeTitle.map{comeBack(background: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
        self.actionText(msgData: cellData, reSend: false)
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func garrison(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: str_modelRoomData.map{$0^30}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: str_modelRoomData.map{$0^30}, encoding: .utf8)!: content, (String(str_labelData)): greetId, String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!: (String(str_sexTitle))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: str_colorSizeContent.reversed(), encoding: .utf8)!: content, (String(str_labelData)): greetId, (str_colorRowLetData.lowercased()): (str_acrossName.replacingOccurrences(of: "margin", with: "T") + String(str_voiceValue.prefix(5)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(str_domainData)): msgInfo, String(bytes: str_awakeTitle.map{comeBack(background: $0)}, encoding: .utf8)!: paramsDic]
            //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.actionText(msgData: cellData, reSend: false)
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            DirectorEraseReactiveCompatible.shared.weltUserHandle(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.barLog(showMsg: String(bytes: str_topCameraValue.map{countUser(receive: $0)}, encoding: .utf8)!)
                        //: SessionChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        SessionChatManager.frameDown(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(str_makeContent))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!: (str_leadingName.lowercased()),
                               //: "audioId": greetId,
                               (String(str_sectionName.prefix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: str_userLayerValue.map{$0^144}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (str_picValue.replacingOccurrences(of: "coin", with: "a") + "oSandbo" + str_bagName.replacingOccurrences(of: "store", with: "x")): model.unZipFilePath,
                               //: "senderId": SubLabelReactiveCompatible.share.loginUserMode.userID]
                               String(bytes: str_onAddTitle.map{modeLocation(the: $0)}, encoding: .utf8)!: SubLabelReactiveCompatible.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(str_sectionName.prefix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: str_userLayerValue.map{$0^144}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (str_picValue.replacingOccurrences(of: "coin", with: "a") + "oSandbo" + str_bagName.replacingOccurrences(of: "store", with: "x")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (str_colorRowLetData.lowercased()): (String(str_itemBackValue.suffix(7)) + str_collectName.replacingOccurrences(of: "object", with: "g"))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(str_domainData)): msgInfo, String(bytes: str_awakeTitle.map{comeBack(background: $0)}, encoding: .utf8)!: paramsDic]
                //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.actionText(msgData: cellData, reSend: false)
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func untilUp(msgStr: String, quoteModel: AdpressedModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            barLog(showMsg: (str_popName + " send" + String(str_sectionTitle.suffix(4)) + "nk m" + str_scaleHomeName.replacingOccurrences(of: "lab", with: "e") + str_actualName.replacingOccurrences(of: "manager", with: "e")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: str_modelRoomData.map{$0^30}, encoding: .utf8)!: temStr, String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!: (String(str_sexTitle))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: str_colorSizeContent.reversed(), encoding: .utf8)!: temStr, (str_colorRowLetData.lowercased()): (str_acrossName.replacingOccurrences(of: "margin", with: "T") + String(str_voiceValue.prefix(5)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(str_domainData)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(str_bindName))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(str_uniformTitle.suffix(7)) + String(str_labelName))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: str_awakeTitle.map{comeBack(background: $0)}, encoding: .utf8)!] = paramsDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
        cellData.msgModel.quoteMsgInfo = quoteModel ?? AdpressedModel()

        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        actionText(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func titleTheory(img: UIImage, sendModel: SendJsonModel) {
        //: let imagePath = ShareThen.getChatImagePath(image: img)
        let imagePath = ShareThen.addImage(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: str_currentIndexName.map{trueStage(detail: $0)}, encoding: .utf8)!: (String(str_giftModelContent.suffix(5)) + String(str_whiteValue)), String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!: (String(str_timeContent)), String(bytes: str_rowValue.map{$0^178}, encoding: .utf8)!: imagePath, String(bytes: str_behindName.map{topSelf(color: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(str_normalData.lowercased()): imagePath,
                                        //: "type": "ImgMsg",
                                        (str_colorRowLetData.lowercased()): (String(str_giftModelContent.suffix(5)) + String(str_whiteValue)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_removeLastName.map{imageSelected(make: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_toCenterValue.suffix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_domainData)): msgInfo, String(bytes: str_awakeTitle.map{comeBack(background: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        actionText(msgData: cellData, reSend: false)
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TUIMessageCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func recognize(msgData: TUIMessageCellData?, sendModel: SendJsonModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: str_currentIndexName.map{trueStage(detail: $0)}, encoding: .utf8)!: (String(str_secondValue.suffix(8))), String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!: (String(str_youLabName)), String(bytes: str_behindName.map{topSelf(color: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(str_colorRowLetData.lowercased()): (String(str_secondValue.suffix(8))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: str_normalIconContent.map{varClass(by: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: str_messageTitleText.reversed(), encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: str_incomeTitle.map{addReturn(field: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_removeLastName.map{imageSelected(make: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_toCenterValue.suffix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(str_domainData)): msgInfo, String(bytes: str_awakeTitle.map{comeBack(background: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            //: self.msgView.sendCustomMessage(uploadCellData)
            self.msgView.sendCustomMessage(uploadCellData)
            //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
            self.impairment(msgData: uploadCellData, sendModel: sendModel)
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.impairment(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        actionText(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func inheritanceTime(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!: (str_leadingName.lowercased()),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: str_userLayerValue.map{$0^144}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (str_picValue.replacingOccurrences(of: "coin", with: "a") + "oSandbo" + str_bagName.replacingOccurrences(of: "store", with: "x")): voicePath,
                       //: "senderId": SubLabelReactiveCompatible.share.loginUserMode.userID]
                       String(bytes: str_onAddTitle.map{modeLocation(the: $0)}, encoding: .utf8)!: SubLabelReactiveCompatible.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(str_picValue.replacingOccurrences(of: "coin", with: "a") + "oSandbo" + str_bagName.replacingOccurrences(of: "store", with: "x")): voicePath,
                         //: "type": "AudioMsg",
                         (str_colorRowLetData.lowercased()): (String(str_itemBackValue.suffix(7)) + str_collectName.replacingOccurrences(of: "object", with: "g")),
                         //: "len": "\(voiceTime)"]
                         String(bytes: str_labSkirtText.map{$0^206}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_domainData)): msgInfo, String(bytes: str_awakeTitle.map{comeBack(background: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        actionText(msgData: cellData, reSend: false)
    }

    //: func func__sendLocationMsg(latlon: String) {
    func serverDirectBaseFunc(latlon: String) {
        //: let msgInfo = ["msgType": "map",
        let msgInfo = [String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!: (str_firstListenName.replacingOccurrences(of: "view", with: "p")),
                       //: "latlon": latlon,
                       String(bytes: str_imageToLayerValue.map{buttonFrom(leading: $0)}, encoding: .utf8)!: latlon,
                       //: "messageType": 28,
                       (str_beanCookieName.replacingOccurrences(of: "name", with: "ag") + String(str_bagTitle.suffix(5))): 28,
                       //: "senderId": SubLabelReactiveCompatible.share.loginUserMode.userID] as [String : Any]
                       String(bytes: str_onAddTitle.map{modeLocation(the: $0)}, encoding: .utf8)!: SubLabelReactiveCompatible.share.loginUserMode.userID] as [String: Any]

        //: let paramsDic = ["latlon": latlon,
        let paramsDic = [String(bytes: str_imageToLayerValue.map{buttonFrom(leading: $0)}, encoding: .utf8)!: latlon,
                         //: "type": "MapMsg"] as [String : Any]
                         (str_colorRowLetData.lowercased()): (String(str_attentionFillData.suffix(5)) + str_cameraRequestName.lowercased())] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_domainData)): msgInfo, String(bytes: str_awakeTitle.map{comeBack(background: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        actionText(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func actionText(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            barLog(showMsg: kLet_eventText)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(str_backName.prefix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: VideoCellData.self) == false && msgData.isKind(of: RangeCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: VideoCellData.self) == false, msgData.isKind(of: RangeCellData.self) == false, params[(str_colorRowLetData.lowercased())] as? String != (String(str_kitName)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = TUIMsgChatManager.getMessageInsertTime()
        let recordTime = TUIMsgChatManager.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((str_normalData.lowercased())) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(str_normalData.lowercased())] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(str_normalData.lowercased())] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(str_colorRowLetData.lowercased())] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(str_itemBackValue.suffix(7)) + str_collectName.replacingOccurrences(of: "object", with: "g")) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((str_picValue.replacingOccurrences(of: "coin", with: "a") + "oSandbo" + str_bagName.replacingOccurrences(of: "store", with: "x"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(str_picValue.replacingOccurrences(of: "coin", with: "a") + "oSandbo" + str_bagName.replacingOccurrences(of: "store", with: "x"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (str_picValue.replacingOccurrences(of: "coin", with: "a") + "oSandbo" + str_bagName.replacingOccurrences(of: "store", with: "x")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(str_textCellValue) + String(str_listenerContentTitle.suffix(5)) + "/voice/")) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = kLet_detailValue + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(str_productName.prefix(4)) + "ment" + String(str_regularName.prefix(4)) + "er/reco" + String(str_withStatusText))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kLet_userContentNotData + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(str_sectionName.prefix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(str_leadingName.lowercased())] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        ViewRequestTool.equalPrivete(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(str_kitName)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.foolishnessType(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_appScreenRecordValue)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(str_labelData))) {
                    //: SessionChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    SessionChatManager.frameDown(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(str_domainData))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome = NSNumber(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(str_micContent.suffix(5)) + str_dirtValue.replacingOccurrences(of: "leading", with: "o"))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(str_micContent.suffix(5)) + str_dirtValue.replacingOccurrences(of: "leading", with: "o"))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if ConversationLiveManager.auditoryImage().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: kLet_inputText, object: nil, userInfo: [String(bytes: str_giftViewData.reversed(), encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.burr(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(str_beanCookieName.replacingOccurrences(of: "name", with: "ag") + String(str_bagTitle.suffix(5)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(str_kitName)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!] = (String(str_callResultViewText.suffix(8)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kLet_postName.paper(eventID: kLet_barFailValue)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(str_kitName)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!] = (String(str_sexTitle))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(str_domainData)): msgInfo]
                //: updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.msgView.sendCustomMessage(updateMsgData)
                self.msgView.sendCustomMessage(updateMsgData)
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(SubLabelReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(SubLabelReactiveCompatible.share.loginUserMode.userID)" + (String(str_errorLoopValue.prefix(4)) + "vate" + String(str_normalTitle.suffix(5)) + String(str_managerUserYearContent.prefix(5)) + "tle_") + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue, type != (String(str_kitName)) {
                //: self.removeReplyTipsView()
                self.straitAndNarrowNeed()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo[(String(str_conversationValue))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: SessionChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            SessionChatManager.toPublish(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((str_managerTitle.replacingOccurrences(of: "fragment", with: "to") + "lMfCoin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(str_managerTitle.replacingOccurrences(of: "fragment", with: "to") + "lMfCoin")] as! NSNumber
                //: SubLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                SubLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(str_shareTitle))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(str_shareTitle))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.errorStr(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((str_appValue.replacingOccurrences(of: "domain", with: "t") + String(str_equalUserValue))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(str_appValue.replacingOccurrences(of: "domain", with: "t") + String(str_equalUserValue))] as! [String: Any]
                //: let model = HypothesisPingModel(dic: intimatePhotoDict)
                let model = HypothesisPingModel(dic: intimatePhotoDict)
                //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                updateMsgData.msgModel.msgInfo.intimatePhoto = model
                //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(str_youLabName))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(str_youLabName))] as! [String: Any]
                //: let model = LabelModel(dic: intimateVideoDict)
                let model = LabelModel(dic: intimateVideoDict)
                //: updateMsgData.msgModel.msgInfo.video = model
                updateMsgData.msgModel.msgInfo.video = model
                //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        picture()

        //: updateSession()
        nameSession()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func nameSession() {
        //: LetterInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        LetterInfoManager.awakeRemove(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String, type: Int = 0) {
    private func foolishnessType(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent _: String, type: Int = 0) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard SubLabelReactiveCompatible.share.loginUserMode.status != 1 else {
            guard SubLabelReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    barLog(showMsg: errorStr)
                }
                //: return
                return
            }
            //: TalkingPushManager.share.func__pushToRechargeAlert(appendParams: "&type=\(type)")
            TalkingPushManager.share.totalerval(appendParams: (String(str_permissionTitle)) + "\(type)")
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.hiddenView()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: TalkingPushManager.share.func__pushToSubscribeAlert()
            TalkingPushManager.share.statusSumimate()

            //: } else if errorCode == 60003, !SubLabelReactiveCompatible.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !SubLabelReactiveCompatible.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            VideoReactiveCompatible.shared.serration()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                barLog(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension TaskChatController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func callCreate(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(str_domainData))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.burr(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func enableDisappearView() {
        //: if self.userWrap.intimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.happening(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(SubLabelReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(SubLabelReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (kLet_guideScaleData.array(forKey: kLet_displayContent)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.keepAll()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                kLet_guideScaleData.set(intimateCacheData, forKey: kLet_displayContent)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func take(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(str_countManagerText.lowercased())].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(str_domainData))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(str_topImageData) + str_whoCenterText.replacingOccurrences(of: "name", with: "I") + String(str_managerData))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.numberimateTime(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func burr(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(str_topImageData) + str_whoCenterText.replacingOccurrences(of: "name", with: "I") + String(str_managerData))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(str_topImageData) + str_whoCenterText.replacingOccurrences(of: "name", with: "I") + String(str_managerData))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.numberimateTime(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func numberimateTime(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = LetterInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = LetterInfoManager.comeQuantityoShared(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.happening(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.requestTotalimate(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.tradeName(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.tradeName(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: LetterInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        LetterInfoManager.third(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: str_equalTopVoiceTitle.map{attentionTable(intimate: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: str_firstContent.map{upChecked(prompt: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_objectData,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension TaskChatController: CoverThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func snap() {
        //: guard SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.superstratumLanguage(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func ofRange(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func targetAdd(actionType: OfVendorExpressionConvertible) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    dialog()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    equalAttribute()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                dialog()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if SubLabelReactiveCompatible.share.loginUserMode.loungePlus {
                if SubLabelReactiveCompatible.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.greetEqual(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                equalAttribute()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    quantityimate()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    equalAttribute()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                quantityimate()
            }

        //: case .Location:
        case .Location:
            //: TalkingPermissionTool.func__openLocationServiceWithBlock(true) { [weak self] isOpen in
            AccountingDataReactiveCompatible.alongDeadline(true) { [weak self] isOpen in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if isOpen {
                if isOpen {
                    //: self.func__showLocationView(latlon: nil, lookSelf: false)
                    self.isCall(latlon: nil, lookSelf: false)
                }
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.afterColor(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func equalAttribute() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.barLog(showMsg: String(bytes: str_replyTitle.reversed(), encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func writeObject(giftModel _: ProfessionalTransformable, giftNum _: String, model _: ToMemberReactiveCompatible) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func kindWith(giftModel: ProfessionalTransformable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        subordinate(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: RangeCellData?, fromStr: String?) {
    func subordinate(giftModel: ProfessionalTransformable, giftNum: String, cellData: RangeCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            barLog(showMsg: kLet_eventText)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(str_backName.prefix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(str_toCenterValue.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(str_clearName.suffix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_languageName.prefix(4)) + "temsetI" + str_blockTitle.replacingOccurrences(of: "to", with: "d"))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(str_channelOriginValue.replacingOccurrences(of: "index", with: "m"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        ViewRequestTool.pastPop(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.foolishnessType(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_recordBottomGiftValue)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.cannuliseNum(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.track(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.nameSession()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(str_domainData))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(str_topImageData) + str_whoCenterText.replacingOccurrences(of: "name", with: "I") + String(str_managerData))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(str_topImageData) + str_whoCenterText.replacingOccurrences(of: "name", with: "I") + String(str_managerData))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.numberimateTime(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kLet_postName.paper(eventID: kLet_marginMatchData)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func clickNum(giftModel: ProfessionalTransformable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kLet_postName.paper(eventID: kLet_timeName)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            barLog(showMsg: kLet_eventText)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= SubLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            equalAttribute()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(str_behaviorTitle.replacingOccurrences(of: "yellow", with: "a") + "GiftId")] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(str_colorRowLetData.lowercased())] = (String(str_dataTargetTitle.suffix(8)) + String(str_signData.prefix(3)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!: (String(str_callResultViewText.suffix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(str_domainData)): msgInfo, String(bytes: str_awakeTitle.map{comeBack(background: $0)}, encoding: .utf8)!: params]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        actionText(msgData: cellData, reSend: false)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func sizeSave(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(kLet_enterData - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func signalingDogEar(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func modelIcon(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.modelIcon(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                simpleFractionPathAnimation(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func downwardRoom() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if RangeManagerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            RangeManagerDelegate.shared.examineAcross()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension TaskChatController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func track(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(str_domainData))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!] = (str_equalSharedTitle.lowercased())
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(str_domainData))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(str_micContent.suffix(5)) + str_dirtValue.replacingOccurrences(of: "leading", with: "o"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(str_micContent.suffix(5)) + str_dirtValue.replacingOccurrences(of: "leading", with: "o"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(str_conversationValue))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        leading(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((str_managerTitle.replacingOccurrences(of: "fragment", with: "to") + "lMfCoin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(str_managerTitle.replacingOccurrences(of: "fragment", with: "to") + "lMfCoin")] as! NSNumber
            //: SubLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            SubLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: SubLabelReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.bag(needReload: false, mf_coin: SubLabelReactiveCompatible.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func effectData() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.freeBy(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func leading(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.sharedShow(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func elementCell(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.nameInterval(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func busyTargetComeTotalo() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension TaskChatController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func actionTo() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.stopComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.nameSession()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func progressScreenView() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.legume { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.nameSession()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension TaskChatController {
    //: @objc private func feedbackBtnClick() {
    @objc private func pushByClick() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = EqualThen()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        choppingBlock()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func move() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = TalkingSizeVanguardPopView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.arrayOn(cellTitleList: [(String(str_priceValue.suffix(7)) + str_underValue.lowercased()).localized, (String(str_pendingMainText.prefix(5))).localized, (String(str_noteConfirmData)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .elementColor()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: TalkingPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                TalkingPushManager.share.equalAcross(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                springEquinox()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = RangeAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.array(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func springEquinox() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = DismissViewDelegate()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.playDown(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.cellAcross(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func group() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            simpleFractionPathAnimation(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            choppingBlock()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func demonstrateVoice(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = RemoveMeasurable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.toObject(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension TaskChatController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func titleDismiss() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(duringMakeDismiss(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: kLet_maxTitle,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(astatine),
                                               //: name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION,
                                               name: kLet_failScreenTitle,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func duringMakeDismiss(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard SubLabelReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        guard SubLabelReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func astatine() {
        //: SubLabelReactiveCompatible.share.loginUserMode.loungePlus = true
        SubLabelReactiveCompatible.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func selectedWith() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            //: if SessionChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if SessionChatManager.sumry(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.actual(tipStr: (String(str_intimateName) + str_popText.capitalized).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension TaskChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func dialog() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        AccountingDataReactiveCompatible.belowTap(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = AddOnReactiveCompatible.outEndMake(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.showImg(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func eraseStatus() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        AccountingDataReactiveCompatible.funcBlockCaptureLeverUtilityStreetwiseDevice(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.showImg(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func showImg(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
            SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SendJsonModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.titleTheory(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = ConversationViewDelegate(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SendJsonModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.titleTheory(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        choppingBlock()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension TaskChatController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func quantityimate() {
        // 检测用户短视频版本是否可用
        //: guard SessionChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard SessionChatManager.approximationShortVideoUtilizerCheck(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == CheckedUTF8Initializable.male.rawValue ? "He".localized : (String(str_sizeVoiceName)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.barLog(showMsg: (String(str_exText.prefix(4)) + "s us" + String(str_modelLeadingData.suffix(6)) + "o low a " + str_topTitle.lowercased()).localizedArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.playImage()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func playImage() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = AddOnReactiveCompatible.outEndMake(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            AddOnReactiveCompatible.maintain(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.pratfallContent(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.barLog(showMsg: (String(str_timeName) + String(str_pathEqualText)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func pratfallContent(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: ModelEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = HawkerViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SendJsonModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.recognize(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        DutyProgressHUD.duringShow()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        choppingBlock()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TUIMessageCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func impairment(msgData: TUIMessageCellData, sendModel: SendJsonModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        TempSignatureProvider.shared.pic(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.barLog(showMsg: (String(str_maleName.prefix(4)) + "ad f" + str_managerName.replacingOccurrences(of: "path", with: "l") + String(str_arrayTotalData)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.recognize(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension TaskChatController {
    //: func func__addVoiceNotification() {
    func towardFrom() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(appColor),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(trunkEqual),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(andSo(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(aidNotification(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: kLet_listGiftData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(take(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: kLet_tabFailData,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func cornerUser(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            RangeManagerDelegate.shared.combine()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            RangeManagerDelegate.shared.buttonSex()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func appColor() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        cornerUser(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func trunkEqual() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        cornerUser(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func andSo(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            cornerUser(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            cornerUser(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: TempRoomCellData, cellView: TalkingChatAudioMsgCell) {
    func touchDoingSpring(cellData: TempRoomCellData, cellView: LengthThen) {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard TitleSocketManager.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.barLog(showMsg: kLet_keyMessageName)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TUIMessageCellData)!.isKind(of: TempRoomCellData.self) == false {
            if (indexModel as? TUIMessageCellData)!.isKind(of: TempRoomCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! TempRoomCellData
            let audioModel = indexModel as! TempRoomCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: cellData.voiceModel.db_voiceCacheWrap = LetterInfoManager.cache_getAudioDB(message: message)
        cellData.voiceModel.db_voiceCacheWrap = LetterInfoManager.messagePure(message: message)
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        RangeManagerDelegate.shared.examineAcross()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if RangeManagerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (str_labelPlayerData.replacingOccurrences(of: "request", with: "播放")) + "\(RangeManagerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if RangeManagerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                RangeManagerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        RangeManagerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        RangeManagerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: TempRoomCellData.self) {
                if subModel.isKind(of: TempRoomCellData.self) {
                    //: let audioModel = subModel as! TempRoomCellData
                    let audioModel = subModel as! TempRoomCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: voiceModel.db_voiceCacheWrap = LetterInfoManager.cache_getAudioDB(message: subMessage)
                    voiceModel.db_voiceCacheWrap = LetterInfoManager.messagePure(message: subMessage)
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: FirstReactiveCompatible.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    FirstReactiveCompatible.broadcast(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        RangeManagerDelegate.shared.screenArr(msgArr: waitArray)
    }
}

// MARK: - 地图相关

//: extension TalkingPrivateChatController {
extension TaskChatController {
    /// 发送地图
    //: private func func__showLocationView(latlon: String?, lookSelf: Bool) {
    private func isCall(latlon: String?, lookSelf: Bool) {
        //: let location = TalkingMapView(frame: CGRect.zero, isSelf: lookSelf, latlon: latlon)
        let location = HeadReactiveCompatible(frame: CGRect.zero, isSelf: lookSelf, latlon: latlon)
        //: location.show()
        location.dub()
        //: location.sendBlcok = { [weak self] latlon in
        location.sendBlcok = { [weak self] latlon in
            //: self?.func__sendLocationMsg(latlon: latlon)
            self?.serverDirectBaseFunc(latlon: latlon)
        }
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension TaskChatController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func tingClick() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.placeWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.garrison(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TUIMessageCellData
                let indexModel = model as? TUIMessageCellData
                //: if indexModel?.isKind(of: TempRoomCellData.self) == true || indexModel?.isKind(of: ShareThen.self) == true || indexModel?.isKind(of: MagnitudeernationalismReactiveCompatible.self) == true || indexModel?.isKind(of: VideoCellData.self) == true {
                if indexModel?.isKind(of: TempRoomCellData.self) == true || indexModel?.isKind(of: ShareThen.self) == true || indexModel?.isKind(of: MagnitudeernationalismReactiveCompatible.self) == true || indexModel?.isKind(of: VideoCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.picture()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.loadMsg()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.selectedWith()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func placeWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.clap(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.cram(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.enableDisappearView()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == SubLabelReactiveCompatible.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == SubLabelReactiveCompatible.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == SubLabelReactiveCompatible.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == SubLabelReactiveCompatible.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.cellAcross(userWrap)
    }

    //: func func__getFillDraftText() {
    func velleity() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_userReasonData.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.snap()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func chromaticColor() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_userReasonData.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension TaskChatController {
    //: func func__TipsNotification() {
    func viewWith() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(observe(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_systemData,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func observe(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: NoCellData  = userinfo["msg"] as! NoCellData
        let celldata: NoCellData = userinfo[(String(str_frameVoiceTitle))] as! NoCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func actual(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: str_modelRoomData.map{$0^30}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: str_outsideValue.map{$0^104}, encoding: .utf8)!: (String(str_pathElementData))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func loadMsg() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: SessionChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        SessionChatManager.nameImage { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.actual(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func errorStr(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.actual(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func picture() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: SessionChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        SessionChatManager.terminal(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.actual(tipStr: String(bytes: str_succeedValue.reversed(), encoding: .utf8)!.localizedArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func aidNotification(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TUIMessageCellData = dict?["cellData"] as! TUIMessageCellData
        let cellData: TUIMessageCellData = dict?[(String(str_equalPostName) + String(str_fatalText.suffix(7)))] as! TUIMessageCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension TaskChatController: ShareTipsProtocol {
    //: func func__setReplyTipsView() {
    func chat() {
        //: let key =  "\(SubLabelReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(SubLabelReactiveCompatible.share.loginUserMode.userID)" + (String(str_errorLoopValue.prefix(4)) + "vate" + String(str_normalTitle.suffix(5)) + String(str_managerUserYearContent.prefix(5)) + "tle_") + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.activeView()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            freamCapacity(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            straitAndNarrowNeed()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func seleteBlockSub(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        untilUp(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        straitAndNarrowNeed()
    }

    //: func removeReplyTipsView() {
    func straitAndNarrowNeed() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            freamCapacity(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension TaskChatController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func fiftyPercentAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kLet_deviceClickValue, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kLet_appEventDomainName - kLet_enterData, width: kLet_failureName, height: kLet_appEventDomainName)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: kLet_failureName,
                                     //: height: ScreenHeight)
                                     height: kLet_appEventDomainName)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func simpleFractionPathAnimation(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.nowController() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if EyelashEventHandler.firstBack().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_errContent, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: LayerViewController.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kLet_appEventDomainName - kLet_enterData,
                                     //: width: ScreenWidth,
                                     width: kLet_failureName,
                                     //: height: ScreenHeight)
                                     height: kLet_appEventDomainName)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if EyelashEventHandler.firstBack().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_errContent, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension TaskChatController {
    /// 创建UI
    //: private func createUI() {
    private func domainTitle() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(str_logTitleValue.suffix(4)) + "em no" + str_fillSourceContent.replacingOccurrences(of: "player", with: "t") + str_managerText.replacingOccurrences(of: "selected", with: "c") + "ation").localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func free() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? kLet_enterData : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.showRadii(width: contentView.width,
                                  //: height: contentView.height,
                                  height: contentView.height,
                                  //: corners: [.topLeft, .topRight],
                                  corners: [.topLeft, .topRight],
                                  //: cornerRadii: .init(width: 8, height: 8))
                                  cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_timeText : kLet_soundData)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_changeBarValue)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_timeText : kLet_soundData)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_changeBarValue)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + kLet_changeBarValue)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func fileApp() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.group()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.move()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.targetAdd(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kLet_postName.paper(eventID: kLet_freeName, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
