
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_fatalModeName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "TalkingGroupChatTextMsgCellID" :*/
fileprivate let str_managerName:[Character] = ["T","a","l"]
fileprivate let str_momentInfoName:[Character] = ["k","i","n","g","G","r","o","u","p","C","h","a","t","T","e","x","t","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_actionData:[Character] = ["T","a","l","k","i","n","g","C","h","a"]
fileprivate let str_localValue:String = "if post astImag"
fileprivate let str_somethingName:String = "llIDequal background"

/*: "SurfaceMessageCell" :*/
fileprivate let str_intimateTitle:String = "Talkvar index"
fileprivate let str_makeData:String = "rostatusp"
fileprivate let str_mainGestureTitle:[Character] = ["M","s","g","C","e","l","l"]

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_intimateName:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","i","p","s","M","s"]
fileprivate let str_lineMakeErrorValue:[Character] = ["g","C","e","l","l","I","D"]

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_starName:String = "cover table var in ifTSys"
fileprivate let str_layerText:String = "sageCelview in item"
fileprivate let str_centerContent:[Character] = ["l","_","R","e","u","s","e","I","d"]

/*: "roomId" :*/
fileprivate let str_hiddenValue:String = "roomIdas official image button action"

/*: "type" :*/
fileprivate let str_kitContent:[UInt8] = [0x3b,0x40,0x37,0x2c]

fileprivate func marginOfSafety(stop num: UInt8) -> UInt8 {
    let value = Int(num) - 199
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "extra" :*/
fileprivate let str_labelName:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let str_arrayVisibleValue:[UInt8] = [0x79,0x65,0x4b,0x70,0x6d,0x75,0x6a]

/*: "Send Gift" :*/
fileprivate let str_kitValue:[Character] = ["S","e","n","d"," "]
fileprivate let str_seedName:[Character] = ["G","i","f","t"]

/*: "View info" :*/
fileprivate let str_managerModelIndexValue:[Character] = ["V","i","e","w"," ","i","n","f"]
fileprivate let str_dayTitle:String = "view"

/*: "Report" :*/
fileprivate let str_modeListTitle:[Character] = ["R","e","p","o","r","t"]

/*: "uid" :*/
fileprivate let str_indicatorValue:[UInt8] = [0xeb,0xdf,0xda]

fileprivate func showShadow(error num: UInt8) -> UInt8 {
    let value = Int(num) - 118
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "name" :*/
fileprivate let str_makeImageValue:[UInt8] = [0x41,0x4e,0x42,0x4a]

private func pathScale(add num: UInt8) -> UInt8 {
    return num ^ 47
}

/*: "Can't send blank message" :*/
fileprivate let str_priceSeatText:[Character] = ["C","a","n","\'","t"," ","s","e","n"]
fileprivate let str_kitData:String = "d blancalculation self height extension video"
fileprivate let str_labelGreetData:String = "ssagview"

/*: "user" :*/
fileprivate let str_makeName:String = "usrow"

/*: "content" :*/
fileprivate let str_pathHiddenName:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let str_callToData:[Character] = ["m","s","g","T","y","p","e"]

/*: "txt" :*/
fileprivate let str_appData:String = "tfailuret"

/*: "isGroup" :*/
fileprivate let str_effectValue:String = "will during var dataisGro"
fileprivate let str_tempData:String = "contentp"

/*: "jumps" :*/
fileprivate let str_gameValue:[Character] = ["j","u","m","p","s"]

/*: "message" :*/
fileprivate let str_layerTitle:[UInt8] = [0xb5,0xbd,0xab,0xab,0xb9,0xbf,0xbd]

private func topFrame(result num: UInt8) -> UInt8 {
    return num ^ 216
}

/*: "TxtMsg" :*/
fileprivate let str_giveText:String = "number equal list toolTxtMs"
fileprivate let str_highlightText:[Character] = ["g"]

/*: "toUid" :*/
fileprivate let str_sizeDomainTitle:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let str_maleValue:String = "center i i truemsgInfo"

/*: "params" :*/
fileprivate let str_momentTitle:[UInt8] = [0x7f,0x6e,0x7d,0x6e,0x62,0x7c]

private func elementMake(result num: UInt8) -> UInt8 {
    return num ^ 15
}

/*: "contentType" :*/
fileprivate let str_createContent:[UInt8] = [0xc0,0xcc,0xcd,0xd7,0xc6,0xcd,0xd7,0xf7,0xda,0xd3,0xc6]

private func cellText(point num: UInt8) -> UInt8 {
    return num ^ 163
}

/*: "ImgMsg" :*/
fileprivate let str_jumpName:[Character] = ["I","m","g","M","s"]
fileprivate let str_replyName:[Character] = ["g"]

/*: "img" :*/
fileprivate let str_leadingName:String = "iregularg"

/*: "imageUri" :*/
fileprivate let str_requestNameValue:[UInt8] = [0x82,0x86,0x8a,0x8c,0x8e,0xbe,0x99,0x82]

private func modeCustom(succeed num: UInt8) -> UInt8 {
    return num ^ 235
}

/*: "pic" :*/
fileprivate let str_mediumStartData:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let str_hideData:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let str_broadcastValue:[Character] = ["t","o","t","a","l","M","f","C","o","i","n"]

/*: "giftId" :*/
fileprivate let str_blockName:String = "action translate target makegiftI"
fileprivate let str_managerButtonValue:String = "current"

/*: "giftNum" :*/
fileprivate let str_greetText:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let str_managerContent:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "msg" :*/
fileprivate let str_giftModelResultName:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let str_pageTitle:String = "tipequal"

/*: "gift" :*/
fileprivate let str_groupTitle:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let str_profileValue:String = "actualickactualam"
fileprivate let str_touchValue:String = "have"

/*: "headPic" :*/
fileprivate let str_topData:[UInt8] = [0x1f,0x12,0x16,0x13,0x27,0x1e,0x14]

private func valueGame(collection num: UInt8) -> UInt8 {
    return num ^ 119
}

/*: "All Numbers" :*/
fileprivate let str_labelValue:String = "in deviceAll Nu"
fileprivate let str_liveElementValue:String = "MBERS"

/*: "sendNum" :*/
fileprivate let str_topName:[Character] = ["s","e","n","d","N","u","m"]

/*: "toUser" :*/
fileprivate let str_hiddenTitle:String = "coin imagetoUser"

/*: "msgIncome" :*/
fileprivate let str_tapPointName:String = "msgInfor drop"
fileprivate let str_normalTitle:String = "ccolore"

/*: "isConnection" :*/
fileprivate let str_inviteName:String = "return make modelisConn"
fileprivate let str_topOfMessageData:[Character] = ["n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StopControllerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class StopControllerDelegate: EqualVideoReactiveCompatible {
	var timeArray: [AnyHashable] = []
	var translateArray: [AnyHashable] = []

    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = HospitalRoomMeasurable() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = TempConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = HospitalRoomMeasurable.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_fatalModeName.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SubLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
            if SubLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = HadithShieldView(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        viewUi()
        //: createUIConstraints()
        submitCellSituation()
        //: bindInteraction()
        stickVideo()
        //: addTapGestureRecognizer()
        recordContent()
        //: func__chatRoom_insertTipMsg()
        funcMessage()
        //: chatSettings()
        takeOnAndSettings()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            bottomData()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    
            if (someoneView.layer.contentsRect.size.width != 1) && (someoneView.layoutMargins.bottom == 8.25) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let viewLet = CollectionCancelView(frame: someoneView.bounds.intersection(CGRect(x: CGFloat(103.38), y: CGFloat(92), width: CGFloat(505.86), height: CGFloat(0))))
            
            viewLet.nameTheoryArray = { [self] plusArray in
            self.timeArray = plusArray
            
            guard var value = self.timeArray as? [String] else {
                return nil
            }
            return value
            }
                someoneView.addSubview(viewLet)
            }

	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.sendJoin(join: false)
        //: self.destroyTipsTimer()
        self.tips()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatTextMsgCellID")
        msgVC.tableView.register(CostReactiveCompatible.self, forCellReuseIdentifier: (String(str_managerName) + String(str_momentInfoName)))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(TalkingCellThen.self, forCellReuseIdentifier: (String(str_actionData) + String(str_localValue.suffix(5)) + "eMsgCe" + String(str_somethingName.prefix(4))))
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatGiftMsgCell")
        msgVC.tableView.register(SurfaceMessageCell.self, forCellReuseIdentifier: (String(str_intimateTitle.prefix(4)) + "ingG" + str_makeData.replacingOccurrences(of: "status", with: "u") + "ChatGift" + String(str_mainGestureTitle)))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(SampleSearchedMsgCell.self, forCellReuseIdentifier: (String(str_intimateName) + String(str_lineMakeErrorValue)))
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_starName.suffix(4)) + "temMes" + String(str_layerText.prefix(7)) + String(str_centerContent)))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: BorderPlayerThen = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = BorderPlayerThen(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: OffdDoorReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = OffdDoorReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: OfThen = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = OfThen()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: FullMoonTitleView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = FullMoonTitleView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: ShowAppMeView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = ShowAppMeView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension StopControllerDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func takeOnAndSettings() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        HumanityReactiveCompatible.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        sendJoin(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func sendJoin(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(str_hiddenValue.prefix(6))): self.conversationData.groupID, String(bytes: str_kitContent.map{marginOfSafety(stop: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        ViewRequestTool.rowCompletion(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - TMessageControllerDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension StopControllerDelegate: TMessageControllerDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        //: if data.isKind(of: VanguardCellData.self) {
        if data.isKind(of: VanguardCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatTextMsgCellID")
            let cell = CostReactiveCompatible(style: .default, reuseIdentifier: (String(str_managerName) + String(str_momentInfoName)))
            //: let cellData = data as! VanguardCellData
            let cellData = data as! VanguardCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.pathUser(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.translateMsg(with: cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: ShareThen.self) {
        if data.isKind(of: ShareThen.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = TalkingCellThen(style: .default, reuseIdentifier: (String(str_actionData) + String(str_localValue.suffix(5)) + "eMsgCe" + String(str_somethingName.prefix(4))))
            //: let cellData = data as! ShareThen
            let cellData = data as! ShareThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: InsideCellData.self) {
        if data.isKind(of: InsideCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatGiftMsgCell")
            let cell = SurfaceMessageCell(style: .default, reuseIdentifier: (String(str_intimateTitle.prefix(4)) + "ingG" + str_makeData.replacingOccurrences(of: "status", with: "u") + "ChatGift" + String(str_mainGestureTitle)))
            //: let cellData = data as! InsideCellData
            let cellData = data as! InsideCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: NoCellData.self) {
        if data.isKind(of: NoCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = SampleSearchedMsgCell(style: .default, reuseIdentifier: (String(str_intimateName) + String(str_lineMakeErrorValue)))
            //: let cellData = data as! NoCellData
            let cellData = data as! NoCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(SubLabelReactiveCompatible.share.loginUid) {
            if userModel.uid == Int(SubLabelReactiveCompatible.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                pathUser(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                usufructuaryEqual(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: TalkingCellThen.self) { // 图片
            //: let imageCellData = cell.messageData as! ShareThen
            let imageCellData = cell.messageData as! ShareThen
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(kLet_countContent)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = VendorPhotoModel()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = LightMeasurable()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [VendorPhotoModel] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = ConversationViewDelegate(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.nowController()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: CostReactiveCompatible.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! CostReactiveCompatible
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(str_labelName))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: str_arrayVisibleValue.reversed(), encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<InsideJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    TalkingPushManager.share.giftBy(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: SampleSearchedMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! SampleSearchedMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(str_labelName))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: str_arrayVisibleValue.reversed(), encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<InsideJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    TalkingPushManager.share.giftBy(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        pad(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension StopControllerDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func tips() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: RawCurrentModel) {
    private func usufructuaryEqual(user: RawCurrentModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = TalkingSizeVanguardPopView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.arrayOn(cellTitleList: [(String(str_kitValue) + String(str_seedName)).localized, "@", (String(str_managerModelIndexValue) + str_dayTitle.replacingOccurrences(of: "view", with: "o")).localized, (String(str_modeListTitle)).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.tapPath(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.moiety(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.pathUser(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.forbiddance(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: RawCurrentModel) {
    private func tapPath(user: RawCurrentModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.render()
        //: let model = TalkingChatRoomMemberModel()
        let model = ToMemberReactiveCompatible()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.chemistryLaboratoryModel(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: RawCurrentModel) {
    private func moiety(user: RawCurrentModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: str_indicatorValue.map{showShadow(error: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: str_makeImageValue.map{pathScale(add: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.pushButton(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func pathUser(uid: Int) {
        //: TalkingPushManager.share.func__pushToUserDetailVC(uid: String(uid))
        TalkingPushManager.share.equalAcross(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func forbiddance(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = RangeAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.array(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension StopControllerDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func untilUp(msgStr: String, quoteModel _: AdpressedModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            barLog(showMsg: (String(str_priceSeatText) + String(str_kitData.prefix(6)) + "k me" + str_labelGreetData.replacingOccurrences(of: "view", with: "e")).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            for uid in msgInputView.atArr {
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: str_arrayVisibleValue.reversed(), encoding: .utf8)!: (str_makeName.replacingOccurrences(of: "row", with: "er")), String(bytes: str_indicatorValue.map{showShadow(error: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: str_pathHiddenName.reversed(), encoding: .utf8)!: temStr, (String(str_callToData)): (str_appData.replacingOccurrences(of: "failure", with: "x")), (String(str_effectValue.suffix(5)) + str_tempData.replacingOccurrences(of: "content", with: "u")): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(String(str_gameValue))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: str_layerTitle.map{topFrame(result: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: str_kitContent.map{marginOfSafety(stop: $0)}, encoding: .utf8)!: (String(str_giveText.suffix(5)) + String(str_highlightText)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(str_hiddenValue.prefix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(str_sizeDomainTitle))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_maleValue.suffix(7))): msgInfo, String(bytes: str_momentTitle.map{elementMake(result: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        pad(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func showMode(img: UIImage) {
        //: let imagePath = ShareThen.getChatImagePath(image: img)
        let imagePath = ShareThen.addImage(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: str_createContent.map{cellText(point: $0)}, encoding: .utf8)!: (String(str_jumpName) + String(str_replyName)), (String(str_callToData)): (str_leadingName.replacingOccurrences(of: "regular", with: "m")), String(bytes: str_requestNameValue.map{modeCustom(succeed: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(str_mediumStartData)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: str_kitContent.map{marginOfSafety(stop: $0)}, encoding: .utf8)!: (String(str_jumpName) + String(str_replyName)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(str_hiddenValue.prefix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_maleValue.suffix(7))): msgInfo, String(bytes: str_momentTitle.map{elementMake(result: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        pad(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func pad(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            barLog(showMsg: kLet_eventText)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.sendCustomMessage(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(str_mediumStartData))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(str_mediumStartData))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(str_mediumStartData))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        ViewRequestTool.belowSend(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.promptDismiss(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(str_maleValue.suffix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(msgData, msgId: msgInfo[(String(str_hideData))] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(str_broadcastValue))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(str_broadcastValue))] as! NSNumber
                //: SubLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                SubLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func promptDismiss(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard SubLabelReactiveCompatible.share.loginUserMode.status != 1 else {
            guard SubLabelReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    barLog(showMsg: errorStr)
                }
                //: return
                return
            }

            //: TalkingPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            TalkingPushManager.share.withParams(clickEvent: kLet_recordBottomGiftValue, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.hiddenView()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            localError(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: TalkingPushManager.share.func__pushToSubscribeAlert()
            TalkingPushManager.share.statusSumimate()

            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                barLog(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension StopControllerDelegate: CoverThen {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func clickNum(giftModel _: ProfessionalTransformable, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func snap() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func ofRange(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = BirdSEyeViewReactiveCompatible()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.pushButton(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func targetAdd(actionType: OfVendorExpressionConvertible) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            coriolisEffect()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func kindWith(giftModel _: ProfessionalTransformable, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func writeObject(giftModel: ProfessionalTransformable, giftNum: String, model: ToMemberReactiveCompatible) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            barLog(showMsg: kLet_eventText)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(str_sizeDomainTitle))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(str_blockName.suffix(5)) + str_managerButtonValue.replacingOccurrences(of: "current", with: "d"))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(str_greetText))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(str_hiddenValue.prefix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_managerContent))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        ViewRequestTool.solarium(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.promptDismiss(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.cannuliseNum(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.bubbleTarget(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func sizeSave(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func signalingDogEar(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func modelIcon(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.modelIcon(tap)
    }

    //: func func__starRecordBtnDown() {}
    func downwardRoom() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func inheritanceTime(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension StopControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func coriolisEffect() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        AccountingDataReactiveCompatible.belowTap(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = AddOnReactiveCompatible.outEndMake(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: TalkingPushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                TalkingPushManager.share.constraintVc()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.showMode(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func displayCamaraPhotos() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        AccountingDataReactiveCompatible.funcBlockCaptureLeverUtilityStreetwiseDevice(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.showMode(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension StopControllerDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func liveInfo(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(str_hiddenValue.prefix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension StopControllerDelegate {
    //: func func__TipsNotification() {
    func valueNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(sinceClick(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_systemData,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func sinceClick(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: NoCellData  = userinfo["msg"] as! NoCellData
        let celldata: NoCellData = userinfo[(String(str_giftModelResultName))] as! NoCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func localError(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: str_pathHiddenName.reversed(), encoding: .utf8)!: tipStr, (String(str_callToData)): (str_pageTitle.replacingOccurrences(of: "equal", with: "s"))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func funcMessage() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard SubLabelReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            guard SubLabelReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: SubLabelReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.localError(tipStr: SubLabelReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension StopControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func bubbleTarget(extral: [String: Any?], model: ToMemberReactiveCompatible, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(str_maleValue.suffix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(str_effectValue.suffix(5)) + str_tempData.replacingOccurrences(of: "content", with: "u"))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(str_callToData))] = (String(str_groupTitle))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(str_maleValue.suffix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: str_indicatorValue.map{showShadow(error: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (str_profileValue.replacingOccurrences(of: "actual", with: "n") + str_touchValue.replacingOccurrences(of: "have", with: "e")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: str_topData.map{valueGame(collection: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(str_labelValue.suffix(6)) + str_liveElementValue.lowercased()).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(str_groupTitle))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(str_groupTitle))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(str_profileValue.replacingOccurrences(of: "actual", with: "n") + str_touchValue.replacingOccurrences(of: "have", with: "e"))] = "\(giftdic[(String(str_topName))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(str_hiddenTitle.suffix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(str_tapPointName.prefix(5)) + str_normalTitle.replacingOccurrences(of: "color", with: "om"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(str_tapPointName.prefix(5)) + str_normalTitle.replacingOccurrences(of: "color", with: "om"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(str_hideData))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        fortifyModel(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(str_broadcastValue))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(str_broadcastValue))] as! NSNumber
            //: SubLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            SubLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: SubLabelReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.bag(needReload: false, mf_coin: SubLabelReactiveCompatible.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.observerMembers()
    }

    //: func initCachEffectData() {
    func bottomData() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.freeBy(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func fortifyModel(giftDic: [String: Any], model: ToMemberReactiveCompatible) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.chatViewStructuralSociology(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in VideoToReactiveCompatible.share.identityNeed() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [AppModelType]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.clickInfo(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.sharedShow(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func aid(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in VideoToReactiveCompatible.share.identityNeed() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [AppModelType]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.clickInfo(model: giftModel as! RillModelType)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.nameInterval(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension StopControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func viewUi() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func submitCellSituation() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(kLet_soundData)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(kLet_soundData)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - kLet_changeBarValue)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func stickVideo() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = BirdSEyeViewReactiveCompatible()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! TUIMessageCellData).isKind(of: VanguardCellData.self) {
                    if (cellData as! TUIMessageCellData).isKind(of: VanguardCellData.self) {
                        //: let groupCellData = cellData as! VanguardCellData
                        let groupCellData = cellData as! VanguardCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(str_hideData))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: TempStopThen.shared.rx
        TempStopThen.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(str_inviteName.suffix(6)) + "ectio" + String(str_topOfMessageData)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
