
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let str_stateTopValueName:[UInt8] = [0xcd,0xdc,0xcf,0xc9,0xc4,0xc2,0xce,0xc9,0xdc,0xc9,0xc8,0xce,0xc2,0xde,0xd5,0xdc,0xd3,0xda,0xd8,0xc2,0xd3,0xd2,0xc9,0xd4,0xdb,0xd4,0xde,0xdc,0xc9,0xd4,0xd2,0xd3]

private func titleFlush(task num: UInt8) -> UInt8 {
    return num ^ 157
}

/*: "p- :*/
fileprivate let str_responseName:String = "p-left height double"

/*: - :*/
fileprivate let str_errorText:String = "-"

/*: "redirectUrl" :*/
fileprivate let str_topValue:[Character] = ["r","e","d","i","r","e","c","t","U","r"]
fileprivate let str_seedData:[Character] = ["l"]

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let str_nextWillData:String = "viaIC"
fileprivate let str_equalMakeWhiteTitle:String = "M: \u{8fdb}\u{623f}"

/*: "Party room exception" :*/
fileprivate let str_labelTitle:String = "Pashow failure res"
fileprivate let str_conversationValue:String = "rty roview page self"
fileprivate let str_hiddenWithContent:String = "EPTION"

/*: _ :*/
fileprivate let str_intimateTitle:String = "array"

/*: "male" :*/
fileprivate let str_leadingSizeCenterTitle:[UInt8] = [0xbe,0xb2,0xbf,0xb6]

/*: "female" :*/
fileprivate let str_locationName:String = "FEMALE"

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let str_readingColorValue:String = "VOview"
fileprivate let str_normalData:String = ": \u{7fa4}\u{804a}"
fileprivate let str_effectName:String = "sound false self sizeroupI"
fileprivate let str_meCookieValue:[Character] = ["d",":"," "]

/*: ." :*/
fileprivate let str_transformData:String = "custom"

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let str_dayEqualText:String = "VOtitleER"
fileprivate let str_pointPushName:String = " 切\u{623f}\u{6a21}型解"

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let str_normalName:String = "VOintimate"
fileprivate let str_phoneData:String = "群聊切\u{623f}失"
fileprivate let str_valueVideoData:String = "败。grto text"

/*: "errCode" :*/
fileprivate let str_sizeAppValue:[UInt8] = [0x65,0x64,0x6f,0x43,0x72,0x72,0x65]

/*: "custom_kickout_message" :*/
fileprivate let str_packageText:[Character] = ["c","u","s","t","o","m","_","k","i","c","k","o","u","t","_","m","e"]
fileprivate let str_directionContent:String = "SSAGE"

/*: "closeRoom" :*/
fileprivate let str_scaleMakeContent:[Character] = ["c","l","o","s","e"]
fileprivate let str_labelData:String = "let make log selfRoom"

/*: "adminCloseRoom" :*/
fileprivate let str_giftTitleDataValue:String = "layer color frame sexadminC"
fileprivate let str_withSmartInData:String = "size else self leftloseRoom"

/*: "The room is closed, please go to another room" :*/
fileprivate let str_goValue:[UInt8] = [0x8,0x34,0x39,0x7c,0x2e,0x33,0x33,0x31,0x7c,0x35,0x2f,0x7c,0x3f,0x30,0x33,0x2f,0x39,0x38,0x70,0x7c,0x2c,0x30,0x39,0x3d,0x2f,0x39,0x7c,0x3b,0x33,0x7c,0x28,0x33,0x7c,0x3d,0x32,0x33,0x28,0x34,0x39,0x2e,0x7c,0x2e,0x33,0x33,0x31]

private func frameEnableBy(data num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "OK" :*/
fileprivate let str_picInputTrueName:[Character] = ["O","K"]

/*: "You were kicked out of the room" :*/
fileprivate let str_managerTitle:[UInt8] = [0x9e,0xa8,0xb2,0xe7,0xb0,0xa2,0xb5,0xa2,0xe7,0xac,0xae,0xa4,0xac,0xa2,0xa3,0xe7,0xa8,0xb2,0xb3,0xe7,0xa8,0xa1,0xe7,0xb3,0xaf,0xa2,0xe7,0xb5,0xa8,0xa8,0xaa]

private func atShare(equal num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let str_momentData:[Character] = ["V","O","I","C","E","R","O","O","M","："]
fileprivate let str_managerText:[Character] = ["进","\u{623f}","失","败","，","r","o","o","m","I","D",":"," "]

/*: , error:  :*/
fileprivate let str_retViewValue:[Character] = [","," ","e","r"]
fileprivate let str_makeValue:String = "ror: photo make"

/*: , reason:  :*/
fileprivate let str_blockHeadTitle:String = "data text, rea"

/*: , extendedData:  :*/
fileprivate let str_maleData:String = "view data let try label, ex"
fileprivate let str_fatalValue:String = "tenimageeimage"

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let str_officialValue:String = "indexOIC"
fileprivate let str_randomTopValue:String = "M: 当\u{524d}"
fileprivate let str_blockTableText:[Character] = ["，","r","o","o","m","I","D",":"," "]

/*: , errorCode:  :*/
fileprivate let str_channelName:[Character] = [","," ","e","r","r","o","r","C","o","d","e",":"," "]

/*: , streamID:  :*/
fileprivate let str_bagText:String = ", stself select center"

/*: "An error occurred, please try again" :*/
fileprivate let str_databaseTitle:[UInt8] = [0x81,0xae,0xe0,0xa5,0xb2,0xb2,0xaf,0xb2,0xe0,0xaf,0xa3,0xa3,0xb5,0xb2,0xb2,0xa5,0xa4,0xec,0xe0,0xb0,0xac,0xa5,0xa1,0xb3,0xa5,0xe0,0xb4,0xb2,0xb9,0xe0,0xa1,0xa7,0xa1,0xa9,0xae]

private func goSharedNumber(text num: UInt8) -> UInt8 {
    return num ^ 192
}

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let str_leadingName:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","拉","流","失","败","，","r","o","o","m"]
fileprivate let str_tableVoiceValue:[Character] = ["I","D",":"," "]

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let str_keyObjectData:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","信","令","解"]
fileprivate let str_touchCallValue:String = "析失\u{8d25}，com"
fileprivate let str_chartName:[Character] = ["m","a","n","d",":"," "]

/*: , roomID:  :*/
fileprivate let str_addButtonName:[Character] = [","," ","r","o","o","m","I","D",":"," "]

/*: "data" :*/
fileprivate let str_ageData:String = "equalta"

/*: "position" :*/
fileprivate let str_groupData:String = "POSITIO"
fileprivate let str_routeLabValue:[Character] = ["n"]

/*: "xxxx--  :*/
fileprivate let str_distanceData:String = "xxxx-- leading bag put"

/*: "opType" :*/
fileprivate let str_layerTitle:[Character] = ["o","p","T","y","p","e"]

/*: "party:changeMike" :*/
fileprivate let str_fieldText:[Character] = ["p","a","r","t","y",":"]
fileprivate let str_makeTitle:String = "CHANGE"

/*: "type" :*/
fileprivate let str_gestureNamePageValue:String = "tyerrore"

/*: "toPosition" :*/
fileprivate let str_succeedLoadData:[Character] = ["t","o","P","o","s"]
fileprivate let str_readingTitle:String = "iparaon"

/*: "party:adminChangeMike" :*/
fileprivate let str_succeedColorName:String = "partequal"
fileprivate let str_resRoomValue:String = ":adminas string max"
fileprivate let str_actionFrameValue:String = "by to in toeMike"

/*: "You were kicked off the seat" :*/
fileprivate let str_blockData:[Character] = ["Y"]
fileprivate let str_formatFailureValue:String = "content transfer progressou w"
fileprivate let str_fileSlideName:String = "icked view load"
fileprivate let str_coverMessageData:String = "e seatself share self self shared"

/*: "party:changeCoin" :*/
fileprivate let str_clickValue:String = "pashott"
fileprivate let str_maleTitle:String = "to user sharengeCoin"

/*: "party:closeStream" :*/
fileprivate let str_safetyData:[Character] = ["p","a","r","t","y",":","c","l","o","s","e","S","t","r","e","a","m"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EyelashEventHandler.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let kLet_messageUseName = NSNotification.Name(rawValue: String(bytes: str_stateTopValueName.map{titleFlush(task: $0)}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let kLet_senseName = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum InfoConstantTarget: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class EyelashEventHandler: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = TaskModelType()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kLet_messageUseName, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: EyelashEventHandler?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: SearchThen?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: RoomModelType]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = iconTo()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func firstBack() -> EyelashEventHandler {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = EyelashEventHandler()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func involvementVideo() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if EyelashEventHandler._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            EyelashEventHandler._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: OmitMiniView = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = OmitMiniView.currentHandle()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.liveWill()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension EyelashEventHandler {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func cornerOf(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        RoomReqTool.topFileAction(roomId: partyModel.roomId,
                                              //: uid: Int(SubLabelReactiveCompatible.share.loginUid) ?? 0,
                                              uid: Int(SubLabelReactiveCompatible.share.loginUid) ?? 0,
                                              //: type: type,
                                              type: type,
                                              //: position: position,
                                              position: position,
                                              //: toPos: toPosition)
                                              toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.fromBegin()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = RoomModelType.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.circumferent(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.fromBegin()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func changeBy(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        RoomReqTool.bugOut(roomId: partyModel.roomId,
                                       //: uid: uid,
                                       uid: uid,
                                       //: type: type,
                                       type: type,
                                       //: position: position)
                                       position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func fromBegin() -> String {
        //: return "p-\(SubLabelReactiveCompatible.share.loginUid)-\(partyModel.roomId)"
        return "p-\(SubLabelReactiveCompatible.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension EyelashEventHandler {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func froth(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        AccountingDataReactiveCompatible.ofText(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.barLog(showMsg: kLet_changeTitle)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            RoomReqTool.keyByUser(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: TalkingPushManager.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            TalkingPushManager.share.counteractionPlayer(urlStr: result[(String(str_topValue) + String(str_seedData))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.barLog(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isTalking == false else {
                guard TitleSocketManager.shared.isTalking == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.barLog(showMsg: kLet_keyMessageName)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = TaskModelType.deserialize(from: result as? Dictionary) else {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 进房模型解析失败。")
                    MakeLogTool.playingCard(msg: (str_nextWillData.replacingOccurrences(of: "via", with: "VO") + "EROO" + str_equalMakeWhiteTitle + "模型\u{89e3}析失\u{8d25}。"))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.barLog(showMsg: (String(str_labelTitle.prefix(2)) + String(str_conversationValue.prefix(6)) + "om exc" + str_hiddenWithContent.lowercased()).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = SearchThen()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.choppingBlock()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.createView()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.cellSave()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                kLet_postName.paper(eventID: "\(kLet_lineRoomText)_\(SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue ? String(bytes: str_leadingSizeCenterTitle.map{$0^211}, encoding: .utf8)! : (str_locationName.lowercased()))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                ManagingDirectorDanmuManager.completionRoomAnnouncementHideLanthanonAffiliate(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        MakeLogTool.playingCard(msg: (str_readingColorValue.replacingOccurrences(of: "view", with: "IC") + "EROOM" + str_normalData + "进房失败。g" + String(str_effectName.suffix(5)) + String(str_meCookieValue)) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.barLog(showMsg: (String(str_labelTitle.prefix(2)) + String(str_conversationValue.prefix(6)) + "om exc" + str_hiddenWithContent.lowercased()).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.imageOn()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func openBilliardParlor(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        RoomReqTool.since(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.pan()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == SubLabelReactiveCompatible.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == SubLabelReactiveCompatible.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = TaskModelType.deserialize(from: result as? Dictionary) else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM: 切房模型解析失败。")
                MakeLogTool.playingCard(msg: (str_dayEqualText.replacingOccurrences(of: "title", with: "IC") + "OOM:" + str_pointPushName + "析\u{5931}\u{8d25}。"))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.barLog(showMsg: (String(str_labelTitle.prefix(2)) + String(str_conversationValue.prefix(6)) + "om exc" + str_hiddenWithContent.lowercased()).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = SearchThen()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.choppingBlock()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.createView()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.sussOut(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            kLet_postName.paper(eventID: "\(kLet_lineRoomText)_\(SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue ? String(bytes: str_leadingSizeCenterTitle.map{$0^211}, encoding: .utf8)! : (str_locationName.lowercased()))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            ManagingDirectorDanmuManager.completionRoomAnnouncementHideLanthanonAffiliate(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    MakeLogTool.playingCard(msg: (str_normalName.replacingOccurrences(of: "intimate", with: "IC") + "EROOM: " + str_phoneData + String(str_valueVideoData.prefix(4)) + "oupId: ") + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.barLog(showMsg: (String(str_labelTitle.prefix(2)) + String(str_conversationValue.prefix(6)) + "om exc" + str_hiddenWithContent.lowercased()).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.imageOn()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension EyelashEventHandler {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func ofSprechstimme() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.instanceAnimated()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func liveWill() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = nowController() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: TaskChatController.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func imageOn() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.pan() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.cornerOf(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            EyelashEventHandler.involvementVideo() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension EyelashEventHandler {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func cellSave() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = ManagerReactiveCompatible.getVoiceRoomAppId()
        profile.appID = ManagerReactiveCompatible.pathId()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: SubLabelReactiveCompatible.share.loginUid)
        let user = ZegoUser(userID: SubLabelReactiveCompatible.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func sussOut(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension EyelashEventHandler: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            RoomReqTool.equalCompletion(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.someModel(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.voiceIn()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == InfoConstantTarget.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.cornerOf(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                RoomReqTool.equalCompletion(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.someModel(key: key, model: value)
                        //: if String(value.uid) == SubLabelReactiveCompatible.share.loginUid {
                        if String(value.uid) == SubLabelReactiveCompatible.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.fromBegin()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.voiceIn()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kLet_postName.cognizant(eventID: kLet_userToValue, parameterStr: [String(bytes: str_sizeAppValue.reversed(), encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(String(str_packageText) + str_directionContent.lowercased())].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (String(str_scaleMakeContent) + String(str_labelData.suffix(4))) || value == (String(str_giftTitleDataValue.suffix(6)) + String(str_withSmartInData.suffix(8))) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    imageOn()
                    //: let config = ShowAlertConfig()
                    let config = ImageAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    TalkingCostExhibitThen.directionUse(message: String(bytes: str_goValue.map{frameEnableBy(data: $0)}, encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: kLet_beginName, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    barLog(showMsg: String(bytes: str_managerTitle.map{atShare(equal: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                MakeLogTool.playingCard(msg: (String(str_momentData) + String(str_managerText)) + "\(roomID)" + (String(str_retViewValue) + String(str_makeValue.prefix(5))) + "\(errorCode)" + (String(str_blockHeadTitle.suffix(5)) + "son: ") + "\(reason)" + (String(str_maleData.suffix(4)) + str_fatalValue.replacingOccurrences(of: "image", with: "d") + "Data: ") + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                barLog(showMsg: (String(str_labelTitle.prefix(2)) + String(str_conversationValue.prefix(6)) + "om exc" + str_hiddenWithContent.lowercased()).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kLet_postName.cognizant(eventID: kLet_userToValue, parameterStr: [String(bytes: str_sizeAppValue.reversed(), encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            imageOn()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            MakeLogTool.playingCard(msg: (str_officialValue.replacingOccurrences(of: "index", with: "V") + "EROO" + str_randomTopValue + "用户推流失败" + String(str_blockTableText)) + "\(partyModel.roomId)" + (String(str_channelName)) + "\(errorCode)" + (String(str_bagText.prefix(4)) + "reamID: ") + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            barLog(showMsg: String(bytes: str_databaseTitle.map{goSharedNumber(text: $0)}, encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = economicAid()
            //: for (position, model) in dict where String(model.uid) == SubLabelReactiveCompatible.share.loginUid {
            for (position, model) in dict where String(model.uid) == SubLabelReactiveCompatible.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                cornerOf(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                possibility(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                imageOn()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            MakeLogTool.playingCard(msg: (String(str_leadingName) + String(str_tableVoiceValue)) + "\(partyModel.roomId)" + (String(str_channelName)) + "\(errorCode)" + (String(str_bagText.prefix(4)) + "reamID: ") + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                imageOn()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            for zego in streamList {
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            for zego in streamList {
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.eventControl(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = economicAid()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.eventControl(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            MakeLogTool.playingCard(msg: (String(str_keyObjectData) + str_touchCallValue + String(str_chartName)) + "\(command)" + (String(str_addButtonName)) + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(str_ageData.replacingOccurrences(of: "equal", with: "da"))][(str_groupData.lowercased() + String(str_routeLabValue))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < kLet_senseName else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = RoomModelType.deserialize(from: json[(str_ageData.replacingOccurrences(of: "equal", with: "da"))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (String(str_distanceData.prefix(7))) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(str_layerTitle))].stringValue == (String(str_fieldText) + str_makeTitle.lowercased() + "Mike") { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(str_ageData.replacingOccurrences(of: "equal", with: "da"))][(str_gestureNamePageValue.replacingOccurrences(of: "error", with: "p"))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                circumferent(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = passePartoutRoom(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    possibility(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                possibility(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(str_ageData.replacingOccurrences(of: "equal", with: "da"))][(String(str_succeedLoadData) + str_readingTitle.replacingOccurrences(of: "para", with: "ti"))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                circumferent(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                circumferent(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.notEqual()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(str_layerTitle))].stringValue == (str_succeedColorName.replacingOccurrences(of: "equal", with: "y") + String(str_resRoomValue.prefix(6)) + "Chang" + String(str_actionFrameValue.suffix(5))) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(str_ageData.replacingOccurrences(of: "equal", with: "da"))][(str_gestureNamePageValue.replacingOccurrences(of: "error", with: "p"))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                circumferent(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = passePartoutRoom(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    possibility(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.notEqual()
                    //: guard String(oldModel.uid) == SubLabelReactiveCompatible.share.loginUid else { return }
                    guard String(oldModel.uid) == SubLabelReactiveCompatible.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    barLog(showMsg: (String(str_blockData) + String(str_formatFailureValue.suffix(4)) + "ere k" + String(str_fileSlideName.prefix(6)) + "off th" + String(str_coverMessageData.prefix(6))).localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                circumferent(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == SubLabelReactiveCompatible.share.loginUid else { return }
                guard String(mikeModel.uid) == SubLabelReactiveCompatible.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                circumferent(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == SubLabelReactiveCompatible.share.loginUid else { return }
                guard String(mikeModel.uid) == SubLabelReactiveCompatible.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(str_layerTitle))].stringValue == (str_clickValue.replacingOccurrences(of: "shot", with: "r") + "y:cha" + String(str_maleTitle.suffix(7))) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            for (position, model) in EyelashEventHandler.firstBack().economicAid() {
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    circumferent(position: position, model: model)
                    //: return
                    continue
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(str_layerTitle))].stringValue == (String(str_safetyData)) { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension EyelashEventHandler {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func circumferent(position: Int, model: RoomModelType) {
        //: modifyMikePosition(key: position, model: model)
        someModel(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.online(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        currentUid(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func possibility(position: Int, model: RoomModelType) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        someModel(key: position, model: RoomModelType())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.online(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        currentUid(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func currentUid(uid: Int) {
        //: guard String(uid) == SubLabelReactiveCompatible.share.loginUid else { return }
        guard String(uid) == SubLabelReactiveCompatible.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = economicAid()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.alongValue()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension EyelashEventHandler {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func iconTo() -> [Int: RoomModelType] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< kLet_senseName {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            someModel(key: index, model: RoomModelType())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func someModel(key: Int, model: RoomModelType) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func passePartoutRoom(key: Int) -> RoomModelType? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func economicAid() -> [Int: RoomModelType] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
