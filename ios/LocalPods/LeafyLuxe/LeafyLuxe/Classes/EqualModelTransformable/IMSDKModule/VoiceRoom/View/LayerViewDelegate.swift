
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_multiTitle:[UInt8] = [0xc2,0xc7,0xc2,0xcd,0x81,0xbc,0xc8,0xbd,0xbe,0xcb,0x93,0x82,0x79,0xc1,0xba,0xcc,0x79,0xc7,0xc8,0xcd,0x79,0xbb,0xbe,0xbe,0xc7,0x79,0xc2,0xc6,0xc9,0xc5,0xbe,0xc6,0xbe,0xc7,0xcd,0xbe,0xbd]

fileprivate func toolLanguage(awake num: UInt8) -> UInt8 {
    let value = Int(num) - 89
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Followers" :*/
fileprivate let str_stickValue:[Character] = ["F","o","l","l","o","w","e","r","s"]

/*: "Following" :*/
fileprivate let str_midValue:[Character] = ["F","o","l","l","o","w","i","n","g"]

/*: "#E6E6E6" :*/
fileprivate let str_feeData:[Character] = ["#","E","6","E","6","E","6"]

/*: "btn_message_more" :*/
fileprivate let str_targetData:[Character] = ["b","t","n","_","m","e","s"]
fileprivate let str_rateData:String = "titlege"

/*: "UID:  :*/
fileprivate let str_makeValue:String = "top whiteUID: "

/*: "level_ :*/
fileprivate let str_leadingName:String = "level_age if bottom to template"

/*: "Gift" :*/
fileprivate let str_infoTotalData:[UInt8] = [0xd4,0xfa,0xf5,0xe7]

/*: "Mike" :*/
fileprivate let str_aspectData:[UInt8] = [0x65,0x6b,0x69,0x4d]

/*: "Follow" :*/
fileprivate let str_selectedAccountElseData:[UInt8] = [0x77,0x6f,0x6c,0x6c,0x6f,0x46]

/*: "gift_voice_card" :*/
fileprivate let str_kitErrorData:[Character] = ["g","i","f","t","_"]
fileprivate let str_mainTitle:[Character] = ["v","o","i","c","e","_","c","a","r","d"]

/*: "btn_party_voice_nor" :*/
fileprivate let str_centerData:String = "cancel first table bubblebtn_p"
fileprivate let str_defineValue:String = "name data case_voi"
fileprivate let str_textTitleValue:String = "ce_norself manager return"

/*: "btn_party_voice_pre" :*/
fileprivate let str_tailValue:[Character] = ["b","t","n","_","p","a","r","t","y","_","v"]
fileprivate let str_labelValue:[Character] = ["o","i","c","e","_","p","r","e"]

/*: "attentionUid" :*/
fileprivate let str_lastData:String = "viewtten"
fileprivate let str_buttonUnderText:String = "ID"

/*: "source" :*/
fileprivate let str_atUserTextValue:[Character] = ["s","o","u","r","c","e"]

/*: "Kick out" :*/
fileprivate let str_profileValue:String = "path managerKick"

/*: "Unmute" :*/
fileprivate let str_butterData:String = "Unmutepopular super return input center"

/*: "Mute" :*/
fileprivate let str_tabValue:String = "Muteheight for bottom check share"

/*: "Kick off" :*/
fileprivate let str_recommendName:String = "make"
fileprivate let str_viewCancelName:[Character] = ["i","c","k"," ","o","f","f"]

/*: "Block" :*/
fileprivate let str_transformData:String = "guard viewBlock"

/*: "Report" :*/
fileprivate let str_domainData:String = "Reportsystem live should log color"

/*: "#F6F6F6" :*/
fileprivate let str_buttonLengthText:[Character] = ["#","F","6","F","6","F","6"]

/*: "Confirm to kick out the room?" :*/
fileprivate let str_fillValue:String = "Confirextension add size"
fileprivate let str_directTitle:String = "self normal top feedback centerkick "
fileprivate let str_appSizeDataName:String = "view status tool message selfthe r"

/*: "Cancel" :*/
fileprivate let str_nameColorContent:String = "Cancelmodel app"

/*: "Sure" :*/
fileprivate let str_portData:[Character] = ["S","u","r","e"]

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let str_pushData:[UInt8] = [0x37,0x68,0x5b,0x16,0x6f,0x65,0x6b,0x16,0x69,0x6b,0x68,0x5b,0x16,0x6f,0x65,0x6b,0x16,0x6d,0x57,0x64,0x6a,0x16,0x6a,0x65,0x16,0x59,0x57,0x64,0x59,0x5b,0x62,0x16,0x6a,0x5e,0x5b,0x16,0x63,0x6b,0x6a,0x5b,0x35]

fileprivate func errorShare(layer num: UInt8) -> UInt8 {
    let value = Int(num) - 246
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Confirm" :*/
fileprivate let str_hiddenValue:String = "allow userConfir"
fileprivate let str_managerTitle:String = "M"

/*: "The mute has been lifted~" :*/
fileprivate let str_loseMomentShareTitle:String = "The mingredient error self false"
fileprivate let str_sectionPushName:String = "has let height home to true"
fileprivate let str_popName:[Character] = ["b","e"]
fileprivate let str_indicatorValue:String = "en liftype path data party price"

/*: "Confirm to kick off the seat?" :*/
fileprivate let str_mediumContent:[Character] = ["C","o","n","f","i","r","m"," "]
fileprivate let str_screenTargetValue:String = "to kicount manager"
fileprivate let str_visualTitle:String = "ff tedge guard view else lab"

/*: "OK" :*/
fileprivate let str_liveTitle:[Character] = ["O","K"]

/*: "Shielding Success" :*/
fileprivate let str_makeScaleValue:String = "let device after data statusSh"
fileprivate let str_appPoseContent:String = "positionldin"
fileprivate let str_databaseData:String = "viewess"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LayerViewDelegate.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol StopViewDelegate: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func counterfoil(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func liveClick(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func popVideo(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func textModel(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class LayerViewDelegate: UIView {
    //: var popView: TalkingPopView?
    var popView: ChangeShapeRoomReactiveCompatible?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: SmokerMeasurable?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(EyelashEventHandler.firstBack().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = HeadTransformable()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: StopViewDelegate?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == SubLabelReactiveCompatible.share.loginUserMode.userID {
        if uid == SubLabelReactiveCompatible.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        weltanschauung()
        //: setupSubViewsConstraint()
        balloon()
        //: reqUserCardInfo()
        computerSimulation()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_multiTitle.map{toolLanguage(awake: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 12)
        label.font = .magnitudeimateBy(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.videoValue()
        label.textColor = UIColor.videoValue()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(tillIcon), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 15)
        label.font = .magnitudeimateBy(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.elementColor()
        label.textColor = UIColor.elementColor()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 13)
        label.font = .magnitudeimateBy(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.videoValue()
        label.textColor = UIColor.videoValue()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .videoValue()
        lab.textColor = .videoValue()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(str_stickValue)).localized
        //: lab.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 12)
        lab.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .elementColor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 15)
        lab.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .videoValue()
        lab.textColor = .videoValue()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(str_midValue)).localized
        //: lab.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 12)
        lab.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .elementColor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 15)
        lab.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(str_feeData)))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.nameTitle(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_targetData) + str_rateData.replacingOccurrences(of: "title", with: "sa") + "_more")), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(willCell), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: VideoMuteView = {
        //: let v = TalkingLiveRoomMuteView()
        let v = VideoMuteView()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension LayerViewDelegate {
    //: func reqUserCardInfo() {
    func computerSimulation() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        ConversationLiveManager.completionItem(uid: self.uid, streamerUid: liveUserId, roomId: EyelashEventHandler.firstBack().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<HeadTransformable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.equation()
            }
        }
    }

    //: func setUserCardData() {
    func equation() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(str_makeValue.suffix(5))) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.custom(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.nameTitle(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.nameTitle(name: (String(str_leadingName.prefix(6))) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            youngness(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func youngness(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: str_infoTotalData.map{$0^147}, encoding: .utf8)!)
            //: if liveUserId == SubLabelReactiveCompatible.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == SubLabelReactiveCompatible.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: str_aspectData.reversed(), encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: str_selectedAccountElseData.reversed(), encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.dataViewPush(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.dataViewPush(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: str_infoTotalData.map{$0^147}, encoding: .utf8)! {
                //: btn.setImage(UIImage.nameTitle(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.nameTitle(name: (String(str_kitErrorData) + String(str_mainTitle))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: str_aspectData.reversed(), encoding: .utf8)! {
                //: btn.setImage(UIImage.nameTitle(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.nameTitle(name: (String(str_centerData.suffix(5)) + "arty" + String(str_defineValue.suffix(4)) + String(str_textTitleValue.prefix(6)))), for: .normal)
                //: btn.setImage(UIImage.nameTitle(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.nameTitle(name: (String(str_tailValue) + String(str_labelValue))), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.underPlay(fontSize: 15)
                btn.titleLabel?.font = UIFont.underPlay(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(pushSender(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(str_feeData)))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func pushSender(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: str_infoTotalData.map{$0^147}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.liveClick(uid: self.userModel.uid)
            //: self.dismiss()
            self.thoughtImage()
        //: case "Mike":
        case String(bytes: str_aspectData.reversed(), encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.popVideo(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.thoughtImage()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.counterfoil(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.thoughtImage()
        //: case "Follow":
        case String(bytes: str_selectedAccountElseData.reversed(), encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            freshfollow(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func freshfollow(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        doEnd(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.textModel(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func doEnd(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(str_lastData.replacingOccurrences(of: "view", with: "a") + "tionU" + str_buttonUnderText.lowercased())] = uid
        //: dict["source"] = "2"
        dict[(String(str_atUserTextValue))] = "2"
        //: TalkingChatRequestTool.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        ViewRequestTool.tableTalkExecute(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func tillIcon() {
        //: TalkingPushManager.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        TalkingPushManager.share.equalAcross(uid: self.userModel.uid)
        //: self.dismiss()
        self.thoughtImage()
    }
}

//: extension TalkingVoiceRoomCardView {
extension LayerViewDelegate {
    //: @objc func moreBtnClick() {
    @objc func willCell() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == SubLabelReactiveCompatible.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == SubLabelReactiveCompatible.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(str_profileValue.suffix(4)) + " out").localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(str_butterData.prefix(6))).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(str_tabValue.prefix(4))).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((str_recommendName.replacingOccurrences(of: "make", with: "K") + String(str_viewCancelName)).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(str_transformData.suffix(5))).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(str_domainData.prefix(6))).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.CallConfig.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = MakeMenuView.subSave(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(str_buttonLengthText)))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func swaddlingClothes() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = ChangeShapeRoomReactiveCompatible(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName)
        //: popView?.initWithView(view: self)
        popView?.smartText(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? ManagerReactiveCompatible.getWindow())
        popView?.letter(view: self.nowController()?.view ?? ManagerReactiveCompatible.actionWindow())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func thoughtImage() {
        //: popView?.dismissView()
        popView?.referenceView()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension LayerViewDelegate: PlopViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func giftHead(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(str_profileValue.suffix(4)) + " out").localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            TalkingCostExhibitThen.titleConfig(title: nil, message: (String(str_fillValue.prefix(6)) + "m to " + String(str_directTitle.suffix(5)) + "out " + String(str_appSizeDataName.suffix(5)) + "oom?").localized, leftBtnTitle: (String(str_nameColorContent.prefix(6))).localized, rightBtnTitle: (String(str_portData)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                DutyProgressHUD.dowerImage()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                RoomReqTool.methodFor(targetUid: self.userModel.uid, roomId: EyelashEventHandler.firstBack().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    DutyProgressHUD.duringShow()
                    //: self.dismiss()
                    self.thoughtImage()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(str_butterData.prefix(6))).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            TalkingCostExhibitThen.titleConfig(title: nil, message: String(bytes: str_pushData.map{errorShare(layer: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(str_nameColorContent.prefix(6))).localized, rightBtnTitle: (String(str_hiddenValue.suffix(6)) + str_managerTitle.lowercased()).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                DutyProgressHUD.dowerImage()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                RoomReqTool.creamCompletion(targetUid: self.userModel.uid, roomID: EyelashEventHandler.firstBack().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    DutyProgressHUD.duringShow()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.playerMsg(showMsg: (String(str_loseMomentShareTitle.prefix(5)) + "ute " + String(str_sectionPushName.prefix(4)) + String(str_popName) + String(str_indicatorValue.prefix(6)) + "ted~").localized)
                    }
                    //: self.dismiss()
                    self.thoughtImage()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(str_tabValue.prefix(4))).localized: // 设置禁言
            //: muteView.showView()
            muteView.shouldDismiss()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.thoughtImage()

        //: case "Kick off".localized: /// 踢下麦
        case (str_recommendName.replacingOccurrences(of: "make", with: "K") + String(str_viewCancelName)).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            TalkingCostExhibitThen.titleConfig(title: nil, message: (String(str_mediumContent) + String(str_screenTargetValue.prefix(5)) + "ck o" + String(str_visualTitle.prefix(4)) + "he seat?").localized, leftBtnTitle: (String(str_nameColorContent.prefix(6))).localized, rightBtnTitle: (String(str_portData)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                EyelashEventHandler.firstBack().changeBy(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.thoughtImage()
            }

        //: case "Report".localized:
        case (String(str_domainData.prefix(6))).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = RangeAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.array(view: nil)
        //: case "Block".localized:
        case (String(str_transformData.suffix(5))).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            TalkingCostExhibitThen.titleConfig(title: nil, message: kLet_routeData, leftBtnTitle: (String(str_nameColorContent.prefix(6))).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                TalkingCostExhibitThen.harbourToAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                TalkingCostExhibitThen.harbourToAlert()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                StigmatiseThen.deleteCompletion(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.playerMsg(showMsg: (String(str_makeScaleValue.suffix(2)) + str_appPoseContent.replacingOccurrences(of: "position", with: "ie") + "g Su" + str_databaseData.replacingOccurrences(of: "view", with: "cc")).localized)
                        //: self.dismiss()
                        self.thoughtImage()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension LayerViewDelegate {
    // 添加视图
    //: private func setupSubviews() {
    private func weltanschauung() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func balloon() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(kLet_failureName / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-kLet_failureName / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(kLet_failureName / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.nowController()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.nowController()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.thoughtImage()
        }
    }
}
