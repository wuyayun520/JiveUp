
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_requestText:[UInt8] = [0xd1,0xd6,0xd1,0xcc,0x90,0xdb,0xd7,0xdc,0xdd,0xca,0x82,0x91,0x98,0xd0,0xd9,0xcb,0x98,0xd6,0xd7,0xcc,0x98,0xda,0xdd,0xdd,0xd6,0x98,0xd1,0xd5,0xc8,0xd4,0xdd,0xd5,0xdd,0xd6,0xcc,0xdd,0xdc]

private func addLabel(manager num: UInt8) -> UInt8 {
    return num ^ 184
}

/*: "uid" :*/
fileprivate let str_endStartLabName:[UInt8] = [0x8f,0x83,0x7e]

fileprivate func examineedFirst(request num: UInt8) -> UInt8 {
    let value = Int(num) + 230
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let str_layerData:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let str_inputCellData:String = "NICKNAM"
fileprivate let str_listContent:String = "convert"

/*: "birthday" :*/
fileprivate let str_pathOfName:[Character] = ["b","i","r","t","h","d"]
fileprivate let str_requestFrameArrayName:String = "layer"

/*: "videoUrl" :*/
fileprivate let str_fillValue:String = "viduser"

/*: "cover" :*/
fileprivate let str_announcementName:String = "coviewr"

/*: "location" :*/
fileprivate let str_comeName:String = "LOCATIO"
fileprivate let str_countVoiceData:[Character] = ["n"]

/*: "cmd" :*/
fileprivate let str_viewAirEdgeData:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let str_makeValue:String = "relab"
fileprivate let str_lastName:[Character] = ["u","s","e","C","a","l","l"]

/*: "data" :*/
fileprivate let str_timeMainName:[UInt8] = [0xc7,0xc2,0xd7,0xc2]

private func financialAid(lab num: UInt8) -> UInt8 {
    return num ^ 163
}

/*: "Call interruption" :*/
fileprivate let str_requestSumData:String = "color event let data fileCall "
fileprivate let str_rangeMapData:String = "rupsoundion"

/*: "stopCall" :*/
fileprivate let str_videoFillValue:[Character] = ["s","t","o","p","C","a","l"]
fileprivate let str_totalValue:String = "big"

/*: "logId" :*/
fileprivate let str_succeedFormatName:[UInt8] = [0x9c,0x9f,0x97,0xb9,0x94]

private func listenWith(app num: UInt8) -> UInt8 {
    return num ^ 240
}

/*: "cancelCall" :*/
fileprivate let str_onLastLetContent:String = "cdatac"
fileprivate let str_hiddenData:[Character] = ["e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let str_actionName:String = "text trueNo a"

/*: "duration" :*/
fileprivate let str_leadingToValue:String = "dubetweent"
fileprivate let str_naturalModelName:String = "collectionon"

/*: "She has hung up" :*/
fileprivate let str_clearKeyName:[Character] = ["S","h","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let str_transformName:String = "cfailn"
fileprivate let str_leadingValue:String = "celSexcount select effect"

/*: "He has hung up" :*/
fileprivate let str_labelValue:String = "He haposition var off self"
fileprivate let str_textAddValue:[Character] = ["s"," "]
fileprivate let str_actionScaleName:[Character] = ["h","u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let str_touchName:String = "show selfendTy"
fileprivate let str_partyTitle:[Character] = ["p","e"]

/*: "stopSex" :*/
fileprivate let str_clickValue:String = "stopSlabel back layer left"
fileprivate let str_adValue:String = "ecall"

/*: "Insufficient credit, call has ended" :*/
fileprivate let str_cardContent:[UInt8] = [0xca,0xed,0xf0,0xf6,0xe5,0xe5,0xea,0xe0,0xea,0xe6,0xed,0xf7,0xa3,0xe0,0xf1,0xe6,0xe7,0xea,0xf7,0xaf,0xa3,0xe0,0xe2,0xef,0xef,0xa3,0xeb,0xe2,0xf0,0xa3,0xe6,0xed,0xe7,0xe6,0xe7]

private func bagReload(source num: UInt8) -> UInt8 {
    return num ^ 131
}

/*: "The other hung up the phone" :*/
fileprivate let str_locationValue:String = "The ottype pair frame"
fileprivate let str_adjustmentText:[Character] = ["g"," ","u","p"," "]
fileprivate let str_likeName:[Character] = ["t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let str_nameLineTitle:String = "tappe"

/*: "jumpUrl" :*/
fileprivate let str_currentAddLetData:[Character] = ["j","u"]
fileprivate let str_renderKitValue:String = "view cellmpUrl"

/*: "Cancel" :*/
fileprivate let str_nowText:String = "Cancelline manager content image"

/*: "Purchase" :*/
fileprivate let str_labelVideoTitle:String = "Purchaelse index let make"
fileprivate let str_textTransitionTitle:[Character] = ["s","e"]

/*: "&type=3" :*/
fileprivate let str_upMainImageData:String = "video back after&type=3"

/*: "addBean" :*/
fileprivate let str_screenName:[Character] = ["a","d","d","B","e","a"]
fileprivate let str_statusToValue:String = "define"

/*: "refuseSex" :*/
fileprivate let str_popName:String = "rehiddenus"

/*: "onAcceptCall" :*/
fileprivate let str_lineMakeName:[Character] = ["o","n","A","c","c","e"]
fileprivate let str_targetValue:[Character] = ["p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let str_actualValue:String = "accemain"
fileprivate let str_dismissData:String = "manager name smart success resulttCall"

/*: "Calls are cancelled" :*/
fileprivate let str_interactionName:String = "case or now bottom viewCall"
fileprivate let str_extraSharedText:String = "e cancvar make dismiss year self"
fileprivate let str_checkTitle:String = "session"

/*: "The call has ended" :*/
fileprivate let str_lastData:[Character] = ["T","h","e"]
fileprivate let str_elementValue:String = " call player let pair"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TextObserverDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import UIKit
import UIKit

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let kLet_domainData = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class TextObserverDelegate: EqualVideoReactiveCompatible {
	var managerEnable: Bool = false
	var labelText: String = "half"
	var changeName: String = "pending"

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ProfessionalChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: EqualRemovePlayerThen? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: OfPlayerReactiveCompatible? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: CarpetScreenView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        TitleSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        TitleSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        TitleSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_requestText.map{addLabel(manager: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! SearchedReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.elite()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            leaveOnice()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            fromId(logId: self.chatModel?.logId)
        }
    
		if var worldViewValue = videoManager.infoModel?.pairHeadPic { 
	            if (self.tabBarController?.selectedIndex == 8) && (self.userActivity != nil) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let labelTap = CellTimeController()
	            labelTap.completeBeginMagnitude = interactionView.giftView.keyborHeight
	            labelTap.afterTargetContent = worldViewValue
	            labelTap.rangeOff = { [self] tableOpen in
	            self.managerEnable = tableOpen
	            
	            var labelTap = nav.pushing
	                labelTap = false
	                labelTap = !labelTap
	            if labelTap != self.managerEnable {
	                self.managerEnable = labelTap
	            }
	            
	            return self.managerEnable
	            }
	                self.navigationController?.pushViewController(labelTap.self, animated: true)
	            }
	
		}
	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            TitleSocketManager.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.upTalk()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        arrow()
        //: setupSubViewsConstraint()
        atError()
        //: initNotificationObserver()
        currencyAdd()
        //: req_callGetUserInfo()
        coinStart()
        //: timeoutRules()
        upRules()
        //: setUpBGMPlayer()
        belowObject()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.reading()
        }
    
		if var tinkleValue = player?.filePath { 
	            if (windowView.convert(CGPoint.zero, from: windowView.superview).x == 30.43) && (windowView.alpha != 1.0) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let bagLet = CellTimeView()
	            bagLet.endTitle = interactionView.giftView.chatRoomID
	            
	            
	            bagLet.modelTitle = { [self] listAppearTitle in
	            self.labelText = listAppearTitle
	            
	            var bagLet = tinkleValue
	            bagLet = bagLet.capitalized + "model" + bagLet
	            if bagLet.hasPrefix(self.labelText) {
	                self.labelText = bagLet
	            }
	            
	            return self.labelText
	            }
	                windowView.addSubview(bagLet)
	            }
	
		}
	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SubLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
            if SubLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = HadithShieldView(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        TitleSocketManager.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.willUser()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: DeleteReactiveCompatible = {
        //: let v = TalkingBGVideoView()
        let v = DeleteReactiveCompatible()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.theImageShow(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: ImageManagerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = ImageManagerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: ShowWindowView = {
        //: let v = TalkingVideoWindowView()
        let v = ShowWindowView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = kLet_failureName - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PropertyLineThen.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: kLet_messageSumervalName + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.renderIndex()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ServerViewThen = {
        //: let m = TalkingVideoChatManager()
        let m = ServerViewThen()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension TextObserverDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func coinStart() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ServerViewThen.nameData(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_endStartLabName.map{examineedFirst(request: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_layerData))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_inputCellData.lowercased() + str_listContent.replacingOccurrences(of: "convert", with: "e"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(str_pathOfName) + str_requestFrameArrayName.replacingOccurrences(of: "layer", with: "ay"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(str_fillValue.replacingOccurrences(of: "user", with: "e") + "oUrl")].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(str_announcementName.replacingOccurrences(of: "view", with: "ve"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(str_comeName.lowercased() + String(str_countVoiceData))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.active()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.disguise()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func disguise() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = RawTaskCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = RawTaskCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.exhibitCamera(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.toCamera(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension TextObserverDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func leaveOnice() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        AccountingDataReactiveCompatible.ofMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.barLog(showMsg: kLet_changeTitle)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                TitleSocketManager.shared.fromApp(info: [String(bytes: str_viewAirEdgeData.reversed(), encoding: .utf8)!: (str_makeValue.replacingOccurrences(of: "lab", with: "f") + String(str_lastName)), String(bytes: str_timeMainName.map{financialAid(lab: $0)}, encoding: .utf8)!: [String(bytes: str_endStartLabName.map{examineedFirst(request: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.startEqual()
                //: return
                return
            }

            //: self.startPreview()
            self.reading()
            //: self.stopBGM()
            self.afterReport()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = EqualRemovePlayerThen.byEqual(type: .Response)
            //: self.startBGM()
            self.elite()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func reading() {
        //: self.videoManager.startPreview()
        self.videoManager.eventShared()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func willUser() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.afterReport()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        PlayerAddDanmuManager.relinquish()
        //: TalkingSocketManager.shared.callerDelegate = nil
        TitleSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        TitleSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        TitleSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func startEqual(animated: Bool = true) {
        //: self.stopBGM()
        self.afterReport()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func renderIndex() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.blockMessage()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.disguise()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func behindUser() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.mentalFacultyData()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.disguise()
        // 展示投诉说明飘屏（男性）
//        self.show_videoFloatScreenAnimation()
    }

    /// 展示投诉说明飘屏（男性）
//    private func show_videoFloatScreenAnimation() {
//        guard reportView == nil else { return }
//
//        reportView = CarpetScreenView()
//        view.addSubview(reportView!)
//        reportView?.show_animation()
//    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension TextObserverDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func currencyAdd() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(registerAdd(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(requestNotification(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: kLet_viewFirstText,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(latNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_detailPlayPublicName,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_domainData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func registerAdd(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            startEqual()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func latNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard TitleSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        TitleSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        TitleSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        TitleSocketManager.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.recordTo()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func requestNotification(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.disguise()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension TextObserverDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.barLog(showMsg: (String(str_requestSumData.suffix(5)) + "inter" + str_rangeMapData.replacingOccurrences(of: "sound", with: "t")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                TitleSocketManager.shared.fromApp(info: [String(bytes: str_viewAirEdgeData.reversed(), encoding: .utf8)!: (String(str_videoFillValue) + str_totalValue.replacingOccurrences(of: "big", with: "l")), String(bytes: str_timeMainName.map{financialAid(lab: $0)}, encoding: .utf8)!: [String(bytes: str_succeedFormatName.map{listenWith(app: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                TitleSocketManager.shared.fromApp(info: [String(bytes: str_viewAirEdgeData.reversed(), encoding: .utf8)!: (str_onLastLetContent.replacingOccurrences(of: "data", with: "an") + String(str_hiddenData)), String(bytes: str_timeMainName.map{financialAid(lab: $0)}, encoding: .utf8)!: [String(bytes: str_succeedFormatName.map{listenWith(app: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                TitleSocketManager.shared.fromApp(info: [String(bytes: str_viewAirEdgeData.reversed(), encoding: .utf8)!: (str_makeValue.replacingOccurrences(of: "lab", with: "f") + String(str_lastName)), String(bytes: str_timeMainName.map{financialAid(lab: $0)}, encoding: .utf8)!: [String(bytes: str_endStartLabName.map{examineedFirst(request: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.serviceChange()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                TitleSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            startEqual()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension TextObserverDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func belowObject() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? CompleteMagnitude.Call : CompleteMagnitude.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = EqualRemovePlayerThen.byEqual(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func elite() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.infoOfPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func afterReport() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.cardStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension TextObserverDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func upRules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_indexTitle : self.chatModel?.pathInsert()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            buildCover()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.buildCover()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func buildCover() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !TitleSocketManager.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.barLog(showMsg: (String(str_actionName.suffix(4)) + "nswer").localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_viewAirEdgeData.reversed(), encoding: .utf8)!: (str_onLastLetContent.replacingOccurrences(of: "data", with: "an") + String(str_hiddenData)), String(bytes: str_timeMainName.map{financialAid(lab: $0)}, encoding: .utf8)!: [String(bytes: str_succeedFormatName.map{listenWith(app: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_viewAirEdgeData.reversed(), encoding: .utf8)!: (str_makeValue.replacingOccurrences(of: "lab", with: "f") + String(str_lastName)), String(bytes: str_timeMainName.map{financialAid(lab: $0)}, encoding: .utf8)!: [String(bytes: str_endStartLabName.map{examineedFirst(request: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        TitleSocketManager.shared.fromApp(info: socketDict)
        //: popCurrentViewController()
        startEqual()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension TextObserverDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func continueData(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_succeedFormatName.map{listenWith(app: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(str_leadingToValue.replacingOccurrences(of: "between", with: "ra") + str_naturalModelName.replacingOccurrences(of: "collection", with: "i"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.pushObserver(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.constraintPhoto()
            //: createTalkRoom()
            behindUser()
            //: self.stopBGM()
            self.afterReport()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func fromId(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.blockMessage()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.theImageShow(show: false)
        //: self.stopBGM()
        self.afterReport()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.constraintPhoto()
        //: self.createTalkRoom()
        self.behindUser()
    }
}

// MARK: - RowObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension TextObserverDelegate: RowObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func laughDoing(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_clearKeyName)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(str_transformName.replacingOccurrences(of: "fail", with: "a") + String(str_leadingValue.prefix(6)))].stringValue == CheckedUTF8Initializable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_labelValue.prefix(5)) + String(str_textAddValue) + String(str_actionScaleName)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.barLog(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.serviceChange()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            TitleSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        startEqual()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func toStart(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        fromId(logId: json[String(bytes: str_succeedFormatName.map{listenWith(app: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func smolder(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_touchName.suffix(5)) + String(str_partyTitle))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_clearKeyName)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_clickValue.prefix(5)) + str_adValue.replacingOccurrences(of: "call", with: "x"))].stringValue == CheckedUTF8Initializable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_labelValue.prefix(5)) + String(str_textAddValue) + String(str_actionScaleName)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue {
                //: typeStr = "Insufficient credit, call has ended".localized
                typeStr = String(bytes: str_cardContent.map{bagReload(source: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_locationValue.prefix(6)) + "her hun" + String(str_adjustmentText) + String(str_likeName)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.barLog(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.serviceChange()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            TitleSocketManager.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.matchDismiss()
        //: popCurrentViewController()
        startEqual()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func hemAndHaw(msg: String, data: [String: Any]) {
        //: guard SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(str_nameLineTitle.replacingOccurrences(of: "app", with: "yp"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_currentAddLetData) + String(str_renderKitValue.suffix(5)))] as? String else { return }
            //: let httpsUrl = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = RoomConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TalkingPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            TalkingPushManager.share.counteractionPlayer(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            TalkingCostExhibitThen.titleConfig(title: nil,
                                         //: message: msg,
                                         message: msg,
                                         //: leftBtnTitle: "Cancel".localized,
                                         leftBtnTitle: (String(str_nowText.prefix(6))).localized,
                                         //: rightBtnTitle: "Purchase".localized) {
                                         rightBtnTitle: (String(str_labelVideoTitle.prefix(6)) + String(str_textTransitionTitle)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                TalkingCostExhibitThen.harbourToAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingPushManager.share.func__pushToRechargeAlert(appendParams: "&type=3")
                TalkingPushManager.share.totalerval(appendParams: (String(str_upMainImageData.suffix(7))))
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func isoclinalSuccess(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.modelPunctuateThird(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func withButton(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_screenName) + str_statusToValue.replacingOccurrences(of: "define", with: "n"))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.screenOutFail(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.overdo(point: addBean)
        }
    }
}

// MARK: - LabelObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension TextObserverDelegate: LabelObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func socketTableAssemblage(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_clearKeyName)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(str_popName.replacingOccurrences(of: "hidden", with: "f") + "eSex")].stringValue == CheckedUTF8Initializable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_labelValue.prefix(5)) + String(str_textAddValue) + String(str_actionScaleName)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.barLog(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.serviceChange()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            TitleSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        startEqual()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func pushData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        fromId(logId: json[String(bytes: str_succeedFormatName.map{listenWith(app: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - DataObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension TextObserverDelegate: DataObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func choiceHandle(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (str_makeValue.replacingOccurrences(of: "lab", with: "f") + String(str_lastName)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.barLog(showMsg: (String(str_actionName.suffix(4)) + "nswer").localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.serviceChange()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                TitleSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            startEqual()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_lineMakeName) + String(str_targetValue)) && errorNo == 101 { // 接听通话时，余额不足
            //: TalkingPushManager.share.func__pushToRechargeAlert(appendParams: "&type=3")
            TalkingPushManager.share.totalerval(appendParams: (String(str_upMainImageData.suffix(7))))

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_lineMakeName) + String(str_targetValue)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.barLog(showMsg: msg)
            //: popCurrentViewController()
            startEqual()
        }
    }
}

// MARK: - OfViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension TextObserverDelegate: OfViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func pointOfReference() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        TitleSocketManager.shared.fromApp(info: [String(bytes: str_viewAirEdgeData.reversed(), encoding: .utf8)!: (str_actualValue.replacingOccurrences(of: "main", with: "p") + String(str_dismissData.suffix(5))), String(bytes: str_timeMainName.map{financialAid(lab: $0)}, encoding: .utf8)!: [String(bytes: str_endStartLabName.map{examineedFirst(request: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func incomeEnd() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.barLog(showMsg: (String(str_interactionName.suffix(4)) + "s ar" + String(str_extraSharedText.prefix(6)) + "elle" + str_checkTitle.replacingOccurrences(of: "session", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            TitleSocketManager.shared.fromApp(info: [String(bytes: str_viewAirEdgeData.reversed(), encoding: .utf8)!: (str_onLastLetContent.replacingOccurrences(of: "data", with: "an") + String(str_hiddenData)), String(bytes: str_timeMainName.map{financialAid(lab: $0)}, encoding: .utf8)!: [String(bytes: str_succeedFormatName.map{listenWith(app: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.barLog(showMsg: (String(str_interactionName.suffix(4)) + "s ar" + String(str_extraSharedText.prefix(6)) + "elle" + str_checkTitle.replacingOccurrences(of: "session", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            TitleSocketManager.shared.fromApp(info: [String(bytes: str_viewAirEdgeData.reversed(), encoding: .utf8)!: (str_makeValue.replacingOccurrences(of: "lab", with: "f") + String(str_lastName)), String(bytes: str_timeMainName.map{financialAid(lab: $0)}, encoding: .utf8)!: [String(bytes: str_endStartLabName.map{examineedFirst(request: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.barLog(showMsg: (String(str_lastData) + String(str_elementValue.prefix(6)) + "has ended").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            TitleSocketManager.shared.fromApp(info: [String(bytes: str_viewAirEdgeData.reversed(), encoding: .utf8)!: (String(str_videoFillValue) + str_totalValue.replacingOccurrences(of: "big", with: "l")), String(bytes: str_timeMainName.map{financialAid(lab: $0)}, encoding: .utf8)!: [String(bytes: str_succeedFormatName.map{listenWith(app: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        startEqual()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func enablely(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.makeRemove(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func showFill(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.theImageShow(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.enjoyFirstOf(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.disguise()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func prod() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        TitleSocketManager.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = OfPlayerReactiveCompatible.afterVideoView()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.startConversation()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.toStop()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.nowController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        startEqual()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func groin() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension TextObserverDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func arrow() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.colorView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func atError() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
