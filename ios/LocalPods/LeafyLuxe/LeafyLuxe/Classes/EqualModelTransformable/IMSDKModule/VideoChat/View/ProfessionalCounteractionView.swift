
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_labelValue:[UInt8] = [0x45,0x42,0x45,0x58,0x4,0x4f,0x43,0x48,0x49,0x5e,0x16,0x5,0xc,0x44,0x4d,0x5f,0xc,0x42,0x43,0x58,0xc,0x4e,0x49,0x49,0x42,0xc,0x45,0x41,0x5c,0x40,0x49,0x41,0x49,0x42,0x58,0x49,0x48]

private func momentUsed(index num: UInt8) -> UInt8 {
    return num ^ 44
}

/*: "#322870" :*/
fileprivate let str_applicationValue:[Character] = ["#","3","2","2","8","7","0"]

/*: "icon_home_position" :*/
fileprivate let str_layerData:[Character] = ["i","c","o","n","_","h","o","m","e","_"]
fileprivate let str_makeContent:[Character] = ["p","o","s","i","t","i","o","n"]

/*: "btn_video_minimize" :*/
fileprivate let str_transformName:String = "data makebtn_"
fileprivate let str_appTitle:String = "var color self_minim"
fileprivate let str_piText:String = "spende"

/*: "btn_video_drop_nor" :*/
fileprivate let str_labelData:String = "true white view color cellbtn_vid"
fileprivate let str_aContent:String = "if current type selfeo_d"

/*: "btn_video_call_nor" :*/
fileprivate let str_makePointName:String = "btn_make chemistry margin"
fileprivate let str_talkContent:String = "_calaccept input var self"

/*: "transform.scale" :*/
fileprivate let str_infoLabelData:String = "text"
fileprivate let str_makeName:[Character] = ["r","a","n","s","f","o","r","m",".","s","c","a","l","e"]

/*: "animation" :*/
fileprivate let str_makeGiftValue:[Character] = ["a","n","i","m","a"]
fileprivate let str_leadingData:[Character] = ["t","i","o","n"]

/*: "icon_videocall_female_answer_bg" :*/
fileprivate let str_topTitle:[UInt8] = [0x67,0x62,0x5f,0x72,0x65,0x77,0x73,0x6e,0x61,0x5f,0x65,0x6c,0x61,0x6d,0x65,0x66,0x5f,0x6c,0x6c,0x61,0x63,0x6f,0x65,0x64,0x69,0x76,0x5f,0x6e,0x6f,0x63,0x69]

/*: "icon_videocall_female_answer" :*/
fileprivate let str_refuseText:String = "equal model row coloricon_v"
fileprivate let str_managerData:String = "ideokey"
fileprivate let str_theFireTitle:String = "emplayere"

/*: "icon_calltalk_points" :*/
fileprivate let str_renderData:[Character] = ["i","c","o","n","_"]
fileprivate let str_targetNameValue:[Character] = ["c","a","l","l","t","a"]
fileprivate let str_loadName:[Character] = ["l","k","_","p","o","i","n","t","s"]

/*: "get json error" :*/
fileprivate let str_viewModeTitle:String = "get jsonstop current data below import"
fileprivate let str_toToolData:[Character] = [" ","e","r","r","o","r"]

/*: "btn_voice_Micr_nor" :*/
fileprivate let str_blockEnableText:String = "to streambtn_vo"
fileprivate let str_requestToTitle:String = "novideo"

/*: "btn_voice_Micr_pre" :*/
fileprivate let str_starData:[Character] = ["b","t","n","_","v","o","i","c","e","_","M","i"]
fileprivate let str_statusArrayActionText:[Character] = ["c","r","_","p","r","e"]

/*: "btn_voice_hands_nor" :*/
fileprivate let str_topContent:[Character] = ["b","t","n","_","v","o","i","c","e","_","h","a","n","d","s","_","n","o","r"]

/*: "btn_voice_hands_pre" :*/
fileprivate let str_backContent:[Character] = ["b","t","n","_","v"]
fileprivate let str_makeFillValue:String = "oice_background make"
fileprivate let str_liveTitle:String = "s_preself self"

/*: "Waiting to answer the phone..." :*/
fileprivate let str_viewControlTitle:String = "Waitcookie max model count color"
fileprivate let str_succeedName:String = " answeprice self accept self"
fileprivate let str_shareName:String = "bottom adde pho"
fileprivate let str_partyValue:String = "ne...color color mode"

/*: "yyyy-MM-dd" :*/
fileprivate let str_insideTitle:String = "last equal labelyyyy-"
fileprivate let str_thanAddValue:[Character] = ["M","M","-","d","d"]

/*: "00:00" :*/
fileprivate let str_indicatorName:String = "top:top"

/*: "Waiting for %@ to answer the phone..." :*/
fileprivate let str_managerValue:[UInt8] = [0x9e,0xa8,0xa0,0xbd,0xa0,0xa7,0xae,0xe9,0xaf,0xa6,0xbb,0xe9,0xec,0x89,0xe9,0xbd,0xa6,0xe9,0xa8,0xa7,0xba,0xbe,0xac,0xbb,0xe9,0xbd,0xa1,0xac,0xe9,0xb9,0xa1,0xa6,0xa7,0xac,0xe7,0xe7,0xe7]

private func courseOfAction(view num: UInt8) -> UInt8 {
    return num ^ 201
}

/*: " +" :*/
fileprivate let str_normalTitle:String = "model+"

/*: "%@ points/min" :*/
fileprivate let str_voiceMakeTitle:String = "%@ poitrue return game"

/*: "CIGaussianBlur" :*/
fileprivate let str_valueToTitleData:[Character] = ["C","I","G","a","u","s","s","i","a","n","B"]
fileprivate let str_compareWithUserValue:[Character] = ["l","u","r"]

/*: "toUid" :*/
fileprivate let str_mediumAddTitle:String = "toUiddaily let"

/*: "giftId" :*/
fileprivate let str_detailValue:String = "giftIdnest auto wrap size"

/*: "giftNum" :*/
fileprivate let str_indexGiftTitle:String = "instance datagiftNum"

/*: "from" :*/
fileprivate let str_cleanTitle:String = "ftextom"

/*: "audioCall" :*/
fileprivate let str_labModeTitle:[Character] = ["a","u","d","i","o","C","a","l","l"]

/*: "pkgItemsetId" :*/
fileprivate let str_smartData:String = "format user sizepkgItem"
fileprivate let str_topValue:[Character] = ["s","e","t","I","d"]

/*: "msgInfo" :*/
fileprivate let str_sharedRangeTitle:[Character] = ["m","s","g","I","n"]
fileprivate let str_lengthModelName:String = "collection"

/*: "msgType" :*/
fileprivate let str_replyData:[Character] = ["m"]
fileprivate let str_imageYetValue:[Character] = ["s","g","T","y","p","e"]

/*: "gift" :*/
fileprivate let str_managerAddValue:[Character] = ["g","i","f","t"]

/*: "totalMfCoin" :*/
fileprivate let str_useValue:String = "totalMelse name if icon touch"
fileprivate let str_selectionName:[Character] = ["f","C","o","i","n"]

/*: "cellData" :*/
fileprivate let str_shareValue:[UInt8] = [0x61,0x74,0x61,0x44,0x6c,0x6c,0x65,0x63]

/*: "%02i:%02i" :*/
fileprivate let str_editTitle:[Character] = ["%","0","2","i"]
fileprivate let str_normalGroupValue:String = ":%02ileft make video"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ProfessionalCounteractionView.swift
//  AbroadTalking
//
//  Created by young on 2022/12/15.
//

//: import UIKit
import UIKit

//: protocol Talking1v1VoiceInteractionViewDelegate: NSObjectProtocol {
protocol HairObjectProtocol: NSObjectProtocol {
    /// 发送方结束通话
    //: func interactionView_shouldEndCall()
    func boardPush()

    /// 接听音频通话
    //: func interactionView_shouldAcceptCall()
    func releaseTime()

    /// 是否静音
    /// - Parameter isMute: 是否静音（默认否）
    //: func interactionView_shouldChangeMute(isMute: Bool)
    func enableFinish(isMute: Bool)

    /// 是否免提
    /// - Parameter isHandsfree: 是否免提（默认否）
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool)
    func loadIn(isHandsfree: Bool)

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow()
    func topMedium()

    /// 更新小窗口通话时间
    /// - Parameter time: 通话时间
    //: func interactionView_shouldUpdateTime(time: Double)
    func tableTime(time: Double)
}

//: class Talking1v1VoiceInteractionView: UIView {
class ProfessionalCounteractionView: UIView {
    //: weak var delegate: Talking1v1VoiceInteractionViewDelegate?
    weak var delegate: HairObjectProtocol?
    //: private var chatModel = TalkingVideoChatModel()
    private var chatModel = ProfessionalChatModel() // 引用类型，整个音频通话共用同一个model
    //: private var waitTimer: Timer?
    private var waitTimer: Timer? // 等待接听动画定时器
    //: private var startTime: TimeInterval?
    private var startTime: TimeInterval? // 开始的时间戳记 since1970
    //: private var countTimer: Timer?
    private var countTimer: Timer? // 通话时间定时器

    //: init(frame: CGRect, chatModel: TalkingVideoChatModel?) {
    init(frame: CGRect, chatModel: ProfessionalChatModel?) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        header()
        //: setupSubViewsConstraint()
        lowliness()
        //: if chatModel != nil {
        if chatModel != nil {
            //: self.chatModel = chatModel!
            self.chatModel = chatModel!
            //: self.startTime = chatModel!.startTime
            self.startTime = chatModel!.startTime
        }
        //: showViewsViaStage()
        reinIn()
        //: initCachEffectData()
        cach()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_labelValue.map{momentUsed(index: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: if waitTimer != nil {
        if waitTimer != nil {
            //: waitTimer?.invalidate()
            waitTimer?.invalidate()
            //: waitTimer = nil
            waitTimer = nil
        }
        //: if countTimer != nil {
        if countTimer != nil {
            //: countTimer?.invalidate()
            countTimer?.invalidate()
            //: countTimer = nil
            countTimer = nil
        }
//        self.player.stopPlay()
    }

    // MARK: - Lazy Load

    // 渐变背景视图
    //: lazy var bgView: UIImageView = {
    lazy var bgView: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        // 渐变色
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        // 宽高+1是为了适配在某些设备上出现渐变填充不满的情况
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: kLet_failureName + 1, height: kLet_appEventDomainName + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(str_applicationValue)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: v.layer.addSublayer(layer)
        v.layer.addSublayer(layer)

        //: return v
        return v
        //: }()
    }()

    // 点击展示/隐藏交互按钮
    //: private lazy var touchHidenBtn: UIButton = {
    private lazy var touchHidenBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(changeInteractionShowState), for: .touchUpInside)
        btn.addTarget(self, action: #selector(addCell), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像
    //: private lazy var pairPicV: UIImageView = {
    private lazy var pairPicV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = actualWidth(w: 100)/2
        v.layer.cornerRadius = actualWidth(w: 100) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: return v
        return v
        //: }()
    }()

    // 接收方昵称
    //: private lazy var pairNameLab: UILabel = {
    private lazy var pairNameLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.underPlay(fontSize: 20)
        lab.font = UIFont.underPlay(fontSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var locationBtn: TalkingButton = {
    private lazy var locationBtn: OfTalkingButton = {
        //: let btn = TalkingButton()
        let btn = OfTalkingButton()
        //: btn.setImage(UIImage.nameTitle(name: "icon_home_position"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_layerData) + String(str_makeContent))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.rugularSize(fontSize: 12)
        btn.titleLabel?.font = UIFont.rugularSize(fontSize: 12)
        //: btn.titleLabel?.lineBreakMode = .byTruncatingTail
        btn.titleLabel?.lineBreakMode = .byTruncatingTail
        //: return btn
        return btn
        //: }()
    }()

    // 呼叫中描述
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = UIColor(white: 1, alpha: 1)
        lab.textColor = UIColor(white: 1, alpha: 1)
        //: lab.font = UIFont.rugularSize(fontSize: 17)
        lab.font = UIFont.rugularSize(fontSize: 17)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    // 最小化按钮
    //: private lazy var minimizeBtn: UIButton = {
    private lazy var minimizeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nameTitle(name: "btn_video_minimize"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_transformName.suffix(4)) + "video" + String(str_appTitle.suffix(6)) + str_piText.replacingOccurrences(of: "spend", with: "iz"))), for: .normal)
        //: btn.addTarget(self, action: #selector(minimizeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(product), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nameTitle(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_labelData.suffix(7)) + String(str_aContent.suffix(4)) + "rop_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(conference), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nameTitle(name: "btn_video_call_nor"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_makePointName.prefix(4)) + "video" + String(str_talkContent.prefix(4)) + "l_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fixingDisappear), for: .touchUpInside)

        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (str_infoLabelData.replacingOccurrences(of: "text", with: "t") + String(str_makeName))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80/70), y: actualWidth(w: 80/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80 / 70), y: actualWidth(w: 80 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (String(str_makeGiftValue) + String(str_leadingData)))

        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBGBtn: UIButton = {
    private lazy var femaleAcceptBGBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setBackgroundImage(UIImage.nameTitle(name: "icon_videocall_female_answer_bg"), for: .normal)
        btn.setBackgroundImage(UIImage.nameTitle(name: String(bytes: str_topTitle.reversed(), encoding: .utf8)!), for: .normal)
        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (str_infoLabelData.replacingOccurrences(of: "text", with: "t") + String(str_makeName))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
        //: keyAnimation.duration = 0.6
        keyAnimation.duration = 0.6
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (String(str_makeGiftValue) + String(str_leadingData)))
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBtn: UIButton = {
    private lazy var femaleAcceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nameTitle(name: "icon_videocall_female_answer"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_refuseText.suffix(6)) + str_managerData.replacingOccurrences(of: "key", with: "c") + "all_f" + str_theFireTitle.replacingOccurrences(of: "player", with: "al") + "_answer")), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fixingDisappear), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听积分提示
    //: private lazy var femaleAcceptTipsBtn: UIButton = {
    private lazy var femaleAcceptTipsBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nameTitle(name: "icon_calltalk_points"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_renderData) + String(str_targetNameValue) + String(str_loadName))), for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.rugularSize(fontSize: 15)
        btn.titleLabel?.font = UIFont.rugularSize(fontSize: 15)
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var acceptPlayer: SVGAPlayer = {
    lazy var acceptPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .CallTalk_Accept)
        let url = PuncherEffectTool.default.betweenPath(type: .CallTalk_Accept)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_viewModeTitle.prefix(8)) + String(str_toToolData)))
        }
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PropertyLineThen.shared.direction == .rightToLeft {
            //: player.transform = CGAffineTransform(scaleX: -1, y: 1)
            player.transform = CGAffineTransform(scaleX: -1, y: 1)
        }
        //: return player
        return player
        //: }()
    }()

    // 开启/关闭麦克风按钮
    //: private lazy var micBtn: UIButton = {
    private lazy var micBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nameTitle(name: "btn_voice_Micr_nor"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_blockEnableText.suffix(6)) + "ice_Micr_" + str_requestToTitle.replacingOccurrences(of: "video", with: "r"))), for: .normal)
        //: btn.setImage(UIImage.nameTitle(name: "btn_voice_Micr_pre"), for: .selected)
        btn.setImage(UIImage.nameTitle(name: (String(str_starData) + String(str_statusArrayActionText))), for: .selected)
        //: btn.addTarget(self, action: #selector(microphoneButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(toTable), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭免提按钮
    //: lazy var handsfreeBtn: UIButton = {
    lazy var handsfreeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nameTitle(name: "btn_voice_hands_nor"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_topContent))), for: .normal)
        //: btn.setImage(UIImage.nameTitle(name: "btn_voice_hands_pre"), for: .selected)
        btn.setImage(UIImage.nameTitle(name: (String(str_backContent) + String(str_makeFillValue.prefix(5)) + "hand" + String(str_liveTitle.prefix(5)))), for: .selected)
        //: btn.addTarget(self, action: #selector(handsfreeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(handsfreeAndMoveClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

//    // 充值按钮
//    private lazy var rechargeBtn: UIButton = {
//        let btn = UIButton()
//        btn.setImage(UIImage.nameTitle(name: "icon_coin_nor"), for: .normal)
//        btn.setImage(UIImage.nameTitle(name: "icon_coin_nor"), for: .selected)
//        btn.addTarget(self, action: #selector(rechargeBtnClick), for: .touchUpInside)
//        return btn
//    }()
//    // 送礼按钮
//    private lazy var sendGiftBtn: UIButton = {
//        let btn = UIButton()
//        btn.setImage(UIImage.nameTitle(name: "btn_voice_gift_nor"), for: .normal)
//        btn.setImage(UIImage.nameTitle(name: "btn_voice_gift_nor"), for: .selected)
//        btn.addTarget(self, action: #selector(sendGiftButtonClick), for: .touchUpInside)
//        return btn
//    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.underPlay(fontSize: 18)
        lab.font = UIFont.underPlay(fontSize: 18)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: ChatTitleViewDelegate = {
        //: let giftV = TalkingChatGiftView.init(style: .call)
        let giftV = ChatTitleViewDelegate(style: .call)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: OffdDoorReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView()
        let effectView = OffdDoorReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var giftTrackView: TalkingGiftTrackView = {
    lazy var giftTrackView: OfThen = {
        //: let trackView = TalkingGiftTrackView()
        let trackView = OfThen()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: lazy var pointAnimView: TalkingPointAnimationView = {
    lazy var pointAnimView: ComputerFilenameView = {
        //: let pointView = TalkingPointAnimationView()
        let pointView = ComputerFilenameView()
        //: pointView.isUserInteractionEnabled = false
        pointView.isUserInteractionEnabled = false
        //: return pointView
        return pointView
        //: }()
    }()
}

// MARK: - Event

//: extension Talking1v1VoiceInteractionView {
extension ProfessionalCounteractionView {
    /// 展示视图状态
    //: func showViewsViaStage() {
    func reinIn() {
        //: switch chatModel.videoStage {
        switch chatModel.videoStage {
        //: case .Waiting:
        case .Waiting:
            //: desLab.text = "Waiting to answer the phone...".localized
            desLab.text = (String(str_viewControlTitle.prefix(4)) + "ing to" + String(str_succeedName.prefix(6)) + "r th" + String(str_shareName.suffix(5)) + String(str_partyValue.prefix(5))).localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: micBtn.isHidden = true
            micBtn.isHidden = true
            //: handsfreeBtn.isHidden = true
            handsfreeBtn.isHidden = true
//            sendGiftBtn.isHidden = true
//            rechargeBtn.isHidden = true
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false

        //: case .Recieved:
        case .Recieved:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: if SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue {
                //: acceptBtn.isHidden = false
                acceptBtn.isHidden = false
                //: femaleAcceptBtn.isHidden = true
                femaleAcceptBtn.isHidden = true
                //: femaleAcceptTipsBtn.isHidden = true
                femaleAcceptTipsBtn.isHidden = true
                //: acceptPlayer.isHidden = true
                acceptPlayer.isHidden = true
                //: femaleAcceptBGBtn.isHidden = true
                femaleAcceptBGBtn.isHidden = true
                //: } else {
            } else {
                //: acceptBtn.isHidden = true
                acceptBtn.isHidden = true
                //: femaleAcceptBtn.isHidden = false
                femaleAcceptBtn.isHidden = false
                //: femaleAcceptTipsBtn.isHidden = false
                femaleAcceptTipsBtn.isHidden = false
                //: femaleAcceptBGBtn.isHidden = false
                femaleAcceptBGBtn.isHidden = false
                //: let date = Defaults.object(forKey: TalkingCallShowTag)
                let date = kLet_guideScaleData.object(forKey: kLet_useContent)
                //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                let today = NSDate.finishM(date: Date(), dateFormat: (String(str_insideTitle.suffix(5)) + String(str_thanAddValue)))
                //: if date == nil || (date as! String) != today {
                if date == nil || (date as! String) != today {
                    //: Defaults.set(today, forKey: TalkingCallShowTag)
                    kLet_guideScaleData.set(today, forKey: kLet_useContent)
                    //: acceptPlayer.isHidden = false
                    acceptPlayer.isHidden = false
                    //: } else {
                } else {
                    //: acceptPlayer.isHidden = true
                    acceptPlayer.isHidden = true
                }
            }
            //: micBtn.isHidden = true
            micBtn.isHidden = true
            //: handsfreeBtn.isHidden = true
            handsfreeBtn.isHidden = true
//            sendGiftBtn.isHidden = true
//            rechargeBtn.isHidden = true
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false

        //: case .Calling:
        case .Calling:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: minimizeBtn.isHidden = false
            minimizeBtn.isHidden = false
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: micBtn.isHidden = false
            micBtn.isHidden = false
            //: handsfreeBtn.isHidden = false
            handsfreeBtn.isHidden = false
//            sendGiftBtn.isHidden = false
//            rechargeBtn.isHidden = false
            //: timeLab.isHidden = false
            timeLab.isHidden = false
            //: touchHidenBtn.isEnabled = true
            touchHidenBtn.isEnabled = true
            //: locationBtn.isHidden = true
            locationBtn.isHidden = true
            //: if startTime != nil {
            if startTime != nil {
                //: changeTalkTime()
                messageTime()
                //: } else {
            } else {
                //: timeLab.text = "00:00"
                timeLab.text = (str_indicatorName.replacingOccurrences(of: "top", with: "00"))
            }
            //: startTalkCount()
            subPath()
            //: touchHiddenTimer()
            concealed()
        }

        //: updateLayout()
        bite()
        //: afterGetInfo()
        get()
    }

    /// 刷新通话视图数据
    //: func afterGetInfo() {
    func get() {
        //: if chatModel.videoStage == .Waiting {
        if chatModel.videoStage == .Waiting {
            //: desLab.text = "Waiting for %@ to answer the phone...".localizedArguments(chatModel.pairNickName)
            desLab.text = String(bytes: str_managerValue.map{courseOfAction(view: $0)}, encoding: .utf8)!.localizedArguments(chatModel.pairNickName)
        }
        //: pairNameLab.text = chatModel.pairNickName
        pairNameLab.text = chatModel.pairNickName
        //: locationBtn.isHidden = chatModel.location.count <= 0
        locationBtn.isHidden = chatModel.location.count <= 0
        //: locationBtn.setTitle("\(chatModel.location)", for: .normal)
        locationBtn.setTitle("\(chatModel.location)", for: .normal)
        //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 20))]
        let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 20))]
        //: pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        pairPicV.overMake(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        //: femaleAcceptTipsBtn.setTitle(" +" + "%@ points/min".localizedArguments(chatModel.points), for: .normal)
        femaleAcceptTipsBtn.setTitle(" +" + (String(str_voiceMakeTitle.prefix(6)) + "nts/min").localizedArguments(chatModel.points), for: .normal)
        // 设置背景头像
        //: if !chatModel.pairHeadPic.isEmptyString {
        if !chatModel.pairHeadPic.isEmptyString {
            //: bgView.setUrlImage(urlStr: chatModel.pairHeadPic, options: nil) { [weak self] _, image in
            bgView.overMake(urlStr: chatModel.pairHeadPic, options: nil) { [weak self] _, image in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if image != nil {
                if image != nil {
                    //: self.bgView.image = self.blurImage(image!)
                    self.bgView.image = self.currentStatus(image!)
                }
            }
        }
    }

    /// 最小化按钮事件
    //: @objc private func minimizeButtonClick() {
    @objc private func product() {
        //: delegate?.interactionView_shouldDisplayMiniWindow()
        delegate?.topMedium()
    }

    /// 挂断按钮事件
    //: @objc private func hangupButtonClick() {
    @objc private func conference() {
        //: delegate?.interactionView_shouldEndCall()
        delegate?.boardPush()
    }

    /// 接听按钮事件
    //: @objc private func acceptButtonClick() {
    @objc private func fixingDisappear() {
        //: delegate?.interactionView_shouldAcceptCall()
        delegate?.releaseTime()
    }

    /// 麦克风按钮事件
    //: @objc private func microphoneButtonClick() {
    @objc private func toTable() {
        //: micBtn.isSelected = !micBtn.isSelected
        micBtn.isSelected = !micBtn.isSelected
        //: delegate?.interactionView_shouldChangeMute(isMute: micBtn.isSelected)
        delegate?.enableFinish(isMute: micBtn.isSelected)
    }

    /// 免提按钮事件
    //: @objc private func handsfreeButtonClick() {
    @objc private func handsfreeAndMoveClick() {
        //: delegate?.interactionView_shouldChangeSpeaker(isHandsfree: !handsfreeBtn.isSelected)
        delegate?.loadIn(isHandsfree: !handsfreeBtn.isSelected)
    }

    /// 送礼按钮事件
    //: @objc private func sendGiftButtonClick() {
    @objc private func throwVideo() {
        //: func__sendGift()
        rowFrom()
    }

    /// 充值按钮事件
    //: @objc private func rechargeBtnClick() {
    @objc private func needMagnitudeimate() {
        //: TalkingPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
        TalkingPushManager.share.combineLikeOccurrence(webViewType: .RechargeHalfPage)
    }

    /// 点击展示/隐藏通话中的按钮
    //: @objc private func changeInteractionShowState() {
    @objc private func addCell() {
        //: let isHidden = !hangupBtn.isHidden
        let isHidden = !hangupBtn.isHidden
        //: minimizeBtn.isHidden = isHidden
        minimizeBtn.isHidden = isHidden
        //: hangupBtn.isHidden = isHidden
        hangupBtn.isHidden = isHidden
        //: micBtn.isHidden = isHidden
        micBtn.isHidden = isHidden
        //: handsfreeBtn.isHidden = isHidden
        handsfreeBtn.isHidden = isHidden
//        sendGiftBtn.isHidden = isHidden
//        rechargeBtn.isHidden = isHidden
        //: if isHidden {
        if isHidden {
            //: invalidateIdleTimer()
            viewIdle()
            //: } else {
        } else {
            //: touchHiddenTimer()
            concealed()
        }
    }

    //: func blurImage(_ image: UIImage) -> UIImage {
    func currentStatus(_ image: UIImage) -> UIImage {
        //: let context = CIContext(options: nil)
        let context = CIContext(options: nil)
        //: let inputImage = CIImage(image: image)
        let inputImage = CIImage(image: image)
        // 使用高斯模糊滤镜
        //: let filter = CIFilter(name: "CIGaussianBlur")!
        let filter = CIFilter(name: (String(str_valueToTitleData) + String(str_compareWithUserValue)))!
        //: filter.setValue(inputImage, forKey: kCIInputImageKey)
        filter.setValue(inputImage, forKey: kCIInputImageKey)
        // 设置模糊半径值（越大越模糊）
        //: filter.setValue(5, forKey: kCIInputRadiusKey)
        filter.setValue(5, forKey: kCIInputRadiusKey)
        //: let outputCIImage = filter.outputImage!
        let outputCIImage = filter.outputImage!
        //: let rect = CGRect(origin: CGPoint.zero, size: image.size)
        let rect = CGRect(origin: CGPoint.zero, size: image.size)
        //: let cgImage = context.createCGImage(outputCIImage, from: rect)
        let cgImage = context.createCGImage(outputCIImage, from: rect)
        // 显示生成的模糊图片
        //: let newImage = UIImage(cgImage: cgImage!)
        let newImage = UIImage(cgImage: cgImage!)
        //: return newImage
        return newImage
    }

    //: func showPointAnimaData(point: String) {
    func joint(point: String) {
        //: addSubview(pointAnimView)
        addSubview(pointAnimView)
        //: pointAnimView.snp.remakeConstraints { make in
        pointAnimView.snp.remakeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pointAnimView.setPointData(point: point)
        pointAnimView.seed(point: point)
    }
}

// MARK: - 送礼

//: extension Talking1v1VoiceInteractionView {
extension ProfessionalCounteractionView {
    //: func func__sendGift() {
    func rowFrom() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent {
        VideoToReactiveCompatible.share.refreshAlbum {
            //: self.func__showGiftChoiceView()
            self.objectUp()
        }
    }

    //: func func__showGiftChoiceView() {
    func objectUp() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: SubLabelReactiveCompatible.share.loginUserMode.mf_coin)
        giftView.bag(needReload: true, mf_coin: SubLabelReactiveCompatible.share.loginUserMode.mf_coin)
        //: window?.addSubview(giftView)
        window?.addSubview(giftView)
        //: giftView.showView()
        giftView.sight()
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: ProfessionalTransformable, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftModel: giftModel, giftNum: num)
            self.aboveMessage(giftModel: giftModel, giftNum: num)
        }
    }

    //: func func_giftViewDismiss() {
    func shimmer() {
        //: giftView.dismissView()
        giftView.hiddenView()
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func aboveMessage(giftModel: ProfessionalTransformable, giftNum: String) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            barLog(showMsg: kLet_eventText)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if chatModel.iUid == SubLabelReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == SubLabelReactiveCompatible.share.loginUserMode.userID {
            //: params["toUid"] = chatModel.pairUid
            params[(String(str_mediumAddTitle.prefix(5)))] = chatModel.pairUid
            //: } else {
        } else {
            //: params["toUid"] = chatModel.iUid
            params[(String(str_mediumAddTitle.prefix(5)))] = chatModel.iUid
        }
        //: params["giftId"] = giftModel.gid
        params[(String(str_detailValue.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(str_indexGiftTitle.suffix(7)))] = giftNum
        //: params["from"] = "audioCall"
        params[(str_cleanTitle.replacingOccurrences(of: "text", with: "r"))] = (String(str_labModeTitle))
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_smartData.suffix(7)) + String(str_topValue))] = giftModel.pkgItemsetId
        }

        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        ViewRequestTool.pastPop(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.cheesecakeResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.cannuliseNum(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any?])
            self.backName(extral: result as! [String: Any?])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any?]) {
    func backName(extral: [String: Any?]) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        var msgInfoDic: [String: Any] = extralInfo[(String(str_sharedRangeTitle) + str_lengthModelName.replacingOccurrences(of: "collection", with: "fo"))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(str_replyData) + String(str_imageYetValue))] = (String(str_managerAddValue))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(str_sharedRangeTitle) + str_lengthModelName.replacingOccurrences(of: "collection", with: "fo"))] = msgInfoDic

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        outsideGiftIntervaleraction(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(str_useValue.prefix(6)) + String(str_selectionName))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(str_useValue.prefix(6)) + String(str_selectionName))] as! NSNumber
            //: SubLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            SubLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: SubLabelReactiveCompatible.share.loginUserMode.mf_coin)
        giftView.bag(needReload: false, mf_coin: SubLabelReactiveCompatible.share.loginUserMode.mf_coin)

        //: var toUid = ""
        var toUid = ""
        //: if chatModel.iUid == SubLabelReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == SubLabelReactiveCompatible.share.loginUserMode.userID {
            //: toUid = chatModel.pairUid
            toUid = chatModel.pairUid
            //: } else {
        } else {
            //: toUid = chatModel.iUid
            toUid = chatModel.iUid
        }

        //: let cellData = TUISocialChatManager.insertTXMessageModel(withExtral: extralInfo as [AnyHashable: Any], toUid: toUid, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.insertTXMessageModel(withExtral: extralInfo as [AnyHashable: Any], toUid: toUid, isSender: true, isTip: false)

        //: NotificationCenter.default.post(name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION, object: self, userInfo: ["cellData": cellData])
        NotificationCenter.default.post(name: kLet_listGiftData, object: self, userInfo: [String(bytes: str_shareValue.reversed(), encoding: .utf8)!: cellData])
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func cheesecakeResend(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard SubLabelReactiveCompatible.share.loginUserMode.status != 1 else {
            guard SubLabelReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    barLog(showMsg: errorStr)
                }
                //: return
                return
            }
            //: TalkingPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            TalkingPushManager.share.withParams(clickEvent: kLet_recordBottomGiftValue, sufficient: false)
            //: giftView.dismissView()
            giftView.hiddenView()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: TalkingPushManager.share.func__pushToSubscribeAlert()
            TalkingPushManager.share.statusSumimate()

            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                barLog(showMsg: errorStr)
            }
        }
    }

    //: func initCachEffectData() {
    func cach() {
        //: var targetId = ""
        var targetId = ""
        //: if chatModel.iUid == SubLabelReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == SubLabelReactiveCompatible.share.loginUserMode.userID {
            //: targetId = chatModel.pairUid
            targetId = chatModel.pairUid
            //: } else {
        } else {
            //: targetId = chatModel.iUid
            targetId = chatModel.iUid
        }
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.freeBy(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: [String: Any]) {
    func outsideGiftIntervaleraction(giftDic: [String: Any]) {
        //: let giftModel = giftEffectView.changeGiftToModel(dict: giftDic) ?? TalkingGiftMsgAnimatModel()
        let giftModel = giftEffectView.pageNet(dict: giftDic) ?? RillModelType()
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in VideoToReactiveCompatible.share.identityNeed() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [AppModelType]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.clickInfo(model: giftModel)
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.sharedShow(dict: giftDic)
    }

    //: func addGiftEffectModelArr(giftArr: [Any]) {
    func modelArr(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in VideoToReactiveCompatible.share.identityNeed() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [AppModelType]
        }
        //: for i in 0 ..< giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.clickInfo(model: giftModel as! RillModelType)
        }
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.nameInterval(modelArr: giftArr)
    }
}

// MARK: - 通话计时器

//: extension Talking1v1VoiceInteractionView {
extension ProfessionalCounteractionView {
    /// 开始通话计时
    //: private func startTalkCount() {
    private func subPath() {
        //: guard countTimer == nil else { return }
        guard countTimer == nil else { return }

        //: if startTime == nil {
        if startTime == nil {
            //: let interval = Date().timeIntervalSince1970
            let interval = Date().timeIntervalSince1970
            //: startTime = interval
            startTime = interval
            //: chatModel.startTime = startTime
            chatModel.startTime = startTime
        }
        //: countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeTalkTime()
            self.messageTime()
            //: }, repeats: true)
        }, repeats: true)
    }

    // 展示通话时间
    //: private func changeTalkTime() {
    private func messageTime() {
        //: let duration = getCurrentTalkingTime()
        let duration = sizeEqual()
        //: let min = floor(duration/60)
        let min = floor(duration / 60)
        //: let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        //: timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        // 更新小窗口通话时间
        //: delegate?.interactionView_shouldUpdateTime(time: duration)
        delegate?.tableTime(time: duration)
    }

    /// 恢复通话，更新通话时间
    /// - Parameter startTime: 通话时间
    //: func updateStartTime(startTime: TimeInterval) {
    func indicator(startTime: TimeInterval) {
        //: self.startTime = startTime
        self.startTime = startTime
        //: changeTalkTime()
        messageTime()
    }

    /// 获取当前通话时间
    /// - Returns: 秒
    //: func getCurrentTalkingTime() -> Double {
    func sizeEqual() -> Double {
        //: guard chatModel.videoStage == .Calling else { return 0 }
        guard chatModel.videoStage == .Calling else { return 0 }
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let duration = floor(interval-(startTime ?? 0))
        let duration = floor(interval - (startTime ?? 0))
        //: return duration
        return duration
    }
}

// MARK: - 【通话中】触屏隐藏交互按钮逻辑

//: extension Talking1v1VoiceInteractionView {
extension ProfessionalCounteractionView {
    /// 触摸隐藏开启延时
    //: private func touchHiddenTimer() {
    private func concealed() {
        //: invalidateIdleTimer()
        viewIdle()
        //: perform(#selector(hideInteractionView), with: nil, afterDelay: 10)
        perform(#selector(everyPlayer), with: nil, afterDelay: 10)
    }

    /// 销毁延迟
    //: private func invalidateIdleTimer() {
    private func viewIdle() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(hideInteractionView), object: nil)
        NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(everyPlayer), object: nil)
    }

    /// 延时到时隐藏视图
    //: @objc private func hideInteractionView() {
    @objc private func everyPlayer() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.changeInteractionShowState()
            self.addCell()
        }
    }
}

// MARK: - Layout

//: extension Talking1v1VoiceInteractionView {
extension ProfessionalCounteractionView {
    /// 添加视图
    //: private func setupSubviews() {
    private func header() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(giftEffectView)
        addSubview(giftEffectView)
        //: insertSubview(giftTrackView, belowSubview: giftEffectView)
        insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: addSubview(touchHidenBtn)
        addSubview(touchHidenBtn)
        //: addSubview(pairPicV)
        addSubview(pairPicV)
        //: addSubview(pairNameLab)
        addSubview(pairNameLab)
        //: addSubview(locationBtn)
        addSubview(locationBtn)
        //: addSubview(minimizeBtn)
        addSubview(minimizeBtn)
        //: addSubview(hangupBtn)
        addSubview(hangupBtn)
        //: addSubview(acceptBtn)
        addSubview(acceptBtn)
        //: addSubview(micBtn)
        addSubview(micBtn)
        //: addSubview(handsfreeBtn)
        addSubview(handsfreeBtn)
//        addSubview(sendGiftBtn)
//        addSubview(rechargeBtn)
        //: addSubview(desLab)
        addSubview(desLab)
        //: addSubview(timeLab)
        addSubview(timeLab)
        //: addSubview(femaleAcceptBGBtn)
        addSubview(femaleAcceptBGBtn)
        //: addSubview(femaleAcceptBtn)
        addSubview(femaleAcceptBtn)
        //: femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        //: addSubview(acceptPlayer)
        addSubview(acceptPlayer)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func lowliness() {
        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(self)
            make.leading.trailing.top.bottom.equalTo(self)
        }
        //: touchHidenBtn.snp.makeConstraints { make in
        touchHidenBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: pairPicV.snp.makeConstraints { make in
        pairPicV.snp.makeConstraints { make in
            //: make.bottom.equalTo(self.snp.centerY).offset(actualHeight(h: -148))
            make.bottom.equalTo(self.snp.centerY).offset(actualHeight(h: -148))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pairNameLab.snp.makeConstraints { make in
        pairNameLab.snp.makeConstraints { make in
            //: make.top.equalTo(pairPicV.snp.bottom).offset(14)
            make.top.equalTo(pairPicV.snp.bottom).offset(14)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(actualWidth(w: 133))
            make.width.equalTo(actualWidth(w: 133))
        }
        //: locationBtn.snp.makeConstraints { make in
        locationBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(8)
            make.top.equalTo(pairNameLab.snp.bottom).offset(8)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(self)
            make.leading.equalTo(self)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(90)
            make.top.equalTo(pairNameLab.snp.bottom).offset(90)
        }
        //: minimizeBtn.snp.makeConstraints { make in
        minimizeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(kLet_messageSumervalName + 10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(actualWidth(w: 42))
            make.width.height.equalTo(actualWidth(w: 42))
        }
        //: hangupBtn.snp.makeConstraints { make in
        hangupBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
            make.bottom.equalTo(-kLet_changeBarValue - 161)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }
        //: acceptBtn.snp.makeConstraints { make in
        acceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: femaleAcceptBtn.snp.makeConstraints { make in
        femaleAcceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: femaleAcceptTipsBtn.snp.makeConstraints { make in
        femaleAcceptTipsBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-9)
            make.bottom.equalTo(-9)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
//        let letftPlace = (ScreenWidth-actualWidth(w: 45)*4-actualWidth(w: 25)*3)/2
        //: let letftPlace = (ScreenWidth-actualWidth(w: 45)*2-actualWidth(w: 25))/2
        let letftPlace = (kLet_failureName - actualWidth(w: 45) * 2 - actualWidth(w: 25)) / 2
        //: micBtn.snp.makeConstraints { make in
        micBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(letftPlace)
            make.leading.equalTo(letftPlace)
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-16)
            make.bottom.equalTo(-kLet_changeBarValue - 16)
            //: make.size.equalTo(actualWidth(w: 45))
            make.size.equalTo(actualWidth(w: 45))
        }
        //: handsfreeBtn.snp.makeConstraints { make in
        handsfreeBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(micBtn.snp.trailing).offset(actualWidth(w: 25))
            make.leading.equalTo(micBtn.snp.trailing).offset(actualWidth(w: 25))
            //: make.bottom.size.equalTo(micBtn)
            make.bottom.size.equalTo(micBtn)
        }
//        rechargeBtn.snp.makeConstraints { make in
//            make.leading.equalTo(handsfreeBtn.snp.trailing).offset(actualWidth(w: 25))
//            make.bottom.size.equalTo(micBtn)
//        }
//        sendGiftBtn.snp.makeConstraints { make in
//            make.leading.equalTo(rechargeBtn.snp.trailing).offset(actualWidth(w: 25))
//            make.bottom.size.equalTo(micBtn)
//        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalTo(locationBtn.snp.bottom).offset(10)
            make.top.equalTo(locationBtn.snp.bottom).offset(10)
            //: make.leading.equalTo(54)
            make.leading.equalTo(54)
            //: make.trailing.equalTo(-54)
            make.trailing.equalTo(-54)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(StatusBarHeight+16)
            make.top.equalTo(kLet_messageSumervalName + 16)
        }
    }

    /// 更新约束
    //: private func updateLayout() {
    private func bite() {
        //: if chatModel.videoStage == .Recieved {
        if chatModel.videoStage == .Recieved {
            //: if SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2-42-70)
                    make.leading.equalTo(kLet_failureName / 2 - 42 - 70)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-kLet_changeBarValue - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: acceptBtn.snp.remakeConstraints { make in
                acceptBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+42)
                    make.leading.equalTo(kLet_failureName / 2 + 42)
                    //: make.bottom.equalTo(hangupBtn)
                    make.bottom.equalTo(hangupBtn)
                    //: make.size.equalTo(hangupBtn)
                    make.size.equalTo(hangupBtn)
                }
                //: } else {
            } else {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(34)
                    make.leading.equalTo(34)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-kLet_changeBarValue - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: femaleAcceptBGBtn.snp.remakeConstraints { make in
                femaleAcceptBGBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-33)
                    make.trailing.equalTo(-33)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-147)
                    make.bottom.equalTo(-kLet_changeBarValue - 147)
                    //: make.size.equalTo(CGSize.init(width: 208, height: 99))
                    make.size.equalTo(CGSize(width: 208, height: 99))
                }
                //: femaleAcceptBtn.snp.remakeConstraints { make in
                femaleAcceptBtn.snp.remakeConstraints { make in
                    //: make.centerX.equalTo(femaleAcceptBGBtn)
                    make.centerX.equalTo(femaleAcceptBGBtn)
                    //: make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    //: make.size.equalTo(CGSize.init(width: 180, height: 70))
                    make.size.equalTo(CGSize(width: 180, height: 70))
                }
                //: acceptPlayer.snp.remakeConstraints { make in
                acceptPlayer.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-8)
                    make.trailing.equalTo(-8)
                    //: make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    //: make.size.equalTo(85)
                    make.size.equalTo(85)
                }
            }

            //: } else {
        } else {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.centerX.equalToSuperview()
                make.centerX.equalToSuperview()
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-kLet_changeBarValue - 161)
                //: make.size.equalTo(actualWidth(w: 70))
                make.size.equalTo(actualWidth(w: 70))
            }
        }
    }
}
