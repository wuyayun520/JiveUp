
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_touchTitle:[UInt8] = [0xcd,0xca,0xcd,0xd0,0x8c,0xc7,0xcb,0xc0,0xc1,0xd6,0x9e,0x8d,0x84,0xcc,0xc5,0xd7,0x84,0xca,0xcb,0xd0,0x84,0xc6,0xc1,0xc1,0xca,0x84,0xcd,0xc9,0xd4,0xc8,0xc1,0xc9,0xc1,0xca,0xd0,0xc1,0xc0]

private func inputSignal(medium num: UInt8) -> UInt8 {
    return num ^ 164
}

/*: "uid" :*/
fileprivate let str_makeName:[UInt8] = [0x35,0x29,0x24]

private func colorShareCount(content num: UInt8) -> UInt8 {
    return num ^ 64
}

/*: "headPic" :*/
fileprivate let str_upStyleName:String = "progress icon frame come upheadPic"

/*: "nickname" :*/
fileprivate let str_turnValue:String = "nifacility"
fileprivate let str_labelTitle:String = "KNAME"

/*: "birthday" :*/
fileprivate let str_fillValue:String = "birthindex"
fileprivate let str_lastName:[Character] = ["a","y"]

/*: "videoUrl" :*/
fileprivate let str_wrapContent:String = "make name reply main tovideoUr"
fileprivate let str_nextSucceedData:String = "fill"

/*: "location" :*/
fileprivate let str_shareData:String = "losenseation"

/*: "cmd" :*/
fileprivate let str_orientationName:[UInt8] = [0xcd,0xc3,0xca]

/*: "refuseCall" :*/
fileprivate let str_eachValue:[Character] = ["r","e","f","u","s","e","C"]
fileprivate let str_domainUserDisplayValue:String = "talkll"

/*: "data" :*/
fileprivate let str_trackNameValue:[UInt8] = [0x37,0x34,0x47,0x34]

fileprivate func topPlayerCancel(her num: UInt8) -> UInt8 {
    let value = Int(num) - 211
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let str_atModelValue:String = "Call iself text equal"
fileprivate let str_backTitle:[Character] = ["p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let str_managerValue:String = "stopCallmodel view path"

/*: "logId" :*/
fileprivate let str_callSizeValue:[UInt8] = [0xf4,0xf7,0xef,0xd1,0xec]

fileprivate func actualRaw(view num: UInt8) -> UInt8 {
    let value = Int(num) - 136
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let str_modelBottomTitle:String = "view titlecancelC"
fileprivate let str_appText:String = "layerl"

/*: "No answer" :*/
fileprivate let str_moreValue:String = "equal"
fileprivate let str_topValue:String = "var return make voiceo an"

/*: "duration" :*/
fileprivate let str_makeText:String = "dappra"

/*: "She has hung up" :*/
fileprivate let str_scaleTableName:String = "search source actionShe h"
fileprivate let str_listContent:String = "as hmodel source true for her"

/*: "cancelSex" :*/
fileprivate let str_pairText:String = "ccenter"
fileprivate let str_statuteData:[Character] = ["n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let str_addTitleData:String = "play load front locationHe ha"
fileprivate let str_enableTitle:String = "s height content return self text"
fileprivate let str_viewTableTitle:[Character] = ["h","u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let str_locationData:String = "endTypeper my"

/*: "stopSex" :*/
fileprivate let str_levelData:String = "stopSview false to text"
fileprivate let str_othersTitle:[Character] = ["e","x"]

/*: "Insufficient credit, call has ended" :*/
fileprivate let str_imageLeadingText:[UInt8] = [0xbb,0x9c,0x81,0x87,0x94,0x94,0x9b,0x91,0x9b,0x97,0x9c,0x86,0xd2,0x91,0x80,0x97,0x96,0x9b,0x86,0xde,0xd2,0x91,0x93,0x9e,0x9e,0xd2,0x9a,0x93,0x81,0xd2,0x97,0x9c,0x96,0x97,0x96]

/*: "The other hung up the phone" :*/
fileprivate let str_colorIconData:String = "table layerThe ot"
fileprivate let str_inputNextData:String = "ung upplayer no"
fileprivate let str_femaleName:String = "image"

/*: "type" :*/
fileprivate let str_centerTitle:String = "typrow"

/*: "jumpUrl" :*/
fileprivate let str_cardValue:[Character] = ["j"]
fileprivate let str_clearData:[Character] = ["u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let str_scaleName:String = "Canceltop fail"

/*: "Purchase" :*/
fileprivate let str_tempTitle:String = "path color size title dataPurc"

/*: "&type=4" :*/
fileprivate let str_giftArrayData:[Character] = ["&","t","y","p","e","="]
fileprivate let str_rowSexName:[Character] = ["4"]

/*: "addBean" :*/
fileprivate let str_timeValue:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let str_attributeName:[Character] = ["r","e","f","u","s","e","S"]
fileprivate let str_layerName:[Character] = ["e","x"]

/*: "onAcceptCall" :*/
fileprivate let str_onlineDateData:String = "onAcchange source table to equal"
fileprivate let str_dataModelTitle:String = "acurrentcurrent"

/*: "acceptCall" :*/
fileprivate let str_enableData:String = "makeccep"

/*: "Calls are cancelled" :*/
fileprivate let str_giftPaperText:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n"]
fileprivate let str_equalTheTitle:String = "top"
fileprivate let str_observerMoveValue:String = "eofed"

/*: "The call has ended" :*/
fileprivate let str_viewRequestValue:String = "The ctime section view"
fileprivate let str_playerData:String = "data point in pan selectas ended"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TalkingSubObserverDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import UIKit
import UIKit

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let kLet_senseEnableData = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class TalkingSubObserverDelegate: EqualVideoReactiveCompatible {
	var momentStatusTotal: Int = 66
	var pastCount: Double = -98.6
	var programArray: [AnyHashable] = []
	var throughQuantity: Int = 5
	var dataSum: Double = -57.3
	var moreNameArray: [AnyHashable] = []

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ProfessionalChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: EqualRemovePlayerThen? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: DisappearMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        TitleSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        TitleSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        TitleSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_touchTitle.map{inputSignal(medium: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! SearchedReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.noneCon()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            join()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            TitleSocketManager.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.cach()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        demonstrateSubviews()
        //: setupSubViewsConstraint()
        confinement()
        //: initNotificationObserver()
        front()
        //: req_callGetUserInfo()
        skinny()
        //: timeoutRules()
        hidden()
        //: resumeTalk()
        merriment()
        //: setUpBGMPlayer()
        ofTitle()
    
		if var sexValue = chatModel?.pairAge { 
		if let miniView = miniView {
	
	            if (miniView.pointAnimView.inputViewController != nil) && (miniView.pointAnimView.convert(CGRect(x: 0, y: CGFloat(0), width: 0, height: 0), from: miniView.pointAnimView.superview).origin.y == 84.19) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let saveFrom = InfoOfView(frame: miniView.pointAnimView.frame.offsetBy(dx: CGFloat(67), dy: CGFloat(542.28)))
	            saveFrom.listCount = interactionView.giftView.contentHeight
	            
	            saveFrom.nameCompleteMagnitude = { [self] sumCount in
	            self.momentStatusTotal = sumCount
	            
	            var saveFrom = sexValue
	            saveFrom &+= 1
	            if saveFrom > self.momentStatusTotal {
	                self.momentStatusTotal = saveFrom
	            }
	            
	            return self.momentStatusTotal
	            }
	            saveFrom.viewEnableTotal = { [self] collectionMagnitude in
	            self.pastCount = collectionMagnitude
	            
	            var saveFrom = interactionView.giftView.contentHeight
	                saveFrom += 1
	                if saveFrom >= 0 {
	                    saveFrom = saveFrom - 1
	                }
	            if saveFrom < self.pastCount {
	                self.pastCount = saveFrom
	            }
	            
	                self.pastCount += 1
	                if self.pastCount >= 0 {
	                    self.pastCount = self.pastCount - 1
	                }
	            return self.pastCount
	            }
	            saveFrom.endArray = { [self] imagePlayArray in
	            self.programArray = imagePlayArray
	            
	            guard var value = self.programArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	                miniView.pointAnimView.addSubview(saveFrom)
	            }
	
		}
	
		}
	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SubLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
            if SubLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = HadithShieldView(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        TitleSocketManager.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.doColor()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: ProfessionalCounteractionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = ProfessionalCounteractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: ConstraintChatManager = {
        //: let m = TalkingVoiceChatManager()
        let m = ConstraintChatManager()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension TalkingSubObserverDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func skinny() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ServerViewThen.nameData(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_makeName.map{colorShareCount(content: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_upStyleName.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_turnValue.replacingOccurrences(of: "facility", with: "c") + str_labelTitle.lowercased())].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(str_fillValue.replacingOccurrences(of: "index", with: "d") + String(str_lastName))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(str_wrapContent.suffix(7)) + str_nextSucceedData.replacingOccurrences(of: "fill", with: "l"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(str_shareData.replacingOccurrences(of: "sense", with: "c"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.get()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension TalkingSubObserverDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func join() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        AccountingDataReactiveCompatible.ofText(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.barLog(showMsg: kLet_changeTitle)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                TitleSocketManager.shared.fromApp(info: [String(bytes: str_orientationName.map{$0^174}, encoding: .utf8)!: (String(str_eachValue) + str_domainUserDisplayValue.replacingOccurrences(of: "talk", with: "a")), String(bytes: str_trackNameValue.map{topPlayerCancel(her: $0)}, encoding: .utf8)!: [String(bytes: str_makeName.map{colorShareCount(content: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.weltanschauungAnimated()
                //: return
                return
            }

            //: self.stopBGM()
            self.hopUp()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = EqualRemovePlayerThen.byEqual(type: .Response)
            //: self.startBGM()
            self.noneCon()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func doColor() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.hopUp()
        //: TalkingSocketManager.shared.callerDelegate = nil
        TitleSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        TitleSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        TitleSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func weltanschauungAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.hopUp()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func anSearched() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.positionTo(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func merriment() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            anSearched()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension TalkingSubObserverDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func front() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(gameNotif(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(statusNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_detailPlayPublicName,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_senseEnableData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func gameNotif(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            weltanschauungAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func statusNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard TitleSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        TitleSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        TitleSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        TitleSocketManager.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.nearNett(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension TalkingSubObserverDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.barLog(showMsg: (String(str_atModelValue.prefix(6)) + "nterru" + String(str_backTitle)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                TitleSocketManager.shared.fromApp(info: [String(bytes: str_orientationName.map{$0^174}, encoding: .utf8)!: (String(str_managerValue.prefix(8))), String(bytes: str_trackNameValue.map{topPlayerCancel(her: $0)}, encoding: .utf8)!: [String(bytes: str_callSizeValue.map{actualRaw(view: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                TitleSocketManager.shared.fromApp(info: [String(bytes: str_orientationName.map{$0^174}, encoding: .utf8)!: (String(str_modelBottomTitle.suffix(7)) + str_appText.replacingOccurrences(of: "layer", with: "al")), String(bytes: str_trackNameValue.map{topPlayerCancel(her: $0)}, encoding: .utf8)!: [String(bytes: str_callSizeValue.map{actualRaw(view: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                TitleSocketManager.shared.fromApp(info: [String(bytes: str_orientationName.map{$0^174}, encoding: .utf8)!: (String(str_eachValue) + str_domainUserDisplayValue.replacingOccurrences(of: "talk", with: "a")), String(bytes: str_trackNameValue.map{topPlayerCancel(her: $0)}, encoding: .utf8)!: [String(bytes: str_makeName.map{colorShareCount(content: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.serviceChange()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                TitleSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            weltanschauungAnimated()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.audio(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension TalkingSubObserverDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func ofTitle() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? CompleteMagnitude.Call : CompleteMagnitude.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = EqualRemovePlayerThen.byEqual(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func noneCon() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.infoOfPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func hopUp() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.cardStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension TalkingSubObserverDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func hidden() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_indexTitle : self.chatModel?.pathInsert()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            objectOf()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.objectOf()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func objectOf() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !TitleSocketManager.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.barLog(showMsg: (str_moreValue.replacingOccurrences(of: "equal", with: "N") + String(str_topValue.suffix(4)) + "swer").localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_orientationName.map{$0^174}, encoding: .utf8)!: (String(str_modelBottomTitle.suffix(7)) + str_appText.replacingOccurrences(of: "layer", with: "al")), String(bytes: str_trackNameValue.map{topPlayerCancel(her: $0)}, encoding: .utf8)!: [String(bytes: str_callSizeValue.map{actualRaw(view: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_orientationName.map{$0^174}, encoding: .utf8)!: (String(str_eachValue) + str_domainUserDisplayValue.replacingOccurrences(of: "talk", with: "a")), String(bytes: str_trackNameValue.map{topPlayerCancel(her: $0)}, encoding: .utf8)!: [String(bytes: str_makeName.map{colorShareCount(content: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        TitleSocketManager.shared.fromApp(info: socketDict)
        //: popCurrentViewController()
        weltanschauungAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension TalkingSubObserverDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func feedData(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_callSizeValue.map{actualRaw(view: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(str_makeText.replacingOccurrences(of: "app", with: "u") + "tion")].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.indicator(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.reinIn()
            //: createTalkRoom()
            anSearched()
            //: self.stopBGM()
            self.hopUp()
        }
    }
}

// MARK: - RowObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension TalkingSubObserverDelegate: RowObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func laughDoing(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_scaleTableName.suffix(5)) + String(str_listContent.prefix(4)) + "ung up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(str_pairText.replacingOccurrences(of: "center", with: "a") + String(str_statuteData))].stringValue == CheckedUTF8Initializable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_addTitleData.suffix(5)) + String(str_enableTitle.prefix(2)) + String(str_viewTableTitle)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.barLog(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.serviceChange()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            TitleSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        weltanschauungAnimated()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func toStart(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_callSizeValue.map{actualRaw(view: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.hopUp()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.reinIn()
        //: self.createTalkRoom()
        self.anSearched()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func smolder(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_locationData.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_scaleTableName.suffix(5)) + String(str_listContent.prefix(4)) + "ung up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_levelData.prefix(5)) + String(str_othersTitle))].stringValue == CheckedUTF8Initializable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_addTitleData.suffix(5)) + String(str_enableTitle.prefix(2)) + String(str_viewTableTitle)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue {
                //: typeStr = "Insufficient credit, call has ended".localized
                typeStr = String(bytes: str_imageLeadingText.map{$0^242}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_colorIconData.suffix(6)) + "her h" + String(str_inputNextData.prefix(6)) + " the phon" + str_femaleName.replacingOccurrences(of: "image", with: "e")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.barLog(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.serviceChange()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            TitleSocketManager.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.shimmer()
        //: self.popCurrentViewController()
        self.weltanschauungAnimated()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kLet_cellName, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func hemAndHaw(msg: String, data: [String: Any]) {
        //: guard SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(str_centerTitle.replacingOccurrences(of: "row", with: "e"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_cardValue) + String(str_clearData))] as? String else { return }
            //: let httpsUrl = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = RoomConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TalkingPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            TalkingPushManager.share.counteractionPlayer(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            TalkingCostExhibitThen.titleConfig(title: nil,
                                         //: message: msg,
                                         message: msg,
                                         //: leftBtnTitle: "Cancel".localized,
                                         leftBtnTitle: (String(str_scaleName.prefix(6))).localized,
                                         //: rightBtnTitle: "Purchase".localized) {
                                         rightBtnTitle: (String(str_tempTitle.suffix(4)) + "hase").localized)
            {
                //: TalkingAlertShow.hideAlert()
                TalkingCostExhibitThen.harbourToAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingPushManager.share.func__pushToRechargeAlert(appendParams: "&type=4")
                TalkingPushManager.share.totalerval(appendParams: (String(str_giftArrayData) + String(str_rowSexName)))
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func withButton(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_timeValue))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.joint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.firstTransform(point: addBean)
        }
    }
}

// MARK: - LabelObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension TalkingSubObserverDelegate: LabelObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func socketTableAssemblage(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_scaleTableName.suffix(5)) + String(str_listContent.prefix(4)) + "ung up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_attributeName) + String(str_layerName))].stringValue == CheckedUTF8Initializable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_addTitleData.suffix(5)) + String(str_enableTitle.prefix(2)) + String(str_viewTableTitle)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.barLog(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.serviceChange()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            TitleSocketManager.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        weltanschauungAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func pushData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_callSizeValue.map{actualRaw(view: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.hopUp()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.reinIn()
        //: self.createTalkRoom()
        self.anSearched()
    }
}

// MARK: - DataObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension TalkingSubObserverDelegate: DataObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func choiceHandle(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(str_eachValue) + str_domainUserDisplayValue.replacingOccurrences(of: "talk", with: "a")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.barLog(showMsg: (str_moreValue.replacingOccurrences(of: "equal", with: "N") + String(str_topValue.suffix(4)) + "swer").localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.serviceChange()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                TitleSocketManager.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.weltanschauungAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_onlineDateData.prefix(4)) + "ceptC" + str_dataModelTitle.replacingOccurrences(of: "current", with: "l")) && errorNo == 101 { // 接听通话时，余额不足
            //: TalkingPushManager.share.func__pushToRechargeAlert(appendParams: "&type=4")
            TalkingPushManager.share.totalerval(appendParams: (String(str_giftArrayData) + String(str_rowSexName)))

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_onlineDateData.prefix(4)) + "ceptC" + str_dataModelTitle.replacingOccurrences(of: "current", with: "l")) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.barLog(showMsg: msg)
            //: self.popCurrentViewController()
            self.weltanschauungAnimated()
        }
    }
}

// MARK: - HairObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension TalkingSubObserverDelegate: HairObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func releaseTime() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        TitleSocketManager.shared.fromApp(info: [String(bytes: str_orientationName.map{$0^174}, encoding: .utf8)!: (str_enableData.replacingOccurrences(of: "make", with: "a") + "tCall"), String(bytes: str_trackNameValue.map{topPlayerCancel(her: $0)}, encoding: .utf8)!: [String(bytes: str_makeName.map{colorShareCount(content: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func boardPush() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.barLog(showMsg: (String(str_giftPaperText) + str_equalTheTitle.replacingOccurrences(of: "top", with: "c") + str_observerMoveValue.replacingOccurrences(of: "of", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            TitleSocketManager.shared.fromApp(info: [String(bytes: str_orientationName.map{$0^174}, encoding: .utf8)!: (String(str_modelBottomTitle.suffix(7)) + str_appText.replacingOccurrences(of: "layer", with: "al")), String(bytes: str_trackNameValue.map{topPlayerCancel(her: $0)}, encoding: .utf8)!: [String(bytes: str_callSizeValue.map{actualRaw(view: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.barLog(showMsg: (String(str_giftPaperText) + str_equalTheTitle.replacingOccurrences(of: "top", with: "c") + str_observerMoveValue.replacingOccurrences(of: "of", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            TitleSocketManager.shared.fromApp(info: [String(bytes: str_orientationName.map{$0^174}, encoding: .utf8)!: (String(str_eachValue) + str_domainUserDisplayValue.replacingOccurrences(of: "talk", with: "a")), String(bytes: str_trackNameValue.map{topPlayerCancel(her: $0)}, encoding: .utf8)!: [String(bytes: str_makeName.map{colorShareCount(content: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.barLog(showMsg: (String(str_viewRequestValue.prefix(5)) + "all h" + String(str_playerData.suffix(8))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            TitleSocketManager.shared.fromApp(info: [String(bytes: str_orientationName.map{$0^174}, encoding: .utf8)!: (String(str_managerValue.prefix(8))), String(bytes: str_trackNameValue.map{topPlayerCancel(her: $0)}, encoding: .utf8)!: [String(bytes: str_callSizeValue.map{actualRaw(view: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.shimmer()
        //: self.popCurrentViewController()
        self.weltanschauungAnimated()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func enableFinish(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.mute(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func loadIn(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.handsfree(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func topMedium() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        TitleSocketManager.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = DisappearMiniView.per()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.nowController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.sizeEqual()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.commentTime(talkingTime)
        //: popCurrentViewController()
        weltanschauungAnimated()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func tableTime(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.commentTime(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func targetAfter(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.modelArr(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension TalkingSubObserverDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func demonstrateSubviews() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func confinement() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
