
//: Declare String Begin

/*: "formatType" :*/
fileprivate let str_neverName:String = "formatTyvalue view text"
fileprivate let str_afterAdData:String = "share"

/*: "plain" :*/
fileprivate let str_yetName:String = "platitle"

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_editData:[UInt8] = [0x6e,0x69,0x6e,0x73,0x2f,0x64,0x68,0x63,0x62,0x75,0x3d,0x2e,0x27,0x6f,0x66,0x74,0x27,0x69,0x68,0x73,0x27,0x65,0x62,0x62,0x69,0x27,0x6e,0x6a,0x77,0x6b,0x62,0x6a,0x62,0x69,0x73,0x62,0x63]

private func toShe(click num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "#FF2348" :*/
fileprivate let str_wrapPathData:String = "true return var temp return#FF23"
fileprivate let str_voiceName:String = "popular8"

/*: "icon_lounge" :*/
fileprivate let str_deadlineContent:[Character] = ["i","c"]
fileprivate let str_guideNeedForTitle:[Character] = ["o","n","_","l","o","u","n","g","e"]

/*: "icon_home_v" :*/
fileprivate let str_managerValueData:String = "icon_for let class data"

/*: "icon_chat_list_new" :*/
fileprivate let str_constraintValue:[Character] = ["i","c","o","n","_","c","h","a","t","_","l","i","s","t"]
fileprivate let str_brushText:String = "video var empty_new"

/*: "Official" :*/
fileprivate let str_nowValue:[Character] = ["O","f","f","i","c","i","a","l"]

/*: "#30D00B" :*/
fileprivate let str_blockValue:String = "#30D0super live"
fileprivate let str_childName:[Character] = ["0","B"]

/*: "iv_message_honey" :*/
fileprivate let str_betweenName:String = "play value regulariv_messa"
fileprivate let str_playerValue:[Character] = ["g","e","_","h","o","n","e","y"]

/*: "#FF60B9" :*/
fileprivate let str_videoRangeData:[Character] = ["#","F","F","6","0","B","9"]

/*: "#EEEEEE" :*/
fileprivate let str_cosData:[Character] = ["#","E","E","E","E","E"]
fileprivate let str_sectionData:[Character] = ["E"]

/*: "opacity" :*/
fileprivate let str_errorText:[Character] = ["o"]
fileprivate let str_stopKitTitle:String = "PACITY"

/*: "path" :*/
fileprivate let str_arrayText:String = "paframe"

/*: "extra" :*/
fileprivate let str_nameRoomSectionContent:String = "extrfrom"

/*: "msgInfo" :*/
fileprivate let str_largeData:String = "msgInfor app"
fileprivate let str_effectTitleName:String = "make"

/*: "content" :*/
fileprivate let str_feedbackName:[Character] = ["c","o","n","t","e","n","t"]

/*: "messageType" :*/
fileprivate let str_centerContent:String = "messageTtext let make"
fileprivate let str_pathProgressData:String = "YPE"

/*: "[Reply messages to get points]" :*/
fileprivate let str_ofManagerValue:[Character] = ["[","R","e","p","l","y"," ","m"]
fileprivate let str_makeText:String = "essadetail"
fileprivate let str_frameAppText:String = " gethidden of"
fileprivate let str_pushTitle:String = "nts]status share"

/*: "title" :*/
fileprivate let str_effectCellName:String = "transformitransformle"

/*: "img" :*/
fileprivate let str_inviteName:String = "labelg"

/*: "[Photo]" :*/
fileprivate let str_indexShareData:String = "label self in var[Photo]"

/*: "[Audio]" :*/
fileprivate let str_crushValue:[Character] = ["[","A","u","d","i"]
fileprivate let str_infoAspectTitle:String = "o]country live main path equal"

/*: "rYMsgType" :*/
fileprivate let str_buttonFailureData:String = "listen at callrYMsgTy"
fileprivate let str_ingredientText:String = "pmini"

/*: "GJ:CallCustom" :*/
fileprivate let str_voiceData:String = "Gview"
fileprivate let str_playerName:String = "mode false view self:CallC"

/*: "callCmd" :*/
fileprivate let str_intimateName:[Character] = ["c","a","l","l","C","m","d"]

/*: "missCall" :*/
fileprivate let str_makeData:[Character] = ["m","i","s","s","C","a","l","l"]

/*: "[Missed Call]" :*/
fileprivate let str_blockData:String = "[Misssocial make index"
fileprivate let str_redRowName:[Character] = ["e","d"," "]
fileprivate let str_photoIconData:String = "Call]return bottom let in status"

/*: "logType" :*/
fileprivate let str_placeShowName:[Character] = ["l","o","g","T","y","p","e"]

/*: "[Voice Call]" :*/
fileprivate let str_timeData:String = "name"
fileprivate let str_fieldTitle:[Character] = ["V","o","i","c","e"," ","C","a","l","l","]"]

/*: "[Video Call]" :*/
fileprivate let str_lengthSoundEditData:String = "action sex row[Video "
fileprivate let str_normalLeadingData:String = "Call]self party string"

/*: "video" :*/
fileprivate let str_makeModeValue:String = "vhiddendeo"

/*: "contentType" :*/
fileprivate let str_headPathData:String = "videoonte"
fileprivate let str_popularNumberName:[Character] = ["n","t","T","y","p","e"]

/*: "VideoMsg" :*/
fileprivate let str_dismissValue:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "[Short Video]" :*/
fileprivate let str_momentValue:[Character] = ["[","S","h","o","r","t"," ","V"]
fileprivate let str_layerValue:String = "ideo]icon name message time"

/*: "match" :*/
fileprivate let str_pathKindData:String = "mrowtch"

/*: "[Cupid messaging]" :*/
fileprivate let str_voiceText:[Character] = ["[","C","u","p","i","d"," ","m","e","s","s"]
fileprivate let str_editValue:String = "aging]var import"

/*: "GJ:Sess:TxtCustom" :*/
fileprivate let str_appContent:[Character] = ["G","J",":","S","e","s","s",":","T","x","t","C","u","s","t","o"]
fileprivate let str_topText:[Character] = ["m"]

/*: "isSend" :*/
fileprivate let str_voiceValue:[Character] = ["i","s","S","e","n","d"]

/*: "Sent Gift" :*/
fileprivate let str_appAutomaticallyName:[Character] = ["S","e","n"]
fileprivate let str_upContent:[Character] = ["t"," ","G","i","f","t"]

/*: "Receive Gift" :*/
fileprivate let str_mainName:String = "Receivemode temp"
fileprivate let str_colorScreenData:String = " Giftplayer edit by text"

/*: "uid" :*/
fileprivate let str_yearValue:String = "sharedd"

/*: "gift" :*/
fileprivate let str_stopRowName:[Character] = ["g","i","f","t"]

/*: "name" :*/
fileprivate let str_whiteTitle:String = "NAME"

/*: "num" :*/
fileprivate let str_galleryData:String = "NUM"

/*:   :*/
fileprivate let str_editIconData:[Character] = [" "]

/*:  x :*/
fileprivate let str_touchText:String = " xmargin data view window"

/*: "imageUri" :*/
fileprivate let str_picMData:[Character] = ["i","m","a"]
fileprivate let str_directionName:[Character] = ["g","e","U","r","i"]

/*: "imageData" :*/
fileprivate let str_makeValue:String = "ielementage"
fileprivate let str_clearTitle:String = "as nameData"

/*: "[Location]" :*/
fileprivate let str_modelUserTrueValue:[Character] = ["[","L","o","c","a","t","i"]
fileprivate let str_orientationValue:[Character] = ["o","n","]"]

/*: "<img>" :*/
fileprivate let str_imageTermsData:String = "<img>transform frame hidden"

/*: "</img>" :*/
fileprivate let str_groupValue:String = "<"
fileprivate let str_sharedEqualValue:[Character] = ["/","i","m","g",">"]

/*: "wantGift" :*/
fileprivate let str_replyTitle:[Character] = ["w","a","n","t","G","i","f","t"]

/*: "intimatePhoto" :*/
fileprivate let str_indexLetterViewTitle:[Character] = ["i","n","t","i","m","a","t","e","P","h","o"]
fileprivate let str_titleGiftContent:String = "make"

/*: "audioData" :*/
fileprivate let str_containerObjectText:String = "AU"
fileprivate let str_observerText:String = "dioDataclass view model model"

/*: "audioUri" :*/
fileprivate let str_labElementData:String = "AUDI"

/*: "draftContent" :*/
fileprivate let str_backData:[Character] = ["d","r","a","f","t","C","o","n","t","e","n","t"]

/*: "[draft]%@" :*/
fileprivate let str_sizeGroupValue:[Character] = ["[","d","r","a","f","t","]","%","@"]

/*: "New" :*/
fileprivate let str_announcementData:[Character] = ["N","e","w"]

/*: "99+" :*/
fileprivate let str_leadingValue:[Character] = ["9","9","+"]

/*: "Account is restricted！" :*/
fileprivate let str_singleValue:String = "Account iof color"
fileprivate let str_pathMessageValue:[Character] = ["s"," ","r","e","s","t","r","i","c","t","e","d","！"]

/*: "%@ Online" :*/
fileprivate let str_oldData:[Character] = ["%","@"," ","O","n","l","i","n","e"]

/*: "Messages not been replied here" :*/
fileprivate let str_toCurrentValue:String = "Messagcomplete in"
fileprivate let str_managerData:String = "ot bto else to model"
fileprivate let str_equalMakeContent:String = "view var usereen r"
fileprivate let str_succeedTitle:String = "d hereicon let return layer view"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AtTableCell.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/12.
//

//: import UIKit
import UIKit

//: let RCSystemFormatType                 = "formatType"
let kLet_tableValue = (String(str_neverName.prefix(8)) + str_afterAdData.replacingOccurrences(of: "share", with: "pe"))
//: let RCSystemFormatTypePlain            = "plain"
let kLet_cameraBarValue = (str_yetName.replacingOccurrences(of: "title", with: "in"))

//: class TalkingChatListTableCell: UITableViewCell {
class AtTableCell: UITableViewCell {
    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: super.init(coder: coder)
        super.init(coder: coder)
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_editData.map{toShe(click: $0)}, encoding: .utf8)!)
    }

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: createCellUI()
        layerFor()
        //: createCellUIConstraints()
        imageValue()
    }

    // MARK: - Lazy load

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let imageView = UIImageView.init()
        let imageView = UIImageView()
        //: imageView.layer.cornerRadius = 30
        imageView.layer.cornerRadius = 30
        //: imageView.clipsToBounds = true
        imageView.clipsToBounds = true
        //: imageView.backgroundColor = UIColor.white
        imageView.backgroundColor = UIColor.white
        //: imageView.contentMode = .scaleAspectFill
        imageView.contentMode = .scaleAspectFill
        //: return imageView
        return imageView
        //: }()
    }()

    //: private lazy var iconBorder: UIButton = {
    private lazy var iconBorder: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: self.addSubview(btn)
        self.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var badgeLab: UILabel = {
    private lazy var badgeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 13)
        label.font = .magnitudeimateBy(type: .Medium, fontSize: 13)
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.textColor = .white
        label.textColor = .white
        //: label.backgroundColor = UIColor(hex: "#FF2348")
        label.backgroundColor = UIColor(hex: (String(str_wrapPathData.suffix(5)) + str_voiceName.replacingOccurrences(of: "popular", with: "4")))
        //: label.layer.cornerRadius = 10
        label.layer.cornerRadius = 10
        //: label.layer.masksToBounds = true
        label.layer.masksToBounds = true
        //: label.layer.borderColor = UIColor.white.cgColor
        label.layer.borderColor = UIColor.white.cgColor
        //: label.layer.borderWidth = 1
        label.layer.borderWidth = 1
        //: label.isHidden = true
        label.isHidden = true
        //: return label
        return label
        //: }()
    }()

    //: private lazy var nickNameLabel: UILabel = {
    private lazy var nickNameLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 18)
        label.font = .magnitudeimateBy(type: .Medium, fontSize: 18)
        //: label.textColor = .appTitleColor()
        label.textColor = .elementColor()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var msgShowLabel: UILabel = {
    private lazy var msgShowLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .magnitudeimateBy(type: .Regular, fontSize: 15)
        //: label.textColor = .videoValue()
        label.textColor = .videoValue()
        //: return label
        return label
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 12)
        label.font = .magnitudeimateBy(type: .Medium, fontSize: 12)
        //: label.textColor = .videoValue()
        label.textColor = .videoValue()
        //: return label
        return label
        //: }()
    }()

    //: lazy var loungeImgV: UIImageView = {
    lazy var loungeImgV: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.nameTitle(name: "icon_lounge")
        img.image = UIImage.nameTitle(name: (String(str_deadlineContent) + String(str_guideNeedForTitle)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var authImgV: UIImageView = {
    private lazy var authImgV: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image = UIImage.nameTitle(name: "icon_home_v")
        imgV.image = UIImage.nameTitle(name: (String(str_managerValueData.prefix(5)) + "home_v"))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var newImgV: TalkingButton = {
    private lazy var newImgV: OfTalkingButton = {
        //: let imgV = TalkingButton()
        let imgV = OfTalkingButton()
        //: imgV.setBackgroundImage(UIImage.nameTitle(name: "icon_chat_list_new"), for: .normal)
        imgV.setBackgroundImage(UIImage.nameTitle(name: (String(str_constraintValue) + String(str_brushText.suffix(4)))), for: .normal)
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var officialImgV: UIButton = {
    private lazy var officialImgV: UIButton = {
        //: let img = UIButton()
        let img = UIButton()
        //: img.setBackgroundColor(color: UIColor.msgTipsColor(), forState: .normal)
        img.sectionDrop(color: UIColor.estimatedColor(), forState: .normal)
        //: img.setTitle("Official".localized, for: .normal)
        img.setTitle((String(str_nowValue)).localized, for: .normal)
        //: img.setTitleColor(.white, for: .normal)
        img.setTitleColor(.white, for: .normal)
        //: img.titleLabel?.font = UIFont.rugularSize(fontSize: 11)
        img.titleLabel?.font = UIFont.rugularSize(fontSize: 11)
        //: img.layer.cornerRadius = 8
        img.layer.cornerRadius = 8
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var onlineStatus: UIView = {
    private lazy var onlineStatus: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(hex: "#30D00B")
        v.backgroundColor = UIColor(hex: (String(str_blockValue.prefix(5)) + String(str_childName)))
        //: v.layer.cornerRadius = 5
        v.layer.cornerRadius = 5
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var imtimateBtn: TalkingButton = {
    private lazy var imtimateBtn: OfTalkingButton = {
        //: let btn = TalkingButton.init()
        let btn = OfTalkingButton()
        //: btn.setImage(UIImage.nameTitle(name: "iv_message_honey"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_betweenName.suffix(8)) + String(str_playerValue))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 12)
        btn.titleLabel?.font = .magnitudeimateBy(type: .Medium, fontSize: 12)
        //: btn.setTitleColor(UIColor.init(hex: "#FF60B9"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_videoRangeData))), for: .normal)
        //: btn.contentHorizontalAlignment = .right
        btn.contentHorizontalAlignment = .right
        //: btn.isHidden = true
        btn.isHidden = true
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PropertyLineThen.shared.direction == .rightToLeft {
            //: btn.contentHorizontalAlignment = .left
            btn.contentHorizontalAlignment = .left
        }
        //: return btn
        return btn
        //: }()
    }()
}

//: extension TalkingChatListTableCell {
extension AtTableCell {
    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        //: let animationColor = UIColor.init(hex: "#EEEEEE")
        let animationColor = UIColor(hex: (String(str_cosData) + String(str_sectionData)))
        //: let animationDuration: Double = 0.4
        let animationDuration = 0.4

        //: super.touchesBegan(touches, with: event)
        super.touchesBegan(touches, with: event)
        //: let coverView = UIView(frame: bounds)
        let coverView = UIView(frame: bounds)
        //: coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        coverView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: coverView.backgroundColor = UIColor.clear
        coverView.backgroundColor = UIColor.clear
        //: self.addSubview(coverView)
        self.addSubview(coverView)

        //: let touch = touches.first!
        let touch = touches.first!
        //: let point = touch.location(in: self)
        let point = touch.location(in: self)

        //: let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))
        let ourTouchView = UIView(frame: CGRect(x: point.x - 5, y: point.y - 5, width: 10, height: 10))

        //: let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        let circleMaskPathInitial = UIBezierPath(ovalIn: ourTouchView.frame)
        //: let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        let pathRect = self.bounds.inset(by: UIEdgeInsets(top: 1, left: 1, bottom: 1, right: 1))
        //: let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)
        let circleMaskPathFinal = UIBezierPath(roundedRect: pathRect, cornerRadius: 5)

        //: let rippleLayer = CAShapeLayer()
        let rippleLayer = CAShapeLayer()
        //: rippleLayer.opacity = 1.0
        rippleLayer.opacity = 1.0
        //: rippleLayer.fillColor = animationColor?.cgColor
        rippleLayer.fillColor = animationColor?.cgColor
        //: rippleLayer.path = circleMaskPathFinal.cgPath
        rippleLayer.path = circleMaskPathFinal.cgPath
        //: coverView.layer.addSublayer(rippleLayer)
        coverView.layer.addSublayer(rippleLayer)

        // fade up
        //: let fadeUp = CABasicAnimation(keyPath: "opacity")
        let fadeUp = CABasicAnimation(keyPath: (String(str_errorText) + str_stopKitTitle.lowercased()))
        //: fadeUp.beginTime = CACurrentMediaTime()
        fadeUp.beginTime = CACurrentMediaTime()
        //: fadeUp.duration = 0.1
        fadeUp.duration = 0.1
        //: fadeUp.toValue = 0.8
        fadeUp.toValue = 0.8
        //: fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fadeUp.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fadeUp.fillMode = CAMediaTimingFillMode.forwards
        fadeUp.fillMode = CAMediaTimingFillMode.forwards
        //: fadeUp.isRemovedOnCompletion = false
        fadeUp.isRemovedOnCompletion = false
        //: rippleLayer.add(fadeUp, forKey: nil)
        rippleLayer.add(fadeUp, forKey: nil)

        // fade down
        //: let fade = CABasicAnimation(keyPath: "opacity")
        let fade = CABasicAnimation(keyPath: (String(str_errorText) + str_stopKitTitle.lowercased()))
        //: fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        fade.beginTime = CACurrentMediaTime() + animationDuration * 0.60
        //: fade.duration = 0.1
        fade.duration = 0.1
        //: fade.toValue = 0
        fade.toValue = 0
        //: fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        fade.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: fade.fillMode = CAMediaTimingFillMode.forwards
        fade.fillMode = CAMediaTimingFillMode.forwards
        //: fade.isRemovedOnCompletion = false
        fade.isRemovedOnCompletion = false
        //: rippleLayer.add(fade, forKey: nil)
        rippleLayer.add(fade, forKey: nil)

        // change path
        //: CATransaction.begin()
        CATransaction.begin()
        //: let maskLayerAnimation = CABasicAnimation(keyPath: "path")
        let maskLayerAnimation = CABasicAnimation(keyPath: (str_arrayText.replacingOccurrences(of: "frame", with: "th")))
        //: maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        maskLayerAnimation.fromValue = circleMaskPathInitial.cgPath
        //: maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        maskLayerAnimation.toValue = circleMaskPathFinal.cgPath
        //: maskLayerAnimation.beginTime = CACurrentMediaTime()
        maskLayerAnimation.beginTime = CACurrentMediaTime()
        //: maskLayerAnimation.duration = 0.1
        maskLayerAnimation.duration = 0.1
        //: maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        maskLayerAnimation.timingFunction = CAMediaTimingFunction(name: CAMediaTimingFunctionName.easeOut)
        //: CATransaction.setCompletionBlock({
        CATransaction.setCompletionBlock {
            //: coverView.removeFromSuperview()
            coverView.removeFromSuperview()
            //: })
        }
        //: rippleLayer.add(maskLayerAnimation, forKey: "path")
        rippleLayer.add(maskLayerAnimation, forKey: (str_arrayText.replacingOccurrences(of: "frame", with: "th")))
        //: CATransaction.commit()
        CATransaction.commit()
    }
}

// MARK: - Public Event

//: extension TalkingChatListTableCell {
extension AtTableCell {
    /// 刷新cell
    /// - Parameter model: 模型
    //: func refreshCell(model: TalkingConversationModel?) {
    func frameworkModel(model: TempConversationModel?) {
        //: guard let model = model else { return }
        guard let model = model else { return }
        //: let isOther = (model.chatType == .system || model.chatType == .service ||
        let isOther = (model.chatType == .system || model.chatType == .service ||
            //: model.chatType == .group || model.chatType == .moreMsg)
            model.chatType == .group || model.chatType == .moreMsg)
        // 更新昵称、头像、vip标识
        //: updateUserInfo(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        modifyViewDifferent(userInfo: model.gj_userInfo, model: model, isOther: isOther)
        // 未读消息数
        //: updateUnreadNumber(unreadCount: Int(model.unreadCount))
        underShare(unreadCount: Int(model.unreadCount))
        // 详情消息
        //: handleC2CMessage(model: model)
        concatenateModel(model: model)

        //: if model.chatType == .group || model.chatType == .moreMsg {
        if model.chatType == .group || model.chatType == .moreMsg {
            //: self.accessoryType = .disclosureIndicator
            self.accessoryType = .disclosureIndicator // 添加箭头
            //: } else {
        } else {
            //: self.accessoryType = .none
            self.accessoryType = .none
        }
        // 时间
        //: timeLab.text = nil
        timeLab.text = nil
        //: if isOther == false && model.hasTimestamp {
        if isOther == false, model.hasTimestamp {
            //: let timestamp: Date? = model.timestamp as Date?
            let timestamp: Date? = model.timestamp as Date?
            //: if timestamp != nil && timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
            if timestamp != nil, timestamp!.timeIntervalSince1970 > NSTimeIntervalSince1970 {
                //: timeLab.text = NSDate.tx_messageTimeString(showDetail: false, date: timestamp!)
                timeLab.text = NSDate.doingNumberimate(showDetail: false, date: timestamp!)
            }
        }
        // 置顶背景色
        //: if model.isPinned == true {
        if model.isPinned == true {
            //: contentView.backgroundColor = UIColor(hex: "#EEEEEE")
            contentView.backgroundColor = UIColor(hex: (String(str_cosData) + String(str_sectionData)))
            //: } else {
        } else {
            //: contentView.backgroundColor = (SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue) ? .clear:.white
            contentView.backgroundColor = (SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.special.rawValue) ? .clear : .white
        }
        //: self.backgroundColor = contentView.backgroundColor
        self.backgroundColor = contentView.backgroundColor
    }

    /// 处理最新消息
    //: static func handleShowC2CDetailTextOfMessage(model: TalkingConversationModel) -> NSMutableAttributedString? {
    static func afterSubmitView(model: TempConversationModel) -> NSMutableAttributedString? {
//        messageType 消息类型：1系统消息 2官方小秘 3自动打招呼 4主动打招呼 5付费消息 6普通消息 7主动打招呼未回复 8.主打招呼回复 9，自动打招呼未回复 10, 自动打招呼回复11.发送礼物消息 12互相关注消息 13 取消关注 22家族邀请跳转消息 25索要礼物
        //: var detailAttrString: NSMutableAttributedString?
        var detailAttrString: NSMutableAttributedString?
        //: let change = false
        let change = false
        //: var speColor = [NSValue: UIColor]()
        var speColor = [NSValue: UIColor]()
        //: let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        let showUnread = model.isShowMsgUnread && model.unreadCount > 0
        //: let direction: TMsgDirection = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing:.MsgDirectionIncoming
        let direction: TMsgDirection = (model.listShowMessage?.isSelf ?? false) ? .MsgDirectionOutgoing : .MsgDirectionIncoming
        //: var voice_msgInfo: JSON?
        var voice_msgInfo: JSON?

        //: guard model.draftText.isEmptyString else {
        guard model.draftText.isEmptyString else { // 草稿
            //: detailAttrString = self.getDraftString(draft: &model.draftText)
            detailAttrString = self.fertilePhase(draft: &model.draftText)
            //: detailAttrString?.addAttribute(.font, value: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            detailAttrString?.addAttribute(.font, value: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            //: return detailAttrString
            return detailAttrString
        }

        //: let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        let extra = String(data: (model.listShowMessage?.customElem.data)!, encoding: .utf8)
        //: let json = JSON(parseJSON: extra!)
        let json = JSON(parseJSON: extra!)
        //: let extraDic = json["extra"]
        let extraDic = json[(str_nameRoomSectionContent.replacingOccurrences(of: "from", with: "a"))]
        //: let msgInfo = extraDic["msgInfo"]
        let msgInfo = extraDic[(String(str_largeData.prefix(5)) + str_effectTitleName.replacingOccurrences(of: "make", with: "fo"))]
        //: var content = msgInfo["content"].stringValue.deleteHtmlTips()
        var content = msgInfo[(String(str_feedbackName))].stringValue.deleteHtmlTips()
        //: let messageType = msgInfo["messageType"].intValue
        let messageType = msgInfo[(String(str_centerContent.prefix(8)) + str_pathProgressData.lowercased())].intValue
        //: let formatTypeStr = extraDic["formatType"].stringValue
        let formatTypeStr = extraDic[(String(str_neverName.prefix(8)) + str_afterAdData.replacingOccurrences(of: "share", with: "pe"))].stringValue

        //: if SessionChatManager.isReplyToGetPointsMsg(direction, messageType, msgTime: model.listShowMessage?.timestamp) {
        if SessionChatManager.noEqual(direction, messageType, msgTime: model.listShowMessage?.timestamp) { // 回复获取积分消息
            //: let content = "[Reply messages to get points]".localized
            let content = (String(str_ofManagerValue) + str_makeText.replacingOccurrences(of: "detail", with: "ge") + "s to" + String(str_frameAppText.prefix(4)) + " poi" + String(str_pushTitle.prefix(4))).localized
            //: let color  = UIColor.appValueColor()
            let color = UIColor.activityColor()
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
            //: speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]
            speColor = [NSValue(range: NSRange(location: 0, length: detailAttrString?.length ?? 0)): color]

            //: } else if model.chatType == .system {
        } else if model.chatType == .system { // 官方消息，只展示文本
            //: var content = extraDic["title"].stringValue
            var content = extraDic[(str_effectCellName.replacingOccurrences(of: "transform", with: "t"))].stringValue
            //: let sysContent = extraDic["content"].stringValue
            let sysContent = extraDic[(String(str_feedbackName))].stringValue
            //: let sysImg = extraDic["img"].stringValue
            let sysImg = extraDic[(str_inviteName.replacingOccurrences(of: "label", with: "im"))].stringValue
            //: if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 {
            if content.count > 0 || sysContent.count > 0 || sysImg.count > 0 { // 跳转系统消息
                //: if content.count == 0 {
                if content.count == 0 {
                    //: content.append(sysContent)
                    content.append(sysContent)
                }
                //: if sysImg.count > 0 {
                if sysImg.count > 0 {
                    //: content.append("[Photo]".localized)
                    content.append((String(str_indexShareData.suffix(7))).localized)
                }

                //: } else {
            } else { // 默认系统消息
                //: content = msgInfo["content"].stringValue
                content = msgInfo[(String(str_feedbackName))].stringValue
                //: if content.isEmptyString {
                if content.isEmptyString {
                    //: content = json["content"].stringValue
                    content = json[(String(str_feedbackName))].stringValue
                }
                //: content = content.deleteHtmlTips()
                content = content.deleteHtmlTips()
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
        } else if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) { // 语音
            //: voice_msgInfo = msgInfo
            voice_msgInfo = msgInfo
            //: content = "[Audio]".localized
            content = (String(str_crushValue) + String(str_infoAspectTitle.prefix(2))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话
        } else if json[(String(str_buttonFailureData.suffix(7)) + str_ingredientText.replacingOccurrences(of: "mini", with: "e"))].stringValue == (str_voiceData.replacingOccurrences(of: "view", with: "J") + String(str_playerName.suffix(6)) + "ustom") { // 音视频通话
            if msgInfo["callCmd"] == "missCall" { // 未接来电
                //: content = "[Missed Call]".localized
                content = (String(str_blockData.prefix(5)) + String(str_redRowName) + String(str_photoIconData.prefix(5))).localized
                //: } else {
            } else {
                //: if msgInfo["logType"].intValue == 1 { // 音频
                if msgInfo[(String(str_placeShowName))].intValue == 1 { // 音频
                    //: content = "[Voice Call]".localized
                    content = (str_timeData.replacingOccurrences(of: "name", with: "[") + String(str_fieldTitle)).localized
                    //: } else {
                } else {
                    //: content = "[Video Call]".localized
                    content = (String(str_lengthSoundEditData.suffix(7)) + String(str_normalLeadingData.prefix(5))).localized
                }
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

        } else if msgInfo["video"].exists() || msgInfo["contentType"] == "VideoMsg" { // 短视频
            //: content = "[Short Video]".localized
            content = (String(str_momentValue) + String(str_layerValue.prefix(5))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if (extraDic["match"].stringValue == "1" || msgInfo["match"].stringValue == "1") { // 匹配消息
        } else if extraDic[(str_pathKindData.replacingOccurrences(of: "row", with: "a"))].stringValue == "1" || msgInfo[(str_pathKindData.replacingOccurrences(of: "row", with: "a"))].stringValue == "1" { // 匹配消息
            //: content = "[Cupid messaging]".localized
            content = (String(str_voiceText) + String(str_editValue.prefix(6))).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if json["rYMsgType"].stringValue == "GJ:Sess:TxtCustom" { // 提示消息
        } else if json[(String(str_buttonFailureData.suffix(7)) + str_ingredientText.replacingOccurrences(of: "mini", with: "e"))].stringValue == (String(str_appContent) + String(str_topText)) { // 提示消息
            //: content = json["content"].stringValue
            content = json[(String(str_feedbackName))].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 11 {
        } else if messageType == 11 { // 送礼
            //: let isSend = msgInfo["isSend"].string
            let isSend = msgInfo[(String(str_voiceValue))].string
            //: var string1: String?
            var string1: String?
            //: if isSend != nil {
            if isSend != nil {
                //: string1 = isSend!.boolValue ? "Sent Gift".localized:"Receive Gift".localized
                string1 = isSend!.boolValue ? (String(str_appAutomaticallyName) + String(str_upContent)).localized : (String(str_mainName.prefix(7)) + String(str_colorScreenData.prefix(5))).localized
                //: } else {
            } else {
                //: let uid = msgInfo["uid"].stringValue
                let uid = msgInfo[(str_yearValue.replacingOccurrences(of: "shared", with: "ui"))].stringValue
                //: string1 = (uid == SubLabelReactiveCompatible.share.loginUserMode.userID) ? "Sent Gift".localized:"Receive Gift".localized
                string1 = (uid == SubLabelReactiveCompatible.share.loginUserMode.userID) ? (String(str_appAutomaticallyName) + String(str_upContent)).localized : (String(str_mainName.prefix(7)) + String(str_colorScreenData.prefix(5))).localized
            }
            //: let name = extraDic["gift"]["name"].stringValue
            let name = extraDic[(String(str_stopRowName))][(str_whiteTitle.lowercased())].stringValue
            //: let num = extraDic["gift"]["num"].stringValue
            let num = extraDic[(String(str_stopRowName))][(str_galleryData.lowercased())].stringValue

            //: content = "\(string1!) \(name) x\(num)"
            content = "\(string1!) \(name) x\(num)"
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 4 {
        } else if messageType == 4 { // 主动打招呼
//            let uid = msgInfo["uid"].stringValue
//            let toUid = msgInfo["toUid"].stringValue
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 3 {
        } else if messageType == 3 { // 自动打招呼
            //: let imageUri = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(String(str_picMData) + String(str_directionName))].stringValue
            //: let imageData = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(str_makeValue.replacingOccurrences(of: "element", with: "m") + String(str_clearTitle.suffix(4)))].stringValue

            //: if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(str_crushValue) + String(str_infoAspectTitle.prefix(2))).localized
                //: } else if (imageUri.count > 0 || imageData.count > 0) {
            } else if imageUri.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(str_indexShareData.suffix(7))).localized
            }
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: }else if messageType == 28 {
        } else if messageType == 28 { // 位置
            //: content = "[Location]".localized
            content = (String(str_modelUserTrueValue) + String(str_orientationValue)).localized
            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if formatTypeStr == RCSystemFormatTypePlain {
        } else if formatTypeStr == kLet_cameraBarValue {
            //: var content  = msgInfo["content"].stringValue
            var content = msgInfo[(String(str_feedbackName))].stringValue
            //: var imageUrl = extraDic["imageUri"].stringValue
            var imageUrl = extraDic[(String(str_picMData) + String(str_directionName))].stringValue
            //: let imageData = extraDic["imageData"].stringValue
            let imageData = extraDic[(str_makeValue.replacingOccurrences(of: "element", with: "m") + String(str_clearTitle.suffix(4)))].stringValue
            //: if imageUrl.count == 0 || imageData.count > 0 {
            if imageUrl.count == 0 || imageData.count > 0 {
                //: if content.hasPrefix("<img>") && content.hasSuffix("</img>") {
                if content.hasPrefix((String(str_imageTermsData.prefix(5)))), content.hasSuffix((str_groupValue.capitalized + String(str_sharedEqualValue))) {
                    //: var str = NSString(string: content)
                    var str = NSString(string: content)
                    //: str = str.replacingOccurrences(of: "<img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (String(str_imageTermsData.prefix(5))), with: "") as NSString
                    //: str = str.replacingOccurrences(of: "</img>", with: "") as NSString
                    str = str.replacingOccurrences(of: (str_groupValue.capitalized + String(str_sharedEqualValue)), with: "") as NSString
                    //: imageUrl = str as String
                    imageUrl = str as String
                }
            }

            //: if (imageUrl.count > 0 || imageData.count > 0) {
            if imageUrl.count > 0 || imageData.count > 0 {
                //: content = "[Photo]".localized
                content = (String(str_indexShareData.suffix(7))).localized
                //: } else {
            } else {
                //: content = content.deleteHtmlTips()
                content = content.deleteHtmlTips()
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)

            //: } else if messageType == 25 {
        } else if messageType == 25 { // 索要礼物
            //: if let wantGift  = msgInfo["wantGift"].dictionary {
            if let wantGift = msgInfo[(String(str_replyTitle))].dictionary {
                //: let content = wantGift["content"]?.string ?? ""
                let content = wantGift[(String(str_feedbackName))]?.string ?? ""
                //: detailAttrString = NSMutableAttributedString(string: content)
                detailAttrString = NSMutableAttributedString(string: content)
            }

            //: } else {
        } else {
            // 图片

            //: let imageUri             = msgInfo["imageUri"].stringValue
            let imageUri = msgInfo[(String(str_picMData) + String(str_directionName))].stringValue
            //: let imageData            = msgInfo["imageData"].stringValue
            let imageData = msgInfo[(str_makeValue.replacingOccurrences(of: "element", with: "m") + String(str_clearTitle.suffix(4)))].stringValue
            //: let intimatePhotoDict    = msgInfo["intimatePhoto"].dictionaryValue // 私密照片
            let intimatePhotoDict = msgInfo[(String(str_indexLetterViewTitle) + str_titleGiftContent.replacingOccurrences(of: "make", with: "to"))].dictionaryValue // 私密照片
            //: if (imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0) {
            if imageUri.count > 0 || imageData.count > 0 || intimatePhotoDict.keys.count > 0 {
                //: content = "[Photo]".localized
                content = (String(str_indexShareData.suffix(7))).localized
            }
            // 语音消息
            //: if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
            if TUIMsgChatManager.checkVoiceJsonMsg(msginfo: msgInfo.dictionaryValue) {
                //: content = "[Audio]".localized
                content = (String(str_crushValue) + String(str_infoAspectTitle.prefix(2))).localized
            }

            //: detailAttrString = NSMutableAttributedString(string: content)
            detailAttrString = NSMutableAttributedString(string: content)
        }

        //: if (!change) {
        if !change {
            //: if detailAttrString?.string == "[Audio]".localized {
            if detailAttrString?.string == (String(str_crushValue) + String(str_infoAspectTitle.prefix(2))).localized {
                //: let sendUid = model.sender
                let sendUid = model.sender
                //: let mid = SubLabelReactiveCompatible.share.loginUserMode.userID
                let mid = SubLabelReactiveCompatible.share.loginUserMode.userID
                //: if sendUid == mid {
                if sendUid == mid { // 我发送的
                    //: } else {
                } else { // 别人发的
                    //: var downurl = ""
                    var downurl = ""
                    //: if (voice_msgInfo?["audioData"].stringValue.isEmptyString) != nil {
                    if (voice_msgInfo?[(str_containerObjectText.lowercased() + String(str_observerText.prefix(7)))].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioData"].stringValue
                        downurl = voice_msgInfo![(str_containerObjectText.lowercased() + String(str_observerText.prefix(7)))].stringValue
                        //: } else if (voice_msgInfo?["audioUri"].stringValue.isEmptyString) != nil {
                    } else if (voice_msgInfo?[(str_labElementData.lowercased() + "oUri")].stringValue.isEmptyString) != nil {
                        //: downurl = voice_msgInfo!["audioUri"].stringValue
                        downurl = voice_msgInfo![(str_labElementData.lowercased() + "oUri")].stringValue
                    }
                }
                //: let unreadColor = UIColor.videoValue()
                let unreadColor = UIColor.videoValue()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

                //: } else {
            } else {
                //: let unreadColor = UIColor.videoValue()
                let unreadColor = UIColor.videoValue()
                //: detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
                detailAttrString?.addAttribute(.foregroundColor, value: unreadColor, range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
            }
        }
        //: if speColor.keys.count > 0 {
        if speColor.keys.count > 0 {
            //: for (key, value) in speColor {
            for (key, value) in speColor {
                //: detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
                detailAttrString?.addAttribute(.foregroundColor, value: value, range: key.rangeValue)
            }
        }
        //: detailAttrString?.addAttribute(.font, value: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))
        detailAttrString?.addAttribute(.font, value: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: detailAttrString?.length ?? 0))

        //: return detailAttrString
        return detailAttrString
    }

    /// 处理草稿消息
    //: static func getDraftString(draft: inout String) -> NSMutableAttributedString {
    static func fertilePhase(draft: inout String) -> NSMutableAttributedString {
        //: if draft.contains("draftContent") {
        if draft.contains((String(str_backData))) {
            //: let dict = NSDictionary.init(jsonString: draft)
            let dict = NSDictionary(jsonString: draft)
            //: draft = "\(String(describing: dict["draftContent"]))"
            draft = "\(String(describing: dict[(String(str_backData))]))"
        }
        //: let draftString = NSMutableAttributedString.init(string: "[draft]%@".localizedArguments(draft))
        let draftString = NSMutableAttributedString(string: (String(str_sizeGroupValue)).localizedArguments(draft))
        //: draftString.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: NSRange(location: 0, length: 7))
        draftString.addAttribute(.foregroundColor, value: UIColor.estimatedColor(), range: NSRange(location: 0, length: 7))
        //: return draftString
        return draftString
    }
}

// MARK: - Private Event

//: extension TalkingChatListTableCell {
extension AtTableCell {
    /// 更新昵称、头像
    /// - Parameters:
    ///   - userInfo: 数据库信息
    ///   - model: tx数据
    ///   - isOther: 是否其他消息
    //: private func updateUserInfo(userInfo: AppMinThen?, model: TalkingConversationModel, isOther: Bool) {
    private func modifyViewDifferent(userInfo: AppMinThen?, model: TempConversationModel, isOther: Bool) {
        // 在线状态
        //: onlineStatus.isHidden = (model.listOnlineStatus == 0)
        onlineStatus.isHidden = (model.listOnlineStatus == 0)
        //: newImgV.isHidden = !model.isNewUser
        newImgV.isHidden = !model.isNewUser
        // 是否存在用户信息
        //: if userInfo == nil || userInfo?.uid == ManagerReactiveCompatible.getXiaoMiID() || userInfo?.uid == ManagerReactiveCompatible.getCustomerServiceID() {
        if userInfo == nil || userInfo?.uid == ManagerReactiveCompatible.heterosexuality() || userInfo?.uid == ManagerReactiveCompatible.facilityGetWhoremonger() {
            //: nickNameLabel.textColor = .appTitleColor()
            nickNameLabel.textColor = .elementColor()
            //: nickNameLabel.text = model.showName
            nickNameLabel.text = model.showName
            //: nickNameLabel.snp.updateConstraints { make in
            nickNameLabel.snp.updateConstraints { make in
                //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
                make.trailing.lessThanOrEqualToSuperview().offset(-40)
            }
            //: if model.chatType == .group || model.chatType == .moreMsg {
            if model.chatType == .group || model.chatType == .moreMsg {
                //: headImgView.image = UIImage.nameTitle(name: model.faceUrl)
                headImgView.image = UIImage.nameTitle(name: model.faceUrl)
                //: } else {
            } else {
                //: headImgView.setUrlImage(urlStr: model.faceUrl, placeImg: UIImage.placeSquareBigImgBanner())
                headImgView.overMake(urlStr: model.faceUrl, placeImg: UIImage.priceObjectOpen())
            }
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
            //: imtimateBtn.isHidden = true
            imtimateBtn.isHidden = true
            //: return
            return
        }
        //: guard let userInfo = userInfo else {
        guard let userInfo = userInfo else {
            //: return
            return
        }
        // 昵称
        //: nickNameLabel.textColor = userInfo.loungePlus ? .userVipColor():.appTitleColor()
        nickNameLabel.textColor = userInfo.loungePlus ? .requestUser() : .elementColor()
        //: nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname:model.userID
        nickNameLabel.text = userInfo.nickname.count > 0 ? userInfo.nickname : model.userID
        // 头像
        //: headImgView.setUrlImage(urlStr: userInfo.headPic, placeImg: UIImage.placeHolderImage(sex: userInfo.sex))
        headImgView.overMake(urlStr: userInfo.headPic, placeImg: UIImage.barrelFromHeterosexual(sex: userInfo.sex))
        // VIP头像框
        //: if isOther == false && !userInfo.headPicFrame.isEmptyString {
        if isOther == false, !userInfo.headPicFrame.isEmptyString {
            //: iconBorder.isHidden = false
            iconBorder.isHidden = false
            //: iconBorder.setHeadFrameUrlImage(urlStr: userInfo.headPicFrame)
            iconBorder.modelPriceStr(urlStr: userInfo.headPicFrame)
            //: } else {
        } else {
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
        }
//        imtimateBtn.setTitle(model.totalIntimate > 99999 ? "99999+": "\(model.totalIntimate)", for: .normal)
//        imtimateBtn.isHidden = model.totalIntimate <= 0
//        if model.totalIntimate > 0 {
//            let size = imtimateBtn.titleLabel?.text?.boundingRect(with: CGSize(width: CGFloat(MAXFLOAT), height: 0.0), options: .usesLineFragmentOrigin, attributes: [NSAttributedString.Key.font: imtimateBtn.titleLabel?.font ?? .pingfangFont(type: .Medium, fontSize: 12)], context: nil)
//            imtimateBtn.snp.updateConstraints { make in
//                make.width.equalTo((size?.width ?? 50) + 20)
//            }
//        }
        // 是否其他消息
        //: if isOther == false {
        if isOther == false {
            //: loungeImgV.isHidden = !userInfo.loungePlus
            loungeImgV.isHidden = !userInfo.loungePlus
            //: authImgV.isHidden = !userInfo.tpAuth
            authImgV.isHidden = !userInfo.tpAuth
            //: officialImgV.isHidden = !userInfo.isOfficial
            officialImgV.isHidden = !userInfo.isOfficial
            //: } else {
        } else {
            //: loungeImgV.isHidden = true
            loungeImgV.isHidden = true
            //: authImgV.isHidden = true
            authImgV.isHidden = true
            //: officialImgV.isHidden = true
            officialImgV.isHidden = true
        }

        //: nickNameLabel.snp.updateConstraints { make in
        nickNameLabel.snp.updateConstraints { make in
            //: make.trailing.lessThanOrEqualToSuperview().offset( isOther == true ? -40 : -130)
            make.trailing.lessThanOrEqualToSuperview().offset(isOther == true ? -40 : -130)
        }
        //: loungeImgV.snp.updateConstraints { make in
        loungeImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0:4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(loungeImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(loungeImgV.isHidden == true ? 0:20)
            make.width.equalTo(loungeImgV.isHidden == true ? 0 : 20)
        }
        //: authImgV.snp.updateConstraints { make in
        authImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0:4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(authImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(authImgV.isHidden == true ? 0:16)
            make.width.equalTo(authImgV.isHidden == true ? 0 : 16)
        }
        //: let text = "New".localized
        let text = (String(str_announcementData)).localized
        //: let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.underPlay(fontSize: 11)], context: nil)
        let rect = text.boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.underPlay(fontSize: 11)], context: nil)
        //: newImgV.snp.updateConstraints { make in
        newImgV.snp.updateConstraints { make in
            //: make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0:4)
            make.leading.equalTo(authImgV.snp.trailing).offset(newImgV.isHidden == true ? 0 : 4)
            //: make.width.equalTo(newImgV.isHidden == true ? 0:rect.width+10)
            make.width.equalTo(newImgV.isHidden == true ? 0 : rect.width + 10)
        }
        //: officialImgV.snp.remakeConstraints { make in
        officialImgV.snp.remakeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.height.equalTo(15)
            make.height.equalTo(15)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0:4)
            make.leading.equalTo(newImgV.snp.trailing).offset(officialImgV.isHidden == true ? 0 : 4)
            //: if officialImgV.isHidden {
            if officialImgV.isHidden {
                //: make.width.equalTo(0)
                make.width.equalTo(0)
                //: } else {
            } else {
                //: make.width.greaterThanOrEqualTo(50)
                make.width.greaterThanOrEqualTo(50)
            }
        }
    }

    /// 更新未读数
    /// - Parameter unreadCount: 未读消息数
    //: private func updateUnreadNumber(unreadCount: Int) {
    private func underShare(unreadCount: Int) {
        //: guard unreadCount > 0 else {
        guard unreadCount > 0 else {
            //: self.badgeLab.isHidden = true
            self.badgeLab.isHidden = true
            //: return
            return
        }

        //: self.badgeLab.isHidden = false
        self.badgeLab.isHidden = false
        //: var unreadCountStr = String(unreadCount)
        var unreadCountStr = String(unreadCount)
        //: if unreadCount > 99 {
        if unreadCount > 99 {
            //: unreadCountStr = String(unreadCount) + "+"
            unreadCountStr = String(unreadCount) + "+"
            //: badgeLab.text = "99+"
            badgeLab.text = "99+"
            //: } else {
        } else {
            //: badgeLab.text = unreadCountStr
            badgeLab.text = unreadCountStr
        }
        //: var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        var width = badgeLab.text!.size(withAttributes: [.font: self.badgeLab.font!]).width + 12
        //: if width < 20 { width = 20 }
        if width < 20 { width = 20 }
        //: badgeLab.snp.updateConstraints { make in
        badgeLab.snp.updateConstraints { make in
            //: make.width.greaterThanOrEqualTo(width)
            make.width.greaterThanOrEqualTo(width)
        }
    }

    /// 处理详情消息
    //: private func handleC2CMessage(model: TalkingConversationModel) {
    private func concatenateModel(model: TempConversationModel) {
        //: if model.userStatus == 5 {
        if model.userStatus == 5 {
            //: let str = "Account is restricted！".localized
            let str = (String(str_singleValue.prefix(9)) + String(str_pathMessageValue)).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.videoValue()
                NSAttributedString.Key.foregroundColor: UIColor.videoValue(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            //: return
            return
        }
        //: if model.listShowMessage != nil {
        if model.listShowMessage != nil {
            //: msgShowLabel.attributedText = TalkingChatListTableCell.handleShowC2CDetailTextOfMessage(model: model)
            msgShowLabel.attributedText = AtTableCell.afterSubmitView(model: model)
            //: } else if model.chatType == .group {
        } else if model.chatType == .group { // 公共聊天室入口
            //: let str = "%@ Online".localizedArguments(model.num)
            let str = (String(str_oldData)).localizedArguments(model.num)
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.videoValue()
                NSAttributedString.Key.foregroundColor: UIColor.videoValue(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else if model.chatType == .moreMsg {
        } else if model.chatType == .moreMsg { // 更多消息入口，无数据时，占位文案
            //: let str = "Messages not been replied here".localized
            let str = (String(str_toCurrentValue.prefix(6)) + "es n" + String(str_managerData.prefix(4)) + String(str_equalMakeContent.suffix(5)) + "eplie" + String(str_succeedTitle.prefix(6))).localized
            //: let attributes: [NSAttributedString.Key: Any] = [
            let attributes: [NSAttributedString.Key: Any] = [
                //: NSAttributedString.Key.font: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15),
                NSAttributedString.Key.font: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15),
                //: NSAttributedString.Key.foregroundColor: UIColor.videoValue()
                NSAttributedString.Key.foregroundColor: UIColor.videoValue(),
            ]
            //: msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)
            msgShowLabel.attributedText = NSAttributedString(string: str, attributes: attributes)

            //: } else {
        } else { // 草稿箱
            //: var attrStr: NSMutableAttributedString?
            var attrStr: NSMutableAttributedString?
            //: if !model.draftText.isEmptyString {
            if !model.draftText.isEmptyString {
                //: attrStr = TalkingChatListTableCell.getDraftString(draft: &model.draftText)
                attrStr = AtTableCell.fertilePhase(draft: &model.draftText)
            }
            //: attrStr?.addAttribute(.font, value: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            attrStr?.addAttribute(.font, value: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15), range: NSRange(location: 0, length: attrStr?.length ?? 0))
            //: msgShowLabel.attributedText = attrStr
            msgShowLabel.attributedText = attrStr
        }
    }
}

// MARK: - UI

//: extension TalkingChatListTableCell {
extension AtTableCell {
    //: private func createCellUI() {
    private func layerFor() {
        //: contentView.addSubview(headImgView)
        contentView.addSubview(headImgView)
        //: contentView.addSubview(iconBorder)
        contentView.addSubview(iconBorder)
        //: contentView.addSubview(onlineStatus)
        contentView.addSubview(onlineStatus)
        //: contentView.addSubview(badgeLab)
        contentView.addSubview(badgeLab)
        //: contentView.addSubview(nickNameLabel)
        contentView.addSubview(nickNameLabel)
        //: contentView.addSubview(loungeImgV)
        contentView.addSubview(loungeImgV)
        //: contentView.addSubview(authImgV)
        contentView.addSubview(authImgV)
        //: contentView.addSubview(newImgV)
        contentView.addSubview(newImgV)
        //: contentView.addSubview(officialImgV)
        contentView.addSubview(officialImgV)
        //: contentView.addSubview(msgShowLabel)
        contentView.addSubview(msgShowLabel)
        //: contentView.addSubview(timeLab)
        contentView.addSubview(timeLab)
        //: contentView.addSubview(imtimateBtn)
        contentView.addSubview(imtimateBtn)
    }

    //: private func createCellUIConstraints() {
    private func imageValue() {
        //: headImgView.snp.makeConstraints { make in
        headImgView.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.width.height.equalTo(60)
            make.width.height.equalTo(60)
        }
        //: iconBorder.snp.makeConstraints { make in
        iconBorder.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(69.5)
            make.width.height.equalTo(69.5)
        }
        //: onlineStatus.snp.makeConstraints { make in
        onlineStatus.snp.makeConstraints { make in
            //: make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            make.trailing.equalTo(headImgView.snp.trailing).offset(-3)
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-3)
            //: make.width.height.equalTo(10)
            make.width.height.equalTo(10)
        }
        //: badgeLab.snp.makeConstraints { make in
        badgeLab.snp.makeConstraints { make in
            //: make.top.equalTo(self.headImgView.snp.top).offset(-5)
            make.top.equalTo(self.headImgView.snp.top).offset(-5)
            //: make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            make.trailing.equalTo(self.headImgView.snp.trailing).offset(6)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
            //: make.width.greaterThanOrEqualTo(20)
            make.width.greaterThanOrEqualTo(20)
        }
        //: nickNameLabel.snp.makeConstraints { make in
        nickNameLabel.snp.makeConstraints { make in
            //: make.top.equalTo(headImgView).offset(6)
            make.top.equalTo(headImgView).offset(6)
            //: make.leading.equalTo(headImgView.snp.trailing).offset(10)
            make.leading.equalTo(headImgView.snp.trailing).offset(10)
            //: make.trailing.lessThanOrEqualToSuperview().offset(-40)
            make.trailing.lessThanOrEqualToSuperview().offset(-40)
            //: make.height.equalTo(21)
            make.height.equalTo(21)
        }
        //: loungeImgV.snp.makeConstraints { make in
        loungeImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            make.leading.equalTo(nickNameLabel.snp.trailing).offset(4)
            //: make.height.width.equalTo(20)
            make.height.width.equalTo(20)
        }
        //: authImgV.snp.makeConstraints { make in
        authImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            make.leading.equalTo(loungeImgV.snp.trailing).offset(4)
            //: make.width.equalTo(16)
            make.width.equalTo(16)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: newImgV.snp.makeConstraints { make in
        newImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(authImgV.snp.trailing).offset(4)
            make.leading.equalTo(authImgV.snp.trailing).offset(4)
            //: make.width.equalTo(26)
            make.width.equalTo(26)
            //: if LanguageManager.shared.direction == .rightToLeft {
            if PropertyLineThen.shared.direction == .rightToLeft {
                //: make.height.equalTo(16)
                make.height.equalTo(16)
                //: } else {
            } else {
                //: make.height.equalTo(14)
                make.height.equalTo(14)
            }
        }
        //: officialImgV.snp.makeConstraints { make in
        officialImgV.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.leading.equalTo(newImgV.snp.trailing).offset(4)
            make.leading.equalTo(newImgV.snp.trailing).offset(4)
            //: make.width.greaterThanOrEqualTo(50)
            make.width.greaterThanOrEqualTo(50)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(nickNameLabel)
            make.centerY.equalTo(nickNameLabel)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: imtimateBtn.snp.makeConstraints { make in
        imtimateBtn.snp.makeConstraints { make in
            //: make.centerY.equalTo(msgShowLabel)
            make.centerY.equalTo(msgShowLabel)
            //: make.width .equalTo(50)
            make.width.equalTo(50)
            //: make.trailing.equalToSuperview().offset(-15)
            make.trailing.equalToSuperview().offset(-15)
        }
        //: msgShowLabel.snp.makeConstraints { make in
        msgShowLabel.snp.makeConstraints { make in
            //: make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            make.bottom.equalTo(headImgView.snp.bottom).offset(-6)
            //: make.leading.equalTo(nickNameLabel)
            make.leading.equalTo(nickNameLabel)
            //: make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
            make.trailing.equalTo(imtimateBtn.snp.leading).offset(-10)
        }
    }
}
