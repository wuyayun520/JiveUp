
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_timeData:[UInt8] = [0x82,0x85,0x82,0x9f,0xc3,0x88,0x84,0x8f,0x8e,0x99,0xd1,0xc2,0xcb,0x83,0x8a,0x98,0xcb,0x85,0x84,0x9f,0xcb,0x89,0x8e,0x8e,0x85,0xcb,0x82,0x86,0x9b,0x87,0x8e,0x86,0x8e,0x85,0x9f,0x8e,0x8f]

private func buttonTransform(normal num: UInt8) -> UInt8 {
    return num ^ 235
}

/*: "intimate_lock" :*/
fileprivate let str_appModelName:String = "itimetim"
fileprivate let str_numberNameValue:String = "index area video show titleate_lock"

/*: "Unlock" :*/
fileprivate let str_titleTipValue:[Character] = ["U","n","l","o","c","k"]

/*: "nav_back_black_nor" :*/
fileprivate let str_viewPointName:String = "nav_bacapp push"
fileprivate let str_imagePlayerName:String = "to var icon showk_"
fileprivate let str_dismissData:[Character] = ["b","l","a","c","k","_","n","o","r"]

/*: "toUid" :*/
fileprivate let str_actualData:String = "content row value theoreticaltoUid"

/*: "giftId" :*/
fileprivate let str_actionName:String = "background let text cellgiftId"

/*: "giftNum" :*/
fileprivate let str_flexibleName:[Character] = ["g"]
fileprivate let str_youValue:String = "iftNumgift lucky self"

/*: "unlockMsgId" :*/
fileprivate let str_detailBottomData:[Character] = ["u","n","l","o","c","k","M","s"]
fileprivate let str_textStyleValue:String = "send path linegId"

/*: "galleryId" :*/
fileprivate let str_simultaneouslyTitle:String = "galleryIdtotal type failure self height"

/*: "Load failed, return to retry" :*/
fileprivate let str_iconModeValue:String = "Load to background return any self"
fileprivate let str_nowData:String = "ed, rbutton in equal data"
fileprivate let str_backText:String = "etblockrn"
fileprivate let str_femaleName:String = "formattry"

/*: "You will spend 1 chance to see the private video, you have  :*/
fileprivate let str_rowName:[UInt8] = [0x4,0x1a,0x20,0xcb,0x22,0x14,0x17,0x17,0xcb,0x1e,0x1b,0x10,0x19,0xf,0xcb,0xdc,0xcb,0xe,0x13,0xc,0x19,0xe,0x10,0xcb,0x1f,0x1a,0xcb,0x1e,0x10,0x10,0xcb,0x1f,0x13,0x10,0xcb,0x1b,0x1d,0x14,0x21,0xc,0x1f,0x10,0xcb,0x21,0x14,0xf,0x10,0x1a,0xd7,0xcb,0x24,0x1a,0x20,0xcb,0x13,0xc,0x21,0x10,0xcb]

fileprivate func forUser(center num: UInt8) -> UInt8 {
    let value = Int(num) + 85
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*:  chance left." :*/
fileprivate let str_layerTitle:String = " chance value cookie"
fileprivate let str_priceTitle:String = "left.return back path table if"

/*: "icon_photo_lock" :*/
fileprivate let str_deviceText:[Character] = ["i","c","o","n","_","p","h","o","t","o","_","l","o","c"]
fileprivate let str_tabData:[Character] = ["k"]

/*: "Click on the screen to play this video(Burn After Reading)" :*/
fileprivate let str_tapText:[UInt8] = [0x72,0x9b,0x98,0x92,0x9a,0x4f,0x9e,0x9d,0x4f,0xa3,0x97,0x94,0x4f,0xa2,0x92,0xa1,0x94,0x94,0x9d,0x4f,0xa3,0x9e,0x4f,0x9f,0x9b,0x90,0xa8,0x4f,0xa3,0x97,0x98,0xa2,0x4f,0xa5,0x98,0x93,0x94,0x9e,0x57,0x71,0xa4,0xa1,0x9d,0x4f,0x70,0x95,0xa3,0x94,0xa1,0x4f,0x81,0x94,0x90,0x93,0x98,0x9d,0x96,0x58]

fileprivate func blockMedium(data num: UInt8) -> UInt8 {
    let value = Int(num) - 47
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "icon_photo_burn" :*/
fileprivate let str_hiddenContent:String = "the return layericon_"
fileprivate let str_equalPathTitle:String = "data none room_bur"
fileprivate let str_topValue:[Character] = ["n"]

/*: "Already burned" :*/
fileprivate let str_cellMeText:[Character] = ["A","l","r","e","a","d","y"," ","b"]
fileprivate let str_selectedFaceValue:[Character] = ["u","r","n","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  NoRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/2/15.
//

//: import UIKit
import UIKit

//: typealias IntimateVideoBlock = (_ model: LabelModel) -> Void
typealias IntimateVideoBlock = (_ model: LabelModel) -> Void
//: typealias VideoStatusBlock = (_ status: VideoPlayerStatus) -> Void
typealias VideoStatusBlock = (_ status: AcrossBorderPlayerStatus) -> Void

//: class TalkingIntimateVideoPreviewVC: TalkingBaseViewController {
class NoRecognizerDelegate: EqualVideoReactiveCompatible {
	var promptQuantity: Int = 29
	var addCount: Double = 91.4
	var terminalArray: [AnyHashable] = []
	var exceptAtDictionary: [AnyHashable: String] = [:]
	var disguiseTotal: Int = 41
	var infoCount: Double = 18.1
	var utilizerArray: [AnyHashable] = []
	var managerTargetDictionary: [AnyHashable: String] = [:]
	var tillCount: Int = 67
	var cellTotal: Double = -39.4
	var keyArray: [AnyHashable] = []
	var dataDictionary: [AnyHashable: String] = [:]

    //: var statusBlock: IntimateVideoBlock?
    var statusBlock: IntimateVideoBlock? // 同步状态block
    //: var sendGiftResultBlock: FinishBlock?
    var sendGiftResultBlock: FinishBlock? // 送礼结果block
    //: var videoStatusBlock: VideoStatusBlock?
    var videoStatusBlock: VideoStatusBlock? // 视频状态block【目前用在阅后即焚】
    //: private var videoTotalData = VideoCellData(direction: .MsgDirectionIncoming)
    private var videoTotalData = VideoCellData(direction: .MsgDirectionIncoming) // 视频消息数据
    //: private var videoModel = LabelModel()
    private var videoModel = LabelModel() // 当前私密视频模型
    //: private var targetId = ""                                       // 私密视频发送方ID
    private var targetId = "" // 私密视频发送方ID
    //: private var countdownTimer: Timer?
    private var countdownTimer: Timer? // 倒计时
    //: private var isMySelf = false
    private var isMySelf = false // 是否本人
    //: private var playDuraction = 0.0
    private var playDuraction = 0.0 // 视频播放位置
    //: private var previewNum = 0
    private var previewNum = 0 //  次数

    /// 初始化
    /// - Parameters:
    ///   - videoCellData: 视频模型
    ///   - toUid: 对方Id
    //: init(videoCellData: VideoCellData, toUid: String, preNum: Int) {
    init(videoCellData: VideoCellData, toUid: String, preNum: Int) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: videoTotalData = videoCellData
        videoTotalData = videoCellData
        //: previewNum = preNum
        previewNum = preNum
        //: videoModel = videoTotalData.msgModel.msgInfo.video
        videoModel = videoTotalData.msgModel.msgInfo.video // 引用类型，修改该数据，videoCellData会同步修改
        //: targetId = toUid
        targetId = toUid
        //: isMySelf = (videoTotalData.msgModel.user.uid == Int(SubLabelReactiveCompatible.share.loginUserMode.userID))
        isMySelf = (videoTotalData.msgModel.user.uid == Int(SubLabelReactiveCompatible.share.loginUserMode.userID))
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_timeData.map{buttonTransform(normal: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        //: popGesture(isOpen: true)
        textDown(isOpen: true)
    
            if (imageView.superview != nil && imageView.superview!.isHidden) && (imageView.tintAdjustmentMode == .dimmed) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let penLoad = HideNumberView(frame: imageView.frame.offsetBy(dx: CGFloat(104.39), dy: CGFloat(77)))
            
            penLoad.playerInsideNumber = { [self] appearCount in
            self.tillCount = appearCount
            
            var penLoad = previewNum
            penLoad &*= 6
            if penLoad > self.tillCount {
                self.tillCount = penLoad
            }
            
            return self.tillCount
            }
            penLoad.arraySum = { [self] feeQuantity in
            self.cellTotal = feeQuantity
            
            var penLoad = playDuraction
                penLoad += 1
                if penLoad < 62 {
                    penLoad = penLoad - 1
                }
            if penLoad > self.cellTotal {
                self.cellTotal = penLoad
            }
            
            return self.cellTotal
            }
            penLoad.sectionArray = { [self] meArray in
            self.keyArray = meArray
            
            guard var value = self.keyArray as? [String] else {
                return nil
            }
            return value
            }
            penLoad.labelDictionary = { [self] backDictionary in
            self.dataDictionary = backDictionary
            
            guard var value = self.dataDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                imageView.addSubview(penLoad)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: popGesture(isOpen: false)
        textDown(isOpen: false)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        design()
        //: setupSubViewsConstraint()
        stud()
        //: bindInteraction()
        popQuantityeraction()
        //: refreshUI()
        comeOn()
    
            if (giftBgView.superview != nil && giftBgView.superview!.isHidden) && (giftBgView.tintAdjustmentMode == .dimmed) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let penLoad = HideNumberView(frame: giftBgView.frame.offsetBy(dx: CGFloat(104.39), dy: CGFloat(77)))
            
            penLoad.playerInsideNumber = { [self] appearCount in
            self.promptQuantity = appearCount
            
            var penLoad = previewNum
            penLoad &*= 6
            if penLoad > self.promptQuantity {
                self.promptQuantity = penLoad
            }
            
            return self.promptQuantity
            }
            penLoad.arraySum = { [self] feeQuantity in
            self.addCount = feeQuantity
            
            var penLoad = playDuraction
                penLoad += 1
                if penLoad < 62 {
                    penLoad = penLoad - 1
                }
            if penLoad > self.addCount {
                self.addCount = penLoad
            }
            
            return self.addCount
            }
            penLoad.sectionArray = { [self] meArray in
            self.terminalArray = meArray
            
            guard var value = self.terminalArray as? [String] else {
                return nil
            }
            return value
            }
            penLoad.labelDictionary = { [self] backDictionary in
            self.exceptAtDictionary = backDictionary
            
            guard var value = self.exceptAtDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                giftBgView.addSubview(penLoad)
            }

	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SubLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
            if SubLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-StatusBarNavigationBarHeight))
                let bgView = HadithShieldView(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName - kLet_soundData))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyTimer()
        priceCallTimer()
        //: self.player?.stopPlay()
        self.player?.positionGreet()
        //: self.player = nil
        self.player = nil
    }

    // MARK: - Lazy Load

    // 封面
    //: private lazy var imageView: UIImageView = {
    private lazy var imageView: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.backgroundColor = .black
        img.backgroundColor = .black
        //: img.contentMode = .scaleAspectFit
        img.contentMode = .scaleAspectFit
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    // 视频播放器容器
    //: private lazy var videoView: UIView = {
    private lazy var videoView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .black
        v.backgroundColor = .black
        //: return v
        return v
        //: }()
    }()

    // 视频播放器
    //: private lazy var player: TalkingVideoPlayerManager? = {
    private lazy var player: FinishErasePlayerManager? = {
        //: let  player = TalkingVideoPlayerManager()
        let player = FinishErasePlayerManager()
        //: player.delegate = self
        player.delegate = self
        //: player.setMute(bEnable: false)
        player.bigness(bEnable: false)
        //: return player
        return player
        //: }()
    }()

    // 视频进度条
    //: private lazy var videoProgressView: TalkingVideoPlayerInteractionView = {
    private lazy var videoProgressView: TotaleractionAtCounteractionView = {
        //: let view = TalkingVideoPlayerInteractionView()
        let view = TotaleractionAtCounteractionView()
        //: view.toolBarView.isHidden = false
        view.toolBarView.isHidden = false
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var maskView: UIView = {
    private lazy var maskView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor.init(white: 0, alpha: 0.5)
        v.backgroundColor = UIColor(white: 0, alpha: 0.5)
        //: return v
        return v
        //: }()
    }()

    //: private lazy var giftBgView: UIImageView = {
    private lazy var giftBgView: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.nameTitle(name: "intimate_lock")
        img.image = UIImage.nameTitle(name: (str_appModelName.replacingOccurrences(of: "time", with: "n") + String(str_numberNameValue.suffix(8))))
        //: return img
        return img
        //: }()
    }()

//    private lazy var giftIcon: UIImageView = {
//        let img = UIImageView()
//        img.setUrlImage(urlStr: videoModel.giftIcon)
//        return img
//    }()
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.underPlay(fontSize: 19)
        lab.font = UIFont.underPlay(fontSize: 19)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var giftSendBtn: UIButton = {
    private lazy var giftSendBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setTitle("Unlock".localized, for: .normal)
        btn.setTitle((String(str_titleTipValue)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 19)
        btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 19)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 220, height: 44)), for: .normal)
        btn.setBackgroundImage(UIImage.gradientShared(colors: UIColor.endColor(), size: CGSize(width: 220, height: 44)), for: .normal)
        //: btn.layer.cornerRadius = 22
        btn.layer.cornerRadius = 22
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(giftSendButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(selectedFor), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var intimateStatusImgV: UIImageView = {
    private lazy var intimateStatusImgV: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.alpha = 0.9
        img.alpha = 0.9
        //: return img
        return img
        //: }()
    }()

    //: private lazy var progressView: CircleCountdownView = {
    private lazy var progressView: WorkroomReactiveCompatible = {
        //: let frame = CGRect(x: ScreenWidth-42-15, y: StatusBarHeight+10, width: 42, height: 42)
        let frame = CGRect(x: kLet_failureName - 42 - 15, y: kLet_messageSumervalName + 10, width: 42, height: 42)
        //: let v = CircleCountdownView(frame: frame)
        let v = WorkroomReactiveCompatible(frame: frame)
        //: self.view.addSubview(v)
        self.view.addSubview(v)
        //: return v
        return v
        //: }()
    }()

    //: lazy var backBtn: UIButton = {
    lazy var backBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let img = UIImage.nameTitle(name: "nav_back_black_nor").withRenderingMode(.alwaysTemplate)
        let img = UIImage.nameTitle(name: (String(str_viewPointName.prefix(7)) + String(str_imagePlayerName.suffix(2)) + String(str_dismissData))).withRenderingMode(.alwaysTemplate)
        //: btn.setImage(img, for: .normal)
        btn.setImage(img, for: .normal)
        //: btn.tintColor = .white
        btn.tintColor = .white
        //: btn.addTarget(self, action: #selector(backButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(targetEffect), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Load Data

//: extension TalkingIntimateVideoPreviewVC {
extension NoRecognizerDelegate {
    /// 调用送礼接口
    /// - Parameters:
    ///   - giftId: 礼物Id
    ///   - giftNum: 礼物数量
    //: func req_sendGiftMsg(giftId: String, giftNum: String) {
    func itemTitle(giftId: String, giftNum: String) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            barLog(showMsg: kLet_eventText)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(str_actualData.suffix(5)))] = self.targetId
        //: params["giftId"] = giftId
        params[(String(str_actionName.suffix(6)))] = giftId
        //: params["giftNum"] = giftNum
        params[(String(str_flexibleName) + String(str_youValue.prefix(6)))] = giftNum
        //: params["unlockMsgId"] = self.videoTotalData.msgModel.msgInfo.msgId
        params[(String(str_detailBottomData) + String(str_textStyleValue.suffix(3)))] = self.videoTotalData.msgModel.msgInfo.msgId
        //: params["galleryId"] = self.videoModel.galleryId
        params[(String(str_simultaneouslyTitle.prefix(9)))] = self.videoModel.galleryId

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        ViewRequestTool.pastPop(param: params, completion: { succeed, result, errorModel in
            //: if succeed {
            if succeed { // 送礼成功，更改私密视频状态为已解锁
                //: self.videoModel.lockStatus = 0
                self.videoModel.lockStatus = 0
                //: if self.statusBlock != nil {
                if self.statusBlock != nil {
                    //: self.statusBlock!(self.videoModel)
                    self.statusBlock!(self.videoModel)
                }

                // 刷新UI
                //: self.refreshUI()
                self.comeOn()
            }

            // 同步状态到私聊页面
            //: if self.sendGiftResultBlock != nil {
            if self.sendGiftResultBlock != nil {
                //: self.sendGiftResultBlock!(succeed, result, errorModel)
                self.sendGiftResultBlock!(succeed, result, errorModel)
            }
            //: })
        })
    }
}

// MARK: - Event

//: extension TalkingIntimateVideoPreviewVC {
extension NoRecognizerDelegate {
    /// 返回按钮事件
    //: @objc private func backButtonClick() {
    @objc private func targetEffect() {
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 发送礼物解锁视频
    //: @objc private func giftSendButtonClick() {
    @objc private func selectedFor() {
        //: req_sendGiftMsg(giftId: videoModel.giftId, giftNum: "1")
        itemTitle(giftId: videoModel.giftId, giftNum: "1")
    }

    /// 阅后即焚15s倒计时开始
    //: @objc private func burnVideoTapGesture() {
    @objc private func darkroom() {
        //: guard SubLabelReactiveCompatible.share.networkStatus != .Unavailable else {
        guard SubLabelReactiveCompatible.share.networkStatus != .Unavailable else {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            barLog(showMsg: kLet_eventText)
            //: return
            return
        }
        //: guard videoModel.burnStatus == 0 else { return }
        guard videoModel.burnStatus == 0 else { return }

        // 1. 更新视图状态
        //: self.maskView.isHidden = true
        self.maskView.isHidden = true
        //: self.intimateStatusImgV.isHidden = true
        self.intimateStatusImgV.isHidden = true
        //: self.desLab.isHidden = true
        self.desLab.isHidden = true
        //: self.videoView.isHidden = false
        self.videoView.isHidden = false

        // 2. 加载视频资源
        //: ProgressHUD.show(superView: self.view)
        DutyProgressHUD.appearRequest(superView: self.view)
        //: self.videoStatusBlock = { [weak self] status in
        self.videoStatusBlock = { [weak self] status in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard self.videoModel.burnStatus == 0 else { return }
            guard self.videoModel.burnStatus == 0 else { return }

            // 3. 判断视频是否可播放
            //: guard status == .Playing else {
            guard status == .Playing else {
                //: if status == .Failed {
                if status == .Failed {
                    //: ProgressHUD.dismiss()
                    DutyProgressHUD.duringShow()
                    //: self.videoProgressView.isHidden = true
                    self.videoProgressView.isHidden = true
                    //: self.func__showStatusBarErrorMsg(showMsg: "Load failed, return to retry".localized)
                    self.barLog(showMsg: (String(str_iconModeValue.prefix(5)) + "fail" + String(str_nowData.prefix(5)) + str_backText.replacingOccurrences(of: "block", with: "u") + " to " + str_femaleName.replacingOccurrences(of: "format", with: "re")).localized)
                }
                //: return
                return
            }

            // 4. 可播放，更改状态
            //: ProgressHUD.dismiss()
            DutyProgressHUD.duringShow()
            //: self.imageView.isHidden = true
            self.imageView.isHidden = true
            //: self.videoStatusBlock = nil
            self.videoStatusBlock = nil

            // 5. 更改私密视频状态为已销毁
            //: self.videoModel.burnStatus = 1
            self.videoModel.burnStatus = 1
            //: if self.statusBlock != nil {
            if self.statusBlock != nil {
                //: self.statusBlock!(self.videoModel)
                self.statusBlock!(self.videoModel)
            }

            // 6. 展示倒计时
            //: self.progressView.isHidden = false
            self.progressView.isHidden = false
            //: self.destroyTimer()
            self.priceCallTimer()
            //: var time = 600
            var time = 600
            //: self.countdownTimer = Timer.scheduledTimer(withTimeInterval: 0.1, block: { [weak self] timer in
            self.countdownTimer = Timer.scheduledTimer(withTimeInterval: 0.1, block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard time > 0 else {
                guard time > 0 else { // 结束倒计时，刷新视图
                    //: self.destroyTimer()
                    self.priceCallTimer()
                    //: self.refreshUI()
                    self.comeOn()
                    //: return
                    return
                }
                //: self.progressView.updateProgress(totalTime: 600, currTime: time)
                self.progressView.flowering(totalTime: 600, currTime: time)
                //: time -= 1
                time -= 1
                //: }, repeats: true)
            }, repeats: true)
            //: self.countdownTimer?.fire()
            self.countdownTimer?.fire()
            //: RunLoop.current.add(self.countdownTimer!, forMode: .common)
            RunLoop.current.add(self.countdownTimer!, forMode: .common)
        }
        // 播放视频
        //: playVideo()
        insertMentally()
    }

    /// 销毁倒计时
    //: func destroyTimer() {
    func priceCallTimer() {
        //: if countdownTimer != nil {
        if countdownTimer != nil {
            //: countdownTimer?.invalidate()
            countdownTimer?.invalidate()
            //: countdownTimer = nil
            countdownTimer = nil
        }
    }
}

// MARK: - SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION【收到音视频通话通知】

//: extension TalkingIntimateVideoPreviewVC {
extension NoRecognizerDelegate {
    /// 开始音视频通话
    //: @objc private func startTalking() {
    @objc private func addTalking() {
        //: self.player?.setMute(bEnable: true)
        self.player?.bigness(bEnable: true)
    }
}

// MARK: - 加载封面、视频

//: extension TalkingIntimateVideoPreviewVC {
extension NoRecognizerDelegate {
    /// 加载私密视频封面是否需要马赛克
    /// - Parameters:
    ///   - urlStr: 图片地址
    ///   - isMosaic: 是否需要马赛克
    //: private func loadIntimatePhoto(urlStr: String, isMosaic: Bool) {
    private func concealMosaic(urlStr: String, isMosaic: Bool) {
        //: guard isMySelf == false else {
        guard isMySelf == false else { // 本人发的图片，展示本地缓存资源
            //: let filePath = videoTotalData.msgModel.msgInfo.customSend.localCoverImg
            let filePath = videoTotalData.msgModel.msgInfo.customSend.localCoverImg
            //: let isExists = FileManager.default.fileExists(atPath: filePath)
            let isExists = FileManager.default.fileExists(atPath: filePath)
            //: guard isExists else { return }
            guard isExists else { return }
            //: let data = NSData.init(contentsOfFile: filePath)
            let data = NSData(contentsOfFile: filePath)
            //: guard data != nil else { return }
            guard data != nil else { return }
            //: let image = UIImage(data: data! as Data)
            let image = UIImage(data: data! as Data)
            //: if isMosaic {
            if isMosaic {
                //: self.imageView.image = UIImage.name(image, transFrameExtremeObject: 90)
                self.imageView.image = UIImage.name(image, transFrameExtremeObject: 90)
                //: } else {
            } else {
                //: self.imageView.image = image
                self.imageView.image = image
            }

            //: return
            return
        }

        // 对方发的图片，需要网络加载
        //: imageView.setCookieToUrlImage(urlStr: urlStr) {[weak self] image in
        imageView.showEqual(urlStr: urlStr) { [weak self] image in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if image != nil && isMosaic {
            if image != nil, isMosaic {
                //: self.imageView.image = UIImage.name(image, transFrameExtremeObject: 90)
                self.imageView.image = UIImage.name(image, transFrameExtremeObject: 90)
            }
        }
    }

    /// 播放视频
    //: private func playVideo() {
    private func insertMentally() {
        //: var videoPath = videoModel.videoUrl
        var videoPath = videoModel.videoUrl
        //: if isMySelf == true {
        if isMySelf == true { // 本人发的，优先使用本地视频
            //: let localPath = videoTotalData.msgModel.msgInfo.customSend.localVideoUrl
            let localPath = videoTotalData.msgModel.msgInfo.customSend.localVideoUrl
            //: if localPath.count > 0 && FileManager.default.fileExists(atPath: localPath) {
            if localPath.count > 0, FileManager.default.fileExists(atPath: localPath) {
                //: videoPath = localPath
                videoPath = localPath
            }
        }

        //: guard videoPath.count > 0 else {
        guard videoPath.count > 0 else {
            //: if self.videoStatusBlock != nil {
            if self.videoStatusBlock != nil {
                //: self.videoStatusBlock!(.Failed)
                self.videoStatusBlock!(.Failed)
            }
            //: return
            return
        }
        //: self.player?.playerWithUrlAndVideoView(url: videoPath, view: self.videoView)
        self.player?.nose(url: videoPath, view: self.videoView)
    }
}

// MARK: - CostRowReactiveCompatible, HairViewDelegate

//: extension TalkingIntimateVideoPreviewVC: TalkingVideoPlayerDelegate, InteractionViewDelegate {
extension NoRecognizerDelegate: CostRowReactiveCompatible, HairViewDelegate {
    // MARK: - CostRowReactiveCompatible

    //: func func__playerStatus(player: TalkingVideoPlayerManager, status: VideoPlayerStatus) {
    func infoAcrossStatus(player _: FinishErasePlayerManager, status: AcrossBorderPlayerStatus) {
        //: self.videoProgressView.interactionStatus(status: status)
        self.videoProgressView.face(status: status)
        //: if status == .Playing {
        if status == .Playing {
            //: self.player?.setRenderMode(renderMode: .FILL_EDGE)
            self.player?.sumeraction(renderMode: .FILL_EDGE)
        }
        //: if self.videoStatusBlock != nil {
        if self.videoStatusBlock != nil {
            //: self.videoStatusBlock!(status)
            self.videoStatusBlock!(status)
        }
    }

    //: func func__playerDuration(player: TalkingVideoPlayerManager, duration: Int, currentTime: Int) {
    func contextOfUse(player _: FinishErasePlayerManager, duration: Int, currentTime: Int) {
        //: self.playDuraction = Double(duration)
        self.playDuraction = Double(duration)
        //: self.videoProgressView.updateProgressDurationAndCurrentTime(duration: duration, currentTime: currentTime)
        self.videoProgressView.forwarding(duration: duration, currentTime: currentTime)
    }

    //: func func__playerProgress(player: TalkingVideoPlayerManager, progress: CGFloat) {
    func sizePop(player _: FinishErasePlayerManager, progress: CGFloat) {
        //: self.videoProgressView.updateProgress(value: progress)
        self.videoProgressView.searchMagnitudeo(value: progress)
    }

    // MARK: - HairViewDelegate

    //: func func__interactionViewStatus(view: TalkingVideoPlayerInteractionView, status: InteractionViewStatus) {
    func lowness(view _: TotaleractionAtCounteractionView, status: TextViewStatus) {
        //: switch status {
        switch status {
        //: case .StartPlay:
        case .StartPlay:
            //: self.player?.resume()
            self.player?.technique()
        //: break
        //: case .StopPlay:
        case .StopPlay:
            //: self.player?.pause()
            self.player?.clickPause()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__interactionViewSlideValue(view: TalkingVideoPlayerInteractionView, value: CGFloat) {
    func numberEqualValue(view _: TotaleractionAtCounteractionView, value: CGFloat) {
        //: let dragedSeconds = floorf(Float(value * self.playDuraction))
        let dragedSeconds = floorf(Float(value * self.playDuraction))
        //: self.player?.seek(time: dragedSeconds)
        self.player?.smear(time: dragedSeconds)
    }
}

// MARK: - Layout

//: extension TalkingIntimateVideoPreviewVC {
extension NoRecognizerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func design() {
        //: self.view.addSubview(videoView)
        self.view.addSubview(videoView)
        //: self.view.addSubview(imageView)
        self.view.addSubview(imageView)
        //: self.view.addSubview(videoProgressView)
        self.view.addSubview(videoProgressView)
        //: self.view.addSubview(maskView)
        self.view.addSubview(maskView)
        //: self.view.addSubview(desLab)
        self.view.addSubview(desLab)
        //: self.view.addSubview(giftBgView)
        self.view.addSubview(giftBgView)
//        giftBgView.addSubview(giftIcon)
        //: self.view.addSubview(giftSendBtn)
        self.view.addSubview(giftSendBtn)
        //: self.view.addSubview(intimateStatusImgV)
        self.view.addSubview(intimateStatusImgV)
        //: self.view.addSubview(backBtn)
        self.view.addSubview(backBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func stud() {
        //: videoView.snp.makeConstraints { make in
        videoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: videoProgressView.snp.makeConstraints { make in
        videoProgressView.snp.makeConstraints { make in
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.top.equalToSuperview().offset(StatusBarNavigationBarHeight+30)
            make.top.equalToSuperview().offset(kLet_soundData + 30)
            //: make.bottom.equalToSuperview().offset(-(kDeviceSafeBottomHeight+64))
            make.bottom.equalToSuperview().offset(-(kLet_changeBarValue + 64))
        }
        //: imageView.snp.makeConstraints { make in
        imageView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }

        //: maskView.snp.makeConstraints { make in
        maskView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.leading.equalTo(40)
            make.leading.equalTo(40)
            //: make.trailing.equalTo(-40)
            make.trailing.equalTo(-40)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.centerY.equalToSuperview().offset(StatusBarNavigationBarHeight/2)
            make.centerY.equalToSuperview().offset(kLet_soundData / 2)
        }

        //: giftBgView.snp.makeConstraints { make in
        giftBgView.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 85, height: 87))
            make.size.equalTo(CGSize(width: 85, height: 87))
            //: make.bottom.equalTo(desLab.snp.top).offset(-17)
            make.bottom.equalTo(desLab.snp.top).offset(-17)
        }
//        giftIcon.snp.makeConstraints { make in
//            make.leading.equalToSuperview().offset(35)
//            make.top.equalToSuperview().offset(28)
//            make.size.equalTo(CGSize(width: 80, height: 80))
//        }
        //: giftSendBtn.snp.makeConstraints { make in
        giftSendBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 220, height: 44))
            make.size.equalTo(CGSize(width: 220, height: 44))
            //: make.bottom.equalToSuperview().offset(-(kDeviceSafeBottomHeight + 10))
            make.bottom.equalToSuperview().offset(-(kLet_changeBarValue + 10))
        }

        //: intimateStatusImgV.snp.makeConstraints { make in
        intimateStatusImgV.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(desLab.snp.top).offset(-20)
            make.bottom.equalTo(desLab.snp.top).offset(-20)
        }

        //: backBtn.snp.makeConstraints { make in
        backBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(0)
            make.leading.equalTo(0)
            //: make.width.equalTo(40)
            make.width.equalTo(40)
            //: make.height.equalTo(NavigationBarHeight)
            make.height.equalTo(kLet_timeText)
            //: make.top.equalTo(StatusBarHeight)
            make.top.equalTo(kLet_messageSumervalName)
        }

        //: progressView.snp.remakeConstraints { make in
        progressView.snp.remakeConstraints { make in
            //: make.leading.equalTo(ScreenWidth-42-15)
            make.leading.equalTo(kLet_failureName - 42 - 15)
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(kLet_messageSumervalName + 10)
            //: make.width.height.equalTo(42)
            make.width.height.equalTo(42)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func popQuantityeraction() {
        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(startTalking),
                                               selector: #selector(addTalking),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: kLet_liveSizeContent,
                                               //: object: nil)
                                               object: nil)
    }

    // 刷新视图
    //: private func refreshUI() {
    private func comeOn() {
        //: self.player?.stopPlay()
        self.player?.positionGreet()
        //: self.videoView.isHidden = true
        self.videoView.isHidden = true
        //: self.imageView.isHidden = true
        self.imageView.isHidden = true
        //: self.videoProgressView.isHidden = true
        self.videoProgressView.isHidden = true
        //: self.maskView.isHidden = true
        self.maskView.isHidden = true
        //: self.desLab.isHidden = true
        self.desLab.isHidden = true
        //: self.giftBgView.isHidden = true
        self.giftBgView.isHidden = true
        //: self.giftSendBtn.isHidden = true
        self.giftSendBtn.isHidden = true
        //: self.intimateStatusImgV.isHidden = true
        self.intimateStatusImgV.isHidden = true
        //: self.progressView.isHidden = true
        self.progressView.isHidden = true

        //: if (videoModel.lockStatus == 1) {
        if videoModel.lockStatus == 1 { // 锁定状态，需要送礼解锁
            //: self.imageView.isHidden = false
            self.imageView.isHidden = false
            //: self.loadIntimatePhoto(urlStr: videoModel.coverImg, isMosaic: true)
            self.concealMosaic(urlStr: videoModel.coverImg, isMosaic: true)
            //: self.videoProgressView.isHidden = false
            self.videoProgressView.isHidden = false
            //: self.videoProgressView.updateProgressDurationAndCurrentTime(duration: videoModel.duration, currentTime: 0)
            self.videoProgressView.forwarding(duration: videoModel.duration, currentTime: 0)
            //: self.maskView.isHidden = false
            self.maskView.isHidden = false
//            self.desLab.text = "Need a %@ (%@ Gold coins) to unlock".localizedArguments(videoModel.giftName, videoModel.giftPrice)
            //: self.desLab.text = "You will spend 1 chance to see the private video, you have \(self.previewNum) chance left."
            self.desLab.text = String(bytes: str_rowName.map{forUser(center: $0)}, encoding: .utf8)! + "\(self.previewNum)" + (String(str_layerTitle.prefix(8)) + String(str_priceTitle.prefix(5)))
            //: self.desLab.isHidden = false
            self.desLab.isHidden = false
            //: self.giftBgView.isHidden = false
            self.giftBgView.isHidden = false
            //: if isMySelf == false {
            if isMySelf == false { // 不是本人
                //: self.giftSendBtn.isHidden = false
                self.giftSendBtn.isHidden = false
            }
            //: } else if (videoModel.burnStatus == 0 || videoModel.burnStatus == 1) {
        } else if videoModel.burnStatus == 0 || videoModel.burnStatus == 1 { // 设置了阅后即焚，但还未销毁 || 已销毁
            //: self.imageView.isHidden = false
            self.imageView.isHidden = false
            //: self.loadIntimatePhoto(urlStr: videoModel.coverImg, isMosaic: true)
            self.concealMosaic(urlStr: videoModel.coverImg, isMosaic: true)
            //: self.maskView.isHidden = false
            self.maskView.isHidden = false
            //: self.intimateStatusImgV.isHidden = false
            self.intimateStatusImgV.isHidden = false
            //: self.desLab.isHidden = false
            self.desLab.isHidden = false

            //: if videoModel.burnStatus == 0 {
            if videoModel.burnStatus == 0 { // 未销毁
                //: self.videoProgressView.isHidden = false
                self.videoProgressView.isHidden = false
                //: self.videoProgressView.updateProgressDurationAndCurrentTime(duration: videoModel.duration, currentTime: 0)
                self.videoProgressView.forwarding(duration: videoModel.duration, currentTime: 0)
                //: self.intimateStatusImgV.image = UIImage.nameTitle(name: "icon_photo_lock")
                self.intimateStatusImgV.image = UIImage.nameTitle(name: (String(str_deviceText) + String(str_tabData)))
                //: self.desLab.text = "Click on the screen to play this video(Burn After Reading)".localized
                self.desLab.text = String(bytes: str_tapText.map{blockMedium(data: $0)}, encoding: .utf8)!.localized
                // 添加手势
                //: let tap = UITapGestureRecognizer(target: self, action: #selector(burnVideoTapGesture))
                let tap = UITapGestureRecognizer(target: self, action: #selector(darkroom))
                //: self.maskView.addGestureRecognizer(tap)
                self.maskView.addGestureRecognizer(tap)

                //: } else {
            } else {
                //: self.intimateStatusImgV.image = UIImage.nameTitle(name: "icon_photo_burn")
                self.intimateStatusImgV.image = UIImage.nameTitle(name: (String(str_hiddenContent.suffix(5)) + "photo" + String(str_equalPathTitle.suffix(4)) + String(str_topValue)))
                //: self.desLab.text = "Already burned".localized
                self.desLab.text = (String(str_cellMeText) + String(str_selectedFaceValue)).localized
            }

            //: } else {
        } else { // 已解锁的永久私密视频
            //: self.playVideo()
            self.insertMentally()
            //: self.videoView.isHidden = false
            self.videoView.isHidden = false
            //: self.videoProgressView.isHidden = false
            self.videoProgressView.isHidden = false
        }
    }
}
