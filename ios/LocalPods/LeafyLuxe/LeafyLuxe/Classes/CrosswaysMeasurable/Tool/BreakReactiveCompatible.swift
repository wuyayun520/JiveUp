
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let str_executeValue:String = "mf/mometable content gift title height"
fileprivate let str_picName:String = "with kit make self innt/list"

/*: "mf/moment/moments" :*/
fileprivate let str_labelValue:String = "mf/mtool gift model"
fileprivate let str_adjustTheName:String = "t/momenscale data"
fileprivate let str_sexNameValue:String = "tdata"

/*: "mf/moment/like" :*/
fileprivate let str_soundValue:String = "let the viewmf/mom"
fileprivate let str_keySharedData:String = "ent/likeshared return view center push"

/*: "mf/crush/send" :*/
fileprivate let str_domainData:String = "mf/crmode let view user premium"

/*: "mf/moment/view" :*/
fileprivate let str_normalValue:String = "mf/mdata lab model"
fileprivate let str_appData:String = "view list status returnt/view"

/*: "mf/momentReply/list" :*/
fileprivate let str_fieldTitle:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l"]
fileprivate let str_directValue:String = "about"
fileprivate let str_atTextTitle:[Character] = ["/","l","i","s","t"]

/*: "mf/momentReply/save" :*/
fileprivate let str_dateModeText:String = "other lastmf/mome"
fileprivate let str_pushTitle:String = "self voice return varntRep"

/*: "mf/moment/videoSign" :*/
fileprivate let str_normalStatusTitle:[Character] = ["m","f","/","m","o","m","e","n","t","/","v","i","d","e","o"]
fileprivate let str_sizeFileValue:[Character] = ["S","i","g","n"]

/*: "dynamicPic.zip" :*/
fileprivate let str_startName:String = "DYNAMI"
fileprivate let str_finishName:[Character] = [".","z","i","p"]

/*: "pic" :*/
fileprivate let str_statusTopTitle:[UInt8] = [0x22,0x3b,0x31]

private func afterTitle(location num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "content" :*/
fileprivate let str_giftTextVideoContent:String = "covaluetevaluet"

/*: "mf/moment/save" :*/
fileprivate let str_modelSendText:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let str_fieldContent:[Character] = ["/","s","a","v","e"]

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let str_succeedData:[UInt8] = [0x77,0x65,0x69,0x76,0x20,0x6f,0x74,0x20,0x73,0x63,0x69,0x6d,0x61,0x6e,0x79,0x44,0x20,0x79,0x4d,0x20,0x6f,0x74,0x20,0x6f,0x47,0x20,0x2c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x53,0x20,0x68,0x73,0x69,0x6c,0x62,0x75,0x50]

/*: "mf/moment/saveVideo" :*/
fileprivate let str_requestToData:String = "mf/molab to self"
fileprivate let str_slideTitle:String = "contentent"
fileprivate let str_gameMapTopData:String = "submit custom max now/sav"

/*: .png" :*/
fileprivate let str_informationTitle:String = ".pngshare color scale view"

/*: "dynamicPic" :*/
fileprivate let str_sizeNameValue:String = "dynavalue"
fileprivate let str_stopName:String = "cPicmodel cell"

/*: "freeImage" :*/
fileprivate let str_acrossText:[Character] = ["f","r","e","e","I"]
fileprivate let str_voiceTitle:String = "mastatus"

/*: "mf/momentReply/replylist" :*/
fileprivate let str_spreadMeValue:String = "hidden"
fileprivate let str_labelMessageData:[Character] = ["f","/","m","o","m","e","n","t","R","e","p","l","y","/","r","e","p","l","y","l","i","s","t"]

/*: "mf/moment/delete" :*/
fileprivate let str_selectValue:[Character] = ["m","f","/","m","o","m","e","n","t","/","d","e","l"]
fileprivate let str_extraShowData:[Character] = ["e","t","e"]

/*: "mf/momentReply/delete" :*/
fileprivate let str_onlineValue:String = "time addmf/mo"
fileprivate let str_addHeadTableValue:String = "icon verticalRepl"
fileprivate let str_partyValue:[Character] = ["y","/","d","e","l","e","t","e"]

/*: "mf/moment/pin" :*/
fileprivate let str_assessmentName:String = "size makemf/mo"
fileprivate let str_remoteValue:String = "greet"
fileprivate let str_toTheValue:[Character] = ["e","n","t","/","p","i","n"]

/*: "mf/moment/remindNum" :*/
fileprivate let str_withBlockName:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e"]
fileprivate let str_managerData:String = "star message table button ifmindNum"

/*: "mf/moment/remindList" :*/
fileprivate let str_playerData:String = "color make observer viewmf/m"
fileprivate let str_inviteName:String = "REMI"
fileprivate let str_screenValue:[Character] = ["n","d","L","i","s","t"]

/*: "page" :*/
fileprivate let str_roomTitle:[UInt8] = [0x86,0x97,0x91,0x93]

private func speechRead(user num: UInt8) -> UInt8 {
    return num ^ 246
}

/*: "cos/getCredential" :*/
fileprivate let str_failureNatureTopTitle:String = "place path equal viewcos/get"
fileprivate let str_fromRestData:[Character] = ["C","r","e","d","e"]
fileprivate let str_tableColorName:[Character] = ["n","t","i","a","l"]

/*: "type" :*/
fileprivate let str_labelTitle:[Character] = ["t","y","p","e"]

/*: "user/reportUserInfo" :*/
fileprivate let str_blockValue:String = "user/rbottom equal path show"
fileprivate let str_overName:String = "white texttUserInfo"

/*: "uid" :*/
fileprivate let str_equalPhotoHiddenName:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let str_foundationData:[Character] = ["d","e"]
fileprivate let str_userProData:String = "text count videoviceId"

/*: "deviceName" :*/
fileprivate let str_outsideName:String = "super chiefdeviceNa"
fileprivate let str_addRoomData:[Character] = ["m","e"]

/*: "iphone" :*/
fileprivate let str_labelContentName:String = "iphsincee"

/*: "fileUrl" :*/
fileprivate let str_removePostTitle:String = "var var var customfileU"
fileprivate let str_topTitle:String = "rstyle"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BreakReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import UIKit
import UIKit

//: import Adjust
import Adjust

//: class TalkingMomentRequestTool: NSObject {
class BreakReactiveCompatible: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func dismissPath(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(str_executeValue.prefix(7)) + String(str_picName.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func honoursList(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(str_labelValue.prefix(4)) + "omen" + String(str_adjustTheName.prefix(7)) + str_sexNameValue.replacingOccurrences(of: "data", with: "s"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func hideCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(str_soundValue.suffix(6)) + String(str_keySharedData.prefix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func skim(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(str_domainData.prefix(5)) + "ush/send")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func isoclinic(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(str_normalValue.prefix(4)) + "omen" + String(str_appData.suffix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func belowVideo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(str_fieldTitle) + str_directValue.replacingOccurrences(of: "about", with: "y") + String(str_atTextTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func manoeuvre(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(str_dateModeText.suffix(7)) + String(str_pushTitle.suffix(5)) + "ly/save")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func makeCount(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(str_normalStatusTitle) + String(str_sizeFileValue))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func clingData(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        chart()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            backgroundForm(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((str_startName.lowercased() + "cPic" + String(str_finishName)))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: str_statusTopTitle.map{afterTitle(location: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(str_giftTextVideoContent.replacingOccurrences(of: "value", with: "n"))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = ClearRequestModel()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(str_modelSendText) + String(str_fieldContent))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.playerMsg(showMsg: String(bytes: str_succeedData.reversed(), encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.chart()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func addUp(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(str_requestToData.prefix(5)) + str_slideTitle.replacingOccurrences(of: "content", with: "m") + String(str_gameMapTopData.suffix(4)) + "eVideo")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func backgroundForm(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.length()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(str_informationTitle.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((str_sizeNameValue.replacingOccurrences(of: "value", with: "mi") + String(str_stopName.prefix(4))))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func chart() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(str_acrossText) + str_voiceTitle.replacingOccurrences(of: "status", with: "ge")))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func map(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (str_spreadMeValue.replacingOccurrences(of: "hidden", with: "m") + String(str_labelMessageData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func responseCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(str_selectValue) + String(str_extraShowData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func appCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(str_onlineValue.suffix(5)) + "ment" + String(str_addHeadTableValue.suffix(4)) + String(str_partyValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func params(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(str_assessmentName.suffix(5)) + str_remoteValue.replacingOccurrences(of: "greet", with: "m") + String(str_toTheValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func actionView(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(str_withBlockName) + String(str_managerData.suffix(7)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func thanAction(page: Int, completion: @escaping (_ data: [CrosswaysMeasurable]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(str_playerData.suffix(4)) + "oment/" + str_inviteName.lowercased() + String(str_screenValue))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: str_roomTitle.map{speechRead(user: $0)}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [CrosswaysMeasurable] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<CrosswaysMeasurable>.deserialize(from: arr as? Array) as? [CrosswaysMeasurable] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func viewFrom(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(str_failureNatureTopTitle.suffix(7)) + String(str_fromRestData) + String(str_tableColorName))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(String(str_labelTitle)): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func fallout(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ClearRequestModel()
        //: reqModel.requestServer = SubLabelReactiveCompatible.share.appConfigMode.reportDomain
        reqModel.requestServer = SubLabelReactiveCompatible.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(str_blockValue.prefix(6)) + "epor" + String(str_overName.suffix(9)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = SubLabelReactiveCompatible.share.loginUserMode.userID
        params[(String(str_equalPhotoHiddenName))] = SubLabelReactiveCompatible.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(str_foundationData) + String(str_userProData.suffix(6)))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(str_outsideName.suffix(8)) + String(str_addRoomData))] = (str_labelContentName.replacingOccurrences(of: "since", with: "on"))
        //: params["type"] = 1
        params[(String(str_labelTitle))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(str_removePostTitle.suffix(5)) + str_topTitle.replacingOccurrences(of: "style", with: "l"))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
