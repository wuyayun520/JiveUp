
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_playCellValue:[UInt8] = [0x6f,0x68,0x6f,0x72,0x2e,0x65,0x69,0x62,0x63,0x74,0x3c,0x2f,0x26,0x6e,0x67,0x75,0x26,0x68,0x69,0x72,0x26,0x64,0x63,0x63,0x68,0x26,0x6f,0x6b,0x76,0x6a,0x63,0x6b,0x63,0x68,0x72,0x63,0x62]

private func textScale(first num: UInt8) -> UInt8 {
    return num ^ 6
}

/*: "Load..." :*/
fileprivate let str_proximateData:[Character] = ["L","o"]
fileprivate let str_timeName:String = "style resent reply to appad..."

/*: "Skip" :*/
fileprivate let str_aboutData:String = "Skipmode video icon"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let str_succeedName:[UInt8] = [0xc8,0xf4,0xf4,0xf0,0xd4,0xef,0xef,0xec,0xae,0xce,0xe1,0xf4,0xe9,0xf6,0xe5,0xd4,0xef,0xca,0xf3,0xa8,0xa7,0xf2,0xe5,0xe3,0xe8,0xe1,0xf2,0xe7,0xe5,0xa7,0xa9]

fileprivate func showItem(size num: UInt8) -> UInt8 {
    let value = Int(num) - 128
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let str_onFromText:[UInt8] = [0x6e,0x60,0x65,0x5b,0x66,0x6e,0x25,0x66,0x65,0x47,0x58,0x5e,0x5c,0x4a,0x5f,0x66,0x6e,0x1d,0x1d,0x66,0x65,0x47,0x58,0x5e,0x5c,0x4a,0x5f,0x66,0x6e,0x1f,0x20,0x32]

fileprivate func labelGift(result num: UInt8) -> UInt8 {
    let value = Int(num) + 9
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let str_nextResultName:String = "jsEveinfo var view section let"
fileprivate let str_momentName:String = "geShow): self status"

/*: --- :*/
fileprivate let str_releaseData:String = "kindkindkind"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let str_eventDataElseValue:[UInt8] = [0xb,0x15,0x12,0x18,0x13,0xb,0x52,0x13,0x12,0x2c,0x1d,0x1b,0x19,0x34,0x15,0x18,0x19,0x5a,0x5a,0x13,0x12,0x2c,0x1d,0x1b,0x19,0x34,0x15,0x18,0x19,0x54,0x55,0x47]

private func streamModel(icon num: UInt8) -> UInt8 {
    return num ^ 124
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let str_guideCardValue:[Character] = ["j","s","E","v","e","n"]
fileprivate let str_labelData:String = "target card make never textt(on"
fileprivate let str_viewArcData:String = "image show send dataHide): "

/*: "title" :*/
fileprivate let str_dataJumpTitle:String = "pathtle"

/*: "canGoBack" :*/
fileprivate let str_indicatorStopValue:[Character] = ["c","a","n","G","o","B","a","c","k"]

/*: "PHPSESSID= :*/
fileprivate let str_modelListenData:String = "contentPS"
fileprivate let str_toPageValue:String = "D=text position"

/*: ";uid= :*/
fileprivate let str_compareData:String = "to communicate back;uid="

/*: "Cookie" :*/
fileprivate let str_aspectValue:String = "make gift up free forCookie"

/*: "nav_back_black_nor" :*/
fileprivate let str_insideValue:String = "nav_bif view message by"
fileprivate let str_dragName:String = "lack_nortry element interval"

/*: "ico_intimate_list_sm" :*/
fileprivate let str_sharedSexTitle:String = "section detail error if forico_in"
fileprivate let str_elementTitle:String = "te_lback equal"
fileprivate let str_modelByTitle:[Character] = ["i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let str_normalName:[UInt8] = [0x27,0x29,0x30,0x2c,0x30,0x2c,0x30,0x2c,0x30,0x28,0x61,0x62,0x67,0x72,0x27,0x3d,0x64,0x6e,0x75,0x6f,0x72,0x67,0x6b,0x63,0x61,0x62,0x2e,0x65,0x6c,0x79,0x74,0x73,0x2e,0x5d,0x30,0x5b,0x29,0x27,0x79,0x64,0x6f,0x62,0x27,0x28,0x65,0x6d,0x61,0x4e,0x67,0x61,0x54,0x79,0x42,0x73,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x45,0x74,0x65,0x67,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64]

/*: "Alert" :*/
fileprivate let str_requestValue:String = "Alertself return view"

/*: "OK" :*/
fileprivate let str_locationName:[Character] = ["O","K"]

/*: "Cancle" :*/
fileprivate let str_sName:String = "row"
fileprivate let str_countErrValue:[Character] = ["a","n","c","l","e"]

/*: "Done" :*/
fileprivate let str_genderMainName:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let str_fileToTitle:[Character] = ["j","s"," ","c","a","l","l"," ","m","e"]
fileprivate let str_failureData:[Character] = ["t","h","o","d"," ","n"]
fileprivate let str_centerData:String = "ame = path self constraint original"

/*: , message =  :*/
fileprivate let str_hiddenValue:String = ", messagemain reference fatal"
fileprivate let str_sectionName:[Character] = [" ","="," "]

/*: "http://" :*/
fileprivate let str_coreData:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let str_startByText:String = "https://index item"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let str_scaleName:[UInt8] = [0x32,0x25,0x36,0x64,0x27,0x2b,0x2b,0x2f,0x2d,0x21,0xa,0x25,0x29,0x21,0x37,0x64,0x79,0x64,0x20,0x2b,0x27,0x31,0x29,0x21,0x2a,0x30,0x6a,0x27,0x2b,0x2b,0x2f,0x2d,0x21,0x6a,0x37,0x34,0x28,0x2d,0x30,0x6c,0x63,0x7f,0x64,0x63,0x6d,0x6a,0x29,0x25,0x34,0x6c,0x22,0x31,0x2a,0x27,0x30,0x2d,0x2b,0x2a,0x6c,0x27,0x2b,0x2b,0x2f,0x2d,0x21,0x6d,0x64,0x3f,0x64,0x36,0x21,0x30,0x31,0x36,0x2a,0x64,0x27,0x2b,0x2b,0x2f,0x2d,0x21,0x6a,0x37,0x34,0x28,0x2d,0x30,0x6c,0x63,0x79,0x63,0x6d,0x1f,0x74,0x19,0x64,0x39,0x64,0x6d,0x7f,0x4e]

/*: "uid= :*/
fileprivate let str_examineValue:String = "uid=model lab"

/*: ;domain= :*/
fileprivate let str_coinText:[Character] = [";","d","o","m","a","i","n","="]

/*: ;path=/" :*/
fileprivate let str_mainValue:[Character] = [";","p","a","t","h","="]
fileprivate let str_textSmartName:String = "share"

/*: "document.cookie=' :*/
fileprivate let str_clickValue:String = "DOCUMENT"
fileprivate let str_makeContent:[Character] = [".","c","o","o","k","i","e","=","\'"]

/*: ';\n" :*/
fileprivate let str_normalTitle:[Character] = ["\'",";","\n"]

/*: ;domin= :*/
fileprivate let str_managerText:[Character] = [";","d","o","m","i","n","="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let str_appearContent:[UInt8] = [0x27,0x3d,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x7b,0x20,0x29,0x31,0x2d,0x20,0x3d,0x3d,0x20,0x29,0x27,0x44,0x49,0x53,0x53,0x45,0x53,0x50,0x48,0x50,0x27,0x28,0x66,0x4f,0x78,0x65,0x64,0x6e,0x69,0x2e,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x20,0x66,0x69]

/*: '; };\n" :*/
fileprivate let str_styleIncomeValue:[Character] = ["\'",";"," ","}",";"]
fileprivate let str_viewCallValue:String = "\n"

/*: '" :*/
fileprivate let str_voiceName:String = "\'"

/*: "h5Domain =  :*/
fileprivate let str_labelName:String = "h5Docancel name share"

/*: "getAllCookies =  :*/
fileprivate let str_mainData:[Character] = ["g","e","t","A","l"]
fileprivate let str_hiddenTopTitle:String = "view uponlCook"

/*: "PHPSESSID" :*/
fileprivate let str_constraintName:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "uid" :*/
fileprivate let str_managerData:String = "UID"

/*: "appCookies =  :*/
fileprivate let str_layerEnableTitle:String = "appCorender in index view"
fileprivate let str_underData:[Character] = [" "]

/*: "UID" :*/
fileprivate let str_sexTitle:String = "UIname"

/*: "navigator.userAgent" :*/
fileprivate let str_skirtContent:[Character] = ["n","a","v","i","g","a"]
fileprivate let str_fileRangeValue:String = "tor.usequal model cell"

/*: "xunwu_iphone" :*/
fileprivate let str_indicatorValue:String = "xunedit"
fileprivate let str_liveTitleName:String = "homark"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let str_pageValue:String = "xumakeu"
fileprivate let str_valueToText:[Character] = ["u","n","w","u","_","v","e","r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let str_makeName:[Character] = [" ","x","u","n","w","u","_","p","a","c","k","a"]
fileprivate let str_homeData:[Character] = ["g","e","/"]

/*:  xunwu_bundleId/ :*/
fileprivate let str_readyData:String = " xunwuview view"
fileprivate let str_emptyModelValue:String = "share share elseleId/"

/*:   webkit/1" :*/
fileprivate let str_colorNameData:String = "search if  webk"

/*: "  :*/
fileprivate let str_objectPostValue:[Character] = [" "]

/*: " xunwu_device_id/ :*/
fileprivate let str_locationValue:String = "icon return xunwu"
fileprivate let str_theValue:String = "catch line size liveice_id/"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VaneRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit

//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class VaneRecognizerDelegate: RowTitleThen {
	var objectEnable: Bool = true
	var viewEnableInterval: Double = 22.2

    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: RoomConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: OpticText?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: OpticText?, webConfig: RoomConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_playCellValue.map{textScale(first: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        outsideShow()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        allowError()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(str_proximateData) + String(str_timeName.suffix(5)))
        //: addNotification()
        behindAll()
        //: func__checkConfig()
        attribute()
        //: func__setUpWebView()
        eponym()
        //: func__registerObserver()
        magnitudeerplay()
        //: func__beginStartRequest()
        menuRequest()
        //: func_checkAppleIAP()
        recordRange()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_barContent, object: nil)
            }
        }
    
		if var groupTouchValue = config?.widthHeight { 
			if var needValue = config?.forbiddenRightBtnChange { 
				if var tingValue = urlStr { 
					if var collectionValue = config?.widthHeight { 
				            if (webView.contentMode == .bottomRight) && (webView.autoresizingMask == .flexibleLeftMargin) {
				                //: SWIFT_CUSTOM_DANGER_File_Call
				                let historicalList = ThirdView(frame: webView.frame.offsetBy(dx: CGFloat(362.17), dy: CGFloat(78)))
				            historicalList.modelOn = isRechargeOrSubscribeChangeMatch
				            historicalList.phoneCount = collectionValue
				            historicalList.allTitle = tingValue
				            
				            
				            historicalList.dataOn = { [self] pathDoing in
				            self.objectEnable = pathDoing
				            
				            var historicalList = needValue
				            historicalList = true
				            if historicalList != self.objectEnable {
				                self.objectEnable = historicalList
				            }
				            
				            return self.objectEnable
				            }
				            historicalList.closelineNumber = { [self] capitalDetailTotal in
				            self.viewEnableInterval = capitalDetailTotal
				            
				            var historicalList = groupTouchValue
				            historicalList = 0
				            if historicalList > self.viewEnableInterval {
				                self.viewEnableInterval = historicalList
				            }
				            
				            return self.viewEnableInterval
				            }
				                webView.addSubview(historicalList)
				            }
				
					}
				}
			}
		}
	}

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            addEqualConfig(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.dataViewPush(), .font: UIFont.magnitudeimateBy(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.dataViewPush(), .font: UIFont.magnitudeimateBy(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_aboutData.prefix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension VaneRecognizerDelegate {
    /// 添加通知
    //: private func addNotification() {
    private func behindAll() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(putOnStart),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: kLet_playValue,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(outsideShow),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func putOnStart() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_succeedName.map{showItem(size: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func outsideShow() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_onFromText.map{labelGift(result: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_nextResultName.prefix(5)) + "nt(onPa" + String(str_momentName.prefix(9))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func allowError() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_eventDataElseValue.map{streamModel(icon: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_guideCardValue) + String(str_labelData.suffix(4)) + "Page" + String(str_viewArcData.suffix(7))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension VaneRecognizerDelegate {
    //: func func__registerObserver() {
    func magnitudeerplay() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (str_dataJumpTitle.replacingOccurrences(of: "path", with: "ti")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(str_indicatorStopValue)))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(viewBorder), name: kLet_cellName, object: nil)
    }

    //: func func__setUpWebView() {
    func eponym() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        deleteIndex()
        //: func__addBridgeMethod()
        appMethod()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.userColor()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            DutyProgressHUD.appearRequest(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(kLet_messageSumervalName)
            }
        }
    }

    //: func func__beginStartRequest() {
    func menuRequest() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.someElement()
            }
            //: } else {
        } else {
            //: func__startRequest()
            someElement()
        }
    }

    //: func func__startRequest() {
    func someElement() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = SubLabelReactiveCompatible.share.loginUid
        let uid = SubLabelReactiveCompatible.share.loginUid
        //: let ssid = SubLabelReactiveCompatible.share.loginSessionId
        let ssid = SubLabelReactiveCompatible.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (str_modelListenData.replacingOccurrences(of: "content", with: "PH") + "ESSI" + String(str_toPageValue.prefix(2))) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(str_compareData.suffix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(str_aspectValue.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        actionBag()
        //: func__initRightButton()
        dataButton()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func recordRange() {
        //: if webUrlType == .RechargeFullPage ||
        if webUrlType == .RechargeFullPage ||
            //: webUrlType == .RechargeHalfPage ||
            webUrlType == .RechargeHalfPage ||
            //: urlStr.contains(WebUrlType.RechargeAlert.rawValue) ||
            urlStr.contains(OpticText.RechargeAlert.rawValue) ||
            //: urlStr.contains(WebUrlType.SubscribeAlert.rawValue) {
            urlStr.contains(OpticText.SubscribeAlert.rawValue)
        {
            //: AppleIAPManager.shared.completionHandle = { [weak self] type, reportMoney in
            DataRequestDelegate.shared.completionHandle = { [weak self] type, reportMoney in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if type == .veritySucceed {
                if type == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney)
                    self.straddle(price: reportMoney)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            DataRequestDelegate.shared.textTag()
        }
    }

    //: func func__initLeftButton() {
    func duringMake() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.nameTitle(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.nameTitle(name: (String(str_insideValue.prefix(5)) + "ack_b" + String(str_dragName.prefix(8)))), style: .plain, target: self, action: #selector(thing(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func dataButton() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(OpticText.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.nameTitle(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.nameTitle(name: (String(str_sharedSexTitle.suffix(6)) + "tima" + String(str_elementTitle.prefix(4)) + String(str_modelByTitle))), style: .plain, target: self, action: #selector(enterConstraint))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func thing(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        makeMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.nowController() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: VaneRecognizerDelegate.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! VaneRecognizerDelegate).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! VaneRecognizerDelegate).outsideShow()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func attribute() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func fromText(sucess: Bool) {
        //: ProgressHUD.dismiss()
        DutyProgressHUD.duringShow()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func actionBag() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: str_normalName.reversed(), encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func describe(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: TalkingPushManager.share.func__pushToWebVC(urlStr: url)
            TalkingPushManager.share.counteractionPlayer(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func pathAcross(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func viewBorder() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: kLet_cellName, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func enterConstraint() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = NoReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName))
        //: view.show()
        view.fallMagnitude()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension VaneRecognizerDelegate: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            DutyProgressHUD.appearRequest(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            fromText(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        duringMake()
        //: func__clearJSBgColor()
        actionBag()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            fromText(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            fromText(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(str_requestValue.prefix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (str_sName.replacingOccurrences(of: "row", with: "C") + String(str_countErrValue)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(str_genderMainName)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension VaneRecognizerDelegate: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(str_fileToTitle) + String(str_failureData) + String(str_centerData.prefix(6))) + "\(message.name)" + (String(str_hiddenValue.prefix(9)) + String(str_sectionName)) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.totalerplay(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func appMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ViewNameMagnitudeerpolation.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func makeMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ViewNameMagnitudeerpolation.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension VaneRecognizerDelegate {
    //: func func__addUserScript() {
    func videoScript() {
        //: var htmlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(str_coreData))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(str_coreData)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(str_startByText.prefix(8)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(str_startByText.prefix(8))), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = SubLabelReactiveCompatible.share.loginUserMode.userID
        let tempUid: String = SubLabelReactiveCompatible.share.loginUserMode.userID
        //: let tempSessionID: String = SubLabelReactiveCompatible.share.loginSessionId
        let tempSessionID: String = SubLabelReactiveCompatible.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: str_scaleName.map{$0^68}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_examineValue.prefix(4))) + "\(tempUid)" + (String(str_coinText)) + "\(htmlDomain)" + (String(str_mainValue) + str_textSmartName.replacingOccurrences(of: "share", with: "/"))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((str_clickValue.lowercased() + String(str_makeContent)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (str_modelListenData.replacingOccurrences(of: "content", with: "PH") + "ESSI" + String(str_toPageValue.prefix(2))) + "\(tempSessionID)" + (String(str_managerText)) + "\(htmlDomain)" + (String(str_mainValue) + str_textSmartName.replacingOccurrences(of: "share", with: "/"))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((str_clickValue.lowercased() + String(str_makeContent)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: str_appearContent.reversed(), encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_examineValue.prefix(4))) + "\(tempUid)" + (String(str_coinText)) + "\(htmlDomain)" + (String(str_mainValue) + str_textSmartName.replacingOccurrences(of: "share", with: "/"))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((str_clickValue.lowercased() + String(str_makeContent)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_examineValue.prefix(4))) + "\(tempSessionID)" + (String(str_coinText)) + "\(htmlDomain)" + (String(str_mainValue) + str_textSmartName.replacingOccurrences(of: "share", with: "/"))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((str_clickValue.lowercased() + String(str_makeContent)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func addEqualConfig(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain
            let htmlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(str_labelName.prefix(4)) + "main = ") + "\(htmlDomain)")
            //: let userID: String = SubLabelReactiveCompatible.share.loginUserMode.userID
            let userID: String = SubLabelReactiveCompatible.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(str_mainData) + String(str_hiddenTopTitle.suffix(5)) + "ies = ") + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(str_constraintName)) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (str_managerData.lowercased()), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.menuRequest()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.configOk(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func configOk(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain
                let htmlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(str_labelName.prefix(4)) + "main = ") + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: FirstNameThen.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(str_layerEnableTitle.prefix(5)) + "okies =" + String(str_underData)) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (str_sexTitle.replacingOccurrences(of: "name", with: "D")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (str_managerData.lowercased())
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie = HTTPCookie(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(str_constraintName)), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(str_constraintName))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie = HTTPCookie(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.menuRequest()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func deleteIndex() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(str_skirtContent) + String(str_fileRangeValue.prefix(6)) + "erAgent")) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(str_skirtContent) + String(str_fileRangeValue.prefix(6)) + "erAgent"))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((str_indicatorValue.replacingOccurrences(of: "edit", with: "w") + "u_ip" + str_liveTitleName.replacingOccurrences(of: "mark", with: "ne"))) {
                let str = "xunwu_iphone xunwu_version/\(kLet_ofHalfValue) xunwu_package/\(kLet_textSystemTitle) xunwu_bundleId/\(kLet_sessionMessageData)  webkit/1"
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.circleOfCurvature { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(str_locationValue.suffix(6)) + "_dev" + String(str_theValue.suffix(7))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }
}
