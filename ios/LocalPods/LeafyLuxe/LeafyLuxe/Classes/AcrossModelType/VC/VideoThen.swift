
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_makeBelowAcceptTitle:[UInt8] = [0x25,0x22,0x25,0x38,0x64,0x2f,0x23,0x28,0x29,0x3e,0x76,0x65,0x6c,0x24,0x2d,0x3f,0x6c,0x22,0x23,0x38,0x6c,0x2e,0x29,0x29,0x22,0x6c,0x25,0x21,0x3c,0x20,0x29,0x21,0x29,0x22,0x38,0x29,0x28]

private func equalQuote(report num: UInt8) -> UInt8 {
    return num ^ 76
}

/*: "Greeting Message Settings" :*/
fileprivate let str_errorText:String = "Greetititle title"
fileprivate let str_addAcceptText:String = "video match more countssage "

/*: "IsometricView" :*/
fileprivate let str_tempWhoTitle:String = "Talkinresponse extension make"
fileprivate let str_topConstraintName:String = "name image pathtingSet"
fileprivate let str_centerData:String = "color type numberTabl"
fileprivate let str_makeViewText:[Character] = ["e","C","e","l","l"]

/*: "Voice greeting" :*/
fileprivate let str_playerData:[Character] = ["V","o","i","c","e"," ","g","r","e","e","t","i"]
fileprivate let str_dismissValue:String = "directiong"

/*: "icon_me_greet_vioce" :*/
fileprivate let str_blockData:[Character] = ["i","c","o","n","_","m","e","_","g","r","e","e","t","_","v","i","o","c","e"]

/*: "Text greeting" :*/
fileprivate let str_writeCardText:String = "Texself equal"
fileprivate let str_managerContent:[Character] = ["t"," ","g","r","e","e","t","i","n","g"]

/*: "icon_me_greet_text" :*/
fileprivate let str_regularValue:String = "local view requesticon_m"
fileprivate let str_feedMakeValue:String = "et_tself raw manager send"
fileprivate let str_pushData:String = "exmap"

/*: "Photo greeting" :*/
fileprivate let str_partyValue:String = "add skipPhoto "
fileprivate let str_sinceTitle:[Character] = ["g"]

/*: "icon_me_greet_photo" :*/
fileprivate let str_cropEqualInText:String = "icon_mbackground equal title at status"
fileprivate let str_blockFromData:String = "let equal bareet_p"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VideoThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingAutoGreetSettingVC: TalkingBaseViewController {
class VideoThen: EqualVideoReactiveCompatible {
	var sortTotal: Int = 40
	var modifyDictionary: [AnyHashable: String] = [:]
	var miniCount: Int = 57
	var giftHitDictionary: [AnyHashable: String] = [:]

    //: var settingModel = TalkingSettingModel()
    var settingModel = FinishMeasurable()

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_makeBelowAcceptTitle.map{equalQuote(report: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: getSettingData()
        holdfast()
    
		if var moveValue = settingModel.isLeaderboardAnonymous { 
			if var modelViewValue = self.settingModel.isCupid { 
		            if (MainTable.contentScaleFactor == 2.85) && (MainTable.canBecomeFirstResponder) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let constraintLet = CurriculumView(frame: MainTable.frame.insetBy(dx: CGFloat(599.99), dy: CGFloat(366.58)))
		            constraintLet.awakeQuantity = modelViewValue
	
	
		            
		            
		            constraintLet.inheritTotal = { [self] observerEnableCount in
		            self.sortTotal = observerEnableCount
		            
		            var constraintLet = moveValue
		            constraintLet &<<= 1
		            if constraintLet < self.sortTotal {
		                self.sortTotal = constraintLet
		            }
		            
		            return self.sortTotal
		            }
		            constraintLet.alongDictionary = { [self] groupDictionary in
		            self.modifyDictionary = groupDictionary
		            
		            guard var value = self.modifyDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                MainTable.addSubview(constraintLet)
		            }
		
			}
		}
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var pathMediumValue = settingModel.isLeaderboardAnonymous { 
			if var priceJumpValue = self.settingModel.isCupid { 
		            if (MainTable.contentScaleFactor == 2.85) && (MainTable.canBecomeFirstResponder) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let constraintLet = CurriculumView(frame: MainTable.frame.insetBy(dx: CGFloat(599.99), dy: CGFloat(366.58)))
		            constraintLet.awakeQuantity = priceJumpValue
	
	
		            
		            
		            constraintLet.inheritTotal = { [self] observerEnableCount in
		            self.miniCount = observerEnableCount
		            
		            var constraintLet = pathMediumValue
		            constraintLet &<<= 1
		            if constraintLet < self.miniCount {
		                self.miniCount = constraintLet
		            }
		            
		            return self.miniCount
		            }
		            constraintLet.alongDictionary = { [self] groupDictionary in
		            self.giftHitDictionary = groupDictionary
		            
		            guard var value = self.giftHitDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                MainTable.addSubview(constraintLet)
		            }
		
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Greeting Message Settings".localized
        self.title = (String(str_errorText.prefix(6)) + "ng Me" + String(str_addAcceptText.suffix(6)) + "Settings").localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.userColor()
        //: setupSubviews()
        comment()
        //: setupSubViewsConstraint()
        constraintBackground()
    
		
		//: if_extract_code "untilView" begin
		
		var untilView = false
		if !untilView {
		    var isValue = false
		    if let nextResponder = next {
		        isValue = nextResponder.inputView != nil
		    }
		    untilView = isValue
		}
		
		//: if_extract_code "untilView" end
		
            if (self.popoverPresentationController != nil && self.popoverPresentationController!.arrowDirection == .right) && (untilView) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let tableImage = CurriculumController()
            tableImage.roomSwitch = self.hideNavi
            
                self.navigationController?.pushViewController(tableImage.self, animated: false)
            }

	}

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName - kLet_rateRequestPathTitle), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: table.register(TalkingGreetingSetTableCell.self, forCellReuseIdentifier: "TalkingGreetingSetTableCell")
        table.register(IsometricView.self, forCellReuseIdentifier: (String(str_tempWhoTitle.prefix(6)) + "gGree" + String(str_topConstraintName.suffix(7)) + String(str_centerData.suffix(4)) + String(str_makeViewText)))
        //: table.separatorStyle = .none
        table.separatorStyle = .none
        //: table.dataSource = self
        table.dataSource = self
        //: table.delegate = self
        table.delegate = self
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingAutoGreetSettingVC {
extension VideoThen {
    //: func getSettingData() {
    func holdfast() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetGreetSetting(params: dict) { succeed, result, errorModel in
        CrosswaysThen.dataSayFarewell(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<FinishMeasurable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingAutoGreetSettingVC: UITableViewDelegate, UITableViewDataSource {
extension VideoThen: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 3
        return 3
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 50)
        return actualHeight(h: 50)
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 15
        return 15
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetingSetTableCell"
        let identifier = (String(str_tempWhoTitle.prefix(6)) + "gGree" + String(str_topConstraintName.suffix(7)) + String(str_centerData.suffix(4)) + String(str_makeViewText))
        //: let cell: TalkingGreetingSetTableCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetingSetTableCell
        let cell: IsometricView = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! IsometricView

        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: cell.setCellMessage(titile: "Voice greeting".localized, iconStr: "icon_me_greet_vioce", isShowDet: self.settingModel.audioIsSet != 0)
            cell.rollOverDisplay(titile: (String(str_playerData) + str_dismissValue.replacingOccurrences(of: "direction", with: "n")).localized, iconStr: (String(str_blockData)), isShowDet: self.settingModel.audioIsSet != 0)

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: cell.setCellMessage(titile: "Text greeting".localized, iconStr: "icon_me_greet_text", isShowDet: self.settingModel.textIsSet != 0)
            cell.rollOverDisplay(titile: (String(str_writeCardText.prefix(3)) + String(str_managerContent)).localized, iconStr: (String(str_regularValue.suffix(6)) + "e_gre" + String(str_feedMakeValue.prefix(4)) + str_pushData.replacingOccurrences(of: "map", with: "t")), isShowDet: self.settingModel.textIsSet != 0)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: cell.setCellMessage(titile: "Photo greeting".localized, iconStr: "icon_me_greet_photo", isShowDet: self.settingModel.intimatePhotoIsSet != 0)
            cell.rollOverDisplay(titile: (String(str_partyValue.suffix(6)) + "greetin" + String(str_sinceTitle)).localized, iconStr: (String(str_cropEqualInText.prefix(6)) + "e_gr" + String(str_blockFromData.suffix(5)) + "hoto"), isShowDet: self.settingModel.intimatePhotoIsSet != 0)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let vc = TalkingGreetAudioVC.init()
            let vc = ShowRecognizerDelegate()
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let vc = TalkingGreetTextVC.init()
            let vc = TextVc()
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let vc = TalkingGreetPhotoVC.init()
            let vc = HeaderDataSource()
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
        }
    }

    //: func tableView(_ tableView: UITableView, viewForHeaderInSection section: Int) -> UIView? {
    func tableView(_: UITableView, viewForHeaderInSection _: Int) -> UIView? {
        //: let view = UIView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 15))
        let view = UIView(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: 15))
        //: view.backgroundColor = UIColor.clear
        view.backgroundColor = UIColor.clear
        //: return view
        return view
    }

    //: func tableView(_ tableView: UITableView, viewForFooterInSection section: Int) -> UIView? {
    func tableView(_: UITableView, viewForFooterInSection _: Int) -> UIView? {
        //: return UIView.init()
        return UIView()
    }
}

// MARK: - Layout

//: extension TalkingAutoGreetSettingVC {
extension VideoThen {
    //: private func setupSubviews() {
    private func comment() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
    }

    //: private func setupSubViewsConstraint() {
    private func constraintBackground() {
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}