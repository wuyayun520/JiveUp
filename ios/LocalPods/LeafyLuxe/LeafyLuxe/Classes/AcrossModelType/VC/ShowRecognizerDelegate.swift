
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_viewSharedCardData:[UInt8] = [0x45,0x42,0x45,0x58,0x4,0x4f,0x43,0x48,0x49,0x5e,0x16,0x5,0xc,0x44,0x4d,0x5f,0xc,0x42,0x43,0x58,0xc,0x4e,0x49,0x49,0x42,0xc,0x45,0x41,0x5c,0x40,0x49,0x41,0x49,0x42,0x58,0x49,0x48]

private func barAccept(free num: UInt8) -> UInt8 {
    return num ^ 44
}

/*: "Voice greeting" :*/
fileprivate let str_enableTitle:String = "height giftVoice"
fileprivate let str_whiteChangeTitle:[Character] = ["i","n","g"]

/*: "Add" :*/
fileprivate let str_colorDisableValue:String = "Addto item room equal"

/*: "type" :*/
fileprivate let str_unknownNumberData:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let str_actionData:[Character] = ["a","d","d","S","t"]
fileprivate let str_shadowGiftData:String = "atuthe"

/*: "list" :*/
fileprivate let str_incomeItemValue:[Character] = ["l","i","s","t"]

/*: "audio" :*/
fileprivate let str_whenText:String = "sizedio"

/*: "length" :*/
fileprivate let str_labelValue:String = "lenresulth"

/*: "url" :*/
fileprivate let str_topData:String = "closel"

/*: "Successfully added, please wait for approval" :*/
fileprivate let str_viewBringValue:[UInt8] = [0x3d,0x1b,0xd,0xd,0xb,0x1d,0x1d,0x8,0x1b,0x2,0x2,0x17,0x4e,0xf,0xa,0xa,0xb,0xa,0x42,0x4e,0x1e,0x2,0xb,0xf,0x1d,0xb,0x4e,0x19,0xf,0x7,0x1a,0x4e,0x8,0x1,0x1c,0x4e,0xf,0x1e,0x1e,0x1c,0x1,0x18,0xf,0x2]

private func finishCollection(mode num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "Are you sure to delete this message？" :*/
fileprivate let str_timeValue:[UInt8] = [0xa0,0x93,0x84,0xc1,0x98,0x8e,0x94,0xc1,0x92,0x94,0x93,0x84,0xc1,0x95,0x8e,0xc1,0x85,0x84,0x8d,0x84,0x95,0x84,0xc1,0x95,0x89,0x88,0x92,0xc1,0x8c,0x84,0x92,0x92,0x80,0x86,0x84,0xe,0x5d,0x7e]

/*: "Cancel" :*/
fileprivate let str_hideTargetName:String = "Cancelfalse self"

/*: "Delete" :*/
fileprivate let str_tableLoadValue:String = "Deleteinside from return extension input"

/*: "id" :*/
fileprivate let str_sexValue:String = "add"

/*: "Please wait for the audit result" :*/
fileprivate let str_observerData:[UInt8] = [0x2b,0x17,0x1e,0x1a,0x8,0x1e,0x5b,0xc,0x1a,0x12,0xf,0x5b,0x1d,0x14,0x9,0x5b,0xf,0x13,0x1e,0x5b,0x1a,0xe,0x1f,0x12,0xf,0x5b,0x9,0x1e,0x8,0xe,0x17,0xf]

private func opName(top num: UInt8) -> UInt8 {
    return num ^ 123
}

/*: "The upper limit has been reached" :*/
fileprivate let str_priceTitle:[UInt8] = [0x52,0x6e,0x63,0x26,0x73,0x76,0x76,0x63,0x74,0x26,0x6a,0x6f,0x6b,0x6f,0x72,0x26,0x6e,0x67,0x75,0x26,0x64,0x63,0x63,0x68,0x26,0x74,0x63,0x67,0x65,0x6e,0x63,0x62]

private func fillFemale(to num: UInt8) -> UInt8 {
    return num ^ 6
}

/*: "filePath" :*/
fileprivate let str_managerName:String = "fmodelle"

/*: "time" :*/
fileprivate let str_indexReloadData:String = "timcontent"

/*: "ImageAudioCell" :*/
fileprivate let str_backData:String = "selected info select textTalki"
fileprivate let str_userRequestValue:[Character] = ["n","g","G","r","e"]
fileprivate let str_choiceData:String = "etAudidata view"

/*: "#FF506D" :*/
fileprivate let str_afterEqualData:[Character] = ["#","F","F","5","0","6"]
fileprivate let str_playerValue:String = "color"

/*: "btn_message_delete_nor" :*/
fileprivate let str_picTitle:String = "var value leftbtn_me"
fileprivate let str_mainTitle:String = "bottom self alonge_de"

/*: "Currently mute" :*/
fileprivate let str_tapValue:[Character] = ["C","u","r","r","e","n","t","l"]
fileprivate let str_valueShowAllText:String = "for byy mute"

/*: "Edit Title" :*/
fileprivate let str_levelTitle:[Character] = ["E","d","i","t"," ","T","i","t","l","e"]

/*: "OK" :*/
fileprivate let str_managerTipData:String = "contentK"

/*: "remark" :*/
fileprivate let str_blockName:String = "repathar"
fileprivate let str_colorErrorName:[Character] = ["k"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let str_miniFirstData:[UInt8] = [0xf3,0x1c,0x19,0x13,0x1b,0xd0,0x24,0x18,0x15,0xd0,0x12,0x25,0x24,0x24,0x1f,0x1e,0xd0,0x12,0x15,0x1c,0x1f,0x27,0xd0,0x24,0x1f,0xd0,0x11,0x14,0x14,0xba,0x13,0x1f,0x1e,0x24,0x15,0x1e,0x24]

fileprivate func everySex(info num: UInt8) -> UInt8 {
    let value = Int(num) + 80
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let str_ofEffectTitle:[UInt8] = []

fileprivate func anyMake(section num: UInt8) -> UInt8 {
    let value = Int(num) + 226
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ShowRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class ShowRecognizerDelegate: EqualVideoReactiveCompatible {
	var toSum: Int = 60
	var listCount: Double = 56.6
	var labArray: [AnyHashable] = []

    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: AudioView?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: VendorPlayTool?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: ImageAudioCell?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_viewSharedCardData.map{barAccept(free: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
		if var trackRenderValue = recordView?.tempTime { 
	            if (addButton.clearsContextBeforeDrawing) && (addButton.forLastBaselineLayout.center.y == 34.09) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let quitFrom = AtTheSameTimeView(frame: addButton.frame.union(CGRect(x: CGFloat(37.84), y: CGFloat(0), width: CGFloat(0), height: CGFloat(622.00))))
	            quitFrom.nameQuantity = trackRenderValue

	            quitFrom.actionSum = { [self] awakeTotal in
	            self.toSum = awakeTotal
	            
	            var quitFrom = self.addStatus
	            quitFrom >>= 1
	            if quitFrom < self.toSum {
	                self.toSum = quitFrom
	            }
	            
	            return self.toSum
	            }
	            quitFrom.cancelComputerTotal = { [self] addManagerTotal in
	            self.listCount = addManagerTotal
	            
	            self.listCount = 0
	            return self.listCount
	            }
	            quitFrom.upArray = { [self] itemArray in
	            self.labArray = itemArray
	            
	            guard var value = self.labArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	                addButton.addSubview(quitFrom)
	            }
	
		}
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(str_enableTitle.suffix(5)) + " greet" + String(str_whiteChangeTitle)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.userColor()
        //: designView()
        voiceAcross()
        //: getAudioListData()
        party()
        //: setMutedDetection()
        agnomen()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(my(notification:)), name: kLet_liveSizeContent, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(find(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(recordPlay),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: kLet_liveSizeContent,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.tab()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [ServerManagerGreetModel] = {
        //: var array = [TalkingGreetModel]()
        var array = [ServerManagerGreetModel]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(str_colorDisableValue.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.gradientShared(colors: UIColor.endColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(counteraction), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension ShowRecognizerDelegate {
    //: func getAudioListData() {
    func party() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(str_unknownNumberData))] = 1
        //: ProgressHUD.show()
        DutyProgressHUD.dowerImage()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        CrosswaysThen.titleEqualHead(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            DutyProgressHUD.duringShow()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.disappear()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(str_actionData) + str_shadowGiftData.replacingOccurrences(of: "the", with: "s"))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(str_incomeItemValue))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [ServerManagerGreetModel] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<ServerManagerGreetModel>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func rawData(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(str_unknownNumberData))] = 1
        //: dict["audio"] = data
        dict[(str_whenText.replacingOccurrences(of: "size", with: "au"))] = data
        //: dict["length"] = Int(time)
        dict[(str_labelValue.replacingOccurrences(of: "result", with: "gt"))] = Int(time)

        //: ProgressHUD.show()
        DutyProgressHUD.dowerImage()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        CrosswaysThen.so(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            DutyProgressHUD.duringShow()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(str_topData.replacingOccurrences(of: "close", with: "ur"))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.playerMsg(showMsg: String(bytes: str_viewBringValue.map{finishCollection(mode: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.party()
                //: self.recordView?.dismiss()
                self.recordView?.cancel()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func mutual(index: Int) {
        //: let config = ShowAlertConfig()
        let config = ImageAlertConfig()
        //: config.textFont = UIFont.magnitudeimateBy(type: .Medium, fontSize: 18)
        config.textFont = UIFont.magnitudeimateBy(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        TalkingCostExhibitThen.directionUse(message: String(bytes: str_timeValue.map{$0^225}, encoding: .utf8)!.localized, leftBtnTitle: (String(str_hideTargetName.prefix(6))).localized, rightBtnTitle: (String(str_tableLoadValue.prefix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            TalkingCostExhibitThen.harbourToAlert()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            TalkingCostExhibitThen.harbourToAlert()
            //: self.stopPlay()
            self.recordPlay()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: ServerManagerGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            CrosswaysThen.shadow(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.party()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension ShowRecognizerDelegate {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func my(notification _: NSNotification) {
        //: stopPlay()
        recordPlay()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func find(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            recordPlay()
        }
    }

    //: @objc func addButtonClick() {
    @objc func counteraction() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard TitleSocketManager.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.barLog(showMsg: kLet_keyMessageName)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        AccountingDataReactiveCompatible.ofText(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.barLog(showMsg: kLet_changeTitle)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.barLog(showMsg: String(bytes: str_observerData.map{opName(top: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.barLog(showMsg: String(bytes: str_priceTitle.map{fillFemale(to: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.recordPlay()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = AudioView(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.first()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(str_managerName.replacingOccurrences(of: "model", with: "i") + "Path")] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.rawData(data: data!, time: dict[(str_indexReloadData.replacingOccurrences(of: "content", with: "e"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension ShowRecognizerDelegate {
    //: func setMutedDetection() {
    func agnomen() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        ArrayReactiveCompatible.shared.superiorQuery()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        ArrayReactiveCompatible.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension ShowRecognizerDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetAudioCell"
        let identifier = (String(str_backData.suffix(5)) + String(str_userRequestValue) + String(str_choiceData.prefix(6)) + "oCell")
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: ImageAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! ImageAudioCell

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.adjustSendLast(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.playing(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.playing(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            ArrayReactiveCompatible.shared.superiorQuery()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.listSize(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.recordPlay()
            //: self.editTitle(index: indexPath.section)
            self.button(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(str_tableLoadValue.prefix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.mutual(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(str_afterEqualData) + str_playerValue.replacingOccurrences(of: "color", with: "D")))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.mutual(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.nameTitle(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.nameTitle(name: (String(str_picTitle.suffix(6)) + "ssag" + String(str_mainTitle.suffix(4)) + "lete_nor")).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = AcrossImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func listSize(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isTalking else {
        guard !TitleSocketManager.shared.isTalking else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.barLog(showMsg: kLet_keyMessageName)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.tab()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? ImageAudioCell
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.playing(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.barLog(showMsg: (String(str_tapValue) + String(str_valueShowAllText.suffix(6))).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = VendorPlayTool.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.tab()
        //: self.player?.play(urlStr: model.content)
        self.player?.tipValue(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? ImageAudioCell
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.playing(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func button(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = TempThen(title: (String(str_levelTitle)).localized, leftBtnTitle: (String(str_hideTargetName.prefix(6))).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.subOfShow()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            DutyProgressHUD.dowerImage()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: ServerManagerGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(str_blockName.replacingOccurrences(of: "path", with: "m") + String(str_colorErrorName))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            CrosswaysThen.audioShow(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                DutyProgressHUD.duringShow()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func recordPlay() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.tab()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.playing(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension ShowRecognizerDelegate: LetterReactiveCompatible {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func info(status: SubMeasurable) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            recordPlay()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            recordPlay()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension ShowRecognizerDelegate {
    //: private func setupEmptyDataView() {
    private func disappear() {
        //: var style = EmptyStyle()
        var style = TotalEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: str_miniFirstData.map{everySex(info: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .userColor()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func voiceAcross() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: "TalkingGreetAudioCell")
        MainTable.register(ImageAudioCell.self, forCellReuseIdentifier: (String(str_backData.suffix(5)) + String(str_userRequestValue) + String(str_choiceData.prefix(6)) + "oCell"))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.videoValue()
        label.textColor = UIColor.videoValue()
        //: label.font = UIFont.rugularSize(fontSize: 15)
        label.font = UIFont.rugularSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: str_ofEffectTitle.map{anyMake(section: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
