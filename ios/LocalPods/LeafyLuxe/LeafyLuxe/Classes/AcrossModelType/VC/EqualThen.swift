
//: Declare String Begin

/*: "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$" :*/
fileprivate let str_followingTitle:[UInt8] = [0x34,0x42,0x31,0xb,0x47,0x10,0x2b,0x47,0x30,0x5a,0x47,0x53,0x35,0x36,0x44,0x47,0x37,0x41,0x43,0x2a,0x42,0x31,0x36,0xe,0xb,0x47,0x10,0x36,0x44,0x47,0x37,0x41,0x43,0x36,0x44,0x42,0x31,0xb,0x47,0x10,0x36,0x44,0x37,0x11,0x58,0x46,0x5c,0x17,0x43,0x4e]

private func importView(fatal num: UInt8) -> UInt8 {
    return num ^ 106
}

/*: "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$" :*/
fileprivate let str_clickData:[UInt8] = [0x81,0xf7,0xf7,0xee,0x84,0xec,0xeb,0xea,0xe8,0xe7,0x82,0x84,0xef,0xf2,0xe6,0x82,0xa4,0xe6,0xa2,0xf6,0xa3,0xf7,0xf7,0xef,0x83,0xbb,0xa4,0xed,0xa2,0xf2,0x83,0xbb,0xa4,0xe7,0xa2,0xf6,0xa3,0xf7,0xef,0x83,0xbb,0xa4,0xec,0xa2,0xf2,0x83,0xbb,0xa4,0xe8,0xf3,0xe7,0xa2,0xf6,0xa3,0xf7,0xef,0x83,0xbb,0xa4,0xee,0xef,0xf3,0xee,0xee,0xa2,0xf6,0xf6,0xfb]

private func callLet(make num: UInt8) -> UInt8 {
    return num ^ 223
}

/*: "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)" :*/
fileprivate let str_removeAddName:[UInt8] = [0x20,0x56,0x25,0x22,0xb,0x4a,0x3b,0x4e,0x4e,0x53,0x22,0xb,0x47,0x38,0x3f,0x4b,0x3f,0x53,0x24,0x1f,0x53,0x4,0x4e,0x53,0x47,0x21,0x23,0x5,0x4c,0x52,0x4f,0x4e,0x3,0x5a,0x57]

private func analogDigitalConverter(content num: UInt8) -> UInt8 {
    return num ^ 126
}

/*: "^[0-8]\\d{5}(?!\\d)$" :*/
fileprivate let str_hiddenData:String = "^[click-8"
fileprivate let str_timeTitle:String = "pull"
fileprivate let str_viewManagerTitle:[Character] = ["(","?","!","\\","d",")","$"]

/*: "^[a-zA-Z0-9]{6,20}+$" :*/
fileprivate let str_touchData:[Character] = ["^","[","a","-","z","A","-","Z","0","-","9","]","{","6",",","2"]
fileprivate let str_mediumValue:[Character] = ["0","}","+","$"]

/*: "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$" :*/
fileprivate let str_messageObjectTextData:[UInt8] = [0x24,0x3f,0x2f,0x5c,0x2a,0x29,0x2a,0x5d,0x2d,0x2e,0x5c,0x20,0x77,0x5c,0x2f,0x5c,0x5b,0x28,0x29,0x7d,0x36,0x2c,0x32,0x7b,0x5d,0x2e,0x5c,0x7a,0x2d,0x61,0x5b,0x28,0x2e,0x5c,0x29,0x2b,0x5d,0x2d,0x2e,0x5c,0x7a,0x2d,0x61,0x64,0x5c,0x5b,0x28,0x3f,0x29,0x2f,0x5c,0x2f,0x5c,0x3a,0x3f,0x73,0x70,0x74,0x74,0x68,0x28,0x5e]

/*: "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$" :*/
fileprivate let str_viewTableName:[UInt8] = [0x24,0x52,0x45,0x40,0x52,0x45,0x40,0x48,0x4f,0x21,0x4a,0x57,0x4f,0x27,0x6,0x48,0x21,0x4a,0x57,0x4e,0x27,0x21,0x4a,0x57,0x43,0x27,0x6,0x21,0x4a,0x4b,0x27,0x45,0x21,0x4a,0x57,0x43,0x27,0x21,0x4a,0x57,0x43,0x27,0x45,0x53,0x26,0x54,0x53,0x5a,0x1,0x49,0x7,0x52,0x45,0x40,0x48,0x4f,0x21,0x4a,0x57,0x4f,0x27,0x6,0x48,0x21,0x4a,0x57,0x4e,0x27,0x21,0x4a,0x57,0x43,0x27,0x6,0x21,0x4a,0x4b,0x27,0x45,0x21,0x4a,0x57,0x43,0x27,0x21,0x4a,0x57,0x43,0x27,0x45,0x53,0x5e]

private func actionButton(equal num: UInt8) -> UInt8 {
    return num ^ 122
}

/*: "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)" :*/
fileprivate let str_emptyCellData:[UInt8] = [0x66,0x10,0x15,0x7e,0x63,0x77,0x13,0x35,0x7f,0x7b,0x33,0x6a,0x67,0x32,0x66,0x15,0x7e,0x63,0x77,0x13,0x35,0x7f,0x79,0x33,0x66,0x15,0x7e,0x63,0x77,0x13,0x32,0x15,0x36,0x16,0x13,0x67,0x6a,0x67]

/*: "SELF MATCHES %@" :*/
fileprivate let str_spaceColorTitle:[Character] = ["S","E","L","F"," ","M"]
fileprivate let str_circleObjectName:[Character] = ["A","T","C","H","E","S"," ","%","@"]

/*: "Feedback" :*/
fileprivate let str_quoteValue:[Character] = ["F","e"]
fileprivate let str_rowName:String = "edbobjectck"

/*: "Enter your feedback…" :*/
fileprivate let str_bagText:String = "Enter length index for"
fileprivate let str_byEqualModelData:String = "to"
fileprivate let str_contentViewName:String = "remote to elseur fe"

/*: "#999999" :*/
fileprivate let str_detailValue:String = "#9"
fileprivate let str_pageValue:String = "timetimetimetimetime"

/*: "0/ :*/
fileprivate let str_talkRestoreValue:String = "free/"

/*: "Your email (Required) " :*/
fileprivate let str_iconModelValue:String = "else for elseYour e"
fileprivate let str_sizeContentName:String = "quired) if open type return select"

/*: "icon_me_feelback_star" :*/
fileprivate let str_modelMakeValue:[Character] = ["i","c","o","n","_","m"]
fileprivate let str_directionText:[Character] = ["e","_"]
fileprivate let str_pathLengthName:String = "fnextl"

/*: "#CCCCCC" :*/
fileprivate let str_startResultText:[Character] = ["#","C","C","C","C","C","C"]

/*: "Send" :*/
fileprivate let str_loadData:[Character] = ["S","e","n","d"]

/*: "#D0D0D0" :*/
fileprivate let str_mapName:String = "flexible end hidden raw#D0D0D0"

/*: "Please fill in the content" :*/
fileprivate let str_labelData:[Character] = ["P","l","e","a","s","e"," ","f","i","l","l"]
fileprivate let str_comeData:String = " in text app super return var"
fileprivate let str_layerName:String = "couserteusert"

/*: "Please enter the correct email address" :*/
fileprivate let str_shareData:[UInt8] = [0x9b,0xb7,0xb0,0xac,0xbe,0xb0,0x6b,0xb0,0xb9,0xbf,0xb0,0xbd,0x6b,0xbf,0xb3,0xb0,0x6b,0xae,0xba,0xbd,0xbd,0xb0,0xae,0xbf,0x6b,0xb0,0xb8,0xac,0xb4,0xb7,0x6b,0xac,0xaf,0xaf,0xbd,0xb0,0xbe,0xbe]

fileprivate func managerFirst(cell num: UInt8) -> UInt8 {
    let value = Int(num) - 75
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let str_modelKeyPriorityData:[Character] = ["c","o","n","t","e","n","t"]

/*: "contactWay" :*/
fileprivate let str_canImageText:[Character] = ["c","o","n","t","a","c","t"]
fileprivate let str_viewClickSuccessData:[Character] = ["W","a","y"]

/*: "platform" :*/
fileprivate let str_backName:String = "PLAT"

/*: "iphone" :*/
fileprivate let str_byNameText:[Character] = ["i","p","h","o","n","e"]

/*: "version" :*/
fileprivate let str_labelModelData:[Character] = ["v","e","r","s","i","o","n"]

/*: "type" :*/
fileprivate let str_privacyName:String = "tcollectionpe"

/*: "Operation succeeded" :*/
fileprivate let str_personGiftName:String = "Operatisize to all search shape"
fileprivate let str_onlineListTitle:String = "on sucview self"

/*: / :*/
fileprivate let str_managerValue:[Character] = ["/"]

/*: "\n" :*/
fileprivate let str_countViewContent:String = "\n"

/*: "Problem statements" :*/
fileprivate let str_targetPlayerName:String = "Problecolor view gender data"
fileprivate let str_regularCommentGiftText:[Character] = ["e"]
fileprivate let str_userRewardForName:[Character] = ["m","e","n","t","s"]

/*: "Feature advice" :*/
fileprivate let str_buttonCellToData:String = "Featurpath for position"
fileprivate let str_statusBarData:[Character] = ["e"," ","a","d","v","i","c","e"]

/*: "Operation questions" :*/
fileprivate let str_viewTableTitle:[Character] = ["O","p","e","r","a","t","i","o"]
fileprivate let str_whiteContent:String = "n quresult height background"

/*: "Others" :*/
fileprivate let str_succeedValue:[Character] = ["O","t","h","e","r"]
fileprivate let str_whiteName:[Character] = ["s"]

/*: "#EFEDFF" :*/
fileprivate let str_blockName:String = "#EFEDmake title string view let"
fileprivate let str_routeContent:String = "fullfull"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EqualThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/15.
//

//: import UIKit
import UIKit

//: enum Validate {
enum ConversationValidate {
    //: case email(_: String)
    case email(_: String) //  邮箱
    //: case phoneNumber(_: String)
    case phoneNumber(_: String) //  手机号
    //: case userName(_: String)
    case userName(_: String) //  用户名
    //: case password(_: String)
    case password(_: String) //  密码
    //: case nickName(_: String)
    case nickName(_: String) //  昵称
    //: case postalCode(_: String)
    case postalCode(_: String) //  邮编
    //: case URL(_: String)
    case URL(_: String) //  URL
    //: case IP(_: String)
    case IP(_: String) //  IP
    //: case isChinese(_: String)
    case isChinese(_: String) //  是否全是中文字符
    //: case idNumber(_: String)
    case idNumber(_: String) //  身份证号

    //: var isRight: Bool {
    var isRight: Bool {
        //: var predicateStr: String!
        var predicateStr: String!
        //: var currentObject: String!
        var currentObject: String!
        //: switch self {
        switch self {
        //: case let .email(str):
        case let .email(str):
            //: predicateStr = "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$"
            predicateStr = String(bytes: str_followingTitle.map{importView(fatal: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .phoneNumber(str):
        case let .phoneNumber(str):
            //: predicateStr = "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$"
            predicateStr = String(bytes: str_clickData.map{callLet(make: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .userName(str):
        case let .userName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: str_removeAddName.map{analogDigitalConverter(content: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .postalCode(str):
        case let .postalCode(str):
            //: predicateStr = "^[0-8]\\d{5}(?!\\d)$"
            predicateStr = (str_hiddenData.replacingOccurrences(of: "click", with: "0") + "]\\d{5" + str_timeTitle.replacingOccurrences(of: "pull", with: "}") + String(str_viewManagerTitle))
            //: currentObject = str
            currentObject = str
        //: case let .password(str):
        case let .password(str):
            //: predicateStr = "^[a-zA-Z0-9]{6,20}+$"
            predicateStr = (String(str_touchData) + String(str_mediumValue))
            //: currentObject = str
            currentObject = str
        //: case let .nickName(str):
        case let .nickName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: str_removeAddName.map{analogDigitalConverter(content: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .URL(str):
        case let .URL(str):
            //: predicateStr = "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$"
            predicateStr = String(bytes: str_messageObjectTextData.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .IP(str):
        case let .IP(str):
            //: predicateStr = "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
            predicateStr = String(bytes: str_viewTableName.map{actionButton(equal: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let . isChinese(str):
        case let .isChinese(str):
            //: predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            //: currentObject = str
            currentObject = str
        //: case let . idNumber(str):
        case let .idNumber(str):
            //: predicateStr = "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)"
            predicateStr = String(bytes: str_emptyCellData.map{$0^78}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        }
        //: let predicate =  NSPredicate(format: "SELF MATCHES %@", predicateStr)
        let predicate = NSPredicate(format: (String(str_spaceColorTitle) + String(str_circleObjectName)), predicateStr)
        //: return predicate.evaluate(with: currentObject)
        return predicate.evaluate(with: currentObject)
    }
}

//: class TalkingFeedbackVC: TalkingBaseViewController {
class EqualThen: EqualVideoReactiveCompatible {
	var viewArray: [AnyHashable] = []

    //: let limitCount = 300
    let limitCount = 300
    //: let EmialCount = 50
    let EmialCount = 50
    //: var seleteBtn: UIButton?
    var seleteBtn: UIButton?

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()

        //: self.title = "Feedback".localized
        self.title = (String(str_quoteValue) + str_rowName.replacingOccurrences(of: "object", with: "a")).localized
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: designView()
        countWrite()
        //: addTapGestureRecognizer()
        recordContent()
        //: addKeyboardNotification()
        keyboard()
    
            if (doneBtn.layer.contentsRect.size.width != 1) && (doneBtn.layoutMargins.bottom == 8.25) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let viewLet = CollectionCancelView(frame: doneBtn.bounds.intersection(CGRect(x: CGFloat(103.38), y: CGFloat(92), width: CGFloat(505.86), height: CGFloat(0))))
            
            viewLet.nameTheoryArray = { [self] plusArray in
            self.viewArray = plusArray
            
            guard var value = self.viewArray as? [String] else {
                return nil
            }
            return value
            }
                doneBtn.addSubview(viewLet)
            }

	}

    // MARK: - Lazy load

    //: lazy var backView: UIScrollView = {
    lazy var backView: UIScrollView = {
        //: let view = UIScrollView.init()
        let view = UIScrollView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.showsHorizontalScrollIndicator = false
        view.showsHorizontalScrollIndicator = false
        //: view.showsVerticalScrollIndicator = false
        view.showsVerticalScrollIndicator = false
        //: return view
        return view
        //: }()
    }()

    //: lazy var contView: UIView = {
    lazy var contView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var inputTView: UIView = {
    lazy var inputTView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: view.backgroundColor = .appBgColor()
        view.backgroundColor = .userColor()
        //: return view
        return view
        //: }()
    }()

    //: lazy var textView: TalkingTextView = {
    lazy var textView: TextScrollView = {
        //: let textView = TalkingTextView.init()
        let textView = TextScrollView()
        //: textView.placeholder = "Enter your feedback…".localized
        textView.placeholder = (String(str_bagText.prefix(6)) + str_byEqualModelData.replacingOccurrences(of: "to", with: "yo") + String(str_contentViewName.suffix(5)) + "edback…").localized
        //: textView.placeholderColor = UIColor.init(hex: "#999999")!
        textView.placeholderColor = UIColor(hex: (str_detailValue.capitalized + str_pageValue.replacingOccurrences(of: "time", with: "9")))!
        //: textView.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 15)
        textView.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 15)
        //: textView.textColor = UIColor.elementColor()
        textView.textColor = UIColor.elementColor()
        //: textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)
        textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)

        //: textView.textContainer.maximumNumberOfLines = 0
        textView.textContainer.maximumNumberOfLines = 0
        //: textView.textContainer.lineBreakMode = .byTruncatingTail
        textView.textContainer.lineBreakMode = .byTruncatingTail
        //: textView.delegate = self
        textView.delegate = self
        //: textView.returnKeyType = UIReturnKeyType.done
        textView.returnKeyType = UIReturnKeyType.done
        //: textView.bounces = false
        textView.bounces = false
        //: textView.backgroundColor = .appBgColor()
        textView.backgroundColor = .userColor()
        //: numberLB.text =  "0/\(limitCount)"
        numberLB.text = "0/\(limitCount)"
        //: return textView
        return textView
        //: }()
    }()

    //: lazy var numberLB: UILabel = {
    lazy var numberLB: UILabel = {
        //: let LB = UILabel.init()
        let LB = UILabel()
        //: LB.textColor = UIColor.init(hex: "#999999")
        LB.textColor = UIColor(hex: (str_detailValue.capitalized + str_pageValue.replacingOccurrences(of: "time", with: "9")))
        //: LB.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 14)
        LB.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 14)
        //: LB.text = "0/\(limitCount)"
        LB.text = "0/\(limitCount)"
        //: LB.textAlignment = .right
        LB.textAlignment = .right
        //: return LB
        return LB
        //: }()
    }()

    //: lazy var emailTF: UITextField = {
    lazy var emailTF: UITextField = {
        //: let tf = UITextField.init()
        let tf = UITextField()
        //: tf.backgroundColor = .appBgColor()
        tf.backgroundColor = .userColor()
        //: tf.textColor = UIColor.elementColor()
        tf.textColor = UIColor.elementColor()
        //: tf.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 16)
        tf.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 16)
        //: tf.delegate = self
        tf.delegate = self
        //: tf.returnKeyType = UIReturnKeyType.done
        tf.returnKeyType = UIReturnKeyType.done
        //: tf.attributedPlaceholder = NSAttributedString.init(string: "Your email (Required) ".localized, attributes: [
        tf.attributedPlaceholder = NSAttributedString(string: (String(str_iconModelValue.suffix(6)) + "mail (Re" + String(str_sizeContentName.prefix(8))).localized, attributes: [
            //: NSAttributedString.Key.foregroundColor: UIColor.init(hex: "#999999")!])
            NSAttributedString.Key.foregroundColor: UIColor(hex: (str_detailValue.capitalized + str_pageValue.replacingOccurrences(of: "time", with: "9")))!])
        //: tf.clipsToBounds = true
        tf.clipsToBounds = true
        //: tf.layer.cornerRadius = 8
        tf.layer.cornerRadius = 8
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PropertyLineThen.shared.direction == .rightToLeft {
            //: tf.textAlignment = .right
            tf.textAlignment = .right
        }
        //: let leftView = UIView()
        let leftView = UIView()
        //: leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))
        leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))

        //: let leftImgView = UIImageView.init()
        let leftImgView = UIImageView()
        //: leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        //: leftImgView.image = UIImage.nameTitle(name: "icon_me_feelback_star")
        leftImgView.image = UIImage.nameTitle(name: (String(str_modelMakeValue) + String(str_directionText) + str_pathLengthName.replacingOccurrences(of: "next", with: "ee") + "back_star"))
        //: leftView.addSubview(leftImgView)
        leftView.addSubview(leftImgView)
        //: tf.leftView = leftView
        tf.leftView = leftView
        //: tf.leftViewMode = .always
        tf.leftViewMode = .always
        //: return tf
        return tf
        //: }()
    }()

    //: lazy var doneBtn: UIButton = {
    lazy var doneBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#CCCCCC")!, forState: .disabled)
        btn.sectionDrop(color: UIColor(hex: (String(str_startResultText)))!, forState: .disabled)
        //: btn.setBackgroundColor(color: UIColor.dataViewPush(), forState: .normal)
        btn.sectionDrop(color: UIColor.dataViewPush(), forState: .normal)
        //: btn.setTitle("Send".localized, for: .normal)
        btn.setTitle((String(str_loadData)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 17)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(equalBlock), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingFeedbackVC {
extension EqualThen {
    //: @objc func TagBtnClick(sender: UIButton) {
    @objc func situate(sender: UIButton) {
        //: seleteBtn?.isSelected = false
        seleteBtn?.isSelected = false
        //: seleteBtn?.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
        seleteBtn?.layer.borderColor = UIColor(hex: (String(str_mapName.suffix(7))))?.cgColor
        //: sender.isSelected = !sender.isSelected
        sender.isSelected = !sender.isSelected
        //: seleteBtn = sender
        seleteBtn = sender
        //: seleteBtn!.layer.borderColor = UIColor.clear.cgColor
        seleteBtn!.layer.borderColor = UIColor.clear.cgColor
    }

    //: @objc func doneBtnClick() {
    @objc func equalBlock() {
        //: if seleteBtn == nil || textView.text.count<=0 || emailTF.text!.count<=0 {
        if seleteBtn == nil || textView.text.count <= 0 || emailTF.text!.count <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please fill in the content".localized)
            self.barLog(showMsg: (String(str_labelData) + String(str_comeData.prefix(4)) + "the " + str_layerName.replacingOccurrences(of: "user", with: "n")).localized)
            //: return
            return
        }

        //: if LanguageManager.shared.direction == .leftToRight {
        if PropertyLineThen.shared.direction == .leftToRight {
            //: if !Validate.email(emailTF.text!).isRight {
            if !ConversationValidate.email(emailTF.text!).isRight {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please enter the correct email address".localized)
                self.barLog(showMsg: String(bytes: str_shareData.map{managerFirst(cell: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["content"] = textView.text
        dict[(String(str_modelKeyPriorityData))] = textView.text
        //: dict["contactWay"] = emailTF.text
        dict[(String(str_canImageText) + String(str_viewClickSuccessData))] = emailTF.text
        //: dict["platform"] = "iphone"
        dict[(str_backName.lowercased() + "form")] = (String(str_byNameText))
        //: dict["version"] = AppVersion
        dict[(String(str_labelModelData))] = kLet_intervalValue
        //: dict["type"] = String((seleteBtn?.tag ?? 100)-100)
        dict[(str_privacyName.replacingOccurrences(of: "collection", with: "y"))] = String((seleteBtn?.tag ?? 100) - 100)

        //: ProgressHUD.show()
        DutyProgressHUD.dowerImage()

        //: TalkingMeRequestTool.req_Feedback(params: dict) { succeed, result, errorModel in
        CrosswaysThen.errorOf(params: dict) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            DutyProgressHUD.duringShow()
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Operation succeeded".localized)
                self.playerMsg(showMsg: (String(str_personGiftName.prefix(7)) + String(str_onlineListTitle.prefix(6)) + "ceeded").localized)
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)
            }
        }
    }

    //: func addKeyboardNotification() {
    func keyboard() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillShow(notification:)),
                                               selector: #selector(colorNotification(notification:)),
                                               //: name: UIResponder.keyboardWillShowNotification,
                                               name: UIResponder.keyboardWillShowNotification,
                                               //: object: nil)
                                               object: nil)
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillHide(notification:)),
                                               selector: #selector(beforeStyleNotification(notification:)),
                                               //: name: UIResponder.keyboardWillHideNotification,
                                               name: UIResponder.keyboardWillHideNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func keyboardWillShow(notification: Notification) {
    @objc func colorNotification(notification: Notification) {
        //: guard emailTF.isFirstResponder else { return }
        guard emailTF.isFirstResponder else { return }
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        //: let frame = value?.cgRectValue ?? .zero
        let frame = value?.cgRectValue ?? .zero
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        //: guard (emailTF.bottom + StatusBarNavigationBarHeight) > frame.minY else { return }
        guard (emailTF.bottom + kLet_soundData) > frame.minY else { return }

        //: let y = (emailTF.bottom + StatusBarNavigationBarHeight) - frame.minY
        let y = (emailTF.bottom + kLet_soundData) - frame.minY
        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
            self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
        }
    }

    //: @objc func keyboardWillHide(notification: Notification) {
    @objc func beforeStyleNotification(notification: Notification) {
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25

        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = .identity
            self.backView.transform = .identity
        }
    }
}

// MARK: - UITextViewDelegate, UITextFieldDelegate

//: extension TalkingFeedbackVC: UITextViewDelegate, UITextFieldDelegate {
extension EqualThen: UITextViewDelegate, UITextFieldDelegate {
    //: public func textViewDidChange(_ textView: UITextView) {
    public func textViewDidChange(_ textView: UITextView) {
        //: if textView.text.count > limitCount {
        if textView.text.count > limitCount {
            // 获得已输出字数与正输入字母数
            //: let selectRange = textView.markedTextRange
            let selectRange = textView.markedTextRange

            // 获取高亮部分 － 如果有联想词则解包成功
            //: if let selectRange = selectRange {
            if let selectRange = selectRange {
                //: let position =  textView.position(from: (selectRange.start), offset: 0)
                let position = textView.position(from: selectRange.start, offset: 0)
                //: if (position != nil) {
                if position != nil {
                    //: return
                    return
                }
            }

            //: let textContent = textView.text ?? ""
            let textContent = textView.text ?? ""
            //: let textNum = textContent.count
            let textNum = textContent.count

            // 截取
            //: if textNum > limitCount && limitCount > 0 {
            if textNum > limitCount && limitCount > 0 {
                //: textView.text = string_prefix(index: limitCount, text: textContent)
                textView.text = shared(index: limitCount, text: textContent)
            }
        }
        //: numberLB.text =  "\(textView.text.count)/\(limitCount)"
        numberLB.text = "\(textView.text.count)/\(limitCount)"
    }

    //: public func textViewShouldBeginEditing(_ textView: UITextView) -> Bool {
    public func textViewShouldBeginEditing(_: UITextView) -> Bool {
        //: numberLB.isHidden = false
        numberLB.isHidden = false
        //: return true
        return true
    }

    //: public func textViewDidEndEditing(_ textView: UITextView) {
    public func textViewDidEndEditing(_ textView: UITextView) {
        //: if textView.text.isEmpty {
        if textView.text.isEmpty {
            //: numberLB.isHidden = false
            numberLB.isHidden = false
            //: } else {
        } else {
            //: numberLB.isHidden = true
            numberLB.isHidden = true
        }
    }

    //: public func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
    public func textView(_ textView: UITextView, shouldChangeTextIn _: NSRange, replacementText text: String) -> Bool {
        //: if textView.returnKeyType == .done {
        if textView.returnKeyType == .done {
            //: if text == "\n" {
            if text == "\n" {
                //: textView.resignFirstResponder()
                textView.resignFirstResponder()
            }
            //: return true
            return true
        }
        //: return true
        return true
    }

    // 字符串的截取 从头截取到指定index
    //: private func string_prefix(index: Int,text:String) -> String {
    private func shared(index: Int, text: String) -> String {
        //: if text.count <= index {
        if text.count <= index {
            //: return text
            return text
            //: } else {
        } else {
            //: let index = text.index(text.startIndex, offsetBy: index)
            let index = text.index(text.startIndex, offsetBy: index)
            //: let str = text.prefix(upTo: index)
            let str = text.prefix(upTo: index)
            //: return String(str)
            return String(str)
        }
    }

    //: func textFieldShouldReturn(_ textField: UITextField) -> Bool {
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
        //: textField.resignFirstResponder()
        textField.resignFirstResponder()

        //: return true
        return true
    }

    //: func textField(_ textField: UITextField, shouldChangeCharactersIn range: NSRange, replacementString string: String) -> Bool {
    func textField(_ textField: UITextField, shouldChangeCharactersIn _: NSRange, replacementString string: String) -> Bool {
        //: if textField.text!.count+string.count>EmialCount || textField.text!.count+string.count <= 0 {
        if textField.text!.count + string.count > EmialCount || textField.text!.count + string.count <= 0 {
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - UI

//: extension TalkingFeedbackVC {
extension EqualThen {
    //: func designView() {
    func countWrite() {
        //: self.view.addSubview(backView)
        self.view.addSubview(backView)
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: backView.addSubview(contView)
        backView.addSubview(contView)
        //: contView.snp.makeConstraints { make in
        contView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(backView)
            make.leading.trailing.top.bottom.equalTo(backView)
        }

        //: contView.addSubview(inputTView)
        contView.addSubview(inputTView)
        //: inputTView.snp.makeConstraints { make in
        inputTView.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(contView).offset(128)
            make.top.equalTo(contView).offset(128)
            //: make.height.equalTo(210)
            make.height.equalTo(210)
        }

        //: inputTView.addSubview(textView)
        inputTView.addSubview(textView)
        //: inputTView.addSubview(numberLB)
        inputTView.addSubview(numberLB)
        //: textView.snp.makeConstraints { make in
        textView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(inputTView)
            make.leading.trailing.top.equalTo(inputTView)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
        }
        //: numberLB.snp.makeConstraints { make in
        numberLB.snp.makeConstraints { make in
            //: make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: contView.addSubview(emailTF)
        contView.addSubview(emailTF)
        //: emailTF.snp.makeConstraints { make in
        emailTF.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(inputTView.snp.bottom).offset(16)
            make.top.equalTo(inputTView.snp.bottom).offset(16)
            //: make.height.equalTo(49)
            make.height.equalTo(49)
        }

        //: contView.addSubview(doneBtn)
        contView.addSubview(doneBtn)
        //: doneBtn.snp.makeConstraints { make in
        doneBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(54)
            make.leading.equalTo(contView).offset(54)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-54)
            make.trailing.equalTo(contView.snp.trailing).offset(-54)
            //: make.top.equalTo(emailTF.snp.bottom).offset(111)
            make.top.equalTo(emailTF.snp.bottom).offset(111)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }

        //: let arr = ["Problem statements".localized,
        let arr = [(String(str_targetPlayerName.prefix(6)) + "m stat" + String(str_regularCommentGiftText) + String(str_userRewardForName)).localized,
                   //: "Feature advice".localized,
                   (String(str_buttonCellToData.prefix(6)) + String(str_statusBarData)).localized,
                   //: "Operation questions".localized,
                   (String(str_viewTableTitle) + String(str_whiteContent.prefix(4)) + "estions").localized,
                   //: "Others".localized]
                   (String(str_succeedValue) + String(str_whiteName)).localized]

        //: for i in 0..<arr.count {
        for i in 0 ..< arr.count {
            //: let xz = 15
            let xz = 15
            //: let yz = 16
            let yz = 16
            //: let Width = (Int(ScreenWidth)-xz*3)/2
            let Width = (Int(kLet_failureName) - xz * 3) / 2
            //: let height = 38
            let height = 38
            //: let X = xz+(xz + Width)*(i%2)
            let X = xz + (xz + Width) * (i % 2)
            //: let Y = yz+(yz+height)*(i/2)
            let Y = yz + (yz + height) * (i / 2)

            //: let btn = UIButton.init()
            let btn = UIButton()
            //: btn.frame =  CGRect.init(x: X, y: Y , width: Width, height: height)
            btn.frame = CGRect(x: X, y: Y, width: Width, height: height)
            //: btn.setTitle(arr[i], for: .normal)
            btn.setTitle(arr[i], for: .normal)
            //: btn.setTitleColor(.black, for: .normal)
            btn.setTitleColor(.black, for: .normal)
            //: btn.setTitleColor(.appThemeColor(), for: .selected)
            btn.setTitleColor(.dataViewPush(), for: .selected)

            //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 15)
            btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 15)
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            if FirstNameThen.share.interfaceLang == ProfessionalComparable.es.rawValue || FirstNameThen.share.interfaceLang == ProfessionalComparable.pt.rawValue {
                //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 13)
                btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 13)
            }
            //: btn.layer.cornerRadius = 19
            btn.layer.cornerRadius = 19
            //: btn.layer.borderWidth = 1
            btn.layer.borderWidth = 1
            //: btn.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
            btn.layer.borderColor = UIColor(hex: (String(str_mapName.suffix(7))))?.cgColor
            //: btn.setBackgroundColor(color: UIColor.init(hex: "#EFEDFF")!, forState: .selected)
            btn.sectionDrop(color: UIColor(hex: (String(str_blockName.prefix(5)) + str_routeContent.replacingOccurrences(of: "full", with: "F")))!, forState: .selected)
            //: btn.setBackgroundColor(color: .white, forState: .normal)
            btn.sectionDrop(color: .white, forState: .normal)
            //: btn.tag = 100+i
            btn.tag = 100 + i
            //: btn.addTarget( self, action: #selector(TagBtnClick(sender:)), for: .touchUpInside)
            btn.addTarget(self, action: #selector(situate(sender:)), for: .touchUpInside)
            //: contView.addSubview(btn)
            contView.addSubview(btn)
        }

        //: contView.snp.remakeConstraints { make in
        contView.snp.remakeConstraints { make in
            //: make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            //: make.width.equalTo(backView)
            make.width.equalTo(backView)
        }
    }
}
