
//: Declare String Begin

/*: "nil" :*/
fileprivate let str_errorInputShareValue:String = "nirandom"

/*: "Edit profiles" :*/
fileprivate let str_tabData:[Character] = ["E","d","i","t"," ","p","r","o","f"]
fileprivate let str_leadingName:String = "blockles"

/*: "Done" :*/
fileprivate let str_labSpecialContent:String = "user model you tableDone"

/*: "#999999" :*/
fileprivate let str_fatalName:String = "#999999"

/*: "uid" :*/
fileprivate let str_playerAboutMakeTitle:String = "uviewd"

/*: "CellNameFieldDelegate" :*/
fileprivate let str_cellResultValue:String = "Talkview call view"
fileprivate let str_replyToImageValue:String = "corner mode delay rawitNameCe"
fileprivate let str_interactionValue:String = "datadata"

/*: "BorderReactiveCompatible" :*/
fileprivate let str_roundText:String = "if image sendTalkin"
fileprivate let str_moveValue:String = "SignCelstart model key"
fileprivate let str_receiveData:String = "to"

/*: "Save the change?" :*/
fileprivate let str_appearanceData:[Character] = ["S","a","v","e"," ","t","h","e"," ","c","h"]
fileprivate let str_hiddenTitle:String = "tap lab model title equalange?"

/*: "Cancel" :*/
fileprivate let str_imageContentFromData:[Character] = ["C","a","n","c","e","l"]

/*: "Save" :*/
fileprivate let str_kitReadValue:[Character] = ["S","a","v","e"]

/*: "nickname" :*/
fileprivate let str_scaleName:[Character] = ["n","i","c","k","n"]
fileprivate let str_sexData:[Character] = ["a","m","e"]

/*: "birthday" :*/
fileprivate let str_firstTitle:[Character] = ["b","i","r","t","h"]
fileprivate let str_screenText:[Character] = ["d","a","y"]

/*: "desc" :*/
fileprivate let str_pathNeedName:[Character] = ["d","e","s","c"]

/*: "Modify the success" :*/
fileprivate let str_serviceTitle:String = "value random player make varModify"
fileprivate let str_opDetailValue:String = " the slet log"

/*: "pic" :*/
fileprivate let str_actualObjectName:[Character] = ["p","i","c"]

/*: "[ :*/
fileprivate let str_mainData:String = "["

/*: ]" :*/
fileprivate let str_streamContent:[Character] = ["]"]

/*: "pids" :*/
fileprivate let str_clickValue:String = "pcolords"

/*: "aboutMe" :*/
fileprivate let str_timeName:String = "areader"
fileprivate let str_assetValue:String = "frame in selfoutMe"

/*: "interest" :*/
fileprivate let str_appearName:[Character] = ["i","n","t","e","r","e","s"]
fileprivate let str_managerValue:[Character] = ["t"]

/*: "tagIds" :*/
fileprivate let str_lineCanVarData:String = "bottom self bonustagId"
fileprivate let str_makeLabelData:String = "hide"

/*: "category" :*/
fileprivate let str_emptyToData:[Character] = ["c","a","t","e","g"]
fileprivate let str_picTitle:String = "orarray"

/*: "An error occurred, please try again" :*/
fileprivate let str_tabSizeData:[UInt8] = [0x38,0x17,0x59,0x1c,0xb,0xb,0x16,0xb,0x59,0x16,0x1a,0x1a,0xc,0xb,0xb,0x1c,0x1d,0x55,0x59,0x9,0x15,0x1c,0x18,0xa,0x1c,0x59,0xd,0xb,0x0,0x59,0x18,0x1e,0x18,0x10,0x17]

/*: "imgUrl" :*/
fileprivate let str_screenValue:String = "name"
fileprivate let str_rowName:[Character] = ["m","g","U","r","l"]

/*: "videoUrl" :*/
fileprivate let str_cellSizeLayerValue:String = "leadingideo"
fileprivate let str_allowValue:String = "text lab varUrl"

/*: "Video cover submitted successfully" :*/
fileprivate let str_listPersonText:[UInt8] = [0x79,0x6c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x73,0x20,0x64,0x65,0x74,0x74,0x69,0x6d,0x62,0x75,0x73,0x20,0x72,0x65,0x76,0x6f,0x63,0x20,0x6f,0x65,0x64,0x69,0x56]

/*: "TalkingViewCell" :*/
fileprivate let str_equalSendName:[Character] = ["T","a","l","k","i","n"]
fileprivate let str_listFeeFrameValue:String = "make data resultgEditVi"
fileprivate let str_kitData:[Character] = ["d","e","o","C","e","l","l"]

/*: "video_url" :*/
fileprivate let str_labelValue:[Character] = ["v","i","d","e","o","_","u","r"]
fileprivate let str_makeColorText:String = "result"

/*: "img_url" :*/
fileprivate let str_playerContent:String = "giftm"
fileprivate let str_timeValue:String = "double tap element elementg_url"

/*: "status" :*/
fileprivate let str_actionName:String = "statapps"

/*: "ImageHeadPhotoCell" :*/
fileprivate let str_mainName:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","P","h","o"]
fileprivate let str_itemText:[Character] = ["t","o","C","e","l","l"]

/*: "NameObjectProtocol" :*/
fileprivate let str_labelName:[Character] = ["T","a","l","k","i","n","g","E","d"]
fileprivate let str_ofModelValue:[Character] = ["i","t","B","i"]
fileprivate let str_cardTitle:String = "rbyhda"

/*: "CensusThen" :*/
fileprivate let str_valueHitName:String = "Talkivar top output title"
fileprivate let str_voiceName:String = "let style asitAb"
fileprivate let str_boardTitle:String = "Cellif var source go quote"

/*: "UITableViewCell" :*/
fileprivate let str_layerTitle:[Character] = ["U","I","T","a","b","l","e","V","i","e","w"]
fileprivate let str_beginPlayerText:String = "Cellvoice view else self"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ContentVisualReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class ContentVisualReactiveCompatible: EqualVideoReactiveCompatible {
	var imageBorderQuantity: Int = 64
	var modeQuantity: Double = -43.7
	var rowErrorLabQuantity: Int = 10
	var playCount: Double = 64.7

    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [CostTagModel] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [CostTagModel] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [DiningRoomAttendantMeasurable] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (str_errorInputShareValue.replacingOccurrences(of: "random", with: "l"))

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.userColor()
        //: self.title = "Edit profiles".localized
        self.title = (String(str_tabData) + str_leadingName.replacingOccurrences(of: "block", with: "i")).localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(str_labSpecialContent.suffix(4))).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (str_fatalName.capitalized)), for: .normal)
        //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(tableTo), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = SubLabelReactiveCompatible.share.loginUserMode.aboutMe!
        seleteAboutme = SubLabelReactiveCompatible.share.loginUserMode.aboutMe!
        //: seleteInters = SubLabelReactiveCompatible.share.loginUserMode.interest!
        seleteInters = SubLabelReactiveCompatible.share.loginUserMode.interest!
        //: designView()
        net()
        //: GetGallery()
        remind()
        //: getVideoInfo()
        maxImage()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(equidistantApp(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
		
		//: if_extract_code "atBack" begin
		
		var atBack = false
		if !atBack {
		    var isValue = false
		    if let nextResponder = MainTable.next {
		        isValue = nextResponder.inputView != nil
		    }
		    atBack = isValue
		}
		
		//: if_extract_code "atBack" end
		
            if (atBack) && (MainTable.layer.position.y == 32.91) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let blockHitShow = LiveUpView()
            blockHitShow.permissionClose = isdeleteInters
            blockHitShow.giftTitle = nameStr
            
            blockHitShow.distanceCount = { [self] itemNumber in
            self.imageBorderQuantity = itemNumber
            
            var blockHitShow = videoStatus
            blockHitShow &*= 6
            if blockHitShow < self.imageBorderQuantity {
                self.imageBorderQuantity = blockHitShow
            }
            
            return self.imageBorderQuantity
            }
            blockHitShow.stillPathNumber = { [self] dataInterval in
            self.modeQuantity = dataInterval
            
            return self.modeQuantity
            }
                MainTable.addSubview(blockHitShow)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            if (MainTable.isFirstResponder) && (MainTable.userActivity != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let contextAwake = LiveUpView(frame: MainTable.frame)
            contextAwake.permissionClose = isTips
            contextAwake.giftTitle = birthday
            
            contextAwake.distanceCount = { [self] itemNumber in
            self.rowErrorLabQuantity = itemNumber
            
            var contextAwake = videoStatus
            contextAwake &= 5
            if contextAwake > self.rowErrorLabQuantity {
                self.rowErrorLabQuantity = contextAwake
            }
            
            return self.rowErrorLabQuantity
            }
            contextAwake.stillPathNumber = { [self] dataInterval in
            self.playCount = dataInterval
            
            return self.playCount
            }
                MainTable.addSubview(contextAwake)
            }

	}

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        DutyProgressHUD.duringShow()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension ContentVisualReactiveCompatible {
    //: func GetGallery() {
    func remind() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = SubLabelReactiveCompatible.share.loginUserMode.userID
        dict[(str_playerAboutMakeTitle.replacingOccurrences(of: "view", with: "i"))] = SubLabelReactiveCompatible.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        CrosswaysThen.currentDismiss(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<DiningRoomAttendantMeasurable>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [DiningRoomAttendantMeasurable])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func fileWith() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_cellResultValue.prefix(4)) + "ingEd" + String(str_replyToImageValue.suffix(8)) + str_interactionValue.replacingOccurrences(of: "data", with: "l")) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: CellNameFieldDelegate = i as! CellNameFieldDelegate
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_roundText.suffix(6)) + "gEdit" + String(str_moveValue.prefix(7)) + str_receiveData.replacingOccurrences(of: "to", with: "l")) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: BorderReactiveCompatible = i as! BorderReactiveCompatible
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&SubLabelReactiveCompatible.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&SubLabelReactiveCompatible.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && SubLabelReactiveCompatible.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && SubLabelReactiveCompatible.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && SubLabelReactiveCompatible.share.loginUserMode.birthday != self.birthday) || (signStr != (str_errorInputShareValue.replacingOccurrences(of: "random", with: "l")) && SubLabelReactiveCompatible.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = ImageAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            TalkingCostExhibitThen.directionUse(message: (String(str_appearanceData) + String(str_hiddenTitle.suffix(5))).localized, leftBtnTitle: (String(str_imageContentFromData)).localized, rightBtnTitle: (String(str_kitReadValue)).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                TalkingCostExhibitThen.harbourToAlert()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                TalkingCostExhibitThen.harbourToAlert()
                //: self.uploadTool(isBack: true)
                self.receive(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func tableTo() {
        //: uploadTool(isBack: false)
        receive(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func receive(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_cellResultValue.prefix(4)) + "ingEd" + String(str_replyToImageValue.suffix(8)) + str_interactionValue.replacingOccurrences(of: "data", with: "l")) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: CellNameFieldDelegate = i as! CellNameFieldDelegate
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_roundText.suffix(6)) + "gEdit" + String(str_moveValue.prefix(7)) + str_receiveData.replacingOccurrences(of: "to", with: "l")) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: BorderReactiveCompatible = i as! BorderReactiveCompatible
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && SubLabelReactiveCompatible.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, SubLabelReactiveCompatible.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(String(str_scaleName) + String(str_sexData))] = nameStr
        }
        //: if birthday.count > 0 && SubLabelReactiveCompatible.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, SubLabelReactiveCompatible.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(String(str_firstTitle) + String(str_screenText))] = birthday
        }
        //: if signStr != "nil" && SubLabelReactiveCompatible.share.loginUserMode.signature != signStr {
        if signStr != (str_errorInputShareValue.replacingOccurrences(of: "random", with: "l")), SubLabelReactiveCompatible.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(String(str_pathNeedName))] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        whenDrunk(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.female.rawValue {
            //: uploadVideo()
            logVideo()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        DutyProgressHUD.dowerImage()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        CrosswaysThen.programmeInstallCompletion(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            DutyProgressHUD.duringShow()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.playerMsg(showMsg: (String(str_serviceTitle.suffix(6)) + String(str_opDetailValue.prefix(6)) + "uccess").localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: SubLabelReactiveCompatible.share.loginUserMode.nickname = self.nameStr
                    SubLabelReactiveCompatible.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: SubLabelReactiveCompatible.share.loginUserMode.birthday = self.birthday
                    SubLabelReactiveCompatible.share.loginUserMode.birthday = self.birthday
                }
                //: SubLabelReactiveCompatible.share.loginUserMode.signature = self.signStr
                SubLabelReactiveCompatible.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.barLog(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func whenDrunk(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            failure()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                telephoneMessage()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            photo(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            photo(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func telephoneMessage() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            let resultData: NSData = seletePhotoArray[i].length()! as NSData
            //: dict["pic"] = resultData
            dict[(String(str_actualObjectName))] = resultData
            //: ProgressHUD.show()
            DutyProgressHUD.dowerImage()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            CrosswaysThen.sizeCompletion(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                DutyProgressHUD.duringShow()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.playerMsg(showMsg: (String(str_serviceTitle.suffix(6)) + String(str_opDetailValue.prefix(6)) + "uccess").localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.remind()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func failure() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(str_clickValue.replacingOccurrences(of: "color", with: "i"))] = str
        //: ProgressHUD.show()
        DutyProgressHUD.dowerImage()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        CrosswaysThen.account(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.telephoneMessage()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                DutyProgressHUD.duringShow()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.playerMsg(showMsg: (String(str_serviceTitle.suffix(6)) + String(str_opDetailValue.prefix(6)) + "uccess").localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.remind()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func photo(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [CostTagModel] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (str_timeName.replacingOccurrences(of: "reader", with: "b") + String(str_assetValue.suffix(5)))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (String(str_appearName) + String(str_managerValue))
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(String(str_lineCanVarData.suffix(5)) + str_makeLabelData.replacingOccurrences(of: "hide", with: "s"))] = str
        //: dict["category"] = category
        dict[(String(str_emptyToData) + str_picTitle.replacingOccurrences(of: "array", with: "y"))] = category
        //: ProgressHUD.show()
        DutyProgressHUD.dowerImage()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        CrosswaysThen.ticket(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            DutyProgressHUD.duringShow()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.playerMsg(showMsg: (String(str_serviceTitle.suffix(6)) + String(str_opDetailValue.prefix(6)) + "uccess").localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: SubLabelReactiveCompatible.share.loginUserMode.aboutMe = self.seleteAboutme
                    SubLabelReactiveCompatible.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: SubLabelReactiveCompatible.share.loginUserMode.interest = self.seleteInters
                    SubLabelReactiveCompatible.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func logVideo() {
        //: ProgressHUD.show()
        DutyProgressHUD.dowerImage()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        TempSignatureProvider.shared.pic(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                DutyProgressHUD.duringShow()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.barLog(showMsg: String(bytes: str_tabSizeData.map{$0^121}, encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(str_screenValue.replacingOccurrences(of: "name", with: "i") + String(str_rowName))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(str_cellSizeLayerValue.replacingOccurrences(of: "leading", with: "v") + String(str_allowValue.suffix(3)))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            CrosswaysThen.safetyMargin(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                DutyProgressHUD.duringShow()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.barLog(showMsg: String(bytes: str_tabSizeData.map{$0^121}, encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.playerMsg(showMsg: String(bytes: str_listPersonText.reversed(), encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(str_equalSendName) + String(str_listFeeFrameValue.suffix(7)) + String(str_kitData)) {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: TalkingViewCell = i as! TalkingViewCell
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.launch(videoPlayPath: dict[(str_cellSizeLayerValue.replacingOccurrences(of: "leading", with: "v") + String(str_allowValue.suffix(3)))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.way(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func maxImage() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        CrosswaysThen.deal { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(String(str_labelValue) + str_makeColorText.replacingOccurrences(of: "result", with: "l"))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(str_playerContent.replacingOccurrences(of: "gift", with: "i") + String(str_timeValue.suffix(5)))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(str_actionName.replacingOccurrences(of: "app", with: "u"))] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension ContentVisualReactiveCompatible {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func equidistantApp(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_cellResultValue.prefix(4)) + "ingEd" + String(str_replyToImageValue.suffix(8)) + str_interactionValue.replacingOccurrences(of: "data", with: "l")) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: CellNameFieldDelegate = i as! CellNameFieldDelegate
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_roundText.suffix(6)) + "gEdit" + String(str_moveValue.prefix(7)) + str_receiveData.replacingOccurrences(of: "to", with: "l")) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: BorderReactiveCompatible = i as! BorderReactiveCompatible
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension ContentVisualReactiveCompatible: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((kLet_failureName - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.tillPerform(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.tillPerform(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(str_equalSendName) + String(str_listFeeFrameValue.suffix(7)) + String(str_kitData))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = TalkingViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.way(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.digitiser(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.launch(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(str_mainName) + String(str_itemText))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? ImageHeadPhotoCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = ImageHeadPhotoCell(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.selete(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.giftPhoto()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(str_cellResultValue.prefix(4)) + "ingEd" + String(str_replyToImageValue.suffix(8)) + str_interactionValue.replacingOccurrences(of: "data", with: "l"))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? CellNameFieldDelegate
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = CellNameFieldDelegate(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(str_labelName) + String(str_ofModelValue) + str_cardTitle.replacingOccurrences(of: "by", with: "t") + "yCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? NameObjectProtocol
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = NameObjectProtocol(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(str_roundText.suffix(6)) + "gEdit" + String(str_moveValue.prefix(7)) + str_receiveData.replacingOccurrences(of: "to", with: "l"))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? BorderReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = BorderReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(str_valueHitName.prefix(5)) + "ngEd" + String(str_voiceName.suffix(4)) + "outMe" + String(str_boardTitle.prefix(4)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? CensusThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = CensusThen(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.bringHome()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.radiogramCell(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.radiogramCell(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = ModelViewDelegate()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.equalsCompareDataConverter(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.equalsCompareDataConverter(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.equalsCompareDataConverter(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.equalsCompareDataConverter(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(str_layerTitle) + String(str_beginPlayerText.prefix(4)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func tillPerform(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [CostTagModel]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if FirstNameThen.share.interfaceLang == ProfessionalComparable.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if FirstNameThen.share.interfaceLang == ProfessionalComparable.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if FirstNameThen.share.interfaceLang == ProfessionalComparable.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.magnitudeimateBy(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > kLet_failureName - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - DigitiserThen

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension ContentVisualReactiveCompatible: DigitiserThen {
    //: func freshSeleteTag() {
    func seleteRequest() {
        //: seleteAboutme =  SubLabelReactiveCompatible.share.loginUserMode.aboutMe!
        seleteAboutme = SubLabelReactiveCompatible.share.loginUserMode.aboutMe!
        //: seleteInters =  SubLabelReactiveCompatible.share.loginUserMode.interest!
        seleteInters = SubLabelReactiveCompatible.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - VisualManagerReactiveCompatible

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension ContentVisualReactiveCompatible: VisualManagerReactiveCompatible {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func trademark(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = DiningRoomAttendantMeasurable()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func everyTo(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func loadAdd(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension ContentVisualReactiveCompatible {
    //: private func designView() {
    private func net() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(TalkingViewCell.self, forCellReuseIdentifier: (String(str_equalSendName) + String(str_listFeeFrameValue.suffix(7)) + String(str_kitData)))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(ImageHeadPhotoCell.self, forCellReuseIdentifier: (String(str_mainName) + String(str_itemText)))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(CellNameFieldDelegate.self, forCellReuseIdentifier: (String(str_cellResultValue.prefix(4)) + "ingEd" + String(str_replyToImageValue.suffix(8)) + str_interactionValue.replacingOccurrences(of: "data", with: "l")))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(NameObjectProtocol.self, forCellReuseIdentifier: (String(str_labelName) + String(str_ofModelValue) + str_cardTitle.replacingOccurrences(of: "by", with: "t") + "yCell"))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(BorderReactiveCompatible.self, forCellReuseIdentifier: (String(str_roundText.suffix(6)) + "gEdit" + String(str_moveValue.prefix(7)) + str_receiveData.replacingOccurrences(of: "to", with: "l")))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(CensusThen.self, forCellReuseIdentifier: (String(str_valueHitName.prefix(5)) + "ngEd" + String(str_voiceName.suffix(4)) + "outMe" + String(str_boardTitle.prefix(4))))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
