
//: Declare String Begin

/*: "Email Address" :*/
fileprivate let str_jumpFeeValue:[Character] = ["E","m","a","i","l"," ","A","d","d"]
fileprivate let str_userValueData:[Character] = ["r","e","s","s"]

/*: "Phone number" :*/
fileprivate let str_thirdContent:String = "star log title mPhone "

/*: "Enter the email code sent to" :*/
fileprivate let str_serviceValue:String = "Entenormal color"
fileprivate let str_positionName:String = "emcountil"
fileprivate let str_methodEditorValue:String = "if self label supersent t"
fileprivate let str_groupValue:[Character] = ["o"]

/*: "Enter the phone code sent to" :*/
fileprivate let str_targetData:String = "picture true make letEnter"
fileprivate let str_textViewValue:String = "phostar"
fileprivate let str_directionData:[Character] = ["e"," ","c","o","d","e"," ","s","e","n","t"," ","t","o"]

/*: "(+ :*/
fileprivate let str_regularTitle:String = "(of"

/*: ) :*/
fileprivate let str_tapText:[Character] = [")"]

/*: "8075F5" :*/
fileprivate let str_engineName:[Character] = ["8","0","7","5","F","5"]

/*: "F4F4F4" :*/
fileprivate let str_detailEditTitle:String = "FpopFpopFpop"

/*: "Resend verification email" :*/
fileprivate let str_appText:String = "Resendguard text date equal"
fileprivate let str_cookieNorUpText:String = "icamodel"
fileprivate let str_sexTitle:String = "in fill propertyion "

/*: "Tips: If you can't receive the verification code, please check whether the message is in spam or not" :*/
fileprivate let str_aByTrueData:[UInt8] = [0xe4,0xd9,0xc0,0xc3,0x8a,0x90,0xf9,0xd6,0x90,0xc9,0xdf,0xc5,0x90,0xd3,0xd1,0xde,0x97,0xc4,0x90,0xc2,0xd5,0xd3,0xd5,0xd9,0xc6,0xd5,0x90,0xc4,0xd8,0xd5,0x90,0xc6,0xd5,0xc2,0xd9,0xd6,0xd9,0xd3,0xd1,0xc4,0xd9,0xdf,0xde,0x90,0xd3,0xdf,0xd4,0xd5,0x9c,0x90,0xc0,0xdc,0xd5,0xd1,0xc3,0xd5,0x90,0xd3,0xd8,0xd5,0xd3,0xdb,0x90,0xc7,0xd8,0xd5,0xc4,0xd8,0xd5,0xc2,0x90,0xc4,0xd8,0xd5,0x90,0xdd,0xd5,0xc3,0xc3,0xd1,0xd7,0xd5,0x90,0xd9,0xc3,0x90,0xd9,0xde,0x90,0xc3,0xc0,0xd1,0xdd,0x90,0xdf,0xc2,0x90,0xde,0xdf,0xc4]

private func titleCos(recording num: UInt8) -> UInt8 {
    return num ^ 176
}

/*: "Bind Email succeed" :*/
fileprivate let str_makeTargetTitle:[Character] = ["B","i","n","d"," ","E","m"]
fileprivate let str_keyQuoteContent:[Character] = ["a","i","l"," ","s","u","c","c","e","e","d"]

/*: "Bind Mobile Phone succeed" :*/
fileprivate let str_hiddenName:String = "source user self true selfBind M"
fileprivate let str_underName:String = "after info actionPhone"
fileprivate let str_mediumValue:String = "statused"

/*: "Resend verification email (%@s)" :*/
fileprivate let str_logAllTitle:[UInt8] = [0x29,0x73,0x40,0x25,0x28,0x20,0x6c,0x69,0x61,0x6d,0x65,0x20,0x6e,0x6f,0x69,0x74,0x61,0x63,0x69,0x66,0x69,0x72,0x65,0x76,0x20,0x64,0x6e,0x65,0x73,0x65,0x52]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AnalogDigitalConverterVc.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/1.
//

//: import UIKit
import UIKit

//: class TalkingLoginBindInputCodeVC: TalkingBaseViewController {
class AnalogDigitalConverterVc: EqualVideoReactiveCompatible {
	var messageOn: Bool = true
	var gameMagnitude: Double = 58.7
	var backlogClose: Bool = false
	var byTotal: Double = -6.6

    //: public var  phoneOrEmailStr: String = ""  // 邮箱或者Email
    public var phoneOrEmailStr: String = "" // 邮箱或者Email
    //: public var  areaCodeNum: String = ""
    public var areaCodeNum: String = ""
    //: public var  codeStr: String = ""
    public var codeStr: String = ""
    //: public var  viewType: TalkingBindVCType?
    public var viewType: DataBindType?
    //: var isBack: Bool!
    var isBack: Bool!
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.setupSubviews()
        self.statusFrom()
        //: self.setupSubViewsConstraint()
        self.setupConstraint()
        //: self.bindInteraction()
        self.range()
        //: func_starCodeTime()
        frontBurner()
    
		if var smartValue = isBack { 
			if var showDataValue = isBack { 
		            if (commitButton.superview != nil && commitButton.superview!.isHidden) && (commitButton.frame.size.height == 296.87) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let longshotLet = ThirdView()
		            longshotLet.modelOn = showDataValue
	
		            longshotLet.allTitle = self.areaCodeNum
		            
		            
		            longshotLet.dataOn = { [self] pathDoing in
		            self.backlogClose = pathDoing
		            
		            var longshotLet = smartValue
		                longshotLet = true
		                longshotLet = false
		            if longshotLet != self.backlogClose {
		                self.backlogClose = longshotLet
		            }
		            
		            return self.backlogClose
		            }
		            longshotLet.closelineNumber = { [self] capitalDetailTotal in
		            self.byTotal = capitalDetailTotal
		            
		            return self.byTotal
		            }
		                commitButton.addSubview(longshotLet)
		            }
		
			}
		}
	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        //: func_endCodeTime()
        addView()
    
		if var blockCollectionValue = self.isBack { 
			if var addValue = self.isBack { 
		            if (desLab.contentMode == .bottomRight) && (desLab.autoresizingMask == .flexibleLeftMargin) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let historicalList = ThirdView(frame: desLab.frame.offsetBy(dx: CGFloat(362.17), dy: CGFloat(78)))
		            historicalList.modelOn = addValue
	
		            historicalList.allTitle = areaCodeNum
		            
		            
		            historicalList.dataOn = { [self] pathDoing in
		            self.messageOn = pathDoing
		            
		            var historicalList = blockCollectionValue
		            historicalList = true
		            if historicalList != self.messageOn {
		                self.messageOn = historicalList
		            }
		            
		            return self.messageOn
		            }
		            historicalList.closelineNumber = { [self] capitalDetailTotal in
		            self.gameMagnitude = capitalDetailTotal
		            
		            return self.gameMagnitude
		            }
		                desLab.addSubview(historicalList)
		            }
		
			}
		}
	}

    //: deinit {
    deinit {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    // MARK: - Lazy Load

    //: lazy var tipsLabel: UILabel = {
    lazy var tipsLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 17)
        label.font = .magnitudeimateBy(type: .Medium, fontSize: 17)
        //: label.textColor = .appTitleColor()
        label.textColor = .elementColor()
        //: if self.viewType == .BindEmail {
        if self.viewType == .BindEmail {
            //: label.text = "Email Address".localized
            label.text = (String(str_jumpFeeValue) + String(str_userValueData)).localized
            //: } else {
        } else {
            //: label.text = "Phone number".localized
            label.text = (String(str_thirdContent.suffix(6)) + "number").localized
        }
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: return label
        return label
        //: }()
    }()

    //: lazy var titleLabel: UILabel = {
    lazy var titleLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 16)
        label.font = .magnitudeimateBy(type: .Regular, fontSize: 16)
        //: label.textColor = .appTitleColor()
        label.textColor = .elementColor()

        //: if self.viewType == .BindEmail {
        if self.viewType == .BindEmail {
            //: label.text = "Enter the email code sent to".localized
            label.text = (String(str_serviceValue.prefix(4)) + "r the " + str_positionName.replacingOccurrences(of: "count", with: "a") + " code " + String(str_methodEditorValue.suffix(6)) + String(str_groupValue)).localized
            //: } else {
        } else {
            //: label.text = "Enter the phone code sent to".localized
            label.text = (String(str_targetData.suffix(5)) + " the " + str_textViewValue.replacingOccurrences(of: "star", with: "n") + String(str_directionData)).localized
        }
        //: return label
        return label
        //: }()
    }()

    //: lazy var inputLabel: UILabel = {
    lazy var inputLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 16)
        label.font = .magnitudeimateBy(type: .Medium, fontSize: 16)
        //: label.textColor = .appThemeColor()
        label.textColor = .dataViewPush()
        //: label.text = (self.viewType == .BindEmail ? "\(phoneOrEmailStr)" : "(+\(areaCodeNum))\(phoneOrEmailStr)")
        label.text = (self.viewType == .BindEmail ? "\(phoneOrEmailStr)" : "(+\(areaCodeNum))\(phoneOrEmailStr)")
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: return label
        return label
        //: }()
    }()

    //: lazy var codeInputView: TalkingCodeInputView = {
    lazy var codeInputView: RangeReactiveCompatible = {
        //: let codeView = TalkingCodeInputView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 70), codeNumber: 6, labelTextColor: .white, mainColor: UIColor(hex: "8075F5")!, normalColor: UIColor(hex: "F4F4F4")!, margin: 12)
        let codeView = RangeReactiveCompatible(frame: CGRect(x: 0, y: 0, width: kLet_failureName, height: 70), codeNumber: 6, labelTextColor: .white, mainColor: UIColor(hex: (String(str_engineName)))!, normalColor: UIColor(hex: (str_detailEditTitle.replacingOccurrences(of: "pop", with: "4")))!, margin: 12)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PropertyLineThen.shared.direction == .rightToLeft {
            //: codeView.layer.transform =  CATransform3DMakeRotation(CGFloat(Double.pi), 0, 1, 0)
            codeView.layer.transform = CATransform3DMakeRotation(CGFloat(Double.pi), 0, 1, 0) // 镜像
        }
        //: return codeView
        return codeView
        //: }()
    }()

    //: lazy var commitButton: UIButton = {
    lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 18)
        btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 18)
        //: btn.setTitle("Resend verification email".localized, for: .normal)
        btn.setTitle((String(str_appText.prefix(6)) + " verif" + str_cookieNorUpText.replacingOccurrences(of: "model", with: "t") + String(str_sexTitle.suffix(4)) + "email").localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.gradientShared(colors: UIColor.endColor(), size: CGSize(width: kLet_failureName - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 12)
        label.font = .magnitudeimateBy(type: .Medium, fontSize: 12)
        //: label.textColor = .videoValue()
        label.textColor = .videoValue()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let str = "Tips: If you can't receive the verification code, please check whether the message is in spam or not".localized
        let str = String(bytes: str_aByTrueData.map{titleCos(recording: $0)}, encoding: .utf8)!.localized
        //: let range = NSRange(location: 5, length: str.count-5)
        let range = NSRange(location: 5, length: str.count - 5)
        //: let attributedString = NSMutableAttributedString(string: str)
        let attributedString = NSMutableAttributedString(string: str)
        //: attributedString.addAttribute(.font, value: UIFont.magnitudeimateBy(type: .Regular, fontSize: 12), range: range)
        attributedString.addAttribute(.font, value: UIFont.magnitudeimateBy(type: .Regular, fontSize: 12), range: range)
        //: label.attributedText = attributedString
        label.attributedText = attributedString
        //: return label
        return label
        //: }()
    }()
}

// MARK: - Public Event

//: extension TalkingLoginBindInputCodeVC {
extension AnalogDigitalConverterVc {
    //: func func__bindEmailAction() {
    func funcAction() {
        //: self.view.endEditing(true)
        self.view.endEditing(true)
        //: ProgressHUD.show(superView: view)
        DutyProgressHUD.appearRequest(superView: view)
        //: if viewType == .BindEmail {
        if viewType == .BindEmail {
            //: TalkingLoginRequestTool.req_bindEmailCode(emailCode: codeStr) { succeed, result, errorModel in
            EqualRequestTool.name(emailCode: codeStr) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                DutyProgressHUD.duringShow()
                //: if succeed {
                if succeed {
                    //: SubLabelReactiveCompatible.share.loginUserMode.email = self.phoneOrEmailStr
                    SubLabelReactiveCompatible.share.loginUserMode.email = self.phoneOrEmailStr
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Bind Email succeed".localized)
                    self.playerMsg(showMsg: (String(str_makeTargetTitle) + String(str_keyQuoteContent)).localized)
                    //: let count: Int = (self.navigationController?.viewControllers.count)!
                    let count: Int = (self.navigationController?.viewControllers.count)!
                    //: if count >= 3 {
                    if count >= 3 {
                        //: let count = count - 3
                        let count = count - 3
                        //: let vc = self.navigationController?.viewControllers[count]
                        let vc = self.navigationController?.viewControllers[count]
                        //: self.navigationController?.popToViewController(vc!, animated: true)
                        self.navigationController?.popToViewController(vc!, animated: true)
                    }
                    //: } else {
                } else {
                    //: self.codeInputView.func__resetCodeInputView()
                    self.codeInputView.along()
                    //: self.codeInputView.startEdit()
                    self.codeInputView.exaggerate()
                }
            }
            //: } else {
        } else {
            //: TalkingLoginRequestTool.req_MobileBind(phoneStr: self.phoneOrEmailStr, areaStr: areaCodeNum, vcode: self.codeStr) { [self]  succeed, result, errorModel in
            EqualRequestTool.settleCompletion(phoneStr: self.phoneOrEmailStr, areaStr: areaCodeNum, vcode: self.codeStr) { [self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                DutyProgressHUD.duringShow()
                //: if !succeed {
                if !succeed {
                    //: self.codeInputView.startEdit()
                    self.codeInputView.exaggerate()
                    //: return
                    return
                }
                //: SubLabelReactiveCompatible.share.loginUserMode.mobile = self.phoneOrEmailStr
                SubLabelReactiveCompatible.share.loginUserMode.mobile = self.phoneOrEmailStr
                //: self.func__showStatusBarSuccessMsg(showMsg: "Bind Mobile Phone succeed".localized)
                self.playerMsg(showMsg: (String(str_hiddenName.suffix(6)) + "obile " + String(str_underName.suffix(5)) + " suc" + str_mediumValue.replacingOccurrences(of: "status", with: "ce")).localized)
                //: for index in 0..<(self.navigationController?.viewControllers.count)! {
                for index in 0 ..< (self.navigationController?.viewControllers.count)! {
                    //: if self.navigationController?.viewControllers[index] .isKind(of: TalkingSettingsUniversalVC.self) == true {
                    if self.navigationController?.viewControllers[index].isKind(of: TradingFloorRecognizerDelegate.self) == true {
                        //: let vc = (self.navigationController?.viewControllers[index] as? TalkingSettingsUniversalVC)!
                        let vc = (self.navigationController?.viewControllers[index] as? TradingFloorRecognizerDelegate)!
                        //: self.navigationController?.popToViewController(vc, animated: true)
                        self.navigationController?.popToViewController(vc, animated: true)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func func__resendEmailCodeAction() {
    func emailEconomyFuncAfterBushido() {
        //: self.view.endEditing(true)
        self.view.endEditing(true)
        //: ProgressHUD.show(superView: view)
        DutyProgressHUD.appearRequest(superView: view)
        //: if viewType == .BindEmail {
        if viewType == .BindEmail {
            //: TalkingLoginRequestTool.req_getBindEmailCode(emailStr: self.phoneOrEmailStr, isBind: true) { succeed, result, errorModel in
            EqualRequestTool.fromDown(emailStr: self.phoneOrEmailStr, isBind: true) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                DutyProgressHUD.duringShow()
                //: if succeed {
                if succeed {
                    //: self.func_starCodeTime()
                    self.frontBurner()
                    //: self.codeInputView.func__resetCodeInputView()
                    self.codeInputView.along()
                }
                //: self.codeInputView.startEdit()
                self.codeInputView.exaggerate()
            }
            //: } else {
        } else {
            //: TalkingLoginRequestTool.req_bindPhoneGetCode(phoneStr: self.phoneOrEmailStr, areaStr: areaCodeNum, completion: { succeed, result, errorModel in
            EqualRequestTool.mixUpAttribute(phoneStr: self.phoneOrEmailStr, areaStr: areaCodeNum, completion: { succeed, _, _ in
                //: ProgressHUD.dismiss()
                DutyProgressHUD.duringShow()
                //: if succeed {
                if succeed {
                    //: self.codeInputView.func__resetCodeInputView()
                    self.codeInputView.along()
                    //: self.func_starCodeTime()
                    self.frontBurner()
                }
                //: self.codeInputView.startEdit()
                self.codeInputView.exaggerate()
                //: })
            })
        }
    }

    //: func func_starCodeTime() {
    func frontBurner() {
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: var timeCount = 300
        var timeCount = 300
        //: timerDispose = Observable<Int>.interval(.seconds(1), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(1), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: timeCount -= 1
                timeCount -= 1
                //: self.commitButton.isEnabled = (timeCount <= 0)
                self.commitButton.isEnabled = (timeCount <= 0)
                //: if timeCount > 0 {
                if timeCount > 0 {
                    //: self.commitButton.setTitle("Resend verification email (%@s)".localizedArguments(timeCount), for: .normal)
                    self.commitButton.setTitle(String(bytes: str_logAllTitle.reversed(), encoding: .utf8)!.localizedArguments(timeCount), for: .normal)
                    //: } else {
                } else {
                    //: self.func_endCodeTime()
                    self.addView()
                    //: self.commitButton.setTitle("Resend verification email".localized, for: .normal)
                    self.commitButton.setTitle((String(str_appText.prefix(6)) + " verif" + str_cookieNorUpText.replacingOccurrences(of: "model", with: "t") + String(str_sexTitle.suffix(4)) + "email").localized, for: .normal)
                }
                //: })
            })
    }

    //: func func_endCodeTime() {
    func addView() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }
}

// MARK: - Layout

//: extension TalkingLoginBindInputCodeVC {
extension AnalogDigitalConverterVc {
    // 添加视图
    //: private func setupSubviews() {
    private func statusFrom() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.addSubview(tipsLabel)
        view.addSubview(tipsLabel)
        //: view.addSubview(titleLabel)
        view.addSubview(titleLabel)
        //: view.addSubview(inputLabel)
        view.addSubview(inputLabel)
        //: view.addSubview(codeInputView)
        view.addSubview(codeInputView)
        //: view.addSubview(commitButton)
        view.addSubview(commitButton)
        //: view.addSubview(desLab)
        view.addSubview(desLab)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func setupConstraint() {
        //: tipsLabel.snp.makeConstraints { make in
        tipsLabel.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.top.equalTo(34)
            make.top.equalTo(34)
        }
        //: titleLabel.snp.makeConstraints { make in
        titleLabel.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(tipsLabel.snp.bottom).offset(40)
            make.top.equalTo(tipsLabel.snp.bottom).offset(40)
        }
        //: inputLabel.snp.makeConstraints { make in
        inputLabel.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(titleLabel.snp.bottom)
            make.top.equalTo(titleLabel.snp.bottom)
        }
        //: codeInputView.snp.makeConstraints { make in
        codeInputView.snp.makeConstraints { make in
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(70)
            make.height.equalTo(70)
            //: make.top.equalTo(inputLabel.snp.bottom).offset(24)
            make.top.equalTo(inputLabel.snp.bottom).offset(24)
        }
        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(codeInputView.snp.bottom).offset(30)
            make.top.equalTo(codeInputView.snp.bottom).offset(30)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalTo(commitButton.snp.bottom).offset(36)
            make.top.equalTo(commitButton.snp.bottom).offset(36)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func range() {
        //: commitButton.rx.controlEvent(.touchUpInside)
        commitButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func__resendEmailCodeAction()
                self.emailEconomyFuncAfterBushido()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: codeInputView.codeBlock = { [weak self] (codeStr: String) -> Void in
        codeInputView.codeBlock = { [weak self] (codeStr: String) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if codeStr.count == 6 {
            if codeStr.count == 6 {
                //: self.codeStr = codeStr
                self.codeStr = codeStr
                //: self.func__bindEmailAction()
                self.funcAction()
            }
            //: return
        }
    }
}
