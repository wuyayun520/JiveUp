
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_saveText:[UInt8] = [0xa0,0xa7,0xa0,0xbd,0xe1,0xaa,0xa6,0xad,0xac,0xbb,0xf3,0xe0,0xe9,0xa1,0xa8,0xba,0xe9,0xa7,0xa6,0xbd,0xe9,0xab,0xac,0xac,0xa7,0xe9,0xa0,0xa4,0xb9,0xa5,0xac,0xa4,0xac,0xa7,0xbd,0xac,0xad]

/*: "#F3E6FF" :*/
fileprivate let str_targetFrameUserValue:String = "sex let text from accept#F"
fileprivate let str_contentCenterValue:String = "3E6FFmodel let var path self"

/*: "#D9E7FF" :*/
fileprivate let str_panTitle:[Character] = ["#","D","9","E","7","F","F"]

/*: "Please wait. The distance is being expanded to filter the right users for you" :*/
fileprivate let str_freeContent:[UInt8] = [0x4c,0x70,0x79,0x7d,0x6f,0x79,0x3c,0x6b,0x7d,0x75,0x68,0x32,0x3c,0x48,0x74,0x79,0x3c,0x78,0x75,0x6f,0x68,0x7d,0x72,0x7f,0x79,0x3c,0x75,0x6f,0x3c,0x7e,0x79,0x75,0x72,0x7b,0x3c,0x79,0x64,0x6c,0x7d,0x72,0x78,0x79,0x78,0x3c,0x68,0x73,0x3c,0x7a,0x75,0x70,0x68,0x79,0x6e,0x3c,0x68,0x74,0x79,0x3c,0x6e,0x75,0x7b,0x74,0x68,0x3c,0x69,0x6f,0x79,0x6e,0x6f,0x3c,0x7a,0x73,0x6e,0x3c,0x65,0x73,0x69]

private func valueTitle(mutual num: UInt8) -> UInt8 {
    return num ^ 28
}

/*: "icon_await" :*/
fileprivate let str_localText:[Character] = ["i"]
fileprivate let str_groupData:[Character] = ["c","o","n","_","a","w","a","i","t"]

/*: "Get Premium" :*/
fileprivate let str_hiddenValue:String = "Geextension item self video user"
fileprivate let str_explainContent:String = "t Premiumsection player let image"

/*: "Become a member now, skip the wait and discover more users instantly." :*/
fileprivate let str_ageTitle:[UInt8] = [0x54,0x77,0x75,0x81,0x7f,0x77,0x32,0x73,0x32,0x7f,0x77,0x7f,0x74,0x77,0x84,0x32,0x80,0x81,0x89,0x3e,0x32,0x85,0x7d,0x7b,0x82,0x32,0x86,0x7a,0x77,0x32,0x89,0x73,0x7b,0x86,0x32,0x73,0x80,0x76,0x32,0x76,0x7b,0x85,0x75,0x81,0x88,0x77,0x84,0x32,0x7f,0x81,0x84,0x77,0x32,0x87,0x85,0x77,0x84,0x85,0x32,0x7b,0x80,0x85,0x86,0x73,0x80,0x86,0x7e,0x8b,0x40]

fileprivate func modelActual(succeed num: UInt8) -> UInt8 {
    let value = Int(num) + 238
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "icon_hourglass" :*/
fileprivate let str_fieldData:String = "icon_view row button model for"
fileprivate let str_contentFirstValue:[Character] = ["h","o","u"]
fileprivate let str_totalTitle:String = "RGLASS"

/*: "All Cards Swiped for Today!" :*/
fileprivate let str_cellValueContent:String = "All let self live color path"
fileprivate let str_infoManagerTitle:String = " Swipeself false back if result"
fileprivate let str_constraintName:String = "lab layer name Today"
fileprivate let str_cancelName:[Character] = ["!"]

/*: "We recommend  :*/
fileprivate let str_modeBurnMainValue:String = "We recdocument user color true"
fileprivate let str_picName:String = "let gesture toommend "

/*:  new friends for you daily." :*/
fileprivate let str_levelText:String = " new choice actual hidden main"
fileprivate let str_burnData:String = "block succeednds fo"
fileprivate let str_liveWhiteValue:String = "daily.info title capture back as"

/*: "&type=0" :*/
fileprivate let str_appContent:String = "&type=0return view view"

/*: "%02d" :*/
fileprivate let str_upVoiceValue:[Character] = ["%","0","2","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PictureView.swift
//  LeafyLuxe
//
//  Created by DouXiu on 2025/3/24.
//

//: import UIKit
import UIKit

//: class TalkingClubCardSwipedFinishView: UIView {
class PictureView: UIView {
	var spendConversationQuantity: Int = 53
	var allCallBlockTotal: Double = -73.9
	var viewArray: [AnyHashable] = []
	var reduceSum: Int = 97
	var titleUserCount: Double = -4.0
	var theGiftVideoArray: [AnyHashable] = []

    //: var countdownBlock: (() -> Void)?
    var countdownBlock: (() -> Void)?

    //: override init(frame: CGRect) {
    override init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        fileNameSubviews()
        //: setupSubViewsConstraint()
        outConstraint()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_saveText.map{$0^201}, encoding: .utf8)!)
    }

    // MARK: - Lazy Load

    //: private lazy var bglayer: CAGradientLayer = {
    private lazy var bglayer: CAGradientLayer = {
        // 渐变色
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight)
        layer.frame = CGRect(x: 0, y: 0, width: kLet_failureName, height: kLet_appEventDomainName - kLet_rateRequestPathTitle)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0.5)
        layer.startPoint = CGPoint(x: 0.5, y: 0.5)
        //: layer.endPoint = CGPoint(x: 1, y: 1)
        layer.endPoint = CGPoint(x: 1, y: 1)
        //: layer.colors = [UIColor(hex: "#F3E6FF")!.cgColor, UIColor(hex: "#D9E7FF")!.cgColor]
        layer.colors = [UIColor(hex: (String(str_targetFrameUserValue.suffix(2)) + String(str_contentCenterValue.prefix(5))))!.cgColor, UIColor(hex: (String(str_panTitle)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: return layer
        return layer
        //: }()
    }()

    //: private lazy var vipContentLab: UILabel = {
    private lazy var vipContentLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.text = "Please wait. The distance is being expanded to filter the right users for you"
        lab.text = String(bytes: str_freeContent.map{valueTitle(mutual: $0)}, encoding: .utf8)!
        //: lab.textColor = .appTitleColor()
        lab.textColor = .elementColor()
        //: lab.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 20)
        lab.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 20)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.isHidden = true
        lab.isHidden = true
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipImageV: UIImageView = {
    private lazy var vipImageV: UIImageView = {
        //: let v = UIImageView(image: UIImage.nameTitle(name: "icon_await"))
        let v = UIImageView(image: UIImage.nameTitle(name: (String(str_localText) + String(str_groupData))))
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var premiumBtn: UIButton = {
    private lazy var premiumBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setTitle("Get Premium".localized, for: .normal)
        btn.setTitle((String(str_hiddenValue.prefix(2)) + String(str_explainContent.prefix(9))).localized, for: .normal)
        //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 17)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-50, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.gradientShared(colors: UIColor.endColor(), size: CGSize(width: kLet_failureName - 50, height: 50)), for: .normal)
        //: btn.addTarget(self, action: #selector(getPremiumBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(iconPlace), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var normalContentLab: UILabel = {
    private lazy var normalContentLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.text = "Become a member now, skip the wait and discover more users instantly."
        lab.text = String(bytes: str_ageTitle.map{modelActual(succeed: $0)}, encoding: .utf8)!
        //: lab.textColor = .appTitleColor()
        lab.textColor = .elementColor()
        //: lab.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 20)
        lab.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 20)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.isHidden = true
        lab.isHidden = true
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var normalImageV: UIImageView = {
    private lazy var normalImageV: UIImageView = {
        //: let v = UIImageView(image: UIImage.nameTitle(name: "icon_hourglass"))
        let v = UIImageView(image: UIImage.nameTitle(name: (String(str_fieldData.prefix(5)) + String(str_contentFirstValue) + str_totalTitle.lowercased())))
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var titleLab: UILabel = {
    private lazy var titleLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.text = "All Cards Swiped for Today!"
        lab.text = (String(str_cellValueContent.prefix(4)) + "Cards" + String(str_infoManagerTitle.prefix(6)) + "d for" + String(str_constraintName.suffix(6)) + String(str_cancelName))
        //: lab.textColor = .appTitleColor()
        lab.textColor = .elementColor()
        //: lab.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 20)
        lab.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.isHidden = true
        lab.isHidden = true
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var subTitleLab: UILabel = {
    private lazy var subTitleLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .elementColor()
        //: lab.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 14)
        lab.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 14)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.isHidden = true
        lab.isHidden = true
        //: return lab
        return lab
        //: }()
    }()

    // MARK: - 倒计时

    //: private var countdownTimer: Timer?
    private var countdownTimer: Timer?
    //: private var remainingSeconds: Int = 0
    private var remainingSeconds: Int = 0 // 剩余秒数

    //: private lazy var countdownContainer: UIStackView = {
    private lazy var countdownContainer: UIStackView = {
        //: let stack = UIStackView()
        let stack = UIStackView()
        //: stack.axis = .horizontal
        stack.axis = .horizontal
        //: stack.spacing = 4
        stack.spacing = 4
        //: stack.alignment = .center
        stack.alignment = .center
        //: stack.distribution = .fill
        stack.distribution = .fill
        //: stack.isHidden = true
        stack.isHidden = true
        //: return stack
        return stack
        //: }()
    }()

    //: private func createNumberLabel() -> UILabel {
    private func intervalIgnoreLabel() -> UILabel {
        //: let label = UILabel()
        let label = UILabel()
        //: label.textColor = .appTitleColor()
        label.textColor = .elementColor()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 20)
        label.font = .magnitudeimateBy(type: .Medium, fontSize: 20)
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.backgroundColor = .white
        label.backgroundColor = .white
        //: label.layer.cornerRadius = 5
        label.layer.cornerRadius = 5
        //: label.layer.borderColor = UIColor.dataViewPush().cgColor
        label.layer.borderColor = UIColor.dataViewPush().cgColor
        //: label.layer.borderWidth = 1
        label.layer.borderWidth = 1
        //: label.clipsToBounds = true
        label.clipsToBounds = true
        //: label.widthAnchor.constraint(equalToConstant: 38).isActive = true
        label.widthAnchor.constraint(equalToConstant: 38).isActive = true
        //: label.heightAnchor.constraint(equalToConstant: 42).isActive = true
        label.heightAnchor.constraint(equalToConstant: 42).isActive = true
        //: return label
        
            if (!titleLab.isUserInteractionEnabled) && (titleLab.isMultipleTouchEnabled) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let timeLet = InfoOfView()

            
            timeLet.nameCompleteMagnitude = { [self] sumCount in
            self.spendConversationQuantity = sumCount
            
            var timeLet = remainingSeconds
            timeLet -= 1
            if timeLet > self.spendConversationQuantity {
                self.spendConversationQuantity = timeLet
            }
            
            return self.spendConversationQuantity
            }
            timeLet.viewEnableTotal = { [self] collectionMagnitude in
            self.allCallBlockTotal = collectionMagnitude
            
                self.allCallBlockTotal -= 1
                if self.allCallBlockTotal != 72 {
                    self.allCallBlockTotal = self.allCallBlockTotal + 1
                }
            return self.allCallBlockTotal
            }
            timeLet.endArray = { [self] imagePlayArray in
            self.viewArray = imagePlayArray
            
            guard var value = self.viewArray as? [String] else {
                return nil
            }
            return value
            }
                titleLab.addSubview(timeLet)
            }

		return label
    }

    //: private func createColonLabel() -> UILabel {
    private func frameAcross() -> UILabel {
        //: let label = UILabel()
        let label = UILabel()
        //: label.textColor = .appTitleColor()
        label.textColor = .elementColor()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 20)
        label.font = .magnitudeimateBy(type: .Medium, fontSize: 20)
        //: label.text = ":"
        label.text = ":"
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: return label
        
            if (vipImageV.inputViewController != nil) && (vipImageV.convert(CGRect(x: 0, y: CGFloat(0), width: 0, height: 0), from: vipImageV.superview).origin.y == 84.19) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let saveFrom = InfoOfView(frame: vipImageV.frame.offsetBy(dx: CGFloat(67), dy: CGFloat(542.28)))

            
            saveFrom.nameCompleteMagnitude = { [self] sumCount in
            self.reduceSum = sumCount
            
            var saveFrom = remainingSeconds
            saveFrom &+= 1
            if saveFrom > self.reduceSum {
                self.reduceSum = saveFrom
            }
            
            return self.reduceSum
            }
            saveFrom.viewEnableTotal = { [self] collectionMagnitude in
            self.titleUserCount = collectionMagnitude
            
                self.titleUserCount += 1
                if self.titleUserCount >= 0 {
                    self.titleUserCount = self.titleUserCount - 1
                }
            return self.titleUserCount
            }
            saveFrom.endArray = { [self] imagePlayArray in
            self.theGiftVideoArray = imagePlayArray
            
            guard var value = self.theGiftVideoArray as? [String] else {
                return nil
            }
            return value
            }
                vipImageV.addSubview(saveFrom)
            }

		return label
    }

    //: private lazy var timeLabels: [UILabel] = {
    private lazy var timeLabels: [UILabel] = {
        //: var labels: [UILabel] = []
        var labels: [UILabel] = []
        // Hours
        //: labels.append(createNumberLabel())
        labels.append(intervalIgnoreLabel())
        //: labels.append(createNumberLabel())
        labels.append(intervalIgnoreLabel())
        // Colon
        //: labels.append(createColonLabel())
        labels.append(frameAcross())
        // Minutes
        //: labels.append(createNumberLabel())
        labels.append(intervalIgnoreLabel())
        //: labels.append(createNumberLabel())
        labels.append(intervalIgnoreLabel())
        // Colon
        //: labels.append(createColonLabel())
        labels.append(frameAcross())
        // Seconds
        //: labels.append(createNumberLabel())
        labels.append(intervalIgnoreLabel())
        //: labels.append(createNumberLabel())
        labels.append(intervalIgnoreLabel())
        //: return labels
        return labels
        //: }()
    }()

    //: deinit {
    deinit {
        //: countdownTimer?.invalidate()
        countdownTimer?.invalidate()
        //: countdownTimer = nil
        countdownTimer = nil
    }
}

// MARK: - Event

//: extension TalkingClubCardSwipedFinishView {
extension PictureView {
    /// 刷新用户滑完卡片的视图
    /// - Parameters:
    ///   - limitNum: 新卡片数量
    ///   - expire: 等待时间
    //: func show(limitNum: Int?, expire: Int?) {
    func postRoomSection(limitNum: Int?, expire: Int?) {
        //: self.isHidden = false
        self.isHidden = false
        //: if SubLabelReactiveCompatible.share.loginUserMode.loungePlus {
        if SubLabelReactiveCompatible.share.loginUserMode.loungePlus { // 会员
            //: vipImageV.isHidden = false
            vipImageV.isHidden = false
            //: vipContentLab.isHidden = false
            vipContentLab.isHidden = false

            //: } else {
        } else { // 非会员
            //: premiumBtn.isHidden = false
            premiumBtn.isHidden = false
            //: normalContentLab.isHidden = false
            normalContentLab.isHidden = false
            //: normalImageV.isHidden = false
            normalImageV.isHidden = false
            //: if let num = limitNum {
            if let num = limitNum {
                //: titleLab.isHidden = false
                titleLab.isHidden = false
                //: subTitleLab.isHidden = false
                subTitleLab.isHidden = false
                //: subTitleLab.text = "We recommend \(num) new friends for you daily."
                subTitleLab.text = (String(str_modeBurnMainValue.prefix(6)) + String(str_picName.suffix(7))) + "\(num)" + (String(str_levelText.prefix(5)) + "frie" + String(str_burnData.suffix(6)) + "r you " + String(str_liveWhiteValue.prefix(6)))
            }

            //: if let expire = expire {
            if let expire = expire {
                //: remainingSeconds = expire
                remainingSeconds = expire
                //: countdownContainer.isHidden = false
                countdownContainer.isHidden = false
                //: addSubview(countdownContainer)
                addSubview(countdownContainer)
                //: timeLabels.forEach { label in
                for label in timeLabels {
                    //: countdownContainer.addArrangedSubview(label)
                    countdownContainer.addArrangedSubview(label)
                }
                //: countdownContainer.snp.makeConstraints { make in
                countdownContainer.snp.makeConstraints { make in
                    //: make.centerX.equalToSuperview()
                    make.centerX.equalToSuperview()
                    //: make.top.equalTo(subTitleLab.snp.bottom).offset(27)
                    make.top.equalTo(subTitleLab.snp.bottom).offset(27)
                }

                //: startCountdown()
                blockSumdown()
            }
        }
    }
    
    /// 隐藏当前视图
    func dismiss() {
        self.isHidden = true
    }
    

    /// 订阅按钮点击事件
    //: @objc func getPremiumBtnClick() {
    @objc func iconPlace() {
        //: TalkingPushManager.share.func__pushToSubscribeAlert(appendParams: "&type=0")
        TalkingPushManager.share.statusSumimate(appendParams: (String(str_appContent.prefix(7))))
    }

    /// 开始倒计时
    //: private func startCountdown() {
    private func blockSumdown() {
        //: updateTimeLabel()
        modifyLabel()
        //: countdownTimer?.invalidate()
        countdownTimer?.invalidate()
        //: countdownTimer = Timer.scheduledTimer(withTimeInterval: 1.0, repeats: true) { [weak self] _ in
        countdownTimer = Timer.scheduledTimer(withTimeInterval: 1.0, repeats: true) { [weak self] _ in
            //: self?.updateTimeLabel()
            self?.modifyLabel()
        }
    }

    /// 更新时间
    //: private func updateTimeLabel() {
    private func modifyLabel() {
        //: if remainingSeconds <= 0 {
        if remainingSeconds <= 0 {
            //: countdownTimer?.invalidate()
            countdownTimer?.invalidate()
            //: countdownTimer = nil
            countdownTimer = nil
            //: countdownContainer.isHidden = true
            countdownContainer.isHidden = true
            //: self.isHidden = true
            self.dismiss()
            //: self.countdownBlock?()
            self.countdownBlock?()
            //: return
            return
        }

        //: let hours = remainingSeconds / 3600
        let hours = remainingSeconds / 3600
        //: let minutes = (remainingSeconds % 3600) / 60
        let minutes = (remainingSeconds % 3600) / 60
        //: let seconds = remainingSeconds % 60
        let seconds = remainingSeconds % 60

        //: let timeComponents = [
        let timeComponents = [
            //: String(format: "%02d", hours),
            String(format: "%02d", hours),
            //: String(format: "%02d", minutes),
            String(format: "%02d", minutes),
            //: String(format: "%02d", seconds)
            String(format: "%02d", seconds),
        ]

        //: var digitIndex = 0
        var digitIndex = 0
        //: for (_, label) in timeLabels.enumerated() {
        for (_, label) in timeLabels.enumerated() {
            //: if label.text != ":" {
            if label.text != ":" {
                //: let timeComponent = timeComponents[digitIndex / 2]
                let timeComponent = timeComponents[digitIndex / 2]
                //: let digit = String(timeComponent[timeComponent.index(timeComponent.startIndex, offsetBy: digitIndex % 2)])
                let digit = String(timeComponent[timeComponent.index(timeComponent.startIndex, offsetBy: digitIndex % 2)])
                //: label.text = digit
                label.text = digit
                //: digitIndex += 1
                digitIndex += 1
            }
        }
        //: remainingSeconds -= 1
        remainingSeconds -= 1
    }
}

// MARK: - Layout

//: extension TalkingClubCardSwipedFinishView {
extension PictureView {
    /// 添加视图
    //: private func setupSubviews() {
    private func fileNameSubviews() {
        //: self.layer.addSublayer(bglayer)
        self.layer.addSublayer(bglayer)
        //: self.addSubview(vipContentLab)
        self.addSubview(vipContentLab)
        //: self.addSubview(vipImageV)
        self.addSubview(vipImageV)
        //: self.addSubview(premiumBtn)
        self.addSubview(premiumBtn)
        //: addSubview(normalContentLab)
        addSubview(normalContentLab)
        //: addSubview(normalImageV)
        addSubview(normalImageV)
        //: addSubview(titleLab)
        addSubview(titleLab)
        //: addSubview(subTitleLab)
        addSubview(subTitleLab)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func outConstraint() {
        //: vipContentLab.snp.makeConstraints { make in
        vipContentLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(self.snp.centerY)
            make.top.equalTo(self.snp.centerY)
            //: make.leading.equalTo(40)
            make.leading.equalTo(40)
            //: make.trailing.equalTo(-40)
            make.trailing.equalTo(-40)
        }

        //: vipImageV.snp.makeConstraints { make in
        vipImageV.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(vipContentLab.snp.top)
            make.bottom.equalTo(vipContentLab.snp.top)
            //: make.width.equalTo(146)
            make.width.equalTo(146)
            //: make.height.equalTo(152)
            make.height.equalTo(152)
        }

        //: premiumBtn.snp.makeConstraints { make in
        premiumBtn.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-50)
            make.bottom.equalToSuperview().offset(-50)
            //: make.leading.equalTo(25)
            make.leading.equalTo(25)
            //: make.trailing.equalTo(-25)
            make.trailing.equalTo(-25)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }

        //: normalContentLab.snp.makeConstraints { make in
        normalContentLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(premiumBtn.snp.top).offset(-30)
            make.bottom.equalTo(premiumBtn.snp.top).offset(-30)
            //: make.leading.equalTo(40)
            make.leading.equalTo(40)
            //: make.trailing.equalTo(-40)
            make.trailing.equalTo(-40)
        }

        //: normalImageV.snp.makeConstraints { make in
        normalImageV.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(normalContentLab.snp.top).offset(-40)
            make.bottom.equalTo(normalContentLab.snp.top).offset(-40)
            //: make.width.equalTo(185)
            make.width.equalTo(185)
            //: make.height.equalTo(221)
            make.height.equalTo(221)
        }

        //: titleLab.snp.makeConstraints { make in
        titleLab.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+60)
            make.top.equalTo(kLet_messageSumervalName + 60)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: subTitleLab.snp.makeConstraints { make in
        subTitleLab.snp.makeConstraints { make in
            //: make.top.equalTo(titleLab.snp.bottom).offset(12)
            make.top.equalTo(titleLab.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
    }
}
