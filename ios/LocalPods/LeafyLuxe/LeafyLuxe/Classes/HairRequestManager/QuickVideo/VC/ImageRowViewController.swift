
//: Declare String Begin

/*: "#201E50" :*/
fileprivate let str_elementDownAwakeData:[Character] = ["#","2","0","1","E","5","0"]

/*: "#1F1624" :*/
fileprivate let str_sendManagerValue:String = "#1F1624half self for aspect right"

/*: "quick_video_topview" :*/
fileprivate let str_gameEventIconName:[Character] = ["q","u","i","c","k","_","v","i","d"]
fileprivate let str_pairData:String = "mode constraint gifteo_to"

/*: "btn_back_white" :*/
fileprivate let str_everyName:[Character] = ["b","t","n","_","b","a","c","k","_","w","h","i","t"]
fileprivate let str_labelName:[Character] = ["e"]

/*: "Random Video" :*/
fileprivate let str_appData:[Character] = ["R","a","n","d","o","m"," ","V","i","d","e","o"]

/*: "icon_rank_coin" :*/
fileprivate let str_actualTitle:String = "medium length appearicon_r"
fileprivate let str_cancelData:String = "type framework labelank_coin"

/*: "btn_quick_back_nor" :*/
fileprivate let str_mName:[Character] = ["b","t","n","_","q","u","i"]
fileprivate let str_pageValue:String = "ck_baclimit path event stop user"
fileprivate let str_directionValue:[Character] = ["k","_","n","o","r"]

/*: "icon_video_skip" :*/
fileprivate let str_labelTitle:String = "icon_view var text to message"
fileprivate let str_picValue:String = "error"

/*: "#9610FF" :*/
fileprivate let str_loadName:String = "succeed path size#9610FF"

/*: "#8566FF" :*/
fileprivate let str_tapTitle:String = "app"
fileprivate let str_imageWillBlockData:[Character] = ["8","5","6","6","F","F"]

/*: "icon_coin_match_line" :*/
fileprivate let str_underValue:String = "let at centericon_"
fileprivate let str_shirtName:String = "lab background self let letatch_"

/*: "matchId" :*/
fileprivate let str_previousAppContent:[UInt8] = [0x3b,0x37,0x22,0x35,0x3e,0x1f,0x32]

private func allTo(white num: UInt8) -> UInt8 {
    return num ^ 86
}

/*: "source" :*/
fileprivate let str_renderValue:[UInt8] = [0xf4,0xe8,0xf2,0xf5,0xe4,0xe2]

private func magnitudeenanceOver(show num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "type" :*/
fileprivate let str_leadingContent:[UInt8] = [0x73,0x7e,0x77,0x62]

/*: "uid" :*/
fileprivate let str_partyTitle:String = "uiview"

/*: "fromFreeCall" :*/
fileprivate let str_constraintName:String = "fromFreview title"
fileprivate let str_rowTitle:String = "eCallproximate time"

/*: "cmd" :*/
fileprivate let str_indexLimitTitle:[UInt8] = [0x64,0x6d,0x63]

/*: "requestCall" :*/
fileprivate let str_shareValue:String = "rsourcequ"

/*: "data" :*/
fileprivate let str_popText:[UInt8] = [0x2c,0x29,0x3c,0x29]

private func titleLayer(to num: UInt8) -> UInt8 {
    return num ^ 72
}

/*: "onRequestCall" :*/
fileprivate let str_barLabVarName:String = "onRereturn let"
fileprivate let str_commitContent:String = "height if model sizetCall"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ImageRowViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/7.
//

//: import UIKit
import UIKit

//: class TalkingMatchResultViewController: TalkingBaseViewController {
class ImageRowViewController: EqualVideoReactiveCompatible {
	var requestQuantity: Double = 36.5
	var tabDictionary: [AnyHashable: String] = [:]
	var exampleCount: Double = -81.3
	var fromDictionary: [AnyHashable: String] = [:]
	var levelInterval: Double = 45.3
	var representationDictionary: [AnyHashable: String] = [:]

    //: private var currentModel = TalkingMatchResultModel()
    private var currentModel = RowHandyJSON() // 当前展示的model

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        // 每次打开更新金额
        //: refreshCoin()
        eightpence()
    
		
		//: if_extract_code "userCollectionSave" begin
		
		var userCollectionSave = false
		if !userCollectionSave {
		    var isValue = false
		    if #available(iOS 13.0, *) {
		        isValue = picturesView.editingInteractionConfiguration == .none
		    }
		    userCollectionSave = isValue
		}
		
		//: if_extract_code "userCollectionSave" end
		
            if (picturesView.convert(CGPoint(x: CGFloat(66), y: CGFloat(0)), to: picturesView.superview).y == 93.49) && (userCollectionSave) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let indexView = LikeClingView()
            indexView.progressName = currentModel.videoUrl
            indexView.imaginationCommentNumber = { [self] styleMagnitude in
            self.levelInterval = styleMagnitude
            
            self.levelInterval += 1
            return self.levelInterval
            }
            indexView.forwardingDictionary = { [self] dataDictionary in
            self.representationDictionary = dataDictionary
            
            guard var value = self.representationDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                picturesView.addSubview(indexView)
            }

	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            if (dzView.isExclusiveTouch) && (dzView.layer.contents != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let sectionEffect = LikeClingView(frame: dzView.frame.offsetBy(dx: CGFloat(227.53), dy: CGFloat(0)))
            sectionEffect.progressName = currentModel.livePlayUrl
            sectionEffect.imaginationCommentNumber = { [self] styleMagnitude in
            self.requestQuantity = styleMagnitude
            
                self.requestQuantity -= 1
                if self.requestQuantity <= 0 {
                    self.requestQuantity = self.requestQuantity + 1
                }
            return self.requestQuantity
            }
            sectionEffect.forwardingDictionary = { [self] dataDictionary in
            self.tabDictionary = dataDictionary
            
            guard var value = self.tabDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                dzView.addSubview(sectionEffect)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        voiceKey()
        //: setupSubViewsConstraint()
        outsideRequest()
        //: refreshUI()
        serviceUi()

        /// 添加刷新余额的通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshCoin),
                                               selector: #selector(eightpence),
                                               //: name: UPDATE_USER_GETINFOCOLUMN_NOTIFICATION,
                                               name: kLet_meetingRecordData,
                                               //: object: nil)
                                               object: nil)
    
            if (dzView.isExclusiveTouch) && (dzView.layer.contents != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let sectionEffect = LikeClingView(frame: dzView.frame.offsetBy(dx: CGFloat(227.53), dy: CGFloat(0)))
            sectionEffect.progressName = currentModel.livePlayUrl
            sectionEffect.imaginationCommentNumber = { [self] styleMagnitude in
            self.exampleCount = styleMagnitude
            
                self.exampleCount -= 1
                if self.exampleCount <= 0 {
                    self.exampleCount = self.exampleCount + 1
                }
            return self.exampleCount
            }
            sectionEffect.forwardingDictionary = { [self] dataDictionary in
            self.fromDictionary = dataDictionary
            
            guard var value = self.fromDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                dzView.addSubview(sectionEffect)
            }

	}

    //: deinit {
    deinit {
        //: TalkingSocketManager.shared.videoMatchDelegate = nil
        TitleSocketManager.shared.videoMatchDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        TitleSocketManager.shared.errorDelegate = nil
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var backView: UIImageView = {
    private lazy var backView: UIImageView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.image = UIImage.imageGradientTopToBottonColor(colors: [UIColor.init(hex: "#201E50")!.cgColor, UIColor.init(hex: "#1F1624")!.cgColor], size: CGSize(width: ScreenWidth, height: ScreenHeight))
        view.image = UIImage.enableLineSize(colors: [UIColor(hex: (String(str_elementDownAwakeData)))!.cgColor, UIColor(hex: (String(str_sendManagerValue.prefix(7))))!.cgColor], size: CGSize(width: kLet_failureName, height: kLet_appEventDomainName))
        //: view.isUserInteractionEnabled = true
        view.isUserInteractionEnabled = true

        //: let topView = UIImageView.init()
        let topView = UIImageView()
        //: topView.image = UIImage.nameTitle(name: "quick_video_topview")
        topView.image = UIImage.nameTitle(name: (String(str_gameEventIconName) + String(str_pairData.suffix(5)) + "pview"))
        //: topView.isUserInteractionEnabled = true
        topView.isUserInteractionEnabled = true
        //: view.addSubview(topView)
        view.addSubview(topView)
        //: topView.snp.makeConstraints { make in
        topView.snp.makeConstraints { make in
            //: make.leading.top.equalTo(0)
            make.leading.top.equalTo(0)
            //: make.width.height.equalTo(actualWidth(w: 218))
            make.width.height.equalTo(actualWidth(w: 218))
        }
        //: return view
        return view
        //: }()
    }()

    //: private lazy var navView: UIView = {
    private lazy var navView: UIView = {
        //: let nav = UIView()
        let nav = UIView()
        //: nav.backgroundColor = .clear
        nav.backgroundColor = .clear
        // 返回
        //: let backBtn = UIButton(type: .custom)
        let backBtn = UIButton(type: .custom)
        //: backBtn.setImage(UIImage.nameTitle(name: "btn_back_white"), for: .normal)
        backBtn.setImage(UIImage.nameTitle(name: (String(str_everyName) + String(str_labelName))), for: .normal)
        //: backBtn.addTarget(self, action: #selector(clickBackButtonAction), for: .touchUpInside)
        backBtn.addTarget(self, action: #selector(cloud), for: .touchUpInside)
        //: nav.addSubview(backBtn)
        nav.addSubview(backBtn)
        //: backBtn.snp.makeConstraints { make in
        backBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(20)
            make.leading.equalTo(20)
            //: make.width.equalTo(20)
            make.width.equalTo(20)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
            //: make.top.equalTo(StatusBarHeight+12)
            make.top.equalTo(kLet_messageSumervalName + 12)
        }
        // 标题
        //: let titleLab = UILabel()
        let titleLab = UILabel()
        //: titleLab.font = UIFont.underPlay(fontSize: 17)
        titleLab.font = UIFont.underPlay(fontSize: 17)
        //: titleLab.textColor = UIColor.white
        titleLab.textColor = UIColor.white
        //: titleLab.textAlignment = .center
        titleLab.textAlignment = .center
        //: titleLab.text = "Random Video".localized
        titleLab.text = (String(str_appData)).localized
        //: nav.addSubview(titleLab)
        nav.addSubview(titleLab)
        //: titleLab.snp.makeConstraints { make in
        titleLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(backBtn)
            make.centerY.equalTo(backBtn)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
        // 余额
        //: let coinBtn = TalkingButton()
        let coinBtn = OfTalkingButton()
        //: coinBtn.tag = 1001
        coinBtn.tag = 1001
        //: coinBtn.setImage(UIImage.nameTitle(name: "icon_rank_coin"), for: .normal)
        coinBtn.setImage(UIImage.nameTitle(name: (String(str_actualTitle.suffix(6)) + String(str_cancelData.suffix(8)))), for: .normal)
        //: coinBtn.setImage(UIImage.nameTitle(name: "icon_rank_coin"), for: .disabled)
        coinBtn.setImage(UIImage.nameTitle(name: (String(str_actualTitle.suffix(6)) + String(str_cancelData.suffix(8)))), for: .disabled)
        //: coinBtn.isEnabled = false
        coinBtn.isEnabled = false
        //: coinBtn.setTitleColor(UIColor.elementColor(), for: .normal)
        coinBtn.setTitleColor(UIColor.elementColor(), for: .normal)
        //: coinBtn.setTitle("\(SubLabelReactiveCompatible.share.loginUserMode.mf_coin)", for: .normal)
        coinBtn.setTitle("\(SubLabelReactiveCompatible.share.loginUserMode.mf_coin)", for: .normal)
        //: coinBtn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 15)
        coinBtn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Semibold, fontSize: 15)
        //: coinBtn.backgroundColor = UIColor.white
        coinBtn.backgroundColor = UIColor.white
        //: coinBtn.layer.cornerRadius = 12
        coinBtn.layer.cornerRadius = 12
        //: coinBtn.imageAlignment = .left
        coinBtn.imageAlignment = .left
//        coinBtn.spaceBetweenTitleAndImage = 3
        //: nav.addSubview(coinBtn)
        nav.addSubview(coinBtn)
        //: coinBtn.snp.makeConstraints { make in
        coinBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(-19)
            make.trailing.equalTo(-19)
            //: make.centerY.equalTo(backBtn)
            make.centerY.equalTo(backBtn)
            //: make.height.equalTo(24)
            make.height.equalTo(24)
        }

        //: return nav
        return nav
        //: }()
    }()

    //: private lazy var picturesView: TalkingMatchResultPicturesView = {
    private lazy var picturesView: LayerPicturesView = {
        //: let v = TalkingMatchResultPicturesView(frame: .zero)
        let v = LayerPicturesView(frame: .zero)
        //: v.matchBackBlock = { [weak self] in
        v.matchBackBlock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: self.clickBackButtonAction()
            self.cloud()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var showLastBtn: UIButton = {
    private lazy var showLastBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setBackgroundImage(UIImage.nameTitle(name: "btn_quick_back_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.nameTitle(name: (String(str_mName) + String(str_pageValue.prefix(6)) + String(str_directionValue))), for: .normal)
        //: btn.addTarget(self, action: #selector(showLastButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(demonstrateFromSnap), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var skipBtn: UIButton = {
    private lazy var skipBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nameTitle(name: "icon_video_skip"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_labelTitle.prefix(5)) + "video_ski" + str_picValue.replacingOccurrences(of: "error", with: "p"))), for: .normal)
        //: btn.addTarget(self, action: #selector(clickBackButtonAction), for: .touchUpInside)
        btn.addTarget(self, action: #selector(cloud), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var callBtn: UIButton = {
    private lazy var callBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(callButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(miniTitleClick), for: .touchUpInside)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: [UIColor.init(hex: "#9610FF")!.cgColor, UIColor.init(hex: "#8566FF")!.cgColor], size: CGSize(width: 140, height: 69)), for: .normal)
        btn.setBackgroundImage(UIImage.gradientShared(colors: [UIColor(hex: (String(str_loadName.suffix(7))))!.cgColor, UIColor(hex: (str_tapTitle.replacingOccurrences(of: "app", with: "#") + String(str_imageWillBlockData)))!.cgColor], size: CGSize(width: 140, height: 69)), for: .normal)
        //: btn.layer.cornerRadius = 69/2
        btn.layer.cornerRadius = 69 / 2
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.titleLabel?.numberOfLines = 2
        btn.titleLabel?.numberOfLines = 2

        //: return btn
        return btn
        //: }()
    }()

    //: lazy var showLastArr: [TalkingMatchResultModel] = {
    lazy var showLastArr: [RowHandyJSON] = //: return Array<RowHandyJSON>()
        .init()
    //: }()

    //: lazy var dzView: UIImageView = {
    lazy var dzView: UIImageView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.image = UIImage.nameTitle(name: "icon_coin_match_line")
        view.image = UIImage.nameTitle(name: (String(str_underValue.suffix(5)) + "coin_m" + String(str_shirtName.suffix(5)) + "line"))
        //: view.isHidden = true
        view.isHidden = true
        //: return view
        return view
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingMatchResultViewController {
extension ImageRowViewController {
    /// 返回按钮
    //: @objc private func clickBackButtonAction() {
    @objc private func cloud() {
        //: if self.presentedViewController != nil {
        if self.presentedViewController != nil { // 可能有重置弹窗未消除的情况，许手动消除
            //: self.presentedViewController?.dismiss(animated: true)
            self.presentedViewController?.dismiss(animated: true)
        }
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 拨打视频通话socket
    //: @objc private func callButtonClick() {
    @objc private func miniTitleClick() {
        //: var data: [String: Any] = ["matchId": self.currentModel.matchId,
        var data: [String: Any] = [String(bytes: str_previousAppContent.map{allTo(white: $0)}, encoding: .utf8)!: self.currentModel.matchId,
                                   //: "source": self.currentModel.source,
                                   String(bytes: str_renderValue.map{magnitudeenanceOver(show: $0)}, encoding: .utf8)!: self.currentModel.source,
                                   //: "type": self.currentModel.type,
                                   String(bytes: str_leadingContent.map{$0^7}, encoding: .utf8)!: self.currentModel.type,
                                   //: "uid": self.currentModel.uid]
                                   (str_partyTitle.replacingOccurrences(of: "view", with: "d")): self.currentModel.uid]
        //: if SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0, SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue {
            //: data.updateValue(1, forKey: "fromFreeCall")
            data.updateValue(1, forKey: (String(str_constraintName.prefix(7)) + String(str_rowTitle.prefix(5))))
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": data]
        let info: [String: Any] = [String(bytes: str_indexLimitTitle.reversed(), encoding: .utf8)!: (str_shareValue.replacingOccurrences(of: "source", with: "e") + "estCall"), String(bytes: str_popText.map{titleLayer(to: $0)}, encoding: .utf8)!: data]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        TitleSocketManager.shared.fromApp(info: info)
        //: TalkingSocketManager.shared.videoMatchDelegate = self
        TitleSocketManager.shared.videoMatchDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        TitleSocketManager.shared.errorDelegate = self
    }

    /// 通知刷新余额
    //: @objc private func refreshCoin() {
    @objc private func eightpence() {
        //: AppManagerRequest.func__getInfoColumn { succeed, result, errorModel in
        ShareCellThen.makeCompletion { succeed, _, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }

            //: let coinBtn = self.navView.viewWithTag(1001) as! TalkingButton
            let coinBtn = self.navView.viewWithTag(1001) as! OfTalkingButton
            //: coinBtn.setTitle(SubLabelReactiveCompatible.share.loginUserMode.mf_coin, for: .normal)
            coinBtn.setTitle(SubLabelReactiveCompatible.share.loginUserMode.mf_coin, for: .normal)
        }
    }

    /// 展示上一个视频速配结果
    //: @objc private func showLastButtonClick() {
    @objc private func demonstrateFromSnap() {
        //: showLastArr.removeLast()
        showLastArr.removeLast()
        //: refreshUI()
        serviceUi()
    }
}

// MARK: - SocketManagerMatchResultDelegate

//: extension TalkingMatchResultViewController: SocketManagerVideoMatchDelegate {
extension ImageRowViewController: VisualCommunicationObjectProtocol {
    //: func socket_match_onVideoMatch(data: [String: Any]) {}
    func playerEqual(data _: [String: Any]) {}

    /// 匹配成功，可以拨打通话（此处跳转特殊处理）
    //: func socket_match_onRequestCall(data: [String: Any]) {
    func topic(data: [String: Any]) {
        //: var arrM = self.navigationController!.viewControllers
        var arrM = self.navigationController!.viewControllers
        //: for (index, vc) in self.navigationController!.viewControllers.enumerated() {
        for (index, vc) in self.navigationController!.viewControllers.enumerated() {
            //: if vc.isKind(of: TalkingMatchResultViewController.self) {
            if vc.isKind(of: ImageRowViewController.self) {
                //: arrM.remove(at: index)
                arrM.remove(at: index)
                //: break
                break
            }
        }
        //: var newData = data
        var newData = data
        //: newData["uid"] = self.currentModel.uid
        newData[(str_partyTitle.replacingOccurrences(of: "view", with: "d"))] = self.currentModel.uid
        //: let vc = TalkingVideoChatViewController()
        let vc = TextObserverDelegate()
        //: vc.chatModel = TalkingVideoChatModel.getVideoModel(info: newData, situation: .Start)
        vc.chatModel = ProfessionalChatModel.rivetLineSituation(info: newData, situation: .Start)

        //: vc.hidesBottomBarWhenPushed = true
        vc.hidesBottomBarWhenPushed = true
        //: arrM.append(vc)
        arrM.append(vc)
        //: self.navigationController?.setViewControllers(arrM, animated: true)
        self.navigationController?.setViewControllers(arrM, animated: true)
    }
}

// MARK: - DataObjectProtocol

//: extension TalkingMatchResultViewController: TalkingSocketManagerErrorDelegate {
extension ImageRowViewController: DataObjectProtocol {
    /// socket收到失败消息
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func choiceHandle(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "onRequestCall" {
        if cmd == (String(str_barLabVarName.prefix(4)) + "ques" + String(str_commitContent.suffix(5))) {
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.barLog(showMsg: msg)
            //: if errorNo == VideoChatErrorCode.CallEnd.rawValue {
            if errorNo == VendorRelatableTarget.CallEnd.rawValue {
                //: clickBackButtonAction()
                cloud()
                //: } else if errorNo == VideoChatErrorCode.MoneyLack.rawValue {
            } else if errorNo == VendorRelatableTarget.MoneyLack.rawValue {
                //: guard SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
                guard SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue else { return }
                //: TalkingPushManager.share.func__jumpToWebRecharge(sufficient: false)
                TalkingPushManager.share.withParams(sufficient: false)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingMatchResultViewController {
extension ImageRowViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func voiceKey() {
        //: self.view.addSubview(backView)
        self.view.addSubview(backView)
        //: backView.addSubview(navView)
        backView.addSubview(navView)
        //: backView.addSubview(picturesView)
        backView.addSubview(picturesView)
        //: backView.addSubview(showLastBtn)
        backView.addSubview(showLastBtn)
        //: backView.addSubview(skipBtn)
        backView.addSubview(skipBtn)
        //: backView.addSubview(callBtn)
        backView.addSubview(callBtn)
        //: callBtn.addSubview(dzView)
        callBtn.addSubview(dzView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func outsideRequest() {
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.top.width.equalToSuperview()
            make.leading.top.width.equalToSuperview()
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(kLet_soundData)
        }
        //: callBtn.snp.makeConstraints { make in
        callBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-(kDeviceSafeBottomHeight+36))
            make.bottom.equalTo(-(kLet_changeBarValue + 36))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(69)
            make.height.equalTo(69)
            //: make.width.equalTo(140)
            make.width.equalTo(140)
        }

        //: picturesView.snp.makeConstraints { make in
        picturesView.snp.makeConstraints { make in
            //: make.top.equalTo(navView.snp.bottom).offset(actualWidth(w: 17))
            make.top.equalTo(navView.snp.bottom).offset(actualWidth(w: 17))
            //: make.leading.equalTo(18)
            make.leading.equalTo(18)
            //: make.trailing.equalTo(-22)
            make.trailing.equalTo(-22)
            //: make.bottom.equalTo(callBtn.snp.top).offset(-32)
            make.bottom.equalTo(callBtn.snp.top).offset(-32)
        }
        //: showLastBtn.snp.makeConstraints { make in
        showLastBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(callBtn.snp.leading).offset(-20)
            make.trailing.equalTo(callBtn.snp.leading).offset(-20)
            //: make.centerY.equalTo(callBtn)
            make.centerY.equalTo(callBtn)
            //: make.size.equalTo(CGSize(width: 50, height: 50))
            make.size.equalTo(CGSize(width: 50, height: 50))
        }

        //: dzView.snp.makeConstraints { make in
        dzView.snp.makeConstraints { make in
            //: make.leading.equalTo(146)
            make.leading.equalTo(146)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(8)
            make.height.equalTo(8)
            //: make.width.equalTo(42)
            make.width.equalTo(42)
        }
        //: skipBtn.snp.makeConstraints { make in
        skipBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(callBtn.snp.trailing).offset(21)
            make.leading.equalTo(callBtn.snp.trailing).offset(21)
            //: make.centerY.equalTo(callBtn)
            make.centerY.equalTo(callBtn)
            //: make.size.equalTo(50)
            make.size.equalTo(50)
        }
    }

    /// 刷新UI
    //: private func refreshUI() {
    private func serviceUi() {
        //: if showLastArr.count > 0 {
        if showLastArr.count > 0 { // 最后一个是最新数据
            //: currentModel = showLastArr.last!
            currentModel = showLastArr.last!
        }
        //: setPriceBtn()
        sumerval()
        //: picturesView.updatePicturesView(model: currentModel)
        picturesView.clickUser(model: currentModel)
        //: picturesView.matchPlayVideoBlock = { [weak self] (videoUrl) in
        picturesView.matchPlayVideoBlock = { [weak self] videoUrl in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let vc = TalkingMomentVideoVC.init(videoPath: videoUrl)
            let vc = HaltViewController(videoPath: videoUrl)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.nowController()?.navigationController?.pushViewController(vc, animated: true)
        }

        //: showLastBtn.isHidden = (showLastArr.count >= 2) ? false:true
        showLastBtn.isHidden = (showLastArr.count >= 2) ? false : true

        // 审核模式隐藏金币
        //: if SubLabelReactiveCompatible.share.appStatus != AppSkinStatus.normal.rawValue {
        if SubLabelReactiveCompatible.share.appStatus != MakePropertyProtocol.normal.rawValue {
            //: let coinBtn = self.navView.viewWithTag(1001) as! TalkingButton
            let coinBtn = self.navView.viewWithTag(1001) as! OfTalkingButton
            //: coinBtn.isHidden = true
            coinBtn.isHidden = true
            //: setPriceBtn()
            sumerval()
        }
    }

    //: func setPriceBtn() {
    func sumerval() {
        //: let attrString = String.setPriceStr(fee: currentModel.fee, originalFee: currentModel.originalFee, freeCallTimes: SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes)
        let attrString = String.setPriceStr(fee: currentModel.fee, originalFee: currentModel.originalFee, freeCallTimes: SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes)
        //: callBtn.isHidden = false
        callBtn.isHidden = false
        //: callBtn.setAttributedTitle(attrString, for: .normal)
        callBtn.setAttributedTitle(attrString, for: .normal)
    }
}
