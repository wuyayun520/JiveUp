
//: Declare String Begin

/*: "#201E50" :*/
fileprivate let str_formatData:String = "lab"
fileprivate let str_selectedSendValue:[Character] = ["2","0","1","E","5","0"]

/*: "#1F1624" :*/
fileprivate let str_itemColorData:String = "#1F1624let view content type"

/*: "quick_video_topview" :*/
fileprivate let str_locationValue:String = "quick_item view lab"
fileprivate let str_bottomHalfValue:String = "to"
fileprivate let str_leadingName:[Character] = ["o","p","v","i","e","w"]

/*: "nav_back_black_nor" :*/
fileprivate let str_playerName:[Character] = ["n"]
fileprivate let str_popName:[Character] = ["a","v","_","b","a","c","k","_","b","l","a","c","k","_","n","o","r"]

/*: "Random Video" :*/
fileprivate let str_indexEqualContent:String = "size in stop self identityRandom"
fileprivate let str_actualManagerFrameTitle:[Character] = [" ","V","i","d","e","o"]

/*: "quick_video_explain" :*/
fileprivate let str_normalValue:String = "data temp length constraint leftquick_"
fileprivate let str_layerData:String = "expline"
fileprivate let str_warningName:[Character] = ["a","i","n"]

/*: "get json error" :*/
fileprivate let str_hiddenValue:String = "get jscontrol for hidden death"
fileprivate let str_labelData:String = "rtime"

/*: "Searching for perfect matches. please be patient." :*/
fileprivate let str_pageObserverData:[UInt8] = [0xf2,0xc4,0xc0,0xd3,0xc2,0xc9,0xc8,0xcf,0xc6,0x81,0xc7,0xce,0xd3,0x81,0xd1,0xc4,0xd3,0xc7,0xc4,0xc2,0xd5,0x81,0xcc,0xc0,0xd5,0xc2,0xc9,0xc4,0xd2,0x8f,0x81,0xd1,0xcd,0xc4,0xc0,0xd2,0xc4,0x81,0xc3,0xc4,0x81,0xd1,0xc0,0xd5,0xc8,0xc4,0xcf,0xd5,0x8f]

private func namePoint(block num: UInt8) -> UInt8 {
    return num ^ 161
}

/*: "Matching..." :*/
fileprivate let str_miniData:[Character] = ["M","a","t","c","h","i","n","g"]
fileprivate let str_tagHeadData:[Character] = [".",".","."]

/*: "matchId" :*/
fileprivate let str_hiddenName:String = "matchIactual edit pic self"
fileprivate let str_coverTextClothesValue:[Character] = ["d"]

/*: "Random Video helps locating your matches online quickly. Random Videos are priced at 40 gold coins per minute and are paid for by the party calling the video." :*/
fileprivate let str_observerName:[UInt8] = []

private func actualWithLayer(cell num: UInt8) -> UInt8 {
    return num ^ 6
}

/*: "OK" :*/
fileprivate let str_productName:String = "player"

/*: "onVideoMatch" :*/
fileprivate let str_makeValue:String = "onVidcolor table source"
fileprivate let str_saveImageValue:String = "lab"

/*: "retryAfter" :*/
fileprivate let str_resourceTitle:String = "retryAftif self self at perspective"
fileprivate let str_underName:String = "rawr"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LabelRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/4.
//

//: import UIKit
import UIKit

//: class TalkingQuickVideoMatchViewController: TalkingBaseViewController {
class LabelRecognizerDelegate: EqualVideoReactiveCompatible {
	var sideSum: Int = 48
	var beggarMyNeighborStrategyQuantity: Double = 84.3
	var offLabelSum: Int = 76
	var hideAtCount: Double = 10.7

    //: private var headTimer: Timer?
    private var headTimer: Timer? // 头像展示定时器
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: req_videoMatchHeadPics()
        stopView()
        //: startVideoMatch()
        blowup()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: stopVideoMatch()
        dataStop()
    
		
		//: if_extract_code "labelNo" begin
		
		var labelNo = false
		if !labelNo {
		    var isValue = false
		    if let nextResponder = bgImgView.next {
		        isValue = nextResponder.inputView != nil
		    }
		    labelNo = isValue
		}
		
		//: if_extract_code "labelNo" end
		
            if (labelNo) && (bgImgView.layer.position.y == 32.91) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let blockHitShow = LiveUpView()
            blockHitShow.permissionClose = animated

            
            blockHitShow.distanceCount = { [self] itemNumber in
            self.offLabelSum = itemNumber
            
            var blockHitShow = matchId
            blockHitShow &*= 6
            if blockHitShow < self.offLabelSum {
                self.offLabelSum = blockHitShow
            }
            
            return self.offLabelSum
            }
            blockHitShow.stillPathNumber = { [self] dataInterval in
            self.hideAtCount = dataInterval
            
            return self.hideAtCount
            }
                bgImgView.addSubview(blockHitShow)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        tarotCard()
        //: setupSubViewsConstraint()
        constraintSize()
    
            if (headImgView.isFirstResponder) && (headImgView.userActivity != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let contextAwake = LiveUpView(frame: headImgView.frame)
            contextAwake.permissionClose = self.hideNavi

            
            contextAwake.distanceCount = { [self] itemNumber in
            self.sideSum = itemNumber
            
            var contextAwake = matchId
            contextAwake &= 5
            if contextAwake > self.sideSum {
                self.sideSum = contextAwake
            }
            
            return self.sideSum
            }
            contextAwake.stillPathNumber = { [self] dataInterval in
            self.beggarMyNeighborStrategyQuantity = dataInterval
            
            return self.beggarMyNeighborStrategyQuantity
            }
                headImgView.addSubview(contextAwake)
            }

	}

    //: deinit {
    deinit {
        //: stopVideoMatch()
        dataStop()
    }

    // MARK: - Lazy Load

    //: private lazy var bgImgView: UIImageView = {
    private lazy var bgImgView: UIImageView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.image = UIImage.imageGradientTopToBottonColor(colors: [UIColor.init(hex: "#201E50")!.cgColor, UIColor.init(hex: "#1F1624")!.cgColor], size: CGSize(width: ScreenWidth, height: ScreenHeight))
        view.image = UIImage.enableLineSize(colors: [UIColor(hex: (str_formatData.replacingOccurrences(of: "lab", with: "#") + String(str_selectedSendValue)))!.cgColor, UIColor(hex: (String(str_itemColorData.prefix(7))))!.cgColor], size: CGSize(width: kLet_failureName, height: kLet_appEventDomainName))
        //: view.isUserInteractionEnabled = true
        view.isUserInteractionEnabled = true

        //: let topView = UIImageView.init()
        let topView = UIImageView()
        //: topView.image = UIImage.nameTitle(name: "quick_video_topview")
        topView.image = UIImage.nameTitle(name: (String(str_locationValue.prefix(6)) + "video_" + str_bottomHalfValue.replacingOccurrences(of: "to", with: "t") + String(str_leadingName)))
        //: topView.isUserInteractionEnabled = true
        topView.isUserInteractionEnabled = true
        //: view.addSubview(topView)
        view.addSubview(topView)
        //: topView.snp.makeConstraints { make in
        topView.snp.makeConstraints { make in
            //: make.leading.top.equalTo(0)
            make.leading.top.equalTo(0)
            //: make.width.height.equalTo(actualWidth(w: 218))
            make.width.height.equalTo(actualWidth(w: 218))
        }
        //: return view
        return view
        //: }()
    }()

    //: private lazy var navView: UIView = {
    private lazy var navView: UIView = {
        //: let nav = UIView()
        let nav = UIView()
        //: nav.backgroundColor = .clear
        nav.backgroundColor = .clear
        // 返回
        //: let backBtn = UIButton(type: .custom)
        let backBtn = UIButton(type: .custom)
        //: let backBImg = UIImage.nameTitle(name: "nav_back_black_nor").withRenderingMode(.alwaysTemplate)
        let backBImg = UIImage.nameTitle(name: (String(str_playerName) + String(str_popName))).withRenderingMode(.alwaysTemplate)
        //: backBtn.setImage(backBImg, for: .normal)
        backBtn.setImage(backBImg, for: .normal)
        //: backBtn.tintColor = .white
        backBtn.tintColor = .white
        //: backBtn.addTarget(self, action: #selector(clickBackButtonAction), for: .touchUpInside)
        backBtn.addTarget(self, action: #selector(reasonLogAction), for: .touchUpInside)
        //: nav.addSubview(backBtn)
        nav.addSubview(backBtn)
        //: backBtn.snp.makeConstraints { make in
        backBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(0)
            make.leading.equalTo(0)
            //: make.width.equalTo(40)
            make.width.equalTo(40)
            //: make.height.equalTo(NavigationBarHeight)
            make.height.equalTo(kLet_timeText)
            //: make.top.equalTo(StatusBarHeight)
            make.top.equalTo(kLet_messageSumervalName)
        }
        // 标题
        //: let titleLab = UILabel()
        let titleLab = UILabel()
        //: titleLab.font = UIFont.underPlay(fontSize: 17)
        titleLab.font = UIFont.underPlay(fontSize: 17)
        //: titleLab.textColor = .white
        titleLab.textColor = .white
        //: titleLab.textAlignment = .center
        titleLab.textAlignment = .center
        //: titleLab.text = "Random Video".localized
        titleLab.text = (String(str_indexEqualContent.suffix(6)) + String(str_actualManagerFrameTitle)).localized
        //: nav.addSubview(titleLab)
        nav.addSubview(titleLab)
        //: titleLab.snp.makeConstraints { make in
        titleLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(backBtn)
            make.centerY.equalTo(backBtn)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
        // 提示（审核模式隐藏）
        //: if SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue {
            //: let matchTipsBtn = UIButton(type: .custom)
            let matchTipsBtn = UIButton(type: .custom)
            //: let matchTipsImg = UIImage.nameTitle(name: "quick_video_explain").withRenderingMode(.alwaysTemplate)
            let matchTipsImg = UIImage.nameTitle(name: (String(str_normalValue.suffix(6)) + "video_" + str_layerData.replacingOccurrences(of: "line", with: "l") + String(str_warningName))).withRenderingMode(.alwaysTemplate)
            //: matchTipsBtn.setImage(matchTipsImg, for: .normal)
            matchTipsBtn.setImage(matchTipsImg, for: .normal)
            //: matchTipsBtn.tintColor = .white
            matchTipsBtn.tintColor = .white
            //: matchTipsBtn.addTarget(self, action: #selector(matchTipsButtonClick), for: .touchUpInside)
            matchTipsBtn.addTarget(self, action: #selector(ok), for: .touchUpInside)
            //: nav.addSubview(matchTipsBtn)
            nav.addSubview(matchTipsBtn)
            //: matchTipsBtn.snp.makeConstraints { make in
            matchTipsBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(0)
                make.trailing.equalTo(0)
                //: make.centerY.equalTo(backBtn)
                make.centerY.equalTo(backBtn)
                //: make.size.equalTo(CGSize(width: 45, height: NavigationBarHeight))
                make.size.equalTo(CGSize(width: 45, height: kLet_timeText))
            }
        }

        //: return nav
        return nav
        //: }()
    }()

    //: lazy var svgaPlayer: SVGAPlayer = {
    lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .Quick_Video_BG)
        let url = PuncherEffectTool.default.betweenPath(type: .Quick_Video_BG)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_hiddenValue.prefix(6)) + "on er" + str_labelData.replacingOccurrences(of: "time", with: "or")))
        }

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.font = UIFont.rugularSize(fontSize: 15)
        lab.font = UIFont.rugularSize(fontSize: 15)
        //: lab.textColor = UIColor.init(white: 1, alpha: 0.7)
        lab.textColor = UIColor(white: 1, alpha: 0.7)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Searching for perfect matches. please be patient.".localized
        lab.text = String(bytes: str_pageObserverData.map{namePoint(block: $0)}, encoding: .utf8)!.localized
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var matchingLab: UILabel = {
    private lazy var matchingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.font = UIFont.underPlay(fontSize: 18)
        lab.font = UIFont.underPlay(fontSize: 18)
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Matching...".localized
        lab.text = (String(str_miniData) + String(str_tagHeadData)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var headImgView: UIImageView = {
    private lazy var headImgView: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: img.layer.cornerRadius = actualWidth(w: 90)/2
        img.layer.cornerRadius = actualWidth(w: 90) / 2
        //: img.image = UIImage.placeHolderImage(sex: Gender.female.rawValue)
        img.image = UIImage.barrelFromHeterosexual(sex: CheckedUTF8Initializable.female.rawValue)
        //: img.layer.masksToBounds = true
        img.layer.masksToBounds = true
        //: return img
        return img
        //: }()
    }()

    //: private lazy var dataArr: [String] = {
    private lazy var dataArr: [String] = //: return Array<String>()
        .init()
    //: }()

    //: private lazy var resultDataArr: [TalkingMatchResultModel] = {
    private lazy var resultDataArr: [RowHandyJSON] = //: return Array<RowHandyJSON>()
        .init()
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingQuickVideoMatchViewController {
extension LabelRecognizerDelegate {
    /// 速配头像列表（男）
    //: private func req_videoMatchHeadPics() {
    private func stopView() {
        //: TalkingQuickVideoManager.req_videoMatchHeadPics { [weak self] succeed, result, errorModel in
        DoReactiveCompatible.offCollection { [weak self] succeed, result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed == true else { return }
            guard succeed == true else { return }

            //: self.dataArr.removeAll()
            self.dataArr.removeAll()
            //: self.dataArr = result as! [String]
            self.dataArr = result as! [String]

            //: var index = 0
            var index = 0
            //: self.headTimer = Timer.scheduledTimer(withTimeInterval: 5, block: { timer in
            self.headTimer = Timer.scheduledTimer(withTimeInterval: 5, block: { _ in
                // 实现无限循环
                //: if index >= self.dataArr.count { index = 0 }
                if index >= self.dataArr.count { index = 0 }
                // 网络加载图片，实现高斯模糊效果
                //: self.headImgView.setUrlImage(urlStr: self.dataArr[index]) { isSucceed in
                self.headImgView.clap(urlStr: self.dataArr[index]) { _ in
                    //: guard let image = self.headImgView.image else { return }
                    guard let image = self.headImgView.image else { return }
                    //: self.headImgView.image = UIImage.blur(theImage: image, blurValue: 17)
                    self.headImgView.image = UIImage.roundTarget(theImage: image, blurValue: 17)
                }
                //: index += 1
                index += 1
                //: }, repeats: true)
            }, repeats: true)
            //: self.headTimer?.fire()
            self.headTimer?.fire()
        }
    }

    /// 开始视频速配（男）
    //: private func req_videoMatchToMatch() {
    private func small() {
        //: TalkingQuickVideoManager.req_videoMatchToMatch(matchId: self.matchId) { succeed, result, errorModel in
        DoReactiveCompatible.telecastingUponCompletion(matchId: self.matchId) { succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(str_hiddenName.prefix(6)) + String(str_coverTextClothesValue))].intValue
        }
    }
}

// MARK: - Event

//: extension TalkingQuickVideoMatchViewController {
extension LabelRecognizerDelegate {
    /// 停止视频速配
    //: private func stopVideoMatch() {
    private func dataStop() {
        // 销毁定时器
        //: if self.headTimer != nil {
        if self.headTimer != nil {
            //: self.headTimer?.invalidate()
            self.headTimer?.invalidate()
            //: self.headTimer = nil
            self.headTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }

        //: TalkingSocketManager.shared.videoMatchDelegate = nil
        TitleSocketManager.shared.videoMatchDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        TitleSocketManager.shared.errorDelegate = nil
    }

    /// 开始视频速配
    //: private func startVideoMatch() {
    private func blowup() {
        //: TalkingSocketManager.shared.videoMatchDelegate = self
        TitleSocketManager.shared.videoMatchDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        TitleSocketManager.shared.errorDelegate = self
        //: req_videoMatchToMatch()
        small()
    }

    /// 返回按钮
    //: @objc private func clickBackButtonAction() {
    @objc private func reasonLogAction() {
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 打开视频速配说明
    //: @objc private func matchTipsButtonClick() {
    @objc private func ok() {
        //: let message = "Random Video helps locating your matches online quickly. Random Videos are priced at 40 gold coins per minute and are paid for by the party calling the video.".localized
        let message = String(bytes: str_observerName.map{actualWithLayer(cell: $0)}, encoding: .utf8)!.localized
        //: TalkingAlertShow.alert(title: nil, message: message, rightBtnTitle: "OK".localized)
        TalkingCostExhibitThen.titleConfig(title: nil, message: message, rightBtnTitle: "OK".localized)
    }
}

// MARK: - TalkingSocketManagerDelegate

//: extension TalkingQuickVideoMatchViewController: SocketManagerVideoMatchDelegate {
extension LabelRecognizerDelegate: VisualCommunicationObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func playerEqual(data: [String: Any]) {
        // 停止速配，跳转速配结果界面
        //: stopVideoMatch()
        dataStop()
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: guard let topVC = self.navigationController?.topViewController else { return }
            guard let topVC = self.navigationController?.topViewController else { return }
            //: guard !topVC.isKind(of: TalkingMatchResultViewController.self) else { return }
            guard !topVC.isKind(of: ImageRowViewController.self) else { return }
            //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
            guard let model = RowHandyJSON.deserialize(from: data) else { return }

            // v1.0.6 订阅用户 当前视频速配成功的次数大于等于2，则在其匹配中页面额外展示回放按钮
            //: if SubLabelReactiveCompatible.share.loginUserMode.loungePlus == true || SubLabelReactiveCompatible.share.loginUserMode.videoPlayback == true {
            if SubLabelReactiveCompatible.share.loginUserMode.loungePlus == true || SubLabelReactiveCompatible.share.loginUserMode.videoPlayback == true {
                //: if self.resultDataArr.count >= 2 {
                if self.resultDataArr.count >= 2 {
                    //: self.resultDataArr.removeFirst()
                    self.resultDataArr.removeFirst()
                }
                //: self.resultDataArr.append(model)
                self.resultDataArr.append(model)

                //: } else {
            } else { // 非订阅用户只展示一条
                //: self.resultDataArr.removeAll()
                self.resultDataArr.removeAll()
                //: self.resultDataArr.append(model)
                self.resultDataArr.append(model)
            }

            //: let vc = TalkingMatchResultViewController()
            let vc = ImageRowViewController()
            //: vc.showLastArr = self.resultDataArr
            vc.showLastArr = self.resultDataArr
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
        }
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {}
    func topic(data _: [String: Any]) {}
}

// MARK: - DataObjectProtocol

//: extension TalkingQuickVideoMatchViewController: TalkingSocketManagerErrorDelegate {
extension LabelRecognizerDelegate: DataObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func choiceHandle(errorNo: Int, cmd: String, msg _: String, data: [String: Any]) {
        //: if cmd == "onVideoMatch" && errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(str_makeValue.prefix(5)) + "eoMatc" + str_saveImageValue.replacingOccurrences(of: "lab", with: "h")) && errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(str_resourceTitle.prefix(8)) + str_underName.replacingOccurrences(of: "raw", with: "e"))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(str_hiddenName.prefix(6)) + String(str_coverTextClothesValue))].intValue
                //: self.startVideoMatch()
                self.blowup()
                //: }, repeats: false)
            }, repeats: false)
        }
    }
}

// MARK: - Layout

//: extension TalkingQuickVideoMatchViewController {
extension LabelRecognizerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func tarotCard() {
        //: self.view.addSubview(bgImgView)
        self.view.addSubview(bgImgView)
        //: self.view.addSubview(navView)
        self.view.addSubview(navView)
        //: self.view.addSubview(svgaPlayer)
        self.view.addSubview(svgaPlayer)
        //: self.view.addSubview(tipsLab)
        self.view.addSubview(tipsLab)
        //: self.view.addSubview(matchingLab)
        self.view.addSubview(matchingLab)
        //: self.view.addSubview(headImgView)
        self.view.addSubview(headImgView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func constraintSize() {
        //: bgImgView.snp.makeConstraints { make in
        bgImgView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.top.width.equalToSuperview()
            make.leading.top.width.equalToSuperview()
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(kLet_soundData)
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.top.equalTo(navView.snp.bottom).offset(actualWidth(w: 50))
            make.top.equalTo(navView.snp.bottom).offset(actualWidth(w: 50))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: ScreenWidth, height: ScreenWidth))
            make.size.equalTo(CGSize(width: kLet_failureName, height: kLet_failureName))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalToSuperview().offset(-88-kDeviceSafeBottomHeight)
            make.bottom.equalToSuperview().offset(-88 - kLet_changeBarValue)
        }
        //: matchingLab.snp.makeConstraints { make in
        matchingLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(tipsLab.snp.top).offset(-20)
            make.bottom.equalTo(tipsLab.snp.top).offset(-20)
        }
        //: headImgView.snp.makeConstraints { make in
        headImgView.snp.makeConstraints { make in
            //: make.center.equalTo(svgaPlayer)
            make.center.equalTo(svgaPlayer)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 90), height: actualWidth(w: 90)))
            make.size.equalTo(CGSize(width: actualWidth(w: 90), height: actualWidth(w: 90)))
        }
    }
}
