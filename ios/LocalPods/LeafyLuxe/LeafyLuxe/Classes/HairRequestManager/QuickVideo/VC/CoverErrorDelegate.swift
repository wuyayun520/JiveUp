
//: Declare String Begin

/*: "cmd" :*/
fileprivate let str_makeGroupContent:[UInt8] = [0xef,0xe1,0xe8]

private func labelState(control num: UInt8) -> UInt8 {
    return num ^ 140
}

/*: "cancelCall" :*/
fileprivate let str_freeData:String = "caapplication"
fileprivate let str_tapValue:String = "toto"

/*: "data" :*/
fileprivate let str_nameHiddenByValue:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "logId" :*/
fileprivate let str_labelName:[Character] = ["l","o","g","I","d"]

/*: "#322870" :*/
fileprivate let str_transformName:String = "#322870"

/*: "Tap to start video chatting" :*/
fileprivate let str_visibleContent:[Character] = ["T","a","p"," ","t","o"," ","s","t","a","r","t"," ","v","i","d","e","o"," ","c","h","a","t","t"]
fileprivate let str_touchData:String = "makeng"

/*: "icon_close_nor" :*/
fileprivate let str_blueMemberValue:[Character] = ["i","c","o","n","_","c","l","o","s","e","_","n","o"]
fileprivate let str_labelValue:String = "full"

/*: "icon_views_number" :*/
fileprivate let str_nameStyleValue:[Character] = ["i","c","o","n","_","v","i","e","w","s","_","n","u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let str_wrapContent:String = "%@ chattof main top class"
fileprivate let str_toSizeValue:[Character] = ["i","n","g"]

/*: "icon_lounge" :*/
fileprivate let str_managerUserValue:String = "icon_loself path task white"
fileprivate let str_centerViewValue:String = "untext"

/*: "icon_rank_coin" :*/
fileprivate let str_mainName:String = "user type byicon_rank"
fileprivate let str_acrossTitle:String = "_coinmake file kit make by"

/*: "vip_btn_top" :*/
fileprivate let str_toReloadValue:[Character] = ["v","i","p","_","b","t","n","_","t","o"]
fileprivate let str_directionData:[Character] = ["p"]

/*: "btn_nearby_settings_nor" :*/
fileprivate let str_numberSystemText:String = "color tool extension forbtn_ne"
fileprivate let str_playerData:[Character] = ["a","r","b"]
fileprivate let str_successSharedValue:[Character] = ["y","_","s","e","t","t","i","n","g","s","_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let str_appValue:String = "%@ Coequal true tip app"
fileprivate let str_remarkName:[Character] = ["i","n","s","/","m","i","n"]

/*: "get json error" :*/
fileprivate let str_greetRowValue:[Character] = ["g","e","t"]
fileprivate let str_storeIndexData:String = "head remark make type for json "

/*: "matchId" :*/
fileprivate let str_makeText:[Character] = ["m","a","t","c","h","I","d"]

/*: "errno" :*/
fileprivate let str_regularData:[UInt8] = [0xa5,0xb2,0xb2,0xae,0xaf]

private func secondWho(by num: UInt8) -> UInt8 {
    return num ^ 192
}

/*: "VIPFee" :*/
fileprivate let str_targetValue:String = "VIPFeeto show pic"

/*: "Matching now" :*/
fileprivate let str_scaleValue:[Character] = ["M","a","t","c","h","i"]
fileprivate let str_itemIconValueTitle:String = "name"
fileprivate let str_labElementEventTitle:String = "g nowkit block"

/*: "userTouch" :*/
fileprivate let str_incomeValue:[Character] = ["u","s","e","r","T","o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let str_giftUserData:String = "mf/frecolor constraint"
fileprivate let str_localData:String = "Viview end"
fileprivate let str_failureTitle:[Character] = ["d","e","o","I","n","f","o"]

/*: "fromType" :*/
fileprivate let str_targetName:[UInt8] = [0x65,0x70,0x79,0x54,0x6d,0x6f,0x72,0x66]

/*: "You've been barred from receiving calls" :*/
fileprivate let str_checkContent:[UInt8] = [0x33,0x49,0x4f,0x1,0x50,0x3f,0xfa,0x3c,0x3f,0x3f,0x48,0xfa,0x3c,0x3b,0x4c,0x4c,0x3f,0x3e,0xfa,0x40,0x4c,0x49,0x47,0xfa,0x4c,0x3f,0x3d,0x3f,0x43,0x50,0x43,0x48,0x41,0xfa,0x3d,0x3b,0x46,0x46,0x4d]

fileprivate func textualMatter(leading num: UInt8) -> UInt8 {
    let value = Int(num) + 38
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "source" :*/
fileprivate let str_unlessValue:[UInt8] = [0x65,0x63,0x72,0x75,0x6f,0x73]

/*: "type" :*/
fileprivate let str_pickTitle:[UInt8] = [0xb1,0xb6,0xad,0xa2]

fileprivate func rawAdd(app num: UInt8) -> UInt8 {
    let value = Int(num) - 61
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let str_failSnapText:[UInt8] = [0xfe,0xf2,0xed]

fileprivate func textExtra(bar num: UInt8) -> UInt8 {
    let value = Int(num) - 137
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "fromFreeCall" :*/
fileprivate let str_editionData:String = "fromFreeon color else message gift"
fileprivate let str_enterData:String = "Calltype view"

/*: "requestCall" :*/
fileprivate let str_cornerUpName:[Character] = ["r","e","q","u","e","s"]
fileprivate let str_leadingData:String = "data voice make voicetCall"

/*: "onVideoMatch" :*/
fileprivate let str_selectText:String = "string size make inside corneronVi"
fileprivate let str_addWithSendTitle:String = "total"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let str_subValue:[UInt8] = [0xc8,0xd2,0xe6,0xe7,0xfc,0xfe,0xf2,0xe7,0xfa,0xf0,0xb3,0xe5,0xfa,0xf7,0xf6,0xfc,0xce,0xb3,0xc1,0xf6,0xf0,0xf6,0xfa,0xe5,0xf6,0xb3,0xe5,0xfa,0xf7,0xf6,0xfc,0xb3,0xf0,0xf2,0xff,0xff,0xb3,0xfa,0xfd,0xf0,0xfc,0xfe,0xfa,0xfd,0xf4]

/*: "refuseCall" :*/
fileprivate let str_nativeValue:String = "effectfu"

/*: "onCancelCall" :*/
fileprivate let str_whiteDismissSuperValue:[Character] = ["o","n","C","a"]
fileprivate let str_appData:String = "ncelCallcontent i"

/*: "retryAfter" :*/
fileprivate let str_locationValue:String = "retryAfself lab"
fileprivate let str_blockSizeVarName:String = "tstopr"

/*: "onRequestCall" :*/
fileprivate let str_upDeviceTitle:String = "label"
fileprivate let str_signalData:[Character] = ["n","R","e","q","u","e","s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let str_shareText:String = "data modelfailType"

/*: "callWaitingTime" :*/
fileprivate let str_proData:String = "callWtime view i show block"
fileprivate let str_pathLengthName:String = "name"

/*: "toUid" :*/
fileprivate let str_hiddenValue:String = "toUiduser insert key make"

/*: "Free 1 min" :*/
fileprivate let str_tapMenuName:[Character] = ["F","r"]
fileprivate let str_topName:String = "ee 1 minelse photo in equal"

/*: "%@/min" :*/
fileprivate let str_containerInfoData:String = "%@/mintitle status"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CoverErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class CoverErrorDelegate: EqualVideoReactiveCompatible {
	var untilQuantity: Int = 77
	var postDictionary: [AnyHashable: String] = [:]
	var targetQuantity: Int = 84
	var observerModelShowDictionary: [AnyHashable: String] = [:]

    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.toHandle()
            }
        }
        //: self.resetCamera()
        self.methodCamera()
    
		if var fromValue = videoManager.infoModel?.pairNickName { 
	            if (norPriceLab.contentScaleFactor == 2.85) && (norPriceLab.canBecomeFirstResponder) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let constraintLet = CurriculumView(frame: norPriceLab.frame.insetBy(dx: CGFloat(599.99), dy: CGFloat(366.58)))
	            constraintLet.awakeQuantity = chattingNum
	
	            constraintLet.smartContent = fromValue
	            
	            
	            constraintLet.inheritTotal = { [self] observerEnableCount in
	            self.untilQuantity = observerEnableCount
	            
	            var constraintLet = vipFee
	            constraintLet &<<= 1
	            if constraintLet < self.untilQuantity {
	                self.untilQuantity = constraintLet
	            }
	            
	            return self.untilQuantity
	            }
	            constraintLet.alongDictionary = { [self] groupDictionary in
	            self.postDictionary = groupDictionary
	            
	            guard var value = self.postDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                norPriceLab.addSubview(constraintLet)
	            }
	
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        miscalculation()
        //: setupSubViewsConstraint()
        notPost()
        //: bindInteraction()
        showData()
        //: videoManager.startPreview(isMatch: true)
        videoManager.eventShared(isMatch: true)
        //: setPlayer(isRandon: false)
        appearRandon(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            auspicateListVideo()
        }
    
		if var bagValue = videoManager.infoModel?.pairNickName { 
	            if (closeBtn.convert(CGRect(x: 0, y: 0, width: CGFloat(95), height: 0), to: closeBtn.superview).origin.x == 36.05) && (closeBtn.layer.contentsRect.size.height != 1) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let equalMatch = CurriculumView()
	            equalMatch.awakeQuantity = chattingNum
	
	            equalMatch.smartContent = bagValue
	            
	            
	            equalMatch.inheritTotal = { [self] observerEnableCount in
	            self.targetQuantity = observerEnableCount
	            
	            var equalMatch = self.fee
	                equalMatch += 1
	                if Int(equalMatch) > -14 {
	                    equalMatch = equalMatch - 1
	                }
	            if equalMatch < self.targetQuantity {
	                self.targetQuantity = equalMatch
	            }
	            
	            return self.targetQuantity
	            }
	            equalMatch.alongDictionary = { [self] groupDictionary in
	            self.observerModelShowDictionary = groupDictionary
	            
	            guard var value = self.observerModelShowDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                closeBtn.addSubview(equalMatch)
	            }
	
		}
	}

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            TitleSocketManager.shared.fromApp(info: [String(bytes: str_makeGroupContent.map{labelState(control: $0)}, encoding: .utf8)!: (str_freeData.replacingOccurrences(of: "application", with: "nc") + "elCa" + str_tapValue.replacingOccurrences(of: "to", with: "l")), String(bytes: str_nameHiddenByValue.reversed(), encoding: .utf8)!: [(String(str_labelName)): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        imaginationImage()
        //: if TalkingSocketManager.shared.isTalking == false {
        if TitleSocketManager.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            TitleSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            TitleSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            TitleSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            TitleSocketManager.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ServerViewThen = {
        //: let m = TalkingVideoChatManager()
        let m = ServerViewThen()
        //: let info = TalkingVideoChatModel()
        let info = ProfessionalChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .elementColor()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(auspicateListVideo), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: kLet_failureName + 1, height: kLet_appEventDomainName + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (str_transformName.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(str_visibleContent) + str_touchData.replacingOccurrences(of: "make", with: "i")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nameTitle(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_blueMemberValue) + str_labelValue.replacingOccurrences(of: "full", with: "r"))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(currentClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: OfTalkingButton = {
        //: let btn = TalkingButton()
        let btn = OfTalkingButton()
        //: btn.setImage(UIImage.nameTitle(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.nameTitle(name: (String(str_nameStyleValue))), for: .normal)
        //: btn.setImage(UIImage.nameTitle(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.nameTitle(name: (String(str_nameStyleValue))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(str_wrapContent.prefix(8)) + String(str_toSizeValue)).localizedArguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.nameTitle(name: "icon_lounge")
        img.image = UIImage.nameTitle(name: (String(str_managerUserValue.prefix(7)) + str_centerViewValue.replacingOccurrences(of: "text", with: "ge")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.nameTitle(name: "icon_rank_coin")
        img.image = UIImage.nameTitle(name: (String(str_mainName.suffix(9)) + String(str_acrossTitle.prefix(5))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 15)
        lab.font = UIFont.magnitudeimateBy(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.nameTitle(name: "vip_btn_top")
        imgV.image = UIImage.nameTitle(name: (String(str_toReloadValue) + String(str_directionData)))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: OfTalkingButton = {
        //: let btn = TalkingButton()
        let btn = OfTalkingButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(upSnap), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.nameTitle(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.nameTitle(name: (String(str_numberSystemText.suffix(6)) + String(str_playerData) + String(str_successSharedValue))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.nameTitle(name: "icon_rank_coin")
        imgV.image = UIImage.nameTitle(name: (String(str_mainName.suffix(9)) + String(str_acrossTitle.prefix(5))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .requestUser()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(str_appValue.prefix(5)) + String(str_remarkName)).localizedArguments(vipFee)
        //: lab.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 15)
        lab.font = UIFont.magnitudeimateBy(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: InsideReactiveCompatible = //: return InsideReactiveCompatible(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension CoverErrorDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func appearRandon(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = PuncherEffectTool.default.betweenPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_greetRowValue) + String(str_storeIndexData.suffix(6)) + "error"))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func makeErrno(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(str_makeText)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: str_makeGroupContent.map{labelState(control: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: str_regularData.map{secondWho(by: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        DoReactiveCompatible.paintTheLily(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.shirtButtonVideo()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(str_makeText))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func sharedAd() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        DoReactiveCompatible.analyseCompletion { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(str_targetValue.prefix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.toHandle()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension CoverErrorDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func upSnap() {
        //: TalkingPushManager.share.func__pushToSubscribeAlert()
        TalkingPushManager.share.statusSumimate()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func currentClick() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        board()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kLet_postName.paper(eventID: kLet_contentData)

        //: if SubLabelReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
        if SubLabelReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.refreshShow()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        nowController()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func auspicateListVideo() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard TitleSocketManager.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.barLog(showMsg: kLet_keyMessageName)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        groupWith()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(str_scaleValue) + str_itemIconValueTitle.replacingOccurrences(of: "name", with: "n") + String(str_labElementEventTitle.prefix(5))).localized
        //: setPlayer(isRandon: true)
        appearRandon(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if SubLabelReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
        if SubLabelReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kLet_postName.paper(eventID: kLet_barPartyValue)
            //: req_getVideoInfo()
            infoAdd()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kLet_postName.paper(eventID: kLet_numberContent)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            TitleSocketManager.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            TitleSocketManager.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            TitleSocketManager.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            TitleSocketManager.shared.errorDelegate = self
            //: restart_randomVideo()
            byIcon()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func byIcon(cmd: String = (String(str_incomeValue)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        imaginationImage()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        makeErrno(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func shirtButtonVideo() {
        // 停止定时器
        //: destroyWaitingTimer()
        imaginationImage()
        //: showCloseBtn()
        board()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(str_visibleContent) + str_touchData.replacingOccurrences(of: "make", with: "i")).localized
        //: setPlayer(isRandon: false)
        appearRandon(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isTalking == false {
        if TitleSocketManager.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            TitleSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            TitleSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            TitleSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            TitleSocketManager.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        toHandle()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func imaginationImage() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func infoAdd() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ClearRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(str_giftUserData.prefix(6)) + "eCall/get" + String(str_localData.prefix(2)) + String(str_failureTitle))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: str_targetName.reversed(), encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_informationStatusTitle.theAll(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.shirtButtonVideo()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: SubLabelReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    SubLabelReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.auspicateListVideo()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.barLog(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = StatuteNameTransformable.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = FinishErasePlayerManager()
                //: player.setMute(bEnable: true)
                player.bigness(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.nose(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                TitleCallManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                AccountingDataReactiveCompatible.ofMicrophone { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.digitiserModel()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func digitiserModel() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kLet_postName.paper(eventID: kLet_readName)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard TitleCallManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.barLog(showMsg: String(bytes: str_checkContent.map{textualMatter(leading: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            TitleCallManager.shared.groupCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.shirtButtonVideo()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = RenameReactiveCompatible()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.nowController()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.shirtButtonVideo()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func groupWith() {
        //: if SubLabelReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if SubLabelReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(SubLabelReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(SubLabelReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func board() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func methodCamera() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.eventShared()
        }
    }
}

// MARK: - VisualCommunicationObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension CoverErrorDelegate: VisualCommunicationObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func playerEqual(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = RowHandyJSON.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(str_makeText)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: str_unlessValue.reversed(), encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: str_pickTitle.map{rawAdd(app: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: str_failSnapText.map{textExtra(bar: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = LabelAdditiveArithmetic.randow
        //: if SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && SubLabelReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && SubLabelReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(str_editionData.prefix(8)) + String(str_enterData.prefix(4))))
            //: TalkingSocketManager.shared.isFreeCall = true
            TitleSocketManager.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: str_makeGroupContent.map{labelState(control: $0)}, encoding: .utf8)!: (String(str_cornerUpName) + String(str_leadingData.suffix(5))), String(bytes: str_nameHiddenByValue.reversed(), encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        TitleSocketManager.shared.fromApp(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            TitleSocketManager.shared.fromApp(info: [String(bytes: str_makeGroupContent.map{labelState(control: $0)}, encoding: .utf8)!: (str_freeData.replacingOccurrences(of: "application", with: "nc") + "elCa" + str_tapValue.replacingOccurrences(of: "to", with: "l")), String(bytes: str_nameHiddenByValue.reversed(), encoding: .utf8)!: [(String(str_labelName)): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.byIcon(cmd: (String(str_selectText.suffix(4)) + "deoMatc" + str_addWithSendTitle.replacingOccurrences(of: "total", with: "h")))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func topic(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(str_labelName))].intValue
    }
}

// MARK: - RowObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension CoverErrorDelegate: RowObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func laughDoing(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func smolder(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func hemAndHaw(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func toStart(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            ShowLetterHelperBaseDelegate.localWait(body: String(bytes: str_subValue.map{$0^147}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = RowHandyJSON.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            roundObject(toUid: String(uid))
        }
        //: stop_randomVideo()
        shirtButtonVideo()
        //: SubLabelReactiveCompatible.share.start1v1TalkCall(info: data, situation: .Calling)
        SubLabelReactiveCompatible.share.localSituation(info: data, situation: .Calling)
    }
}

// MARK: - LabelObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension CoverErrorDelegate: LabelObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func socketTableAssemblage(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        byIcon(cmd: (str_nativeValue.replacingOccurrences(of: "effect", with: "re") + "seCall"))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func pushData(data _: [String: Any]) {}
}

// MARK: - DataObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension CoverErrorDelegate: DataObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func choiceHandle(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(str_whiteDismissSuperValue) + String(str_appData.prefix(8))) { // 取消通话异常
            //: stop_randomVideo()
            shirtButtonVideo()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(str_selectText.suffix(4)) + "deoMatc" + str_addWithSendTitle.replacingOccurrences(of: "total", with: "h")), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            shirtButtonVideo()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(str_makeText))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.barLog(showMsg: msg)
            //: uploadFailTime(type: 2)
            tabEnter(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(str_selectText.suffix(4)) + "deoMatc" + str_addWithSendTitle.replacingOccurrences(of: "total", with: "h")), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(str_locationValue.prefix(7)) + str_blockSizeVarName.replacingOccurrences(of: "stop", with: "e"))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(str_makeText))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.byIcon(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (str_upDeviceTitle.replacingOccurrences(of: "label", with: "o") + String(str_signalData)), errorNo == VendorRelatableTarget.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            shirtButtonVideo()
            //: guard SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue else { return }
            //: TalkingPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            TalkingPushManager.share.withParams(clickEvent: kLet_sessionTitle, sufficient: false)
            //: uploadFailTime(type: 1)
            tabEnter(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        byIcon(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension CoverErrorDelegate {
    //: func uploadFailTime(type: Int) {
    func tabEnter(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(str_shareText.suffix(8)))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(str_proData.prefix(5)) + "aitingTim" + str_pathLengthName.replacingOccurrences(of: "name", with: "e"))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kLet_postName.cognizant(eventID: kLet_progressData, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func roundObject(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(str_proData.prefix(5)) + "aitingTim" + str_pathLengthName.replacingOccurrences(of: "name", with: "e"))] = time
        //: parm["toUid"] = toUid
        parm[(String(str_hiddenValue.prefix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kLet_postName.cognizant(eventID: kLet_intimatePackageContent, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension CoverErrorDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func toHandle() {
        //: guard SubLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !SubLabelReactiveCompatible.share.loginUserMode.loungePlus
        vipIcon.isHidden = !SubLabelReactiveCompatible.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = LabelAdditiveArithmetic.randow
        //: if SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && SubLabelReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if SubLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0, SubLabelReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if SubLabelReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
            if SubLabelReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(str_tapMenuName) + String(str_topName.prefix(8))).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = SubLabelReactiveCompatible.share.loginUserMode.loungePlus ? vipFee:fee
            let price = SubLabelReactiveCompatible.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(str_containerInfoData.prefix(6))).localizedArguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.underPlay(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(str_containerInfoData.prefix(6))).localizedArguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.underPlay(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = SubLabelReactiveCompatible.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = SubLabelReactiveCompatible.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = SubLabelReactiveCompatible.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = SubLabelReactiveCompatible.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !SubLabelReactiveCompatible.share.loginUserMode.loungePlus
            priceStackView.isHidden = !SubLabelReactiveCompatible.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func miscalculation() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func notPost() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(kLet_messageSumervalName + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func showData() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(toHandle),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: kLet_callErrorValue,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(sharedAd),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: kLet_streamTitle,
                                               //: object: nil)
                                               object: nil)
    }
}
