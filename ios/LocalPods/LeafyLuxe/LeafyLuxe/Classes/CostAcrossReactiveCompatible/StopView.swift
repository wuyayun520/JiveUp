
//: Declare String Begin

/*: "#666666" :*/
fileprivate let str_appText:String = "#bybybybyby"
fileprivate let str_labelValue:[Character] = ["6"]

/*: "#8566FF" :*/
fileprivate let str_genderTitle:[Character] = ["#","8","5","6","6","F","F"]

/*: "Home" :*/
fileprivate let str_makeTimeName:String = "Hometransform color right user leading"

/*: "Like" :*/
fileprivate let str_blockViewText:String = "Likefemale content"

/*: "Message" :*/
fileprivate let str_textItemName:String = "Messageheight view view report let"

/*: "Me" :*/
fileprivate let str_blockData:[Character] = ["M","e"]

/*: "Live" :*/
fileprivate let str_sexData:String = "Livecombination guard return"

/*: "icon_randownCall_nor" :*/
fileprivate let str_equalIndexValue:[UInt8] = [0x72,0x6f,0x6e,0x5f,0x6c,0x6c,0x61,0x43,0x6e,0x77,0x6f,0x64,0x6e,0x61,0x72,0x5f,0x6e,0x6f,0x63,0x69]

/*: "icon_randownCall_pre" :*/
fileprivate let str_playerValue:String = "icon_image block height"
fileprivate let str_belowData:String = "live will view giftnCal"
fileprivate let str_attributeText:[Character] = ["l","_","p","r","e"]

/*: "icon_home_match_nor" :*/
fileprivate let str_managerTitleViewText:[UInt8] = [0x2,0x8,0x4,0x5,0x34,0x3,0x4,0x6,0xe,0x34,0x6,0xa,0x1f,0x8,0x3,0x34,0x5,0x4,0x19]

private func cameraFrame(make num: UInt8) -> UInt8 {
    return num ^ 107
}

/*: "icon_home_match_pre" :*/
fileprivate let str_makeViewValue:[Character] = ["i","c","o","n","_","h","o"]
fileprivate let str_actionTitle:[Character] = ["m","e","_","m","a","t","c","h","_","p","r","e"]

/*: "btn_popular_pop_nor" :*/
fileprivate let str_levelContent:[UInt8] = [0x78,0x6e,0x74,0x45,0x6a,0x75,0x6a,0x6f,0x76,0x7b,0x68,0x45,0x6a,0x75,0x6a,0x45,0x74,0x75,0x68]

private func cellMake(file num: UInt8) -> UInt8 {
    return num ^ 26
}

/*: "btn_popular_pop_pre" :*/
fileprivate let str_makeValue:[Character] = ["b","t","n","_","p"]
fileprivate let str_totalTitle:[Character] = ["o"]
fileprivate let str_indicatorValue:String = "putag"
fileprivate let str_titleDeadlineValue:String = "p_prelayer at"

/*: "btn_label_add_nor" :*/
fileprivate let str_topNoteContent:[UInt8] = [0x9c,0xae,0xa8,0x99,0xa6,0x9b,0x9c,0x9f,0xa6,0x99,0x9b,0x9e,0x9e,0x99,0xa8,0xa9,0xac]

fileprivate func dismissQuick(view num: UInt8) -> UInt8 {
    let value = Int(num) + 198
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "btn_label_add_pre" :*/
fileprivate let str_stopDataTitle:String = "btn_lelse in var"
fileprivate let str_mainName:String = "app content true postadd_pre"

/*: "btn_chat_cha_nor" :*/
fileprivate let str_blockCallValue:[UInt8] = [0x83,0x95,0x8f,0xbe,0x82,0x89,0x80,0x95,0xbe,0x82,0x89,0x80,0xbe,0x8f,0x8e,0x93]

/*: "btn_chat_cha_pre" :*/
fileprivate let str_domainValue:[Character] = ["b","t","n","_","c","h","a","t","_","c","h","a","_","p","r","e"]

/*: "btn_me_my_nor" :*/
fileprivate let str_whiteTitle:[UInt8] = [0x89,0x9f,0x85,0xb4,0x86,0x8e,0xb4,0x86,0x92,0xb4,0x85,0x84,0x99]

private func bagTitle(data num: UInt8) -> UInt8 {
    return num ^ 235
}

/*: "btn_me_my_pre" :*/
fileprivate let str_secondName:String = "btn_status add self make"
fileprivate let str_topValue:[Character] = ["m","e","_","m","y","_","p","r","e"]

/*: "btn_live_add_nor" :*/
fileprivate let str_acceptName:[UInt8] = [0x10,0x22,0x1c,0xd,0x1a,0x17,0x24,0x13,0xd,0xf,0x12,0x12,0xd,0x1c,0x1d,0x20]

fileprivate func scaleOfMeasurement(style num: UInt8) -> UInt8 {
    let value = Int(num) + 82
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "btn_live_add_pre" :*/
fileprivate let str_managerValue:String = "angle"
fileprivate let str_shadowColorTitle:[Character] = ["t","n","_","l","i","v","e","_","a","d","d","_","p","r","e"]

/*: "get json error" :*/
fileprivate let str_actionData:[Character] = ["g","e","t"," ","j","s","o","n"]
fileprivate let str_cancelData:[Character] = [" ","e","r","r","o","r"]

/*: "99+" :*/
fileprivate let str_shareData:[Character] = ["9","9","+"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StopView.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/3/29.
//

//: import UIKit
import UIKit

//: class TalkingTabBar: UITabBar {
class StopView: UITabBar {
	var statusTotal: Int = 23
	var elementSum: Double = 86.8
	var imageTotal: Int = 77
	var blockTotal: Double = 83.4
	var labelMagnitude: Int = 96
	var imageCellBorderCount: Double = 66.8

    //: var currentTabType = 0
    var currentTabType = 0
    // 消息tabBar展示头像
    //: private var messageIcon = UIImageView()
    private var messageIcon = UIImageView()

    //: func setupItemsWithArr(itemTypes: Array<TabBarItemType>) {
    func switchce(itemTypes: [SerrationDoingsetTarget]) {
        //: if itemTypes.count == 0 {
        if itemTypes.count == 0 {
            //: return
            return
        }
        //: let viewWidth = ScreenWidth
        let viewWidth = kLet_failureName
        //: let backView = UIView.init()
        let backView = UIView()
        //: self.addSubview(backView)
        self.addSubview(backView)
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.leading.equalTo(0)
            make.leading.equalTo(0)
            //: make.trailing.equalTo(0)
            make.trailing.equalTo(0)
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
            //: make.height.equalTo(Int(TabBarViewHeight))
            make.height.equalTo(Int(kLet_rateRequestPathTitle))
        }
        //: backView.backgroundColor = .white
        backView.backgroundColor = .white
        //: backView.layer.shadowColor = UIColor.init(red: 188/255.0, green: 188/255.0, blue: 188/255.0, alpha: 0.5).cgColor
        backView.layer.shadowColor = UIColor(red: 188 / 255.0, green: 188 / 255.0, blue: 188 / 255.0, alpha: 0.5).cgColor
        //: backView.layer.shadowOffset = CGSize.init(width: 0, height: 0)
        backView.layer.shadowOffset = CGSize(width: 0, height: 0)
        //: backView.layer.shadowOpacity = 1
        backView.layer.shadowOpacity = 1
        //: backView.layer.shadowRadius = 6
        backView.layer.shadowRadius = 6

        //: let width = Int(viewWidth)/itemTypes.count
        let width = Int(viewWidth) / itemTypes.count

        //: for i in 0..<itemTypes.count {
        for i in 0 ..< itemTypes.count {
            //: let itemType = itemTypes[i]
            let itemType = itemTypes[i]
            //: let btn = barBtnFor(itemType: itemType)!
            let btn = playEnable(itemType: itemType)!
            //: btn.tag = itemType.rawValue + 100
            btn.tag = itemType.rawValue + 100
            //: btn.isSelected = (itemType.rawValue == currentTabType)
            btn.isSelected = (itemType.rawValue == currentTabType)
            //: backView.addSubview(btn)
            backView.addSubview(btn)
            //: btn.snp.makeConstraints { make in
            btn.snp.makeConstraints { make in
                //: make.top.equalToSuperview()
                make.top.equalToSuperview()
                //: make.leading.equalTo(width*i)
                make.leading.equalTo(width * i)
                //: make.size.equalTo(CGSize.init(width: width, height: 48))
                make.size.equalTo(CGSize(width: width, height: 48))
            }
            //: if itemType != TabBarItemType.FreeMoment {
            if itemType != SerrationDoingsetTarget.FreeMoment {
                //: setBtnPlace(btn: btn)
                rest(btn: btn)
            }

            //: if itemType == TabBarItemType.Message {
            if itemType == SerrationDoingsetTarget.Message {
                //: messageIcon = UIImageView()
                messageIcon = UIImageView()
                //: messageIcon.contentMode = .scaleAspectFill
                messageIcon.contentMode = .scaleAspectFill
                //: messageIcon.layer.cornerRadius = 15.0
                messageIcon.layer.cornerRadius = 15.0
                //: messageIcon.layer.masksToBounds = true
                messageIcon.layer.masksToBounds = true
                //: messageIcon.isHidden = true
                messageIcon.isHidden = true
                //: btn.addSubview(messageIcon)
                btn.addSubview(messageIcon)
                //: messageIcon.snp.makeConstraints { make in
                messageIcon.snp.makeConstraints { make in
                    //: let y = btn.imageView?.origin.y ?? 0
                    let y = btn.imageView?.origin.y ?? 0
                    //: make.top.equalTo(y-2)
                    make.top.equalTo(y - 2)
                    //: make.centerX.equalToSuperview()
                    make.centerX.equalToSuperview()
                    //: make.size.equalTo(CGSize(width: 30, height: 30))
                    make.size.equalTo(CGSize(width: 30, height: 30))
                }
                //: } else if itemType == TabBarItemType.Live {
            } else if itemType == SerrationDoingsetTarget.Live { // 直播tabBar添加SVGA动效
                //: btn.snp.makeConstraints { make in
                btn.snp.makeConstraints { make in
                    //: make.top.equalToSuperview().offset(-6)
                    make.top.equalToSuperview().offset(-6)
                    //: make.leading.equalTo(width*i)
                    make.leading.equalTo(width * i)
                    //: make.size.equalTo(CGSize.init(width: width, height: 48))
                    make.size.equalTo(CGSize(width: width, height: 48))
                }
                //: liveSvga.isHidden = true
                liveSvga.isHidden = true
                //: btn.addSubview(liveSvga)
                btn.addSubview(liveSvga)
                //: liveSvga.snp.makeConstraints { make in
                liveSvga.snp.makeConstraints { make in
                    //: make.top.equalToSuperview().offset(-4)
                    make.top.equalToSuperview().offset(-4)
                    //: make.centerX.equalToSuperview()
                    make.centerX.equalToSuperview()
                    //: make.size.equalTo(CGSize(width: 44, height: 44))
                    make.size.equalTo(CGSize(width: 44, height: 44))
                }
                //: } else if itemType == TabBarItemType.Randow {
            } else if itemType == SerrationDoingsetTarget.Randow {
                //: btn.snp.makeConstraints { make in
                btn.snp.makeConstraints { make in
                    //: make.top.equalToSuperview().offset(-6)
                    make.top.equalToSuperview().offset(-6)
                    //: make.leading.equalTo(width*i)
                    make.leading.equalTo(width * i)
                    //: make.size.equalTo(CGSize.init(width: width, height: 48))
                    make.size.equalTo(CGSize(width: width, height: 48))
                }
                //: randowSvga.isHidden = true
                randowSvga.isHidden = true
                //: btn.addSubview(randowSvga)
                btn.addSubview(randowSvga)
                //: randowSvga.snp.makeConstraints { make in
                randowSvga.snp.makeConstraints { make in
                    //: make.top.equalToSuperview().offset(-4)
                    make.top.equalToSuperview().offset(-4)
                    //: make.centerX.equalToSuperview()
                    make.centerX.equalToSuperview()
                    //: make.size.equalTo(CGSize(width: 44, height: 44))
                    make.size.equalTo(CGSize(width: 44, height: 44))
                }
            }
        }

        //: if #available(iOS 13.0, *) {
        if #available(iOS 13.0, *) {
            //: let tabBarAppearance = UITabBarAppearance.init()
            let tabBarAppearance = UITabBarAppearance()
            //: tabBarAppearance.backgroundColor = .clear
            tabBarAppearance.backgroundColor = .clear
            //: tabBarAppearance.backgroundEffect = nil
            tabBarAppearance.backgroundEffect = nil
            //: tabBarAppearance.shadowColor = .clear
            tabBarAppearance.shadowColor = .clear
            //: self.standardAppearance = tabBarAppearance
            self.standardAppearance = tabBarAppearance
            //: if #available(iOS 15.0, *) {
            if #available(iOS 15.0, *) {
                //: self.scrollEdgeAppearance = tabBarAppearance
                self.scrollEdgeAppearance = tabBarAppearance
                //: } else {
            } else {
                // Fallback on earlier versions
            }
            //: } else {
        } else {
            // Fallback on earlier versions
        }
    
            if (self.isMultipleTouchEnabled) && (self.layer.contents != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let canvasGroup = ElementView(frame: self.bounds.insetBy(dx: CGFloat(0), dy: CGFloat(75)))
            canvasGroup.drawUpMagnitude = { [self] refuseLoadCount in
            self.imageTotal = refuseLoadCount
            
            var canvasGroup = width
            canvasGroup *= 9
            if canvasGroup > self.imageTotal {
                self.imageTotal = canvasGroup
            }
            
            return self.imageTotal
            }
            canvasGroup.popNumber = { [self] firstNumber in
            self.blockTotal = firstNumber
            
            return self.blockTotal
            }
                self.addSubview(canvasGroup)
            }

	}

    //: func barBtnFor(itemType: TabBarItemType) -> UIButton? {
    func playEnable(itemType: SerrationDoingsetTarget) -> UIButton? {
        //: switch itemType {
        switch itemType {
        //: case .Social, .Moment, .Account, .Message, .Randow, .Live:
        case .Social, .Moment, .Account, .Message, .Randow, .Live:
            //: let btn: UIButton = UIButton.init(type: .custom)
            let btn = UIButton(type: .custom)
            //: btn.tag = itemType.rawValue + 100
            btn.tag = itemType.rawValue + 100
            //: btn.backgroundColor = .clear
            btn.backgroundColor = .clear
            //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 11)
            btn.titleLabel?.font = .magnitudeimateBy(type: .Medium, fontSize: 11)
            //: btn.setTitle(self.itemTitleFor(itemType: itemType), for: .normal)
            btn.setTitle(self.viewTitle(itemType: itemType), for: .normal)
            //: btn .setTitleColor(UIColor.init(hex: "#666666"), for: .normal)
            btn.setTitleColor(UIColor(hex: (str_appText.replacingOccurrences(of: "by", with: "6") + String(str_labelValue))), for: .normal)
            //: btn .setTitleColor(UIColor.init(hex: "#8566FF"), for: .selected)
            btn.setTitleColor(UIColor(hex: (String(str_genderTitle))), for: .selected)
            //: btn.setImage(UIImage.nameTitle(name: self.itemImageFor(itemType: itemType, btnSelect: false)), for: .normal)
            btn.setImage(UIImage.nameTitle(name: self.changeAdd(itemType: itemType, btnSelect: false)), for: .normal)
            //: btn.setImage(UIImage.nameTitle(name: self.itemImageFor(itemType: itemType, btnSelect: true)), for: .selected)
            btn.setImage(UIImage.nameTitle(name: self.changeAdd(itemType: itemType, btnSelect: true)), for: .selected)

            //: btn.imageView?.contentMode = .scaleAspectFill
            btn.imageView?.contentMode = .scaleAspectFill
            //: return btn
            return btn
        //: case .FreeMoment:
        case .FreeMoment:
            //: return nil
            return nil
        //: case .Login:
        case .Login:
            //: return nil
            return nil
        //: case .meet:
        case .meet:
            //: return nil
            return nil
        }
    }

    //: func itemTitleFor(itemType: TabBarItemType) -> String {
    func viewTitle(itemType: SerrationDoingsetTarget) -> String {
        //: switch itemType {
        switch itemType {
        //: case .Randow:
        case .Randow:
            //: return "Home".localized
            return (String(str_makeTimeName.prefix(4))).localized
        //: case .Social:
        case .Social:
//            if SubLabelReactiveCompatible.share.appStatus == MakePropertyProtocol.special.rawValue {
//                return "People".localized
//            } else if SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.male.rawValue {
//                return "Hot".localized
//            } else {
//                return "Home".localized
//            }
            //: return "Home".localized
            return (String(str_makeTimeName.prefix(4))).localized

        //: case .Moment:
        case .Moment:
            //: return "Like".localized
            return (String(str_blockViewText.prefix(4))).localized
//            return "Moment".localized
        //: case .Message:
        case .Message:
            //: return "Message".localized
            return (String(str_textItemName.prefix(7))).localized
        //: case .Account:
        case .Account:
            //: return "Me".localized
            return "Me".localized
        //: case .Live:
        case .Live:
            //: return "Live".localized
            return (String(str_sexData.prefix(4))).localized
        //: case .Login, .FreeMoment, .meet:
        case .Login, .FreeMoment, .meet:
            //: return ""
            return ""
        }
    
            if (self.isMultipleTouchEnabled) && (self.layer.contents != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let canvasGroup = ElementView(frame: self.bounds.insetBy(dx: CGFloat(0), dy: CGFloat(75)))
            canvasGroup.drawUpMagnitude = { [self] refuseLoadCount in
            self.statusTotal = refuseLoadCount
            
            var canvasGroup = currentTabType
            canvasGroup *= 9
            if canvasGroup > self.statusTotal {
                self.statusTotal = canvasGroup
            }
            
            return self.statusTotal
            }
            canvasGroup.popNumber = { [self] firstNumber in
            self.elementSum = firstNumber
            
            return self.elementSum
            }
                self.addSubview(canvasGroup)
            }

	}

    //: func itemImageFor(itemType: TabBarItemType, btnSelect: Bool) -> String {
    func changeAdd(itemType: SerrationDoingsetTarget, btnSelect: Bool) -> String {
        //: switch itemType {
        switch itemType {
        //: case .Randow:
        case .Randow:
            //: return btnSelect == false ? "icon_randownCall_nor":"icon_randownCall_pre"
            return btnSelect == false ? String(bytes: str_equalIndexValue.reversed(), encoding: .utf8)! : (String(str_playerValue.prefix(5)) + "randow" + String(str_belowData.suffix(4)) + String(str_attributeText))
        //: case .Social:
        case .Social:
//            return btnSelect == false ? "btn_popular_pop_nor":"btn_popular_pop_pre"
            //: return btnSelect == false ? "icon_home_match_nor":"icon_home_match_pre"
            return btnSelect == false ? String(bytes: str_managerTitleViewText.map{cameraFrame(make: $0)}, encoding: .utf8)! : (String(str_makeViewValue) + String(str_actionTitle))
        //: case .Moment:
        case .Moment:
//            return btnSelect == false ? "btn_discocer_dis_nor":"btn_discocer_dis_pre"
            //: return btnSelect == false ? "btn_popular_pop_nor":"btn_popular_pop_pre"
            return btnSelect == false ? String(bytes: str_levelContent.map{cellMake(file: $0)}, encoding: .utf8)! : (String(str_makeValue) + String(str_totalTitle) + str_indicatorValue.replacingOccurrences(of: "tag", with: "la") + "r_po" + String(str_titleDeadlineValue.prefix(5)))
        //: case .FreeMoment:
        case .FreeMoment:
            //: return btnSelect == false ? "btn_label_add_nor":"btn_label_add_pre"
            return btnSelect == false ? String(bytes: str_topNoteContent.map{dismissQuick(view: $0)}, encoding: .utf8)! : (String(str_stopDataTitle.prefix(5)) + "abel_" + String(str_mainName.suffix(7)))
        //: case .Message:
        case .Message:
            //: return btnSelect == false ? "btn_chat_cha_nor":"btn_chat_cha_pre"
            return btnSelect == false ? String(bytes: str_blockCallValue.map{$0^225}, encoding: .utf8)! : (String(str_domainValue))
        //: case .Account:
        case .Account:
            //: return btnSelect == false ? "btn_me_my_nor":"btn_me_my_pre"
            return btnSelect == false ? String(bytes: str_whiteTitle.map{bagTitle(data: $0)}, encoding: .utf8)! : (String(str_secondName.prefix(4)) + String(str_topValue))
        //: case .Live:
        case .Live:
            //: return btnSelect == false ? "btn_live_add_nor":"btn_live_add_pre"
            return btnSelect == false ? String(bytes: str_acceptName.map{scaleOfMeasurement(style: $0)}, encoding: .utf8)! : (str_managerValue.replacingOccurrences(of: "angle", with: "b") + String(str_shadowColorTitle))
        //: case .Login, .meet:
        case .Login, .meet:
            //: return ""
            return ""
        }
    
            if (randowSvga.tag == 1024) && (randowSvga.alpha != 1.0) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let penLet = ElementView()
            penLet.drawUpMagnitude = { [self] refuseLoadCount in
            self.labelMagnitude = refuseLoadCount
            
            var penLet = currentTabType
                penLet -= 1
                if penLet <= 0 {
                    penLet = penLet + 1
                }
            if penLet > self.labelMagnitude {
                self.labelMagnitude = penLet
            }
            
            return self.labelMagnitude
            }
            penLet.popNumber = { [self] firstNumber in
            self.imageCellBorderCount = firstNumber
            
            return self.imageCellBorderCount
            }
                randowSvga.addSubview(penLet)
            }

	}

    /// 更改tabbar按钮选中状态
    //: func setSelectedTabItem(itemType: TabBarItemType) {
    func nameType(itemType: SerrationDoingsetTarget) {
        //: let btn = self.viewWithTag(currentTabType + 100) as! UIButton
        let btn = self.viewWithTag(currentTabType + 100) as! UIButton
        //: if itemType.rawValue != currentTabType && btn.isSelected == true {
        if itemType.rawValue != currentTabType && btn.isSelected == true {
            //: btn.isSelected = false
            btn.isSelected = false
        }
        //: let lastBtn = self.viewWithTag(itemType.rawValue + 100) as! UIButton
        let lastBtn = self.viewWithTag(itemType.rawValue + 100) as! UIButton
        //: lastBtn.isSelected = true
        lastBtn.isSelected = true
        //: currentTabType = itemType.rawValue
        currentTabType = itemType.rawValue
        //: if itemType == .Message {
        if itemType == .Message {
            //: hideNewMessageUser()
            onEqualCounteraction()
        }
    }

    //: func setBtnPlace(btn: UIButton) {
    func rest(btn: UIButton) {
        //: let titleW: CGFloat = btn.titleLabel?.intrinsicContentSize.width ?? 0
        let titleW: CGFloat = btn.titleLabel?.intrinsicContentSize.width ?? 0
        //: let titleH: CGFloat = btn.titleLabel?.intrinsicContentSize.height ?? 0
        let titleH: CGFloat = btn.titleLabel?.intrinsicContentSize.height ?? 0

        //: let imageW: CGFloat = btn.imageView?.bounds.width ?? 0
        let imageW: CGFloat = btn.imageView?.bounds.width ?? 0
        //: let imageH: CGFloat = btn.imageView?.bounds.height ?? 0
        let imageH: CGFloat = btn.imageView?.bounds.height ?? 0
        //: if LanguageManager.shared.direction == .leftToRight {
        if PropertyLineThen.shared.direction == .leftToRight {
            //: btn.imageEdgeInsets = UIEdgeInsets(top: -titleH+4, left: 0, bottom: 0, right: -titleW)
            btn.imageEdgeInsets = UIEdgeInsets(top: -titleH + 4, left: 0, bottom: 0, right: -titleW)
            //: btn.titleEdgeInsets = UIEdgeInsets(top: 0, left: -imageW, bottom: -imageH-2, right: 0)
            btn.titleEdgeInsets = UIEdgeInsets(top: 0, left: -imageW, bottom: -imageH - 2, right: 0)
            //: } else {
        } else {
            //: btn.imageEdgeInsets = UIEdgeInsets(top: -titleH+4, left: -titleW, bottom: 0, right: 0)
            btn.imageEdgeInsets = UIEdgeInsets(top: -titleH + 4, left: -titleW, bottom: 0, right: 0)
            //: btn.titleEdgeInsets = UIEdgeInsets(top: 0, left: 0, bottom: -imageH-2, right:  -imageW)
            btn.titleEdgeInsets = UIEdgeInsets(top: 0, left: 0, bottom: -imageH - 2, right: -imageW)
        }
    }

    //: private lazy var liveSvga: SVGAPlayer = {
    private lazy var liveSvga: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: do {
        do {
            //: let url = SVGAEffectTool.default.getZipEffectPath(type: .Live_tabBar)
            let url = PuncherEffectTool.default.betweenPath(type: .Live_tabBar)
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_actionData) + String(str_cancelData)))
        }

        //: return player
        return player
        //: }()
    }()

    //: private lazy var randowSvga: SVGAPlayer = {
    private lazy var randowSvga: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: do {
        do {
            //: let url = SVGAEffectTool.default.getZipEffectPath(type: .Tabbar_randownCall)
            let url = PuncherEffectTool.default.betweenPath(type: .Tabbar_randownCall)
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_actionData) + String(str_cancelData)))
        }

        //: return player
        return player
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingTabBar {
extension StopView {
    /// 刷新消息未读数角标
    //: func refreshBadgeLayoutWith(unread: Int, barType: TabBarItemType) {
    func barCell(unread: Int, barType: SerrationDoingsetTarget) {
        //: let btn = self.viewWithTag(barType.rawValue + 100) as! UIButton
        let btn = self.viewWithTag(barType.rawValue + 100) as! UIButton
        //: let label = getBadgeLabel(btn: btn)
        let label = tabularArrayEqual(btn: btn)
        //: if unread > 0 {
        if unread > 0 {
            //: label.isHidden = false
            label.isHidden = false
            //: if unread > 99 {
            if unread > 99 {
                //: label.text = "99+"
                label.text = "99+"
                //: } else {
            } else {
                //: label.text = "\(unread)"
                label.text = "\(unread)"
            }
            //: } else {
        } else {
            //: label.isHidden = true
            label.isHidden = true
            //: hideNewMessageUser()
            onEqualCounteraction()
        }
        //: var labWidth = 18.0
        var labWidth = 18.0
        //: if unread > 99 {
        if unread > 99 {
            //: labWidth = 30.0
            labWidth = 30.0
            //: } else if unread > 9 {
        } else if unread > 9 {
            //: labWidth = 23.0
            labWidth = 23.0
        }
        //: let delta = KDeviceIsIphoneX ? 5 : 2
        let delta = kLet_succeedVideoTitle ? 5 : 2
        //: label.snp.remakeConstraints { make in
        label.snp.remakeConstraints { make in
            //: make.leading.equalTo(btn.snp.centerX).offset(10)
            make.leading.equalTo(btn.snp.centerX).offset(10)
            //: make.top.equalTo(delta)
            make.top.equalTo(delta)
            //: make.size.equalTo(CGSize(width: labWidth, height: 18.0))
            make.size.equalTo(CGSize(width: labWidth, height: 18.0))
        }
    }

    //: func getBadgeLabel(btn: UIButton) -> UILabel {
    func tabularArrayEqual(btn: UIButton) -> UILabel {
        //: var badgeLabel = btn.viewWithTag(1000)
        var badgeLabel = btn.viewWithTag(1000)
        //: if badgeLabel == nil {
        if badgeLabel == nil {
            //: var tempLabel = UILabel.init()
            var tempLabel = UILabel()
            //: tempLabel = UILabel.init()
            tempLabel = UILabel()
            //: tempLabel.tag = 1000
            tempLabel.tag = 1000
            //: tempLabel.backgroundColor = UIColor.msgTipsColor()
            tempLabel.backgroundColor = UIColor.estimatedColor()
            //: tempLabel.layer.cornerRadius = 9
            tempLabel.layer.cornerRadius = 9
            //: tempLabel.layer.borderWidth = 1
            tempLabel.layer.borderWidth = 1
            //: tempLabel.layer.borderColor = UIColor.white.cgColor
            tempLabel.layer.borderColor = UIColor.white.cgColor
            //: tempLabel.clipsToBounds = true
            tempLabel.clipsToBounds = true
            //: tempLabel.textColor = UIColor.white
            tempLabel.textColor = UIColor.white
            //: tempLabel.font = UIFont.underPlay(fontSize: 13)
            tempLabel.font = UIFont.underPlay(fontSize: 13)
            //: tempLabel.isHidden = true
            tempLabel.isHidden = true
            //: tempLabel.textAlignment = .center
            tempLabel.textAlignment = .center

            //: btn.addSubview(tempLabel)
            btn.addSubview(tempLabel)
            //: badgeLabel = tempLabel
            badgeLabel = tempLabel
        }
        //: return badgeLabel as! UILabel
        return badgeLabel as! UILabel
    }

    /// 展示消息tab头像
    //: func showNewMessageUser(headPic: String) {
    func cellRoot(headPic: String) {
        //: if currentTabType != TabBarItemType.Message.rawValue {
        if currentTabType != SerrationDoingsetTarget.Message.rawValue {
            //: let btn = self.viewWithTag(TabBarItemType.Message.rawValue + 100) as? UIButton
            let btn = self.viewWithTag(SerrationDoingsetTarget.Message.rawValue + 100) as? UIButton
            //: if btn != nil {
            if btn != nil {
                //: self.messageIcon.isHidden = false
                self.messageIcon.isHidden = false
                //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 15))]
                let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: 15))]
                //: self.messageIcon.setUrlImage(urlStr: headPic, options: optionsInfo)
                self.messageIcon.overMake(urlStr: headPic, options: optionsInfo)
            }
        }
    }

    /// 隐藏消息tab头像
    //: func hideNewMessageUser() {
    func onEqualCounteraction() {
        //: let btn = self.viewWithTag(TabBarItemType.Message.rawValue + 100) as? UIButton
        let btn = self.viewWithTag(SerrationDoingsetTarget.Message.rawValue + 100) as? UIButton
        //: if btn != nil {
        if btn != nil {
            //: self.messageIcon.isHidden = true
            self.messageIcon.isHidden = true
        }
    }

    /// 展示女性直播toast提示
    //: func showFemaleLiveTurnOnToast() {
    func showBarOpen() {
        //: guard self.subviews.count > 0 else { return }
        guard self.subviews.count > 0 else { return }
        //: TalkingLiveTabToast().show(superView: self)
        TextTabToast().detectBy(superView: self)
    }

    /// 更改直播按钮图标
    //: func switchLiveTabBarImage() {
    func windowImage() {
        //: guard SubLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard SubLabelReactiveCompatible.share.loginUserMode.sex == CheckedUTF8Initializable.female.rawValue else { return }
        //: let liveBtn = self.viewWithTag(TabBarItemType.Live.rawValue + 100) as? UIButton
        let liveBtn = self.viewWithTag(SerrationDoingsetTarget.Live.rawValue + 100) as? UIButton
        //: liveBtn?.isSelected = false
        liveBtn?.isSelected = false
        //: if TalkingLiveManager.shared().isLive ||
        if ConversationLiveManager.auditoryImage().isLive ||
            //: TalkingVoiceRoomManager.shared().isParty, String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid) == SubLabelReactiveCompatible.share.loginUid {
            EyelashEventHandler.firstBack().isParty, String(EyelashEventHandler.firstBack().partyModel.streamerInfo.uid) == SubLabelReactiveCompatible.share.loginUid
        {
            //: liveSvga.isHidden = false
            liveSvga.isHidden = false
            //: } else {
        } else {
            //: liveSvga.isHidden = true
            liveSvga.isHidden = true
        }
    }

    /// 更改Randow按钮图标
    //: func switchRandowTabBarImage(isHidde: Bool) {
    func isometricHidde(isHidde: Bool) {
        //: let liveBtn = self.viewWithTag(TabBarItemType.Randow.rawValue + 100) as? UIButton
        let liveBtn = self.viewWithTag(SerrationDoingsetTarget.Randow.rawValue + 100) as? UIButton
        //: liveBtn?.isSelected = false
        liveBtn?.isSelected = false
        //: randowSvga.isHidden = !isHidde
        randowSvga.isHidden = !isHidde
    }
}
