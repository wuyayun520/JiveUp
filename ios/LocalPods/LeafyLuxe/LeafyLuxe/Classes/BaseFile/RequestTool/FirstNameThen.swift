
//: Declare String Begin

/*: "http://" :*/
fileprivate let str_connectionUponName:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let str_hideViewAsName:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let str_showByText:String = "app self buttonhttp://"

/*: "serverSelctIndex =  :*/
fileprivate let str_translateData:[Character] = ["s","e","r","v","e","r","S","e","l","c","t","I"]
fileprivate let str_transformText:[Character] = ["n","d","e","x"," ","="," "]

/*: "serverUrlStr =  :*/
fileprivate let str_bottomViewReturnText:String = "imagerver"
fileprivate let str_pathLabText:String = " "

/*: "app. :*/
fileprivate let str_toolManagerValue:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let str_whoName:[Character] = [".","c","o","m"]

/*: "IM/getMyBlackList" :*/
fileprivate let str_topName:[Character] = ["I","M","/"]
fileprivate let str_theMakeName:String = "make view for table textgetMyB"

/*: "mf/business/sendMsg" :*/
fileprivate let str_pathFrameKindValue:String = "mf/bum area"
fileprivate let str_tagTitleModelContent:String = "make maximumss/se"

/*: "mf/business/sendGift" :*/
fileprivate let str_imageHeadLetData:[Character] = ["m","f"]
fileprivate let str_instanceData:String = "/busdata video source else"
fileprivate let str_screenValue:String = "sendGiftelement row view edition data"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let str_shadowSizeReturnName:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d"]
fileprivate let str_timeData:[Character] = ["M","s","g"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let str_actionValue:String = "make height let view modemf/cha"
fileprivate let str_statusTitleName:String = "guidend"

/*: "recharge/applePayNotify" :*/
fileprivate let str_tempText:[Character] = ["r","e","c","h","a","r","g","e","/","a"]
fileprivate let str_pathNameData:String = "popularle"

/*: "user/appleLogin" :*/
fileprivate let str_firstRequestCenterValue:String = "document else first sex reuser/app"
fileprivate let str_managerValue:[Character] = ["l","e","L","o","g","i","n"]

/*: "en" :*/
fileprivate let str_secondVoiceText:[Character] = ["e","n"]

/*: "ar" :*/
fileprivate let str_startContent:String = "AR"

/*: "es" :*/
fileprivate let str_freeData:String = "actions"

/*: "pt" :*/
fileprivate let str_viewNeedName:[Character] = ["p","t"]

/*: "GJ21FB" :*/
fileprivate let str_equalDismissValue:String = "gj21fb"

/*: "完成了检测,请求状态正常" :*/
fileprivate let str_postIndexValue:[Character] = ["完","\u{6210}","\u{4e86}","检","测",",","请","\u{6c42}","状","态","正","常"]

/*: "接口请求失败" :*/
fileprivate let str_errorAccountName:String = "\u{63a5}口请求失\u{8d25}"

/*: "完成了检测,需要更换server url" :*/
fileprivate let str_topicValue:String = "完\u{6210}了检"
fileprivate let str_sizeTargetText:String = "make interaction player self换ser"
fileprivate let str_iconBlockName:[Character] = ["v","e","r"," ","u","r","l"]

/*: "HTTPProxy" :*/
fileprivate let str_fillName:String = "HTTcancel"
fileprivate let str_commitContent:[Character] = ["r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let str_makeViewData:String = "__SCOview extension return constraint let"
fileprivate let str_liveTitle:String = "PED__view result"

/*: "tap" :*/
fileprivate let str_actionTitle:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let str_makeData:String = "showun"

/*: "ipsec" :*/
fileprivate let str_makePersonTitle:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let str_numberHeadData:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let str_viewToName:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o","o","k"]
fileprivate let str_insideTitle:[Character] = ["i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let str_whiteSizeData:String = "https://s add extension sex in"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let str_thirdTitle:[UInt8] = [0x14,0x7,0x1c,0x11,0x2d,0x2d,0x16,0x17,0x1e,0x17,0x6,0x17,0x21,0x13,0x1f,0x17,0x31,0x1d,0x1d,0x19,0x1b,0x17,0x1,0x25,0x1b,0x6,0x1a,0x27,0x0,0x1e,0x52,0x4f,0x52]

private func dataElse(room num: UInt8) -> UInt8 {
    return num ^ 114
}

/*: "UID" :*/
fileprivate let str_progressTableInData:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let str_intoFrameValue:String = "appHappS"

/*: "func__saveAllCookies =  :*/
fileprivate let str_roundData:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l","l","C","o","o","k","i","e","s"," ","="]
fileprivate let str_finishEraseTitle:String = "push"

/*: "func__saveAllCookies  error" :*/
fileprivate let str_creditName:[Character] = ["f","u","n","c","_","_"]
fileprivate let str_playerInfoText:String = "user if work titlesaveAll"
fileprivate let str_makeTimeViewValue:String = "Cooktrue to pan"
fileprivate let str_angleValue:String = " errortitle height cell coin"

/*: "🍪 删除 :*/
fileprivate let str_clickTitle:String = "🍪 删progress"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FirstNameThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit

//: import WebKit
import WebKit

//: enum LangType: String {
enum ProfessionalComparable: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class FirstNameThen: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = FirstNameThen()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        imageSet()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(str_connectionUponName))) || newValue.hasPrefix((String(str_hideViewAsName))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if kLet_mineName && !kLet_localReplaceData {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_showByText.suffix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_showByText.suffix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    searchOfCookies(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func imageSet() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = kLet_guideScaleData.integer(forKey: kLet_requestValue)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(str_translateData) + String(str_transformText)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = blockVideo()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_connectionUponName))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_mineName && !kLet_localReplaceData {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_connectionUponName))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (str_bottomViewReturnText.replacingOccurrences(of: "image", with: "se") + "UrlStr =" + str_pathLabText.capitalized) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func blockVideo() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func relieve(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(str_toolManagerValue)) + "\(kLet_nameStreamLimitData)" + (String(str_whoName)), (String(str_toolManagerValue)) + "\(kLet_nameStreamLimitData)" + (String(str_whoName))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = {
        return ["IM/getMyBlackList",
                "mf/business/sendMsg",
                "mf/business/sendGift",
                "mf/chatRoom/sendMsg",
                "mf/chatRoom/sendGift",
                "recharge/applePayNotify",
                "user/appleLogin"]
    }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = ManagerReactiveCompatible.getSystemLangCode()
        let lang = ManagerReactiveCompatible.salve()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .randomString(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (str_equalDismissValue.uppercased()) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension FirstNameThen {
    //: func checkServerUrl() {
    func searchHead() {
        //: if SubLabelReactiveCompatible.share.networkStatus != .Unavailable {
        if SubLabelReactiveCompatible.share.networkStatus != .Unavailable {
            //: func_requestHost()
            anyWith()
            //: } else {
        } else {
            //: func_starCodeTime()
            viewDown()
        }
    }

    //: func func_starCodeTime() {
    func viewDown() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.anyWith()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func runBasesDegraded() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func anyWith() {
        //: if SubLabelReactiveCompatible.share.networkStatus == .Unavailable {
        if SubLabelReactiveCompatible.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        runBasesDegraded()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        ShareCellThen.titleSub { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (String(str_postIndexValue)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (str_errorAccountName))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.viewDown()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (str_topicValue + "测,\u{9700}要更" + String(str_sizeTargetText.suffix(4)) + String(str_iconBlockName)))
                    //: self.func__changeHostUrl()
                    self.equalMethod()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func equalMethod() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        runBasesDegraded()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        kLet_guideScaleData.set(serverSelctIndex, forKey: kLet_requestValue)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_connectionUponName))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_mineName, !kLet_localReplaceData {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_connectionUponName))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension FirstNameThen {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func anyLoad() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(str_fillName.replacingOccurrences(of: "cancel", with: "PP") + String(str_commitContent))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func fromVpn() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(str_makeViewData.prefix(5)) + String(str_liveTitle.prefix(5)))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(str_actionTitle))) || keyStr.contains((str_makeData.replacingOccurrences(of: "show", with: "t"))) || keyStr.contains((String(str_makePersonTitle))) || keyStr.contains((String(str_numberHeadData))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension FirstNameThen {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func searchOfCookies(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(str_viewToName) + String(str_insideTitle)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = offFromQueryed()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        var urlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(str_connectionUponName)), with: (String(str_hideViewAsName)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(str_hideViewAsName))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(str_whiteSizeData.prefix(8))) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            largenessUrl(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        createPush()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func largenessUrl(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: str_thirdTitle.map{dataElse(room: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func offFromQueryed() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_connectionUponName))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_mineName, !kLet_localReplaceData {
            //: serverPrefix = "https://"
            serverPrefix = (String(str_hideViewAsName))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(str_toolManagerValue)) + "\(kLet_nameStreamLimitData)" + (String(str_whoName)))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func letGoOf(note _: Notification) {
        //: if SubLabelReactiveCompatible.share.loginUid.isEmptyString {
        if SubLabelReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            html()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            doVersion()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func html() {
        //: var htmlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_progressTableInData)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(str_progressTableInData))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID")] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_progressTableInData)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(str_progressTableInData))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID") {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID")] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(str_progressTableInData))), h5Cookie.keys.contains((String(str_progressTableInData))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(str_progressTableInData))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(str_progressTableInData))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID")), h5Cookie.keys.contains((str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID")) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID")]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID")]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func doVersion() {
        //: let htmlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        let htmlDomain: String = SubLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == SubLabelReactiveCompatible.share.loginUserMode.userID {
            if cookie.name == (String(str_progressTableInData)), cookie.value == SubLabelReactiveCompatible.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID") {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(str_progressTableInData)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(str_progressTableInData))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID")] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(str_progressTableInData))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(str_progressTableInData))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID")) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(str_intoFrameValue.replacingOccurrences(of: "app", with: "P") + "ESSID")] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func createPush() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(str_roundData) + str_finishEraseTitle.replacingOccurrences(of: "push", with: " ")) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            kLet_guideScaleData.set(cookieData, forKey: kLet_marginData)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(str_creditName) + String(str_playerInfoText.suffix(7)) + String(str_makeTimeViewValue.prefix(4)) + "ies " + String(str_angleValue.prefix(6))))
        }
    }

    //: func func__setAppCookies() {
    func anima() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = kLet_guideScaleData.object(forKey: kLet_marginData)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func funcThroughTo() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        kLet_guideScaleData.removeObject(forKey: kLet_marginData)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
