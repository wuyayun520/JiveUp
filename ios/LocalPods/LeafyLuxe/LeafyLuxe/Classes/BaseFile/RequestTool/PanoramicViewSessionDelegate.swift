
//: Declare String Begin

/*: / :*/
fileprivate let str_intimateDataModelValue:String = "color"

/*:  ( :*/
fileprivate let str_succeedName:String = " ("

/*: ; build: :*/
fileprivate let str_cellModelContent:String = "camera route; b"
fileprivate let str_remarkData:String = "in background viewuild:"

/*: ; iOS  :*/
fileprivate let str_cameraPlayerData:[Character] = [";"," ","i","O","S"," "]

/*: ;  :*/
fileprivate let str_kitData:[Character] = [";"," "]

/*: )" :*/
fileprivate let str_youText:[Character] = [")"]

/*: "audio" :*/
fileprivate let str_targetData:String = "aunderdio"

/*: + :*/
fileprivate let str_voiceCountValue:String = "shoot"

/*: .wav" :*/
fileprivate let str_backTitle:[Character] = [".","w","a","v"]

/*: "audio/wav" :*/
fileprivate let str_nameMustViewTitle:[Character] = ["a","u","d","i","o","/","w"]
fileprivate let str_callTitle:String = "voicev"

/*: .jpg" :*/
fileprivate let str_tabValue:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let str_dismissTitle:[Character] = ["i","m","a","g","e","/","j","p"]
fileprivate let str_toolMatchData:[Character] = ["e","g"]

/*: "://app." :*/
fileprivate let str_liveContent:String = "://app.player work"

/*: "\"data\":null" :*/
fileprivate let str_coverPathValue:[Character] = ["\"","d","a","t","a","\"",":","n","u","l"]
fileprivate let str_topAddValue:String = "cancel"

/*: "\"data\":{}" :*/
fileprivate let str_modelWillTitle:[Character] = ["\"","d","a","t","a","\""]
fileprivate let str_spaceBottomValue:String = ":{}"

/*: ":null" :*/
fileprivate let str_partyEqualText:[Character] = [":","n","u","l","l"]

/*: ":\"\"" :*/
fileprivate let str_onlineValue:String = ":\"\\"

/*: "Request failed, Try again later" :*/
fileprivate let str_tableCancelName:[UInt8] = [0xaf,0x98,0x8c,0x88,0x98,0x8e,0x89,0xdd,0x9b,0x9c,0x94,0x91,0x98,0x99,0xd1,0xdd,0xa9,0x8f,0x84,0xdd,0x9c,0x9a,0x9c,0x94,0x93,0xdd,0x91,0x9c,0x89,0x98,0x8f]

private func imageUser(manager num: UInt8) -> UInt8 {
    return num ^ 253
}

/*: "json error" :*/
fileprivate let str_observerData:[Character] = ["j","s","o","n"," ","e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let str_hiddenName:String = "plversiontfo"
fileprivate let str_labelText:String = "leading if in equalphone&"
fileprivate let str_insideContent:String = "data manager progressn="

/*: &packageId= :*/
fileprivate let str_feeData:String = "&packvideo square guard size return"

/*: &bundleId= :*/
fileprivate let str_labelValue:String = "in by data make equal&bundl"
fileprivate let str_giftLikeData:[Character] = ["e","I","d","="]

/*: &lang= :*/
fileprivate let str_aspectText:String = "call value&lang="

/*: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| " :*/
fileprivate let str_pushData:[Character] = ["?","!","@","#","$","^","&","%","*","+",",",":",";","=","\'","\"","`","<",">","(",")","[","]","{","}","/","\\","|"]
fileprivate let str_imageShowText:String = " "

/*: "/route/slowindex" :*/
fileprivate let str_makeName:[Character] = ["/","r","o","u","t","e","/","s","l","o","w","i"]
fileprivate let str_coverEdgeName:[Character] = ["n","d","e","x"]

/*: "/route/index" :*/
fileprivate let str_guideText:String = "/routebutton window content para page"
fileprivate let str_officialData:[Character] = ["/","i","n","d","e","x"]

/*: "?path= :*/
fileprivate let str_waitName:String = "info normal close?path="

/*: "&GJNonceStr= :*/
fileprivate let str_outputContent:String = "temp status&GJNo"
fileprivate let str_insertTransactionData:String = "nceStr=app var"

/*: "& :*/
fileprivate let str_domainValue:String = "border"

/*: = :*/
fileprivate let str_retNorText:String = "social"

/*: "&params= :*/
fileprivate let str_meTitle:String = "&params=equal to manager"

/*: "/ :*/
fileprivate let str_limitData:String = "event"

/*: "? :*/
fileprivate let str_belowContentValue:String = "make"

/*: "token" :*/
fileprivate let str_coverControlText:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let str_labIconData:String = "请求\u{6210}功"

/*: "AAAAAAAA" :*/
fileprivate let str_appRoomPopValue:String = "tabletabletabletabletabletabletabletable"

/*: "AA" :*/
fileprivate let str_availableContent:String = "clearclear"

/*: "BB" :*/
fileprivate let str_cellResultValue:[Character] = ["B","B"]

/*: "下载完成 -  :*/
fileprivate let str_pathTableTrueData:String = "\u{4e0b}载\u{5b8c}成 - "

/*: "/Documents/" :*/
fileprivate let str_makeTitle:String = "/Documentable player"
fileprivate let str_formatShareData:String = "ts/in add index index adjustment"

/*: ".mp4" :*/
fileprivate let str_actualTitle:String = "corner make video.mp4"

/*: "移动地址: :*/
fileprivate let str_groupContent:String = "移动permission址:"

/*: "后台任务下载回来" :*/
fileprivate let str_applicationName:[Character] = ["\u{540e}"]
fileprivate let str_voiceData:[Character] = ["台","\u{4efb}","务","\u{4e0b}","载","\u{56de}","来"]

/*: "下载成功 : :*/
fileprivate let str_selectedAddContent:String = "下\u{8f7d}成功 :"

/*: "Net Error" :*/
fileprivate let str_contentTopText:[Character] = ["N","e","t"," ","E","r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let str_kindData:String = "\u{4e0b}\u{8f7d}失败 "
fileprivate let str_appValue:String = ":"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PanoramicViewSessionDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import UIKit
import UIKit

//: import Alamofire
import Alamofire

//: import CoreMedia
import CoreMedia

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let kLet_systemName = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let kLet_inviteSuccessData = "\(kLet_localText)/\(kLet_intervalValue) (\(kLet_sessionMessageData)" + (String(str_cellModelContent.suffix(3)) + String(str_remarkData.suffix(5))) + "\(kLet_versionValue)" + (String(str_cameraPlayerData)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingNoOutcomeErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let kLet_informationStatusTitle = PanoramicViewSessionDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class PanoramicViewSessionDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func theAll(model: ClearRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.ego() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.clickDetailCompletion(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.recordStartOut(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func clickDetailCompletion(model: ClearRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.isochronousModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.dowryBackground(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    buttonSignature(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, TalkingNoOutcomeErrorResponse(errorCode: AppSignedNumeric.NetError.rawValue, errorMsg: kLet_eventText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        barLog(showMsg: kLet_eventText)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (str_targetData.replacingOccurrences(of: "under", with: "u")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.inexpensiveness())+\(kLet_textSystemTitle)" + (String(str_backTitle)), mimeType: (String(str_nameMustViewTitle) + str_callTitle.replacingOccurrences(of: "voice", with: "a")))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(str_tabValue)), mimeType: (String(str_dismissTitle) + String(str_toolMatchData)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    buttonSignature(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, TalkingNoOutcomeErrorResponse(errorCode: AppSignedNumeric.NetError.rawValue, errorMsg: kLet_eventText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        barLog(showMsg: kLet_eventText)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func recordStartOut(model: ClearRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.isochronousModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.dowryBackground(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: kLet_localReplaceData ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_systemName }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    buttonSignature(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, TalkingNoOutcomeErrorResponse(errorCode: AppSignedNumeric.NetError.rawValue, errorMsg: kLet_eventText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        barLog(showMsg: kLet_eventText)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_systemName }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    buttonSignature(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, TalkingNoOutcomeErrorResponse(errorCode: AppSignedNumeric.NetError.rawValue, errorMsg: kLet_eventText))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        barLog(showMsg: kLet_eventText)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func buttonSignature(model: ClearRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            FirstNameThen.share.searchOfCookies(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if kLet_localReplaceData, model.requestServer.contains((String(str_liveContent.prefix(7)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.aes256Decrypt(key: FirstNameThen.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: (String(str_coverPathValue) + str_topAddValue.replacingOccurrences(of: "cancel", with: "l")), with: (String(str_modelWillTitle) + str_spaceBottomValue.capitalized))
        //: if !Encryption_Request {
        if !kLet_localReplaceData {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(str_partyEqualText)), with: ":\"\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<NameShareHandyJSON>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == AppSignedNumeric.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, TalkingNoOutcomeErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    barLog(showMsg: responseModel.msg ?? String(bytes: str_tableCancelName.map{imageUser(manager: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case AppSignedNumeric.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_profileData, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, TalkingNoOutcomeErrorResponse(errorCode: AppSignedNumeric.NetError.rawValue, errorMsg: (String(str_observerData)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func isochronousModel(model: ClearRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (str_hiddenName.replacingOccurrences(of: "version", with: "a") + "rm=i" + String(str_labelText.suffix(6)) + "versio" + String(str_insideContent.suffix(2))) + "\(kLet_ofHalfValue)" + (String(str_feeData.prefix(5)) + "ageId=") + "\(kLet_textSystemTitle)" + (String(str_labelValue.suffix(6)) + String(str_giftLikeData)) + "\(kLet_sessionMessageData)" + (String(str_aspectText.suffix(6))) + "\(FirstNameThen.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if kLet_localReplaceData, serverUrl.contains((String(str_liveContent.prefix(7)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if FirstNameThen.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(str_makeName) + String(str_coverEdgeName)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(str_guideText.prefix(6)) + String(str_officialData))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(str_waitName.suffix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(str_outputContent.suffix(5)) + String(str_insertTransactionData.prefix(7))) + "\(FirstNameThen.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.aes256Encrypt(key: FirstNameThen.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(str_meTitle.prefix(8))) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func dowryBackground(model: ClearRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(kLet_inviteSuccessData)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(str_coverControlText)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func downTitle(model: ClearRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.isochronousModel(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (str_labIconData) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (str_appRoomPopValue.replacingOccurrences(of: "table", with: "A"))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (str_pathTableTrueData) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(str_makeTitle.prefix(8)) + String(str_formatShareData.prefix(3))) + NSDate.inexpensiveness() + (String(str_actualTitle.suffix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (str_groupContent.replacingOccurrences(of: "permission", with: "地")) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (String(str_applicationName) + String(str_voiceData)))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = ShowLetterHelperBaseDelegate.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func shouldBy(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: kLet_nameStreamLimitData)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (str_selectedAddContent) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, TalkingNoOutcomeErrorResponse(errorCode: AppSignedNumeric.Normal.rawValue, errorMsg: (String(str_contentTopText)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, TalkingNoOutcomeErrorResponse(errorCode: AppSignedNumeric.NetError.rawValue, errorMsg: (String(str_contentTopText)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (str_kindData + str_appValue.capitalized) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func getStartedPath(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: kLet_nameStreamLimitData)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (str_selectedAddContent) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, TalkingNoOutcomeErrorResponse(errorCode: AppSignedNumeric.Normal.rawValue, errorMsg: (String(str_contentTopText)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, TalkingNoOutcomeErrorResponse(errorCode: AppSignedNumeric.NetError.rawValue, errorMsg: (String(str_contentTopText)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (str_kindData + str_appValue.capitalized) + "\(errMsg)")
            }
        }
    }
}
